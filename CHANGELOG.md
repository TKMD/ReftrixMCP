# Changelog / 変更履歴

All notable changes to this project will be documented in this file.

このプロジェクトに対する注目すべき変更点はすべてこのファイルに記載されます。

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

形式は [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) に基づき、
[セマンティックバージョニング](https://semver.org/spec/v2.0.0.html) に準拠しています。

## [0.5.1] — 2026-05-07 (target) / [0.5.1] — 2026-05-07 (target)

### Changed / 変更

- **F2 partial revert per ADR-0020 Amendment 1**: `fileParallelism: false` を `vitest.base.config.mts` から削除し Vitest 4.x default `true` に復帰。F1 (`maxWorkers: 2`) は保持。トリガーは TPA-IMPL-H-01 hard gate ≤ 30:00 violation (CI run #1 wall-time 33:22, run id `25209350438`)。F4 + R-11 + F1 が F2 の race-condition mitigation を構造的に compensate。 / Removed `fileParallelism: false` from `vitest.base.config.mts`; restored Vitest 4.x default `true`. F1 (`maxWorkers: 2`) preserved. Triggered by TPA-IMPL-H-01 hard gate ≤ 30:00 violation (CI run #1 wall-time 33:22, run id `25209350438`). F4 + R-11 + F1 structurally compensate for F2's race-condition mitigation.

### Fixed / 修正

- **Drift detection coverage gap closure per ADR-0020 Amendment 2** (FIND-IO-PHASE4-IO-SCOPE-03, M severity, deadline 2026-05-08): IO Final Consistency Check に `git tag --points-at HEAD` vs. publishable `package.json.version` equality verification を追加 (Layer 2: integration-owner Playbook 8 update)。INV-RELEASE-VERSION-COHERENCE-001 standing regression test を新設 (Layer 1: `apps/mcp-server/tests/regression/standing/schema-enum-sync/inv-release-version-coherence-001.test.ts`)。Carryover ID = CO-IO-SCOPE-EXPANSION-01。 / Added `git tag --points-at HEAD` vs. publishable `package.json.version` equality verification to IO Final Consistency Check (Layer 2: integration-owner Playbook 8 update). Introduced INV-RELEASE-VERSION-COHERENCE-001 standing regression (Layer 1). Carryover ID = CO-IO-SCOPE-EXPANSION-01.

- **CI Flaky Systemic Remediation (v0.5.1 patch release)** / **CI flaky 構造的修復 (v0.5.1 パッチリリース)**:
  - **EN**: CI flaky structural remediation across 5 fixes per ADR-0020. F1 (`apps/mcp-server/vitest.base.config.mts` Vitest `maxWorkers: 3 → 2`, SSOT propagation to 3 derived configs) + F2 (top-level `fileParallelism: false` in same file, suppresses intra-worker async I/O race) + F3 (`actions/cache@v4` for Playwright browser at 3 callsites in `.github/workflows/ci.yml`, lines 154/224/382, cuts cold-install ~60s → cache-hit ~5s) + F4 (`LocalStorageProvider` mock-replace in `apps/mcp-server/tests/integration/phase1-integration.test.ts` via `vi.mock`, removes storage I/O timing from test path; new compensation unit test `tests/services/storage/memory-storage-provider.test.ts` lands in same commit per ADR-0020 Decision 4 + R-11 mandate; **note**: Plan/ADR previously referred to this provider as `MemoryStorageProvider` for historical reasons — code and CHANGELOG now uniformly use `LocalStorageProvider` per CLAUDE.md SoT Hierarchy T1 wins) + CO-PR21-REVIEW-01 (CSS-animation parser ReDoS threshold replaced with input-size-proportional formula `expectedMaxMs = input.length / throughputCharsPerMs + 200ms` with env var `REDOS_TEST_THROUGHPUT_CHARS_PER_MS` override; local default 100 chars/ms, CI 80 chars/ms via `.github/workflows/ci.yml` env injection per TPA-04⚯TDA-02 unified). CWE-770 mitigation completeness preserved as **bounded-growth detection** (PR #21 fixed-1000ms hotfix superseded).
  - **JP**: ADR-0020 に従う 5 件の CI flaky 構造的修復。F1 (`apps/mcp-server/vitest.base.config.mts` の Vitest `maxWorkers: 3 → 2`、3 derived config に SSOT 伝播) + F2 (同一 file 内 top-level `fileParallelism: false`、worker 内 async I/O 競合抑制) + F3 (`.github/workflows/ci.yml` の 3 callsite (154/224/382 行) に `actions/cache@v4` for Playwright browser、cold install ~60秒 → cache hit ~5秒) + F4 (`apps/mcp-server/tests/integration/phase1-integration.test.ts` で `LocalStorageProvider` を `vi.mock` で置換、storage I/O timing を test path から構造的撤去、ADR-0020 Decision 4 + R-11 mandate に従い同一 commit で新規補填 unit test `tests/services/storage/memory-storage-provider.test.ts` も landing。**注**: Plan/ADR は historical reasons から `MemoryStorageProvider` と表記していたが、code と CHANGELOG はいずれも CLAUDE.md SoT Hierarchy (T1 wins) に従い `LocalStorageProvider` を使用) + CO-PR21-REVIEW-01 (CSS-animation parser ReDoS test threshold を input-size 比例式 `expectedMaxMs = input.length / throughputCharsPerMs + 200ms` に置換、env var `REDOS_TEST_THROUGHPUT_CHARS_PER_MS` で override 可能。Local default 100 chars/ms、CI は `.github/workflows/ci.yml` env injection で 80 chars/ms、TPA-04⚯TDA-02 unified per)。CWE-770 mitigation completeness は **bounded-growth detection** で維持 (PR #21 固定 1000 ms hotfix を supersede)。

  - **Scope expansion (DRIFT-U20-01)**: IO U-20 Final Consistency Check (`019de395-8c12-723e-b4af-3368c8984a8d`) で発見された `packages/ml/tests/dinov2/service-coverage.test.ts` (v0.2.0 sprint 由来、本 cycle 全体 untouched) にも同 race condition が残存。Option A'' canonical pattern (Pattern (a): single `vi.mock` factory + per-test `mockReset` state) を適用、`vi.doMock` × 5 + `vi.doUnmock` × 4 → 1 `vi.mock` + 1 `vi.hoisted`、9 dynamic imports → static top-level imports。Verification: isolation pass^10 + directory pass^10 = **20/20 PASS** (zero flake)、14 tests preserved、production code untouched。CO-DINOV2-MOCK-ISOLATION-01 closure を **"Path B' + Option A'' + DRIFT-U20-01 application expansion 三段合成"** に再 phrasing。ADR-0020 Amendment 4 §5.5 Scope Expansion に詳細記載。 / **Scope expansion (DRIFT-U20-01)**: IO U-20 Final Consistency Check (`019de395-...`) identified `packages/ml/tests/dinov2/service-coverage.test.ts` (created in v0.2.0 sprint, untouched throughout v0.5.1 cycle) carries the same race. Applied Option A'' canonical pattern (Pattern (a): single `vi.mock` factory + per-test `mockReset` state). Converted `vi.doMock` × 5 + `vi.doUnmock` × 4 → 1 `vi.mock` + 1 `vi.hoisted`; 9 dynamic imports → static top-level imports. Verified isolation pass^10 + directory pass^10 = **20/20 PASS** (zero flake), 14 tests preserved, production code untouched. CO-DINOV2-MOCK-ISOLATION-01 closure re-phrased as **"three-stage composition: Path B' + Option A'' + DRIFT-U20-01 scope expansion"**. Detailed in ADR-0020 Amendment 4 §5.5 Scope Expansion.

### Security / セキュリティ

- **Security content inheritance from v0.5.0 / v0.5.0 SEC content の継承**:
  - **EN**: "v0.5.1 includes all v0.5.0 SEC content: PR-0 protobufjs Critical / PR-0 vite / PR-0 MCP_API_KEYS CWE-521 鍵長検証 / PR-0.5 LP Next.js 16.1.7→16.2.3 GHSA-q4gf-8mx6-v5v3 (DoS with Server Components) / SEC pre-release hotfix CodeQL CWE-829 SRI / Dependabot hono + @hono/node-server + DOMPurify"
  - **JP**: "v0.5.1 は v0.5.0 の SEC content をすべて継承: PR-0 protobufjs Critical / PR-0 vite / PR-0 MCP_API_KEYS CWE-521 鍵長検証 / PR-0.5 LP Next.js 16.1.7→16.2.3 GHSA-q4gf-8mx6-v5v3 (Server Components による DoS) / SEC pre-release hotfix CodeQL CWE-829 SRI / Dependabot hono + @hono/node-server + DOMPurify"
  - **Active exploit watch / Active exploit watch 維持**: SEC sign-off APPROVE WITH CONDITIONS (`019de070-65f5-77bc-b3b9-6017b35d85c3`) condition 2 を継承。security-engineer は v0.5.1 publish 直前 (Phase 4.2.5) に CISA KEV / NVD / GHSA を 6-day window (2026-05-01 〜 2026-05-07) で reassess。新規 advisory が dependency tree に該当する場合は publish BLOCK + IO escalate / SEC condition 2 inherited; security-engineer reassesses CISA KEV / NVD / GHSA over the 2026-05-01 〜 2026-05-07 window immediately before publish; publish BLOCKs and escalates to IO if any new advisory matches the dependency tree.

### Compliance / 法務・コンプライアンス

- **SBOM Invariance / SBOM 不変性 attestation**:
  - **EN**: "SBOM unchanged from v0.5.0 (CycloneDX 1.6, last generated 2026-04-29 with 1300 components). v0.5.1 introduces no production dependency changes; pnpm-lock.yaml is bit-identical to v0.5.0 baseline."
  - **JP**: "SBOM は v0.5.0 から変更なし (CycloneDX 1.6、最終生成日 2026-04-29、1300 コンポーネント)。v0.5.1 は production 依存関係を変更せず、pnpm-lock.yaml は v0.5.0 baseline と bit-identical。"
  - EU CRA Art.13(8)/14(2) transparency obligation 履行証拠として機能。F1-F4 + CO-PR21-REVIEW-01 はいずれも test infrastructure / CI workflow のみの変更で production runtime / dependency tree は不変。/ Functions as evidence of EU CRA Art.13(8)/14(2) transparency obligation; F1-F4 + CO-PR21-REVIEW-01 are test-infrastructure / CI-workflow only with no production runtime / dependency tree change.
- **AGPL §5(a) Modification Notice / AGPL §5(a) 改変通知**:
  - **EN**: v0.5.1 modifies the following files:
    - `apps/mcp-server/vitest.base.config.mts` (F1 + F2)
    - `apps/mcp-server/tests/integration/phase1-integration.test.ts` (F4)
    - `apps/mcp-server/tests/services/page/css-animation-parser.service.test.ts` (CO-PR21-REVIEW-01)
    - `apps/mcp-server/tests/services/storage/memory-storage-provider.test.ts` (NEW, Planned; F4 coverage compensation per RENAME-PROVIDER-01 + ADR-0020 R-11; physical file lands in Phase 2 implementation per Plan §2.2 Step 2.3)
    - `.github/workflows/ci.yml` (F3 Playwright cache + `REDOS_TEST_THROUGHPUT_CHARS_PER_MS` env var injection)
    - `scripts/sync-oss.sh` (Step 1.5a publish dry-run + Step 1.5b SPDX integrity)
  - Modified by Reftrix Contributors on 2026-05-07. See ADR-0020 (`./ for design rationale.
  - **JP**: v0.5.1 で改変されるファイル: 上記英文と同じリスト。改変者: Reftrix Contributors、改変日: 2026-05-07。設計根拠は ADR-0020 (`./ を参照。

### Cross-references / 相互参照

- ADR-0020 (`./ — v0.5.1 architectural contract (Decisions 1-5)
- Plan v1.0 (`./ — 4-phase 13-step implementation plan
- IO BLOCK Decision `019de06a-5e6e-73d8-9d0b-c58eb55375d3` — 7 unblock conditions closure path defined in Plan §7
- SEC sign-off APPROVE WITH CONDITIONS `019de070-65f5-77bc-b3b9-6017b35d85c3` — 3 conditions inherited per Plan §6
- IO Plan Decision `019de2a3` (Phase 1 APPROVE) → IO Impl Decision `019de2a3-...` (Phase 2 Step 8 APPROVE) → IO Final Consistency Check (Phase 3.4)

## [0.5.0] — 2026-04-29 / [0.5.0] — 2026-04-29

### Security / セキュリティ

- **PR-0 SEC hotfix: protobufjs Critical + vite + MCP_API_KEYS 鍵長検証** / **PR-0 SEC hotfix: protobufjs Critical + vite + MCP_API_KEYS key-length check**:
  - **SEC Critical (GHSA-xq3m-2v4x-88gg)**: `@huggingface/transformers > onnxruntime-web > protobufjs < 7.5.5` の transient Critical 脆弱性を `pnpm.overrides` の `protobufjs: >=7.5.5` で強制解消。protobufjs@8.0.1 に pin、`pnpm audit --audit-level=critical` 0 件達成 / Resolved transient Critical `protobufjs < 7.5.5` via `pnpm.overrides` (pinned to protobufjs@8.0.1); `pnpm audit --audit-level=critical` reports 0.
  - **SEC High (GHSA-v2wj-q39q-566r, defense-in-depth)**: `pnpm.overrides` の `vite: ">=7.3.2 <9"` で vite@8.0.8 に pin。dev-only で exploitable ではないが defense-in-depth として適用。main High 3 件中 2 件解消 / Applied `vite: ">=7.3.2 <9"` as defense-in-depth (not exploitable due to dev-only, no `--host`); resolved 2/3 High findings on main.
  - **SEC CWE-521 (M-4a)**: `apps/mcp-server/src/middleware/auth.ts` の `MIN_ENV_KEY_LENGTH = 40` filter で `MCP_API_KEYS` env 由来のキー長不足を検出し拒否。`reftrix_` (8 chars) + 32 chars = 40 文字最低要件、190-bit entropy (base62) で 80-bit 安全閾値を大幅超過。`TEST_API_KEY_MAPPINGS` (開発用) は対象外 / Added `MIN_ENV_KEY_LENGTH = 40` filter for `MCP_API_KEYS` env-derived keys; 190-bit entropy far exceeds the 80-bit baseline. `TEST_API_KEY_MAPPINGS` (dev keys) are excluded.
- **PR-0.5 LP hotfix: Next.js 16.1.7 → 16.2.3 (GHSA-q4gf-8mx6-v5v3)** / **PR-0.5 LP hotfix: Next.js 16.1.7 → 16.2.3**:
  - **SEC High (GHSA-q4gf-8mx6-v5v3)**: `example/reftrix4` の Next.js Denial of Service with Server Components を解消。vulnerable range `>=16.0.0-beta.0 <16.2.3` → patched `>=16.2.3`。`pnpm-workspace.yaml` で `test/test2` を workspace から除外し audit 対象を縮約。`pnpm audit --audit-level=high` 残存 High **1 → 0** 達成 / Resolved Next.js DoS with Server Components in `example/reftrix4`. Excluded `test/test2` from workspace to reduce audit scope. `pnpm audit --audit-level=high` remaining High reduced from 1 to 0.
- **SEC pre-release hotfix: CodeQL CWE-829 SRI integrity + Dependabot hono / @hono/node-server / DOMPurify pnpm.overrides expansion** / **SEC プレリリース hotfix: CodeQL CWE-829 SRI integrity + Dependabot hono / @hono/node-server / DOMPurify pnpm.overrides 拡張**:
  - **JP**: OSS リポジトリ (TKMD/ReftrixMCP) の Security 監査で報告された **2 CodeQL alerts (CWE-829 `js/functionality-from-untrusted-source`)** + **8 Dependabot moderate alerts (hono ×7 + @hono/node-server ×1)** を pre-release で解消。CodeQL は test fixture HTML (`apps/mcp-server/tests/fixtures/js-animations/{gsap-test,anime-js-test}.html`) の cdnjs CDN script 参照に **`integrity="sha384-..."` + `crossorigin="anonymous"` SRI 属性**を付与し、CDN 改竄時の untrusted source 実行を防止。SHA-384 hash は CDN 配信ファイルから `openssl dgst -sha384 -binary | openssl base64 -A` で算出し配信ファイルと bit-exact 照合。Dependabot は `pnpm.overrides` を拡張: `hono: ">=4.12.7"` → `">=4.12.16"` (closes GHSA-26pp-8wgv-hjvm cookie name validation + GHSA-r5rp-j6wh-rvv4 NBSP prefix bypass + GHSA-458j-xx4x-4375 toSSG path traversal + GHSA-xpcf-pg52-r92g serveStatic middleware bypass + GHSA-wmmm-f939-6g9c JSX HTML injection + GHSA-xf4j-xp2r-rqqx ipRestriction IPv4-mapped + 関連 1)、`@hono/node-server: ">=1.19.10"` → `">=1.19.14"` (closes GHSA-92pp-h63x-v22m middleware bypass)、新規 `dompurify: ">=3.4.1"` (defensive override、現状の主依存は 3.4.0 で advisory 対象外だが OSS Dependabot が transitive nested 旧版を検知する可能性に対する深層防御)。Lockfile 解決結果: hono@4.12.7 → 4.12.16、@hono/node-server@1.19.10 → 2.0.1 (latest)、dompurify@3.4.0 → 3.4.1。`pnpm install --lockfile-only` で binary download なしで lockfile 更新。
  - **EN**: Resolves **2 CodeQL alerts (CWE-829 `js/functionality-from-untrusted-source`)** + **8 Dependabot moderate alerts (hono ×7 + @hono/node-server ×1)** reported on the OSS repo (TKMD/ReftrixMCP) at pre-release. CodeQL: added `integrity="sha384-..."` + `crossorigin="anonymous"` SRI attributes to cdnjs CDN script references in test fixture HTML files (`apps/mcp-server/tests/fixtures/js-animations/{gsap-test,anime-js-test}.html`), preventing untrusted-source execution if the CDN is tampered with. SHA-384 hashes were computed from CDN-served files via `openssl dgst -sha384 -binary | openssl base64 -A` for bit-exact match. Dependabot: expanded `pnpm.overrides` — `hono: ">=4.12.7"` → `">=4.12.16"` (closes GHSA-26pp-8wgv-hjvm cookie-name validation + GHSA-r5rp-j6wh-rvv4 NBSP prefix bypass + GHSA-458j-xx4x-4375 toSSG path traversal + GHSA-xpcf-pg52-r92g serveStatic middleware bypass + GHSA-wmmm-f939-6g9c JSX HTML injection + GHSA-xf4j-xp2r-rqqx ipRestriction IPv4-mapped + 1 related), `@hono/node-server: ">=1.19.10"` → `">=1.19.14"` (closes GHSA-92pp-h63x-v22m middleware bypass), and new `dompurify: ">=3.4.1"` (defensive override; main pin already at 3.4.0 unaffected, but defends in depth against any transitive nested older copies that OSS Dependabot may flag). Lockfile resolution: hono@4.12.7 → 4.12.16, @hono/node-server@1.19.10 → 2.0.1 (latest), dompurify@3.4.0 → 3.4.1. Lockfile updated via `pnpm install --lockfile-only` (no binary downloads).
  - **Audit deltas / 監査差分**: pre-fix `pnpm audit` 0 critical / 0 high / 10 moderate (1 ajv + 7 hono + 1 @hono/node-server + 1 uuid + 1 postcss); post-fix 0 critical / 0 high / **3 moderate** (1 ajv + 1 uuid + 1 postcss). 8 advisories closed. PR-0 で確立した protobufjs >=7.5.5 / vite >=7.3.2 <9 (Critical 0 / High 0) は維持。
  - **Quality gate / 品質ゲート**: `pnpm lint` + `pnpm typecheck` + `pnpm format:check` + `pnpm docs:verify` (47/47) all PASS. Test execution は CI に委譲 (lockfile-only update のため runtime API contract 不変)。
  - **Files modified / 編集ファイル**:
    - `apps/mcp-server/tests/fixtures/js-animations/gsap-test.html` (added SRI integrity hash for `gsap.min.js@3.12.2`)
    - `apps/mcp-server/tests/fixtures/js-animations/anime-js-test.html` (added SRI integrity hash for `anime.min.js@3.2.1`)
    - `package.json` (`pnpm.overrides`: `hono` `>=4.12.7` → `>=4.12.16`; `@hono/node-server` `>=1.19.10` → `>=1.19.14`; new `dompurify: ">=3.4.1"`)
    - `pnpm-lock.yaml` (regenerated via `pnpm install --lockfile-only`)
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.32(1)(b) processing integrity**: Strengthens "ongoing confidentiality, integrity, availability and resilience" via dependency vulnerability remediation (hono cookie/IP/path-traversal/middleware-bypass class).
    - **[JP] APPI 第23条 安全管理措置**: Technical safeguarding measures (脆弱性の継続的な低減 / continuous vulnerability reduction).
    - **[EU] EU CRA 2024/2847 Article 14**: Vulnerability handling within published products. SBOM (CycloneDX 1.6) regeneration: dependency version metadata only, no new components added.
  - **AGPL §5(a)/(b) modification notice**: Files listed above modified by Reftrix project as pre-release security fix. Modification timestamp: 2026-04-29.
  - **Cross-ref / 相互参照**: GHSA-26pp-8wgv-hjvm / GHSA-r5rp-j6wh-rvv4 / GHSA-458j-xx4x-4375 / GHSA-xpcf-pg52-r92g / GHSA-wmmm-f939-6g9c / GHSA-xf4j-xp2r-rqqx / GHSA-92pp-h63x-v22m / CWE-829 (CodeQL `js/functionality-from-untrusted-source`).

### Fixed / 修正

- **fix(worker): Item 3 / CO-31 closure — `acquireRedisLockBestEffort` fail-open recovery via `tryAcquireLock` retry-with-backoff (v0.4.0 Item 3) [EU GDPR Art.32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(worker): Item 3 / CO-31 closure — `acquireRedisLockBestEffort` fail-open recovery を `tryAcquireLock` retry-with-backoff で復旧 (v0.4.0 Item 3)**:
  - **JP**: PR-E-1 §1.3 で carryover として登録された **CO-31 (L severity, deadline 2026-Q4, pipeline-engineer)** を Item 3 PR で **早期 closure** (deadline よりも前倒し)。`apps/mcp-server/src/services/worker-supervisor.service.ts:1260 acquireRedisLockBestEffort` の fail-open path に **retry-with-backoff (100/200/400ms、max 3 attempts、total 700ms bounded)** helper (`tryAcquireLockWithRetry` + `runAcquireLockWithRetryOrchestrator`) を追加し、Redis transient failure (network jitter / connection limit、`reason === "redis_unavailable"`) 時の lock-absent degradation window を **14 分超 → ≤700ms に縮小** (NOT elimination per ADR-0011 Amendment 5 §A5.5)。`reason === "already_held"` (legitimate race-lost path) は **no-retry** で即時 `held_by_other` を返却 — CO-31 retry trigger ではない。Helper は `worker-active-lock.service.ts:236 tryAcquireLock` の **discriminated union API** (PR7d-3 SEC M-1 lineage、`AcquireLockResult` = `{ ok: true } | { ok: false; reason: "already_held" } | { ok: false; reason: "redis_unavailable"; error: string }`) を consume し、legacy boolean wrapper `acquireLock` では区別不能だった transient vs held-by-other を構造的に separate。CWE-770 mitigation completeness は **degradation window 縮小 (NOT elimination)** として contract-true に維持 (retry budget exhaustion 後は依然 fail-open final で `lockAcquired=false`、次の `ensureWorkerRunning` cycle / heartbeat tick / restart cycle で再 acquire 試行)。
  - **EN**: Closes the PR-E-1 §1.3 carryover **CO-31 (L severity, deadline 2026-Q4, pipeline-engineer)** **earlier than deadline** via Item 3 PR. Adds **retry-with-backoff (100/200/400ms, max 3 attempts, 700ms total bounded)** helper (`tryAcquireLockWithRetry` + `runAcquireLockWithRetryOrchestrator`) to the fail-open path of `apps/mcp-server/src/services/worker-supervisor.service.ts:1260 acquireRedisLockBestEffort`, **reducing the lock-absent degradation window from 14+ minutes to ≤700ms** on Redis transient failures (network jitter / connection limit, `reason === "redis_unavailable"`) — NOT elimination per ADR-0011 Amendment 5 §A5.5. `reason === "already_held"` (legitimate race-lost path) is **no-retry** and returns `held_by_other` immediately — it is not a CO-31 retry trigger. The helper consumes the `worker-active-lock.service.ts:236 tryAcquireLock` **discriminated union API** (PR7d-3 SEC M-1 lineage, `AcquireLockResult` = `{ ok: true } | { ok: false; reason: "already_held" } | { ok: false; reason: "redis_unavailable"; error: string }`), structurally separating transient vs held-by-other states that the legacy boolean wrapper `acquireLock` could not distinguish. CWE-770 mitigation completeness preserved as **degradation window reduction (NOT elimination)** contract: after retry budget exhaustion the existing fail-open final holds (`lockAcquired=false`), with re-acquire attempted on the next `ensureWorkerRunning` cycle / heartbeat tick / restart cycle.
  - **IO anchors / IO アンカー**: IO Plan Decision Initial CONDITIONAL `019dd66c-3c20-7024-949f-b62a9d8474d2` (SUPERSEDED, 2026-04-28, 1 M FIND-PLAN-SEC-ITEM3-01 + 9 L) → IO Plan Decision Final **APPROVE** `019dd689-77a8-724d-9290-b7998c327b57` (2026-04-29, post targeted re-audit, 4 face APPROVE consensus: TPA `019dd685` + SEC `019dd680` re-audit + TDA `019dd660-7330` reuse + LCC `019dd660-51dd` reuse, M finding RESOLVED via Plan v0.2 §3.2.1 helper rewrite to `tryAcquireLock` discriminated union API) → IO Impl Decision **APPROVE** `019dd6bb-980b-772b-82e8-c3bd980a340f` (Phase 2 Step 8, 2026-04-29, 0 H / 0 M / 1 L = FIND-IMPL-LCC-ITEM3-01 = 3-CHANGELOG bilingual JP/EN Item 3 closure entries Phase 3 forward landing per Plan v0.2 §4.9 / G7, accepted-risk 0 件). Phase 3 Step 11 IO Final Consistency Check + Phase 4 Final commit pending.
  - **Plan / Registry anchors**: Plan v0.2 (` + Finding Registry (` §1.0 IO Plan Decision Final + §1.0z Initial CONDITIONAL appendix + §1.0a-d 4 face Plan audits + §1.0e-f re-audits + §2.0 IO Impl Decision + §2.0a-d 4 face Impl audits).
  - **Files modified / 編集ファイル**:
    - `apps/mcp-server/src/services/worker-supervisor.service.ts` (`acquireRedisLockBestEffort` を thin caller に refactor、`runAcquireLockWithRetryOrchestrator` に callback object `{ onAcquired / onHeldByOther / onExhausted / onError / onSettled }` を渡す形へ移行、max-lines 1500 hard cap 維持: 1494/1500 = 6 LoC margin)
    - `apps/mcp-server/src/services/worker-supervisor-helpers.ts` (NEW: `tryAcquireLockWithRetry` helper consuming `AcquireLockResult` discriminated union → 3-state outcome `acquired` / `held_by_other` / `exhausted` + NEW: `runAcquireLockWithRetryOrchestrator` with `switch + assertNever` exhaustive narrowing for `.then` / `.catch` / `.finally` chain delegation; `sanitizeErrorMessage` rail for `logger.warn` error field per CWE-209 SEC-04 L)
    - `apps/mcp-server/tests/services/worker-supervisor.test.ts` (3 new unit cases under `describe("Item 3 / CO-31: tryAcquireLockWithRetry retry-with-backoff")`, real timer scope, stub-based fault injection per Plan §4.4 SEC-03 L)
    - `apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-lock-003-acq.test.ts` (NEW standing regression file, 2 cases: page worker fail-open recovery via `tryAcquireLockWithRetry` on transient Redis failure + `held_by_other` no-retry race-lost path, INV-WORKER-LOCK-003 cross-binding worker-lifecycle domain, stub-based fault injection)
    - ` (NEW Amendment 5 §A5.1..§A5.7: Background + Acquire-side retry contract / Retry parameters justification + heartbeat orthogonality (`LOCK_HEARTBEAT_INTERVAL_MS = 30_000`, 700ms ≪ 30s, 42x safety margin) / Max-lines hard cap rationale via 2-layer helper extraction / Cross-ref to PR-D-8 release-side shape `tryReleaseLockWithRetry` (intentional pattern unification per TDA-02 L) / CWE-770 mitigation completeness phrasing "window reduction NOT elimination" bilingual JP/EN / Cross-references / Status: Accepted)
    - `(§1.3 CO-31 row status update OPEN →`CLOSED (Item 3, commit <NEW_SHA>)`、Item 3 PR landing 反映)
  - **Standing regression / 常設 regression**: 2 new INV-WORKER-LOCK-003 cases (worker-lifecycle domain) PASS at `tests/regression/standing/worker-lifecycle/inv-worker-lock-003-acq.test.ts`. 0 `.skip` / `.todo` / `describe.skip`. No regression in existing INV-WORKER-LOCK-003 / INV-WORKER-RESPAWN-LOCK-NONCE-007 cases (worker-lifecycle 89/89 PASS preserved).
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.32(1)(b) processing integrity**: Reduces operational reliability gap on `reftrix:worker:active:page` Redis lock acquire path during transient Redis failure, strengthening "ongoing availability and resilience of processing systems" — natural extension of ADR-0011 Amendment 1 §A.2 already-decided dual-run guarantee contract.
    - **[JP] APPI 第23条 安全管理措置**: Technical safeguarding measures extended to acquire-side fail-open recovery for transient Redis failure — improves system reliability orthogonal to user data integrity invariants.
    - **[EU] EU CRA 2024/2847 Article 14**: **NOT a CRA Article 14 vulnerability scope item** — operational reliability blocker, not attacker-exploitable, no Article 14 disclosure obligation; no GDPR Art.33 72-hour breach notification gate triggered. 4-axis disclaimer applies (no external attacker surface / no privilege escalation / no information disclosure / no DoS amplification — `sanitizeErrorMessage` rail prevents CWE-209 information exposure on `logger.warn` error field per SEC-04 L landing).
  - **AGPL §5(a)/(b) modification notice**: Files listed above modified by Reftrix project as Item 3 / CO-31 closure (operational reliability fix). Modification timestamp: 2026-04-29.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged**. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.
  - **Related contracts / 関連契約**: ADR-0011 Amendment 5 (NEW, Acquire-side fail-open recovery via `tryAcquireLock` discriminated union, Status: Accepted) + ADR-0011 Amendment 4 (cross-ref, PR-E-1 lock nonce contract — distinct concern, no regression) + PR7d-3 SEC M-1 (`tryAcquireLock` discriminated union API lineage) + INV-WORKER-LOCK-003 (cross-binding standing test, worker-lifecycle domain) + PR-D-8 §3.2.5 Layer 1 `tryReleaseLockWithRetry` (intentional pattern unification, ~50% jscpd token-level overlap with `tryAcquireLockWithRetry` is intentional per TDA-02 L) + PR-E-1 §1.3 CO-31 (CLOSED Item 3) + Plan v0.2 + Finding Registry §1.0 / §2.0.

### Changed / 変更

- **fix(cron): Item 2 / CO-30 closure — BackfillReconciliationCron polling cadence 1h → 5min (v0.4.0 Item 2) [EU GDPR Art.5(1)(d)/30/32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(cron): Item 2 / CO-30 closure — BackfillReconciliationCron ポーリング間隔 1h → 5min (v0.4.0 Item 2)**:
  - **JP**: PR-E-1 §1.3 で carryover として登録された **CO-30 (M severity, deadline 2026-Q3, pipeline-engineer)** を Item 2 PR で closure。`apps/mcp-server/src/cron/backfill-reconciliation-cron.ts` の `DEFAULT_INTERVAL_MS` を 1h → 5min に短縮し、`embedding-backfill-worker.ts:661 await updateEmbeddingBackfillStatus(webPageId, remainingStatus)` per-job aggregate UPDATE が late-arrive または drop した場合の reconciliation polling 頻度を高めた。**Root cause re-attribution (TPA-01 evidence)**: Plan v0.1 では root cause を `page-analyze-worker.ts safeUpdateBackfillStatus` に attribute していたが codebase 0 hits、aggregate UPDATE owner は `embedding-backfill-worker.ts:661` per-job execution path。4 mechanisms cause UPDATE late/drop: (1) `updateEmbeddingBackfillStatus` catch block DB connection error / (2) BullMQ retry path exit / (3) planned worker restart drop (`maxJobsBeforeRestart=1`) / (4) last-job race during SIGTERM。**SLO claim correction (TPA-02 evidence, path b 採用)**: "12x improvement" / "5min SLO" は arithmetically invalid (`staleThresholdMs=1h` immutable per IO Plan Decision option (b))。実 reconciliation upper bound = `max(staleThresholdMs=1h, intervalMs=5min) = 1h+5min worst-tail`、~12% reduction in worst-tail lag (was `[1h, 2h]`, now `[1h, 1h+5min]`)。True 12x SLO improvement (cadence + staleThresholdMs 両方 + Option B IPC notification path 補強) は **CO-30-FOLLOWUP (M, 2026-Q4, pipeline-engineer)** として dual-registry (PR-E-1 §1.3 + Item 2 §1.3.2) に新規登録。Severity M、deadline 2026-Q3 met。
  - **EN**: Closes **CO-30 (M severity, deadline 2026-Q3, pipeline-engineer)** registered in PR-E-1 §1.3 as a carryover, via Item 2 PR. Reduces `DEFAULT_INTERVAL_MS` from 1h to 5min in `apps/mcp-server/src/cron/backfill-reconciliation-cron.ts`, increasing reconciliation polling frequency for cases where the `embedding-backfill-worker.ts:661 await updateEmbeddingBackfillStatus(webPageId, remainingStatus)` per-job aggregate UPDATE late-arrives or drops. **Root cause re-attribution (TPA-01 evidence)**: Plan v0.1 attributed the root cause to `page-analyze-worker.ts safeUpdateBackfillStatus` but that has 0 hits in codebase; the aggregate UPDATE owner is `embedding-backfill-worker.ts:661` per-job execution path. Four mechanisms cause UPDATE late/drop: (1) DB connection error in `updateEmbeddingBackfillStatus` catch block / (2) BullMQ retry path exit / (3) planned worker restart drop (`maxJobsBeforeRestart=1`) / (4) last-job race during SIGTERM. **SLO claim correction (TPA-02 evidence, path b adopted)**: "12x improvement" / "5min SLO" is arithmetically invalid (`staleThresholdMs=1h` is immutable per IO Plan Decision option (b)). Effective reconciliation upper bound = `max(staleThresholdMs=1h, intervalMs=5min) = 1h+5min worst-tail`, yielding ~12% reduction in worst-tail lag (was `[1h, 2h]`, now `[1h, 1h+5min]`). True 12x SLO improvement (combined cadence + staleThresholdMs + Option B IPC notification path PR) is registered as **CO-30-FOLLOWUP (M, 2026-Q4, pipeline-engineer)** in dual-registry (PR-E-1 §1.3 + Item 2 §1.3.2). Severity M, deadline 2026-Q3 met.
  - **IO anchors / IO アンカー**: IO Plan Decision Initial CONDITIONAL `019dd5f2-ed05-77ec-adc2-4b5e08780123` (SUPERSEDED) → IO Plan Decision Final **APPROVE** `019dd60c-bc74-772c-aa8e-8aad2e64f86a` (post targeted re-audit, 4 face APPROVE consensus) → IO Impl Decision **APPROVE** `019dd63a-e247-763b-bebf-23004582b27e` (Phase 2 Step 8, 0 H / 0 M / 2 L advisory carryover acceptable). Phase 3 Step 11 IO Final Consistency Check + Phase 4 Final commit pending.
  - **Plan / Registry anchors**: Plan v0.2 (` 742 LoC) + Finding Registry (` 843 LoC, §1.0 IO Plan Decision Final + §1.0z Initial CONDITIONAL appendix + §1.0a-f 4 face audits + §2.0 IO Impl Decision + §2.0a-d 4 face Impl audits + §3 Phase 3 docs sync hand-off contract).
  - **Files modified / 編集ファイル**:
    - `apps/mcp-server/src/cron/backfill-reconciliation-cron.ts` (DEFAULT_INTERVAL_MS 1h → 5min + bilingual JP/EN JSDoc explaining cadence rationale + staleThresholdMs orthogonality + CO-30-FOLLOWUP cross-ref, +39/-3 LoC)
    - `apps/mcp-server/src/scripts/start-workers.ts:650-655` (TPA-01 L advisory landing in Phase 3 — bilingual JP/EN comment at env-var override site documenting 5min default + 1h+5min upper bound + CO-30-FOLLOWUP M 2026-Q4 deferral, +12 LoC)
    - `apps/mcp-server/tests/regression/standing/gdpr-delete/inv-data-delete-002-co-30-reconciliation.test.ts` (NEW, 312 LoC, 4 INV-DATA-DELETE-002 cases: in_progress→completed CAS / concurrentUpdatesSkipped CAS guard / in_progress→failed pin on residual gaps / defer when active queue job; per-test `// INV-DATA-DELETE-002` comment + `assertInvName` runtime validation; 0 `.skip` / `.todo`)
    - `apps/mcp-server/DATA_RETENTION.md` §11.9.6.bis (NEW sub-section — Backfill in_progress reconciliation SLO orthogonality from §11.9.4 24h zombie-worker SLO; 5min cadence rationale; CO-30-FOLLOWUP M 2026-Q4 deferral)
    - ` (§1.3 CO-30 row root cause re-attribution per TPA-01 + status update CLOSED Item 2 + dual-registry CO-30-FOLLOWUP M 2026-Q4 row addition)
  - **Standing regression / 常設 regression**: 4 new INV-DATA-DELETE-002 cases (gdpr-delete domain) PASS (8ms total), 0 `.skip` / `.todo` / `describe.skip`. No regression in existing INV-\* tests.
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.5(1)(d) accuracy**: Reduces stale `embedding_backfill_status='in_progress'` window from `[1h, 2h]` to `[1h, 1h+5min]` worst-tail (~12% reduction), strengthening data accuracy invariant for downstream consumers gating on `status='completed'`.
    - **[EU] GDPR Art.30 records of processing activities**: No new audit_logs action; reconciliation Section A path (`reconcileInProgressRows`) remains audit_logs-silent per existing contract (CO-30 closure scope orthogonal to Section B terminal-pin audit emission).
    - **[EU] GDPR Art.32(1)(b) processing integrity**: Reduces operational reliability gap for backfill terminal-state reconciliation, strengthening "ongoing resilience of processing systems".
    - **[JP] APPI 第23条 安全管理措置**: Technical safeguarding measures extended to backfill reconciliation cadence accuracy.
    - **[EU] EU CRA 2024/2847 Article 14**: **NOT a CRA Article 14 vulnerability scope item** — operational reliability blocker, not attacker-exploitable. No Article 14 disclosure obligation; no GDPR Art.33 72-hour breach notification gate triggered. 4-axis disclaimer applies (mirrors ADR-0019:196-211 + DATA_RETENTION §11.8 / §11.9 patterns).
  - **AGPL §5(a)/(b) modification notice**: Files listed above modified by Reftrix project as Item 2 / CO-30 closure (operational reliability fix). Modification timestamp: 2026-04-28.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged**. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.
  - **Related contracts / 関連契約**: ADR-0008 (Skip Recovery / 7d TTL, cross-ref) + ADR-0011 (Worker Dual-run Lock, natural extension claim VALID — no new ADR needed) + INV-DATA-DELETE-002 (cross-binding standing test) + DATA_RETENTION.md §11.9.6.bis (NEW) + PR-E-1 §1.3 CO-30 (CLOSED) + PR-E-1 §1.3 CO-30-FOLLOWUP (NEW M 2026-Q4) + Item 2 §1.3.2 (dual-registry mirror).

### Fixed / 修正

- **fix(worker): WorkerSupervisor lock nonce contract mismatch 恒久対応 (v0.4.0 PR-E-1) [EU GDPR Art.5(1)(d)/30/32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(worker): permanent fix for WorkerSupervisor lock nonce contract mismatch (v0.4.0 PR-E-1)**:
  - **JP**: PR-E-1 (Severity H, current-release-cycle deadline) として WorkerSupervisor 内の lock nonce contract mismatch を恒久対応。`spawnWorker:687` で生成された per-spawn `randomUUID()` が acquire/extend/release path の per-supervisor `bootToken` と不一致のため、planned restart で SIGABRT 発生時に self-chained respawn が `foreign_lock` と誤判定し worker が永久 zombie 化していた問題を Option A (lockNonce を bootToken に統一、1 line modification) で解消。stripe.com page.analyze (jobId `019dcf77-0f77-7719-aae9-3eafe35b7713`, 2026-04-27) で planned restart + SIGABRT 後の embedding-backfill worker permanent zombie 化を再現していた production blocker を closure。NF-6 として `state="crashed"` セット両経路 (`runSelfChainedRespawnAndSchedule` outcome `foreign_lock` AND `handleUnexpectedExit` `crash_max_attempts`) で `clearLockHeartbeatTimer` helper を呼出し CWE-770 heartbeat 永久 refresh を防止。INV-WORKER-RESPAWN-LOCK-NONCE-007 standing test 新設 (8 cases、worker-lifecycle domain) で contract 検証を CI fail-fast で永続化。worker-supervisor unit test U-1..U-5 5 cases 追加。`audit-log.service.ts` SSOT 拡張 (`worker_zombie_recovered` action contract: 365d retention / `operator:<email>` actor / raw workerType target_id / details schema / `[EU GDPR Art.30][JP APPI 第23条]` compliance tags、LCC-04 explicit decision)。WorkerSupervisor LoC は **1500 = max-lines hard cap** に維持 (Phase 2 commit gate `wc -l ≤ 1500` step landing、CO-PRDD9-01 / OBS-7 enforcement layer closure)。standing regression worker-lifecycle 89/89 PASS (was 81/81、+8 INV-007 cases)。Severity H (production availability + GDPR Art. 32(1)(b))、accepted-risk forbidden、本 PR landing で **CO-22 (FIND-IMPL-TPA-D1b-03 M deadline 2026-Q3) + CO-PRDD9-01 / OBS-7 (worker-supervisor.service.ts max-lines enforcement layer) を formal closure**。
  - **EN**: PR-E-1 (Severity H, current-release-cycle deadline) permanently resolves the WorkerSupervisor lock-nonce contract mismatch. The per-spawn `randomUUID()` at `spawnWorker:687` diverged from the per-supervisor `bootToken` in the acquire/extend/release path, causing `foreign_lock` misclassification and permanent worker-zombie state on SIGABRT during planned restart. Resolved via Option A (unify `lockNonce` on `bootToken`, 1-line modification). Closes the production blocker reproduced on stripe.com page.analyze (jobId `019dcf77-0f77-7719-aae9-3eafe35b7713`, 2026-04-27) where the embedding-backfill worker became a permanent zombie after planned restart + SIGABRT. NF-6 invokes a `clearLockHeartbeatTimer` helper at both `state="crashed"` entry paths (`runSelfChainedRespawnAndSchedule` outcome `foreign_lock` AND `handleUnexpectedExit` `crash_max_attempts`) preventing CWE-770 heartbeat permanent refresh. New INV-WORKER-RESPAWN-LOCK-NONCE-007 standing test (8 cases, worker-lifecycle domain) enforces the contract as CI fail-fast. worker-supervisor unit test U-1..U-5 5-case extension. `audit-log.service.ts` SSOT extended (`worker_zombie_recovered` action contract: 365d retention / `operator:<email>` actor / raw workerType target_id / details schema / `[EU GDPR Art.30][JP APPI Art.23]` compliance tags, LCC-04 explicit decision). WorkerSupervisor LoC kept at **1500 = max-lines hard cap** (Phase 2 commit gate `wc -l ≤ 1500` step lands, CO-PRDD9-01 / OBS-7 enforcement layer closure). Standing regression worker-lifecycle 89/89 PASS (was 81/81; +8 INV-007 cases). Severity H (production availability + GDPR Art. 32(1)(b)), accepted-risk forbidden. **Formal closure of CO-22 (FIND-IMPL-TPA-D1b-03 M, deadline 2026-Q3) + CO-PRDD9-01 / OBS-7 (worker-supervisor.service.ts max-lines enforcement layer) at this PR landing**.
  - **Refs**: ADR-0011 Amendment 4 (Lock Nonce Contract Clarification — was Amendment 2 in Plan v1.0, renumbered per LCC-PLAN-PRE1-01 H to avoid collision with PR-D-9 Vision Unload Amendment 2 + PR-D-9-patch CHANGELOG line 66 reserved Amendment 3; **Status: Accepted in Phase 3 docs-sync 2026-04-28**, includes §A4.4 rationale 補記 documenting 3-callsite topology and NF-6 scope intentional limitation) + DATA*RETENTION §11.9 (zombie worker recovery runbook + 24h Stale state SLO §11.9-4 + GDPR Art.5(1)(d) accuracy invariant + GDPR Art.33 + EU CRA Article 14 4-axis disclaimer + worker_zombie_recovered audit action contract) + INV-WORKER-RESPAWN-LOCK-NONCE-007 standing test + worker-supervisor unit test U-1..U-5 + `audit-log.service.ts` `AUDIT_LOG_CONSTANTS.WORKER_ZOMBIE_RECOVERED*\*` SSOT + Plan v1.1 (` 775 LoC) + Finding Registry (`§1 Plan Decision v1 CONDITIONAL + §1.8 Plan Decision v2 APPROVE + §2 IO Impl Decision APPROVE with Option (c) + §2.4 CO-27/CO-28/CO-29 NEW carryovers + §2.5 Phase 3 docs-sync hand-off contract) + CO-22 (FIND-IMPL-TPA-D1b-03 M deadline 2026-Q3, **CLOSED PR-E-1**) + CO-PRDD9-01 / OBS-7 (worker-supervisor.service.ts max-lines enforcement layer, **CLOSED PR-E-1**) + CO-25 (NEW deadline 2026-Q4,`WorkerChildState.lockNonce`field deletion / Option C fallback) + CO-26 (NEW deadline 2026-Q3,`worker-supervisor.service.ts`3-module split) + **CO-27 (NEW L deadline 2026-Q4,`WorkerChildState.lockNonce`JSDoc enforcement clause; Phase 3 carryover維持 due to max-lines 1500 exact cap)** + **CO-28 (NEW L deadline operator-tooling PR landing,`truncateTargetId`action-specific bypass for`worker_zombie_recovered`)** + **CO-29 (NEW M deadline 2026-06-28, `shouldSuppressRespawn`3rd`state="crashed"` callsite NF-6 enforcement + INV-007 9th case + ADR §A wording 3-path rewrite; CO-26 cycle alignment, IO Option (c) consequence)\*\*.
  - **Files modified / 編集ファイル**:
    - `apps/mcp-server/src/services/worker-supervisor.service.ts` (Option A line 687 lockNonce alias + NF-6 clearLockHeartbeatTimer 両 crashed-entry 経路呼出 + helpers import 拡張、LoC 1500 → 1500 unchanged via `buildBootTokens` helper extraction for max-lines compliance)
    - `apps/mcp-server/src/services/worker-supervisor-helpers.ts` (NEW: `clearLockHeartbeatTimer` helper / NEW: `buildBootTokens` helper / EXPORT: `tryReleaseLockWithRetry` per TDA-02; +57 LoC)
    - `apps/mcp-server/src/services/audit-log.service.ts` (NEW: `WORKER_ZOMBIE_RECOVERED_ACTION` / `WORKER_ZOMBIE_RECOVERED_ACTOR_PREFIX` / `WORKER_ZOMBIE_RECOVERY_METHODS` SSOT extension per LCC-04; +25 LoC)
    - `apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-respawn-lock-nonce-007.test.ts` (NEW, 8 cases including (1) Layer 1 retry budget 0 / (4) static-source-grep regression guard / (5) WorkerType-isolation / (7) NF-6 clearInterval / (8) AST/grep verification)
    - `apps/mcp-server/tests/services/worker-supervisor.test.ts` (U-1..U-5 5-case unit test extension)
    - ` (Amendment 4 §A4.1..§A4.9: Background / Clarification / bootToken inequality contract / clearInterval requirement / CRA Article 14 4-axis disclaimer / INV-007 / Future per-child rotation cross-ref / Cross-references / Status)
    - `apps/mcp-server/DATA_RETENTION.md` (§11.9.1..§11.9.7: Detection / Recovery / Audit trail / SLO / GDPR Art.33 + EU CRA Article 14 4-axis disclaimer / Storage limitation + Stale state Art.5(1)(d) / Cross-references)
  - **Standing regression / 常設 regression**: 89/89 worker-lifecycle PASS (+8 INV-007 cases). 38/38 worker-supervisor unit tests PASS (+5 U-1..U-5 cases). 19/19 audit-log.service tests PASS (no regression). Skip/todo check: 0 lines.
  - **IO anchors / IO アンカー**: §1 IO Plan Decision (Phase 1 Step 3, CONDITIONAL, 2026-04-28) + §1.8 IO Plan Decision v2 (Phase 1 Step 6, APPROVE, 2026-04-28, 3-face SEC + TDA + LCC re-audit unanimously APPROVE) + §2 IO Impl Decision (Phase 2 Step 8, APPROVE with Option (c) accepted-risk on SEC M finding via ADR-0011 Amendment 4 §A4.4 rationale 補記 + CO-29 tracked carryover deadline 2026-06-28) + §3 IO Final Consistency Check (Phase 3 docs-sync separate IO session, scheduled post-Phase 3)
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.32(1)(b) processing integrity / ongoing resilience**: lock nonce contract mismatch による embedding-backfill worker permanent zombie 化を恒久対応 — "ongoing availability and resilience of processing systems" 文脈の operational reliability 回復
    - **[EU] GDPR Art.5(1)(d) accuracy**: zombie state 中の `web_pages.embedding_backfill_status='pending'` row が永久 stale 残存するリスクを §11.9-4 24h SLO threshold で運用化、operator manual recovery + `audit_logs.worker_zombie_recovered` emit を義務化
    - **[EU] GDPR Art.30 records of processing activities**: 1 new `worker_zombie_recovered` audit_logs action (365-day retention + raw workerType target_id (PII none) + `operator:<email>` actor naming, LCC-04 explicit decision per Plan §8.4)
    - **[JP] APPI 第23条 安全管理措置**: technical safeguarding measures extended to WorkerSupervisor lock nonce contract integrity + zombie worker recovery audit trail
    - **[EU] EU CRA 2024/2847 Article 14**: **NOT a CRA Article 14 vulnerability scope item** — operational reliability blocker, not attacker-exploitable, no Article 14 disclosure obligation triggered. 4-axis disclaimer (mirrors ADR-0019:196-211 + DATA_RETENTION §11.8): no external attacker surface / no privilege escalation / no information disclosure (CWE-209 SEC-V11-01 Rule 6 §5.0 nonce-name-free assertion form prevents CI log dump) / no DoS amplification (operable factor 1.0). GDPR Art.33 72-hour breach notification gate **not applicable**. Per ADR-0011 Amendment 4 §A4.5 + DATA_RETENTION §11.9.5.
  - **AGPL §5(a)/(b) modification notice**: Files listed above modified by Reftrix project as PR-E-1 (operational reliability fix). Modification timestamp: 2026-04-28.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged**. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.
  - **Related contracts / 関連契約**: ADR-0011 Amendment 4 (NEW, Lock Nonce Contract Clarification) + ADR-0019:196-211 (CRA Article 14 4-axis disclaimer source) + INV-WORKER-RESPAWN-LOCK-NONCE-007 (NEW standing test, 8 cases) + INV-WORKER-LOCK-003 (cross-ref, no regression, 89/89 PASS) + DATA_RETENTION.md §11.9 (NEW) + Plan v1.1 + Finding Registry §1 + §1.8 + CO-22 (closure) + CO-PRDD9-01/OBS-7 (closure) + CO-25 / CO-26 (NEW carryovers).
  - **Post-Landing Runtime Verification Amendment (2026-04-28) / 着地後 runtime verification 改訂**:
    - **JP**: stripe.com `019dd42e-b962-7138-ba6b-47e8c56c59eb` runtime verification (14 分、5/5 core assertions PASS) で PR-E-1 fix が 7 categories 全完走到達後に reachable になった 2 件の orthogonal carryover を Registry §1.3 + Plan Appendix C に追加。**CO-30 (M, 2026-Q3, pipeline-engineer)**: backfill terminal-state reconciliation gap (page worker restart 時の `safeUpdateBackfillStatus` aggregate UPDATE 未到達)。**CO-31 (L, 2026-Q4, pipeline-engineer)**: page worker `acquireRedisLockBestEffort` fail-open recovery 不足 (pre-existing, PR-E-1 fix と無関係)。新規 IO Decision なし、§3 Final Consistency PASS verdict 不変。
    - **EN**: stripe.com runtime verification (14 minutes, 5/5 core assertions PASS, jobId `019dd42e-b962-7138-ba6b-47e8c56c59eb`) surfaced 2 orthogonal carryovers reachable only after PR-E-1 fix enabled full 7-category traversal. Added to Registry §1.3 + Plan Appendix C. **CO-30 (M, 2026-Q3, pipeline-engineer)**: backfill terminal-state reconciliation gap (`safeUpdateBackfillStatus` aggregate UPDATE not delivered after page worker restart). **CO-31 (L, 2026-Q4, pipeline-engineer)**: page worker `acquireRedisLockBestEffort` fail-open recovery missing (pre-existing, unrelated to PR-E-1 fix). No new IO Decision issued; §3 Final Consistency PASS verdict unchanged.

- **fix(infra+worker): PR-D-9-patch Batch D-1 — INFRA-RLS-ADMIN-GRANT-001 (M) + INFRA-EMBEDDING-MOTION-SIGABRT-001 (M) (v0.4.0 PR-D-9-patch Batch D-1) [EU GDPR Art.5(1)(d)/17/30/32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(infra+worker): PR-D-9-patch Batch D-1 — INFRA-RLS-ADMIN-GRANT-001 (M) + INFRA-EMBEDDING-MOTION-SIGABRT-001 (M)**:
  - **JP**: PR-D-9-patch Batch D-1 として 2 件の M severity 永続化 finding を統合 landing (deadline 2026-05-25)。
    - **D-1a INFRA-RLS-ADMIN-GRANT-001 (M)**: `reftrix_admin` role の canonical privilege scope (`GRANT ALL PRIVILEGES`) を Prisma migration として永続化。Pre-existing infra drift (`packages/database/scripts/create-rls-roles.sql:101-108` SSOT が migration として永続化されていなかった、d98fece4 PR7d-1 以前から) を解消し、Phase 4 RLS Tier 2 test failure (`permission denied for table users` SQLSTATE 42501) の root cause を恒久対応。新 migration `20260427120000_restore_admin_role_grants/migration.sql` (idempotent + audit_logs SSOT entry with WHERE NOT EXISTS 4-tuple guard) + INV-RLS-ADMIN-GRANT-005 standing test (schema-enum-sync domain M1 placement、4 cases) + DATA_RETENTION.md §11.7 DDL/permission-change runbook (GDPR Art.32(1)(b) + APPI Art.23 + AGPL §5 legal-basis bilingual table) を landing。
    - **D-1b INFRA-EMBEDDING-MOTION-SIGABRT-001 (M)**: PR-D-9-patch Phase 4 stripe.com runtime verify (2026-04-27 12:13:02 UTC) で発生した motion category embedding-backfill worker の SIGABRT race condition (COW-inherited native pthread × planned restart × motion last-category processing) を 3 階層で恒久対応 — **Fix-1 (close → dispose ordering reversal)**: `embedding-backfill-worker.ts:950-973` で `worker.close()` を `disposeEmbeddingPipeline()` より先に呼び BullMQ drain を待機。**Fix-2 (pre-fork parent dispose)**: `WorkerSupervisor.spawnChild()` の fork 直前に parent Worker Thread + ONNX session を明示 dispose し COW window を最小化 (`pre_fork_dispose_duration_ms` metric 追加)。**Fix-3 (SIGABRT structured detection + suppression)**: `worker-supervisor-helpers.ts:361/371/379/422-435` で SIGABRT 専用 branch + 1/min rate-limited audit_logs emit + 2 連続 SIGABRT で fail-closed respawn 抑制。ADR-0019 (Embedding Worker Close-Before-Dispose Ordering, AGPL-3.0-only SPDX、266 LoC、CRA Article 14 disclaimer 含む) を新規 architectural contract として永続化、INV-EMBEDDING-MOTION-WORKER-SIGABRT-006 standing test (worker-lifecycle domain、6 cases) を CI で常時検証、DATA_RETENTION.md §11.8 motion SIGABRT recovery runbook + CRA Article 14 disclaimer (operational reliability blocker, not attacker-exploitable) を landing。
    - **3-CHANGELOG sync mandatory both plans** per Registry §13.16.9 + §13.17.10: root + internal + apps/mcp-server すべてに bilingual JP/EN entry を併記 (FIND-PLAN-LCC-D1A-02 + FIND-PLAN-LCC-D1B-02 Phase 3 hand-off)。
  - **EN**: PR-D-9-patch Batch D-1 lands two M-severity persistence findings together (deadline 2026-05-25).
    - **D-1a INFRA-RLS-ADMIN-GRANT-001 (M)**: Persists the canonical `reftrix_admin` privilege scope (`GRANT ALL PRIVILEGES`) as a Prisma migration. Resolves a pre-existing infra drift (the `packages/database/scripts/create-rls-roles.sql:101-108` SSOT was never persisted as a migration, since d98fece4 / PR7d-1 before PR-D-9 main) and permanently fixes the Phase 4 RLS Tier 2 test failure (`permission denied for table users` SQLSTATE 42501). New migration `20260427120000_restore_admin_role_grants/migration.sql` (idempotent + audit_logs SSOT entry with WHERE NOT EXISTS 4-tuple guard) + INV-RLS-ADMIN-GRANT-005 standing test (schema-enum-sync domain M1 placement; 4 cases) + DATA_RETENTION.md §11.7 DDL / permission-change runbook (GDPR Art.32(1)(b) + APPI Art.23 + AGPL §5 bilingual legal-basis table).
    - **D-1b INFRA-EMBEDDING-MOTION-SIGABRT-001 (M)**: Permanently resolves the motion-category embedding-backfill worker SIGABRT race condition (COW-inherited native pthread × planned restart × motion last-category processing) observed during PR-D-9-patch Phase 4 stripe.com runtime verify (2026-04-27 12:13:02 UTC) via three layered fixes — **Fix-1 (close → dispose ordering reversal)** at `embedding-backfill-worker.ts:950-973` calls `worker.close()` before `disposeEmbeddingPipeline()` so BullMQ drain completes; **Fix-2 (pre-fork parent dispose)** in `WorkerSupervisor.spawnChild()` explicitly disposes the parent Worker Thread + ONNX session immediately before fork, minimising the COW window (`pre_fork_dispose_duration_ms` metric added); **Fix-3 (SIGABRT structured detection + suppression)** in `worker-supervisor-helpers.ts:361/371/379/422-435` adds a SIGABRT-specific branch + 1/min rate-limited audit_logs emit + fail-closed respawn suppression on two consecutive SIGABRTs. Persists ADR-0019 (Embedding Worker Close-Before-Dispose Ordering, AGPL-3.0-only SPDX, 266 LoC, including the CRA Article 14 disclaimer) as a new architectural contract; INV-EMBEDDING-MOTION-WORKER-SIGABRT-006 standing test (worker-lifecycle domain; 6 cases) verifies it continuously in CI; DATA_RETENTION.md §11.8 motion SIGABRT recovery runbook + CRA Article 14 disclaimer (operational reliability blocker, not attacker-exploitable) land alongside.
    - **3-CHANGELOG sync mandatory both plans** per Registry §13.16.9 + §13.17.10: bilingual JP/EN entries land in root + internal + apps/mcp-server (FIND-PLAN-LCC-D1A-02 + FIND-PLAN-LCC-D1B-02 Phase 3 hand-off).
  - **Files modified / 編集ファイル**:
    - **D-1a**: `packages/database/prisma/migrations/20260427120000_restore_admin_role_grants/migration.sql` (NEW) + `apps/mcp-server/tests/regression/standing/schema-enum-sync/inv-rls-admin-grant-005.test.ts` (NEW) + `apps/mcp-server/DATA_RETENTION.md` §11.7
    - **D-1b**: `apps/mcp-server/src/workers/embedding-backfill-worker.ts:950-973` + `apps/mcp-server/src/queues/embedding-backfill-processors.ts:264-` + `apps/mcp-server/src/services/worker-supervisor.service.ts` + `apps/mcp-server/src/services/worker-supervisor-helpers.ts:361/371/379/422-435/456-472` + `apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-lock-003-motion-sigabrt.test.ts` (NEW, 257 LoC, 6 cases) + `apps/mcp-server/tests/workers/embedding-backfill-worker.test.ts:411-431` (TDA-D-1b-01 H closure) + `(NEW, 266 LoC) +`apps/mcp-server/DATA_RETENTION.md` §11.8
  - **Standing regression / 常設 regression**: 174/174 PASS (新規 INV-005 + INV-006 含む)。INV-RLS-ADMIN-GRANT-005 = schema-enum-sync domain M1 placement (TDA recommend、CI overhead 0)。INV-EMBEDDING-MOTION-WORKER-SIGABRT-006 = worker-lifecycle domain placement (per IO §13.16.6 domain placement decision)。Skip/todo check: 0 lines across all new tests. / 174/174 PASS (including new INV-005 + INV-006). Skip/todo check: 0 lines.
  - **IO anchors / IO アンカー**: §13.16 IO Plan Decision (Batch D Phase 1, dual CONDITIONAL → APPROVE/CONDITIONAL) + §13.17 IO Impl Decision (Batch D Phase 2, Plan 1 APPROVE direct + Plan 2 CONDITIONAL `documentation-only check` for FIND-IMPL-LCC-D1b-01 M Phase 3 docs landing)
  - **TDA-D-1b-01 H severity closure / TDA-D-1b-01 H severity 解決**: `tests/workers/embedding-backfill-worker.test.ts:411-431` ordering + forensic anchor assertion landed via red→green CI cycle, 0 skip/todo (per CLAUDE.md Severity → Landing Rules H mandate)
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.32(1)(b) processing integrity**: D-1a admin role privileges + D-1b worker close-before-dispose ordering both restore "ongoing resilience of processing systems"
    - **[EU] GDPR Art.5(1)(d) accuracy**: D-1b SIGABRT abort no longer leaves `embeddingBackfillStatus` partial-state (close-first ordering guarantees in-flight job completion)
    - **[EU] GDPR Art.30 records of processing activities**: 1 new D-1a `admin_role_grant` audit_logs action + 1 new D-1b `worker_sigabrt_detected` audit_logs action, both 365-day retention + `truncateTargetId` PII minimisation + `system:migration` / `system:worker-supervisor` actor naming
    - **[JP] APPI 第23条 安全管理措置**: technical safeguarding measures extended to admin role lifecycle + worker shutdown ordering
    - **[EU] CRA 2024/2847 Article 14**: **Both Batch D-1a and D-1b are NOT CRA Article 14 vulnerability scope items** — operational reliability blockers, not attacker-exploitable. CRA Annex I §1 essential requirements の cybersecurity properties に該当せず、Article 14 vulnerability disclosure 義務 / GDPR Art.33 72-hour breach notification gate のいずれも trigger なし (per ADR-0019:196-211 + DATA_RETENTION.md §11.8 CRA Article 14 disclaimer)
  - **AGPL §5(a)/(b) modification notice**: Files listed above modified by Reftrix project as PR-D-9-patch Batch D-1 (operational reliability fixes). Modification timestamp: 2026-04-27.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged**. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.
  - **Related contracts / 関連契約**: ADR-0019 (NEW, Embedding Worker Close-Before-Dispose Ordering) + ADR-0011 (cross-ref, Worker dual-run lock infra runbook framework) + ADR-0016 §M1 schema-enum-sync extension (existing, INV-005 placement) + INV-RLS-ADMIN-GRANT-005 (NEW) + INV-EMBEDDING-MOTION-WORKER-SIGABRT-006 (NEW) + DATA_RETENTION.md §11.7 (NEW) + §11.8 (NEW with CRA Article 14 disclaimer + production env hygiene clarification + FORK_ENABLED env-var distinction footnote) + Registry §13.16 (Plan Decision) + §13.17 (Impl Decision).

- **fix(worker): PR-D-9-patch post-merge runtime silent stall regression — IPC schema jobId composite-key acceptance via `BACKFILL_JOB_ID_REGEX` SSOT z.union (v0.4.0 PR-D-9-patch) [EU GDPR Art.5(1)(d)/32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(worker): PR-D-9-patch post-merge runtime silent stall regression — IPC schema jobId composite-key acceptance via `BACKFILL_JOB_ID_REGEX` SSOT z.union (v0.4.0 PR-D-9-patch)**:
  - **JP**: PR-D-9 main 着地後 (stripe.com page.analyze, 2026-04-26-27 時間枠) に backfill drain が再び silent stall する事象を運用観測。原因は PR-D-8 で導入された `apps/mcp-server/src/schemas/worker-ipc.schema.ts` の `jobId: z.string().uuid().optional()` strict UUID 制約と、PR-D-9 の `EmbeddingBackfillWorker` 経路で `buildBackfillJobId(webPageId, category)` が生成する `<UUID>__<category>` 複合キー間の shape mismatch。Worker child → parent IPC message が `jobId` Zod parse fail → message reject → backfill drain handshake が成立せず production rows が `embedding_backfill_status='pending'` で固定。本 patch で **Option B (z.union + SSOT regex)** を採用し landing: (1) `apps/mcp-server/src/queues/embedding-backfill-queue.ts` に `BACKFILL_JOB_ID_REGEX` SSOT を export (RFC 4122 strict-hyphen lowercase + `EMBEDDING_BACKFILL_CATEGORIES` enum SSOT 動的 join, +40 LoC); (2) `worker-ipc.schema.ts` の `jobId` field を `z.union([z.string().uuid(), z.string().regex(BACKFILL_JOB_ID_REGEX)]).optional()` に拡張 (+22/-1 LoC); (3) Standing regression `inv-worker-lock-003-embedding-backfill-supervisor.test.ts` cases #20-#23 (post-renumber, INV-WORKER-LOCK-003 + INV-DATA-ACCURACY-001 cross-binding, +416 LoC net)。**WorkerSupervisor LoC 1497 unchanged** (CO-PRDD9-01 contract STRENGTHENED to absolute neutrality)。Quality Gate Pass^3 5/5: `pnpm lint && pnpm typecheck && pnpm test && pnpm format:check && pnpm docs:verify` (`pnpm test` + `pnpm format:check` を Plan v1.1 §8 enumeration から正式追補、Plan §13.6 corrected enumeration 参照)。Standing regression 167/167 PASS (was 165/167 pre-Wave-5; Wave 5 で 2 pre-existing test bug fix landing: vi.doUnmock cross-contamination + hardcoded `"embeddin..."` truncation literal)。SEC-PATCH-01 M は Path B (ADR accepted-risk per SEC sign-off `019dcabf-becd`) で landing — `BACKFILL_JOB_ID_REGEX` 完全 anchor regex + `EMBEDDING_BACKFILL_CATEGORIES` SSOT + 5.89ms reject 経験的証拠 (10MB jobId DoS attack scenario) で per-string upper bound 不要を SEC が公式承認。**互換性破壊なし** (Backward compatible API)。
  - **EN**: After PR-D-9 main merge (stripe.com page.analyze, 2026-04-26-27 time window), backfill drain was operationally observed to re-enter silent stall. Root cause: shape mismatch between PR-D-8's `apps/mcp-server/src/schemas/worker-ipc.schema.ts` `jobId: z.string().uuid().optional()` strict UUID constraint and the `<UUID>__<category>` composite key generated by `buildBackfillJobId(webPageId, category)` in PR-D-9's `EmbeddingBackfillWorker` path. Worker child→parent IPC messages failed Zod parse on `jobId` → message reject → backfill drain handshake never completed → production rows stuck in `embedding_backfill_status='pending'`. This patch lands **Option B (z.union + SSOT regex)**: (1) export `BACKFILL_JOB_ID_REGEX` SSOT from `apps/mcp-server/src/queues/embedding-backfill-queue.ts` (RFC 4122 strict-hyphen lowercase + dynamic `EMBEDDING_BACKFILL_CATEGORIES` enum SSOT join, +40 LoC); (2) extend `worker-ipc.schema.ts` `jobId` field to `z.union([z.string().uuid(), z.string().regex(BACKFILL_JOB_ID_REGEX)]).optional()` (+22/-1 LoC); (3) standing regression `inv-worker-lock-003-embedding-backfill-supervisor.test.ts` cases #20-#23 (post-renumber, INV-WORKER-LOCK-003 + INV-DATA-ACCURACY-001 cross-binding, +416 LoC net). **WorkerSupervisor LoC 1497 unchanged** (CO-PRDD9-01 contract STRENGTHENED to absolute neutrality). Quality Gate Pass^3 5/5: `pnpm lint && pnpm typecheck && pnpm test && pnpm format:check && pnpm docs:verify` (`pnpm test` + `pnpm format:check` formally added to Plan v1.1 §8 enumeration, see Plan §13.6 corrected enumeration). Standing regression 167/167 PASS (was 165/167 pre-Wave-5; Wave 5 lands 2 pre-existing test bug fixes: vi.doUnmock cross-contamination + hardcoded `"embeddin..."` truncation literal). SEC-PATCH-01 M lands as Path B (ADR accepted-risk per SEC sign-off `019dcabf-becd`) — SEC formally approved that no per-string upper bound is required because `BACKFILL_JOB_ID_REGEX` is a complete-anchor regex + `EMBEDDING_BACKFILL_CATEGORIES` SSOT enum + empirical 5.89ms reject evidence (10MB jobId DoS attack scenario). **Backward compatible** (no breaking change).
  - **6 commits / 6 コミット**: `0d5de99d` (Wave 1+2: SSOT regex + standing test) + `c4c0d3ef` (Wave 3: schema z.union) + `e753400c` (Wave 4: prettier collapse) + `a1d5830d` (chore: docs README format) + `8e040d29` (Wave 5 test: case renumber + 2 bug fixes) + `7337d21b` (Wave 5 doc: Registry §13 prettier fix + §13.11 IO Decision delta).
  - **Standing regression coverage / 常設 regression カバレッジ**: INV-WORKER-LOCK-003 (cases #20-#23 post-renumber, IPC schema jobId composite-key acceptance + Worker IPC spoofing rejection) + INV-DATA-ACCURACY-001 cross-binding (GDPR Art.5(1)(d) accuracy invariant — `embedding_backfill_status` transitions preserved under IPC spoofing attack scenarios, "GDPR Art.5(1)(d) accuracy invariant" literal × 17 occurrences). 167/167 PASS pass^3 3 consecutive.
  - **IO anchors / IO anchor**: §13 IO Impl Decision CONDITIONAL (commit `797de86a`, TPA `019dca89-b4f8` / SEC `019dca8b-6024` / TDA `019dca8c-4fb2` / LCC `019dca8c-09c3`) → §13.11 IO Impl Decision delta APPROVE for Wave 5 (commit `7337d21b`, TPA `019dcabd-47c9` / SEC `019dcabf-becd` Path B sign-off / LCC `019dcabf-1202`).
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.5(1)(d) accuracy**: backfill drain reliability restored — silent stall regression eliminated, terminal-state integrity invariant preserved
    - **[EU] GDPR Art.32(1)(b) processing integrity**: `WorkerSupervisor` + `EmbeddingBackfillWorker` IPC handshake reliability restored (ongoing resilience of processing systems requirement)
    - **[JP] APPI 第23条 安全管理措置**: backfill drain operational reliability gap eliminated (本 patch は backward-compatible API のため migration 不要、operator 設定変更なし)
    - **[EU] CRA 2024/2847 Article 14**: **Not a CRA Article 14 vulnerability scope item** — operational reliability blocker, not attacker-exploitable. CRA Annex I §1 essential requirements の cybersecurity properties に該当せず、Article 14 vulnerability disclosure 義務 trigger なし。Schema-side Zod parse fail は internal worker IPC boundary でのみ観測され、external-attacker accessible surface area 増加なし。
  - **AGPL §5(a)/(b) modification notice**: `apps/mcp-server/src/queues/embedding-backfill-queue.ts` (BACKFILL_JOB_ID_REGEX SSOT export 追加, +40 LoC) + `apps/mcp-server/src/schemas/worker-ipc.schema.ts` (jobId z.union 拡張, +22/-1 LoC) modified by Reftrix project as PR-D-9-patch (post-merge runtime regression fix)。Modification timestamp: 2026-04-27 (commit hashes `0d5de99d` / `c4c0d3ef` 参照)。
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — PR-D-9-patch は既存 `zod` / `@prisma/client` / `bullmq` / `ioredis` / `EMBEDDING_BACKFILL_CATEGORIES` SSOT API のみ使用。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ No new dependency. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.
  - **Related contracts / 関連契約**: ADR-0011 Amendment 3 (PR-D-9-patch post-merge runtime regression fix — regex SSOT export pattern + SEC-PATCH-01 Path B accepted-risk + Plan §4.2 `code: "invalid_string"` empirical note + CWE-178 lowercase-only constraint note)、INV-WORKER-LOCK-003 (cases #20-#23 post-renumber)、INV-DATA-ACCURACY-001 (NEW cross-binding)。

### Changed / 変更

- **feat(worker+embedding): PR-D-9 EmbeddingBackfillWorker auto-spawn-on-enqueue + responsive backfill handler + parts.visual bbox resolution (v0.4.0 PR-D-9) [EU GDPR Art.5(1)(d)/17/30/32(1)(b)][JP APPI 第23条][US CCPA §1798.150][EU CRA 2024/2847]** / **feat(worker+embedding): PR-D-9 EmbeddingBackfillWorker auto-spawn-on-enqueue + responsive backfill handler + parts.visual bbox resolution (v0.4.0 PR-D-9)**:
  - **JP**: PR-D-8 verify セッションで stripe.com page.analyze (job `019dc8dd-8f84-768e-bf70-5cc56aad95bd`, 2026-04-26) において silent stall を観測。`EmbeddingBackfillWorker` が `WorkerSupervisor` 配下で per-type Map に登録されていても、auto-spawn handshake が `analyze.tool.ts` / `batch-analyze.tool.ts` 呼び出し境界で wired されていなかったため、production backfill drain が手動介入依存となっていた。本 PR で 4 領域を統合 landing: (1) auto-spawn handshake (Wave 1) — `apps/mcp-server/src/tools/page/_shared/worker-bootstrap.ts` (NEW per Plan v1.1 §5.1.17) で `getWorkerSupervisor().ensureAllWorkersRunningStaggered()` を staggered 実行し primary `page` heartbeat 確立 → secondary `embedding-backfill` spawn を順序付け、`ENABLE_BACKFILL_AUTOSPAWN` env-var (strict `=== "true"` parse) で fail-safe rollback gate 提供; (2) Vision unload precondition (UNB-IMPL-1) — `apps/mcp-server/src/services/vision/vision-unload-handshake.ts` (NEW, 229 LoC) が Ollama `/api/ps` を 3s timeout で照会し Vision residual loaded 検出時に secondary spawn を defer + audit_logs emit、32GB tier RSS budget 健全性 (~10.6GB Vision + ~1.9GB child + ~4GB peak page = ~16.5GB を回避) を担保、probe failure 時は **fail-closed** (GDPR Art.32(1)(b) integrity); (3) responsive backfill handler (Wave 3) — `embedding-backfill.service.ts:1050-1075` + `:1090-1104` に `AND EXISTS (SELECT 1 FROM web_pages WHERE id = ?::uuid)` 防御節を追加し GDPR Art.17 `data.delete` 同期削除中の TOCTOU resurrection invariant を保証 (PR2d LCC-M-2 contract 継承); (4) parts.visual bbox resolution (Wave 4) — `apps/mcp-server/src/services/part/part-bbox-playwright.service.ts` に failure classification (iframe/shadow_dom/dom_disposed/unknown) 追加 + `classifyBboxFailure` private helper (CC delta ≤ +2) で `bbox_unresolvable` skipReason emit、`audit_logs.embedding_part_visual_skipped` SSOT 経由 (`apps/mcp-server/src/audit/audit-actions.ts`) で routing。Standing regression: INV-WORKER-LOCK-003 case #15-#19 (`inv-worker-lock-003-embedding-backfill-supervisor.test.ts:1928-2042` 統合) + INV-EMBEDDING-INTEGRITY-001 case #1-#6 (`inv-embedding-integrity-001-bbox-and-responsive.test.ts` NEW 613 LoC 10 tests) + INV-SCHEMA-ENUM-004 16-value `EmbeddingSkipReason` exhaustiveness (TS-only `as const` SSOT per ADR-0018 §Decision 1 Supplement) + Integration drain test `tests/integration/page-analyze-backfill-drain.test.ts` (NEW 293 LoC 4 tests, `process.env.CI` gate, fixture URL allowlist 127.0.0.1:38765 + 4 negative SSRF tests CWE-918)。新規環境変数: `ENABLE_BACKFILL_AUTOSPAWN` (default true), `BBOX_RESOLVE_RELOAD_ENABLED` (default false), `BBOX_RESOLVE_RELOAD_ENABLED_MAX_RELOADS_PER_PAGE` (default 5), `BBOX_RESOLVE_RELOAD_TOTAL_TIMEOUT_MS` (default 60000)。WorkerSupervisor LoC 1499 → **1497** (-2 LoC delta、CO-PRDD9-01 contract STRENGTHENED)。**互換性破壊なし** (Backward compatible API)。
  - **EN**: PR-D-8 verify session observed silent stall on stripe.com page.analyze (job `019dc8dd-8f84-768e-bf70-5cc56aad95bd`, 2026-04-26). Even with `EmbeddingBackfillWorker` registered per-type under `WorkerSupervisor`, the auto-spawn handshake at the `analyze.tool.ts` / `batch-analyze.tool.ts` invocation boundary was not wired, so production backfill drain depended on manual intervention. This PR consolidates 4 areas: (1) auto-spawn handshake (Wave 1) — `apps/mcp-server/src/tools/page/_shared/worker-bootstrap.ts` (NEW per Plan v1.1 §5.1.17) staggers `getWorkerSupervisor().ensureAllWorkersRunningStaggered()` to establish primary `page` heartbeat → ordered secondary `embedding-backfill` spawn, with `ENABLE_BACKFILL_AUTOSPAWN` env-var (strict `=== "true"` parse) as a fail-safe rollback gate; (2) Vision unload precondition (UNB-IMPL-1) — `apps/mcp-server/src/services/vision/vision-unload-handshake.ts` (NEW, 229 LoC) queries Ollama `/api/ps` via 3s timeout, defers secondary spawn + emits audit_logs when Vision residual is loaded, preserving 32GB tier RSS budget health (avoiding ~10.6GB Vision + ~1.9GB child + ~4GB peak page = ~16.5GB); on probe failure: **fail-closed** (GDPR Art.32(1)(b) integrity); (3) responsive backfill handler (Wave 3) — adds `AND EXISTS (SELECT 1 FROM web_pages WHERE id = ?::uuid)` defensive predicate to `embedding-backfill.service.ts:1050-1075` + `:1090-1104`, guaranteeing the GDPR Art.17 `data.delete` synchronous-deletion TOCTOU resurrection invariant (inheriting PR2d LCC-M-2 contract); (4) parts.visual bbox resolution (Wave 4) — adds failure classification (iframe/shadow_dom/dom_disposed/unknown) to `apps/mcp-server/src/services/part/part-bbox-playwright.service.ts` + extracts `classifyBboxFailure` private helper (CC delta ≤ +2), emitting `bbox_unresolvable` skipReason routed through the `audit_logs.embedding_part_visual_skipped` SSOT (`apps/mcp-server/src/audit/audit-actions.ts`). Standing regression: INV-WORKER-LOCK-003 case #15-#19 (consolidated into `inv-worker-lock-003-embedding-backfill-supervisor.test.ts:1928-2042`) + INV-EMBEDDING-INTEGRITY-001 case #1-#6 (`inv-embedding-integrity-001-bbox-and-responsive.test.ts` NEW 613 LoC 10 tests) + INV-SCHEMA-ENUM-004 16-value `EmbeddingSkipReason` exhaustiveness (TS-only `as const` SSOT per ADR-0018 §Decision 1 Supplement) + Integration drain test `tests/integration/page-analyze-backfill-drain.test.ts` (NEW 293 LoC 4 tests, `process.env.CI` gate, fixture URL allowlist 127.0.0.1:38765 + 4 negative SSRF tests CWE-918). New environment variables: `ENABLE_BACKFILL_AUTOSPAWN` (default true), `BBOX_RESOLVE_RELOAD_ENABLED` (default false), `BBOX_RESOLVE_RELOAD_ENABLED_MAX_RELOADS_PER_PAGE` (default 5), `BBOX_RESOLVE_RELOAD_TOTAL_TIMEOUT_MS` (default 60000). WorkerSupervisor LoC 1499 → **1497** (-2 LoC delta; CO-PRDD9-01 contract STRENGTHENED). **Backward compatible** (no breaking change).
  - **Related contracts / 関連契約**: ADR-0011 Amendment 2 (Vision unload precondition handshake at `apps/mcp-server/src/services/vision/ollama-readiness-probe.ts` + 7-component RSS budget table); ADR-0018 §Decision 1 Supplement (EmbeddingSkipReason TS-only `as const` SSOT extension to 16 values + `audit_logs.action` SSOT constants per `apps/mcp-server/src/audit/audit-actions.ts`); INV-WORKER-LOCK-003 (5 new cases #15-#19); INV-EMBEDDING-INTEGRITY-001 (6 new cases #1-#6); INV-SCHEMA-ENUM-004 (16-value EmbeddingSkipReason exhaustiveness); INV-DATA-DELETE-002 (cross-tag for case #5 GDPR Art.17 TOCTOU).
  - **IO anchors / IO anchor**: SEC sign-off `019dc95d-c617-7cd1-a7c5-bb778f5b6739` + LCC sign-off `019dc95f-575c-7459-af8b-8da9562e9297` → IO Impl Decision §10 `019dc9a7-4a77-7a7f-812d-d09da79f7b25` → UNB-IMPL-3 TPA targeted re-audit APPROVE 96/100 `019dc9ca-9346-7237-8b60-bbd635faf803` → UNB-IMPL-4 IO Impl Re-Decision §11 APPROVE → Phase 3 docs-sync GO.
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.5(1)(d) accuracy**: backfill drain reliability supervised — eliminates persistent stall window
    - **[EU] GDPR Art.17(1)(a) Right to Erasure**: synchronous deletion contract preserved + defensive WHERE clause prevents post-deletion resurrection (TOCTOU invariant per INV-EMBEDDING-INTEGRITY-001 case #5)
    - **[EU] GDPR Art.30 records of processing activities**: 4 `audit_logs` actions wired through SSOT; 365-day retention + `truncateId` PII minimisation preserved
    - **[EU] GDPR Art.32(1)(b) ongoing resilience**: Vision unload precondition + autospawn fail-closed (RSS budget integrity)
    - **[JP] APPI 第23条 (安全管理措置) / Article 23 (Security control measures)**: technical safeguarding measures extended to backfill auto-spawn boundary
    - **[US] CCPA §1798.150 reasonable security**: proactive Worker-lifecycle audit trail + fail-closed defaults
    - **[EU] EU CRA 2024/2847 (2026-09-11 in force)**: CycloneDX 1.6 SBOM drift 0; Vision-unload handshake + defensive WHERE clause as state-of-the-art evidence
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — PR-D-9 は既存 `@prisma/client` / `bullmq` / `zod` / `ioredis` / `audit-log.service` / `truncateId` SSOT API のみ使用。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ No new dependency. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.

- **feat(worker-supervisor): PR-D-8 extend WorkerSupervisor scope to EmbeddingBackfillWorker (per-type Map + 9 T1 must-fix contracts + 4 audit_logs actions) (v0.4.0 PR-D-8) [EU GDPR Art.30/32(1)(b)][JP APPI 第23条][US CCPA §1798.150][EU CRA 2024/2847]** / **feat(worker-supervisor): PR-D-8 extend WorkerSupervisor scope to EmbeddingBackfillWorker (v0.4.0 PR-D-8)**:
  - **JP**: PR-D-7 empirical verification (linear.app, 2026-04-24) と stripe.com 再現観測 (2026-04-25) で `EmbeddingBackfillWorker` が `WorkerSupervisor` scope 外で稼働している設計 gap を確認。`workerArgs: ["--page"]` ハードコード + `WorkerType="page"` 単一 union により RSS 閾値超過時 self-exit 後の OS-level restart 経路欠落 → production backfill drain が手動介入依存 (GDPR Art.32(1)(b) "ongoing resilience of processing systems" 劣化、APPI 第23条 安全管理措置 scope)。本 PR で `WorkerSupervisor` を **per-type Map 設計 (Option C)** に拡張し、9 T1 Canonical must-fix contracts (Registry v3 §20.1) を landing: (1) per-type child Map (`children: Map<WorkerType, WorkerChildState>`) + (2) IPC binding table dispatch + (3) 5-rule boot token contract (per-type isolation: `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN_PAGE` / `_BACKFILL`) + (4) self-chained respawn 3-layer protocol (Layer 1 release-retry budget=3 / Layer 2 `probeExistingLock` 3000ms timeout / Layer 3 60s TTL fallback) + (5) 4 audit_logs actions wiring (`worker_supervisor_restart` / `worker_type_spoofing_detected` / `worker_lock_ttl_fallback` / `worker_ipc_spoofing_detected` 全 365日保持 + actor=`system:worker-supervisor` + `targetId` `truncateId` PII-truncate + `details` raw payload 不含 = CWE-209 + SEC-V11-01 PII minimisation 保全) + (6) ESLint complexity gate (10 threshold) + (7) WorkerType SSOT (`apps/mcp-server/src/types/worker-type.ts`, 82 LoC) + (8) IPC Zod schema SSOT (`apps/mcp-server/src/schemas/worker-ipc.schema.ts`, 117 LoC, `parseWorkerIpcStrict`) + (9) `WorkerChildState` struct + helper module 抽出 (`worker-supervisor-helpers.ts`, 313 LoC, `verifyWorkerIpcMessage` + `emitSupervisorAuditLog` + binding-table dispatch)。Per-type Redis lock key 拡張: `reftrix:worker:active:page` (PageAnalyzeWorker) + `reftrix:worker:active:embedding-backfill` (EmbeddingBackfillWorker) — 両 key とも UUID nonce + 60s TTL + 30s heartbeat + Lua atomic release/extend を ADR-0011 §A から継承。Staggered scheduling (primary `page` 起動 → heartbeat 確立 → secondary `embedding-backfill` 起動) で 32GB tier RSS budget 健全性を担保 (TPA-V12-01 / TPA-V11-01 Ollama Vision unload coordination は PR-D-9 carryover)。MF-V12-01 unified fix で SEC-V12-01 H 🚫 (worker IPC spoofing audit emit) + LCC-V12-01 M 🚫 (Art.30 audit trail completeness) を同時 RESOLVED — `worker-supervisor-helpers.ts:308-317` で `emitSupervisorAuditLog('worker_ipc_spoofing_detected', expectedWorkerType, { pid, reason }, 'denied')` 発行 + `worker-supervisor.service.ts:839-852` で `dispatchVerifiedIpc` → `escalateSpoofing` (SIGTERM + 60s respawn suppress) wiring。MF-V12-02 で standing test 2 cases #13/#14 を新設 (unknown-workerType + schema-invalid IPC scenarios)。Standing regression INV-WORKER-LOCK-003 (14 cases / `inv-worker-lock-003-embedding-backfill-supervisor.test.ts`) + INV-SCHEMA-ENUM-004 (1 case / `inv-schema-enum-004-worker-type.test.ts`) + INV-DATA-DELETE-002 (audit_logs cross-ref) 全 PASS pass^3 3 consecutive。**8/8 H findings RESOLVED**, **1/1 M finding RESOLVED**, **accepted-risk 0** (Severity → Landing rule compliant)。LoC re-baseline: Plan v1.0 estimate 350-450 → 1849 measured (1499 supervisor + 350 helpers, +302%) — 9 T1 contracts 同 PR 内 absorption により justified、PR-D-9 class split deferred per CO-20 (deadline 2026-06-30) で contract drift 回避。Legacy `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN` deprecated (1-cycle backward compat preserved via fallback、`[DEPRECATION]` warn hard landing は CO-21 deadline 2026-05-31)。
  - **EN**: PR-D-7 empirical verification (linear.app, 2026-04-24) and stripe.com re-reproduction (2026-04-25) confirmed a design gap in which `EmbeddingBackfillWorker` operated **outside `WorkerSupervisor` scope**. Hardcoded `workerArgs: ["--page"]` + single-value `WorkerType="page"` union meant no OS-level restart path post-self-exit, so production backfill drain depended on manual intervention (GDPR Art.32(1)(b) "ongoing resilience of processing systems" degradation; in scope of APPI Article 23 安全管理措置 / safeguarding measures). This PR extends `WorkerSupervisor` to a **per-type Map design (Option C)** and lands 9 T1 Canonical must-fix contracts (Registry v3 §20.1): (1) per-type child Map (`children: Map<WorkerType, WorkerChildState>`) + (2) IPC binding-table dispatch + (3) 5-rule boot token contract (per-type isolation: `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN_PAGE` / `_BACKFILL`) + (4) self-chained respawn 3-layer protocol (Layer 1 release-retry budget=3 / Layer 2 `probeExistingLock` 3000ms / Layer 3 60s TTL fallback) + (5) 4 `audit_logs` action wiring (`worker_supervisor_restart` / `worker_type_spoofing_detected` / `worker_lock_ttl_fallback` / `worker_ipc_spoofing_detected`; 365-day retention + `actor="system:worker-supervisor"` + `targetId` truncated via `truncateId` + `details` excludes raw payload — CWE-209 + SEC-V11-01 PII minimisation preserved) + (6) ESLint complexity gate (threshold 10) + (7) `WorkerType` SSOT (`apps/mcp-server/src/types/worker-type.ts`, 82 LoC) + (8) IPC Zod schema SSOT (`apps/mcp-server/src/schemas/worker-ipc.schema.ts`, 117 LoC, `parseWorkerIpcStrict`) + (9) `WorkerChildState` struct + helper module extraction (`worker-supervisor-helpers.ts`, 313 LoC, `verifyWorkerIpcMessage` + `emitSupervisorAuditLog` + binding-table dispatch). Per-type Redis lock keys: `reftrix:worker:active:page` (PageAnalyzeWorker) + `reftrix:worker:active:embedding-backfill` (EmbeddingBackfillWorker) — both inherit UUID nonce + 60s TTL + 30s heartbeat + Lua atomic release/extend from ADR-0011 §A. Staggered scheduling (primary `page` start → heartbeat established → secondary `embedding-backfill` start) preserves 32GB tier RSS-budget health (TPA-V12-01 / TPA-V11-01 Ollama Vision unload coordination is carried over to PR-D-9). The MF-V12-01 unified fix simultaneously resolves SEC-V12-01 H 🚫 (worker IPC spoofing audit emit) and LCC-V12-01 M 🚫 (Art.30 audit-trail completeness) — `worker-supervisor-helpers.ts:308-317` emits `emitSupervisorAuditLog('worker_ipc_spoofing_detected', expectedWorkerType, { pid, reason }, 'denied')` and `worker-supervisor.service.ts:839-852` wires `dispatchVerifiedIpc` → `escalateSpoofing` (SIGTERM + 60s respawn suppress). MF-V12-02 lands 2 new standing test cases #13/#14 (unknown-workerType + schema-invalid IPC scenarios). Standing regression: INV-WORKER-LOCK-003 (14 cases / `inv-worker-lock-003-embedding-backfill-supervisor.test.ts`) + INV-SCHEMA-ENUM-004 (1 case / `inv-schema-enum-004-worker-type.test.ts`) + INV-DATA-DELETE-002 (audit_logs cross-ref) — all PASS pass^3 3 consecutive. **8/8 H findings RESOLVED**, **1/1 M finding RESOLVED**, **accepted-risk count 0** (Severity → Landing rule compliant). LoC re-baseline: Plan v1.0 estimate 350-450 → 1849 measured (1499 supervisor + 350 helpers, +302%) — justified by 9 T1 contracts absorbed in-PR; PR-D-9 class split deferred per CO-20 (deadline 2026-06-30) to avoid contract drift. Legacy `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN` is deprecated (1-cycle backward compat preserved via fallback; `[DEPRECATION]` warn hard landing tracked in CO-21, deadline 2026-05-31).
  - **Related contracts / 関連契約**: ADR-0011 Amendment 1 (PR-D-8) — per-type WorkerSupervisor scope expansion (§A.1 per-type Redis lock key + §A.2 GDPR Art.32(1)(b) processing-integrity expansion + §A.3 APPI 第23条 per-type expansion + §B.1 per-type BOOT_TOKEN secrets isolation + §B.2 legacy deprecation + §C staggered scheduling rationale + §D PR-D-9 class split footnote); INV-WORKER-LOCK-003 (expansion to embedding-backfill, +14 cases including v12 MF-V12-02 #13/#14); INV-SCHEMA-ENUM-004 (NEW WorkerType SSOT exhaustive switch); INV-DATA-DELETE-002 (audit_logs 4 new action cross-ref, GDPR Art.30 100%); 4-auditor evolution: TPA 88→42→92, SEC 90→42→78→**96** (+18), TDA 89→87→84 (CONDITIONAL absorbed via Phase 3 D-01), LCC 82→78→80→**96** (+16).
  - **IO anchors / IO anchor**: Plan Decision v1 CONDITIONAL → Impl Decision v1 CONDITIONAL → re-impl re-audit v4 CONDITIONAL targeted → MF-V12-01 + MF-V12-02 targeted re-fix → SEC v2 PASS 96 + LCC v2 PASS 96 → **IO Impl Decision v5 APPROVE** (Registry v5 §33, 2026-04-25) → Phase 3 docs-sync (Registry v5 §34.1 D-01..D-09) → Phase 4 Final Consistency Check + commit.
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.30 records of processing activities**: 4 new `audit_logs` actions wired (Worker lifecycle 100% audit trail completeness); `actor="system:worker-supervisor"` + 365-day retention inherits PR7d-3 precedent
    - **[EU] GDPR Art.32(1)(b) ongoing resilience**: Per-type WorkerSupervisor scope expansion completes processing-integrity contract for both `page` and `embedding-backfill` worker-types
    - **[EU] GDPR Art.5(1)(d) accuracy (downstream)**: Backfill drain reliability now supervised — eliminates stale window until BackfillReconciliationCron 1h sweep
    - **[JP] APPI 第23条 (安全管理措置) / Article 23 (Security control measures)**: Technical safeguarding measures (Worker dual-run prevention + boot-token authentication + IPC schema validation) extended to per-type Worker supervision
    - **[US] CCPA §1798.150 reasonable security**: Proactive Worker-lifecycle audit trail + IPC spoofing fail-closed (SIGTERM + 60s suppress)
    - **[EU] EU CRA 2024/2847 (2026-09-11 in force)**: CycloneDX 1.6 SBOM drift 0 (no new dependency added); Worker supervision hardening as state-of-the-art evidence
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — PR-D-8 は既存 `@prisma/client` / `bullmq` / `zod` / `ioredis` / `audit-log.service` / `truncateId` SSOT API のみ使用、外部パッケージ追加なし。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ No new dependency; PR-D-8 uses only existing `@prisma/client` / `bullmq` / `zod` / `ioredis` / `audit-log.service` / `truncateId` SSOT APIs. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.

- **feat(adr+sec): PR-D-7 ADR landing + CWE-209 sanitize carryover (5-shape whitelist enforcement) (v0.4.0 PR-D-7) [EU GDPR Art.5(1)(c)/5(1)(d)/30/32(1)(b)/33][JP APPI 第23条][US CCPA §1798.150][EU CRA 2024/2847]** / **feat(adr+sec): PR-D-7 ADR landing + CWE-209 sanitize carryover (5-shape whitelist enforcement) (v0.4.0 PR-D-7)**:
  - **JP**: PR-D-6 の BullMQ jobId collision 根治を受けて ADR-0016 (standing regression suite) を **Proposed → Accepted** に昇格、ADR-0018 (embedding processing integrity invariants) を **Draft → Accepted** に昇格。`race_lost_atomic` variant を **Option Z-a により型定義ごと削除** し、6-variant `EnqueueResult` discriminated union を **5-variant に narrow** (`enqueued_new` / `reused_active` / `enqueued_retry` / `limbo_forced` / `enqueued_fail_open`) — `limbo_forced` variant は ADR-0018 L1050 契約通り silent-no-op prevention を維持 (GDPR Art.5(1)(d) accuracy continuity)。CWE-209 sanitize pass-through を **5 sites で enforce** (`service-registrar-search.ts` L221 + `part-backfill.service.ts` L211/L223 + `background-design-embedding.service.ts` L472 + `responsive-analysis-embedding.service.ts` L387、合計 5 latent-risk sites を eradicate、EU CRA 2024/2847 "state-of-the-art" evidence として enumerate)。`start-workers.ts` の 9 catch blocks (L337 / L350 / L469 / L483 / L513 / L624 / L652 / L725 / L736) を `sanitizeErrorMessage(error)` 経由化 (FIND-PLAN-SEC-01 M severity resolved)。新規 `extractPrismaCode` helper を `utils/sanitize-error.ts` に landing (strict `/^P\d{4}$/` regex で Prisma error code のみ抽出、CWE-200 Information Exposure defense、object-form callers 向け forward-compat SSOT)。Standing regression +2 files / +35 CI-failing tests: `tests/tools/result-errors-shape.test.ts` (31 tests / INV-RESULT-ERRORS-SHAPE-001 expansion / 5-whitelisted shapes enforcement for H binding FIND-PLAN-TPA-01) + `tests/scripts/start-workers-sanitize.test.ts` (4 tests / CWE-209 AST fail-if-matched regression guard)。ADR-0018 §3.9 wording を "Object-form canonical" から **"5-shape whitelist per §3.5.2 Step 5 (PR-D-7)"** に update (shapes (a) `string[]` / (b) `{name, error}[]` / (c) `{id, error}[]` / (d) `{sectionId, error}[]` / (e) `{patternId, error}[]` の 5-shape T1 production reality を明示、raw `error.message` push 禁止)。ADR-0018 Amendment 6 L984 "6-variant" wording に PR-D-7 Option Z-a landing annotation を付与 (5-variant 化 + limbo_forced silent-no-op prevention 継続の cross-ref)。
  - **EN**: Following PR-D-6's BullMQ jobId collision root-cause eradication, promotes ADR-0016 (standing regression suite) from **Proposed → Accepted** and ADR-0018 (embedding processing integrity invariants) from **Draft → Accepted**. Removes `race_lost_atomic` variant entirely via Option Z-a, narrowing the 6-variant `EnqueueResult` discriminated union to a **5-variant union** (`enqueued_new` / `reused_active` / `enqueued_retry` / `limbo_forced` / `enqueued_fail_open`) — the `limbo_forced` variant preserves silent-no-op prevention per ADR-0018 L1050 contract (GDPR Art.5(1)(d) accuracy continuity). Enforces CWE-209 sanitize pass-through across **5 sites** (`service-registrar-search.ts` L221 + `part-backfill.service.ts` L211/L223 + `background-design-embedding.service.ts` L472 + `responsive-analysis-embedding.service.ts` L387; 5 latent-risk sites eradicated, enumerated as EU CRA 2024/2847 "state-of-the-art" evidence). Routes `start-workers.ts` 9 catch blocks (L337 / L350 / L469 / L483 / L513 / L624 / L652 / L725 / L736) through `sanitizeErrorMessage(error)` (FIND-PLAN-SEC-01 M severity resolved). Lands the new `extractPrismaCode` helper in `utils/sanitize-error.ts` (strict `/^P\d{4}$/` regex, extracting only valid Prisma error codes, CWE-200 Information Exposure defense, forward-compatible SSOT for object-form callers). Standing regression: +2 files / +35 CI-failing tests: `tests/tools/result-errors-shape.test.ts` (31 tests / INV-RESULT-ERRORS-SHAPE-001 expansion / 5-whitelisted shapes enforcement for H binding FIND-PLAN-TPA-01) + `tests/scripts/start-workers-sanitize.test.ts` (4 tests / CWE-209 AST fail-if-matched regression guard). Updates ADR-0018 §3.9 wording from "Object-form canonical" to **"5-shape whitelist per §3.5.2 Step 5 (PR-D-7)"** (making explicit the 5-shape T1 production reality — (a) `string[]` / (b) `{name, error}[]` / (c) `{id, error}[]` / (d) `{sectionId, error}[]` / (e) `{patternId, error}[]` — with raw `error.message` push prohibited). Adds a PR-D-7 Option Z-a landing annotation to ADR-0018 Amendment 6 L984 "6-variant" wording (5-variant narrowing + `limbo_forced` silent-no-op prevention cross-ref).
  - **CWE-209 5 latent-risk sites eliminated (EU CRA 2024/2847 state-of-the-art evidence) / CWE-209 5 latent-risk site 除去 (EU CRA 2024/2847 state-of-the-art 証跡)**:
    1. `apps/mcp-server/src/services/service-registrar-search.ts` L221 — `string[]` shape, Wave 3 landed
    2. `apps/mcp-server/src/services/part/part-backfill.service.ts` L211 — `string[]` shape, Wave 3 landed
    3. `apps/mcp-server/src/services/part/part-backfill.service.ts` L223 — `string[]` shape, Wave 3 landed
    4. `apps/mcp-server/src/services/background/background-design-embedding.service.ts` L472 — `{name, error}[]` shape, Wave 7.1 landed (UB-TPA-IMPL-01 RESOLVED)
    5. `apps/mcp-server/src/services/responsive/responsive-analysis-embedding.service.ts` L387 — `{id, error}[]` shape, Wave 7.1 landed (UB-TPA-IMPL-02 RESOLVED)
  - **Related contracts / 関連契約**: ADR-0016 (Proposed → Accepted, LCC cross-sign inherited from PR-D-5/PR-D-6 chain); ADR-0018 (Draft → Accepted, Amendment 6 L1039 LCC chain preserved); ADR-0018 §3.9 5-shape whitelist (PR-D-7 wording update, sanitizeErrorMessage SSOT binding); ADR-0018 Amendment 6 L984 (PR-D-7 Option Z-a annotation); INV-RESULT-ERRORS-SHAPE-001 (H binding, 31 CI-failing cases).
  - **IO anchors / IO anchor**: Plan Decision v1 CONDITIONAL `019db86c-e190-75bc-a791-b624ed608a89` → v2 APPROVE `019db8a7` / Impl Decision v1 CONDITIONAL `019dbc4b-3043-784d-a115-1d3c1ca56591` → Wave 7.1 landing (3 UB RESOLVED) → Wave 7.2 TPA targeted re-audit APPROVE `019dbc56-4aa1-71d2-a116-4ece70a70033` (Arch 93 → 97, H FULLY RESOLVED, CWE-209 leakage = 0) → Impl Decision v2 APPROVE `019dbc5b-ba38-7af0-816a-bbf860bda623` / Phase 3 Wave 10 docs-sync binding (10 items per Registry v4 §28.2).
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.5(1)(c) data minimisation**: `truncateId` SSOT preserved (Amendment 6 L1049 継承)
    - **[EU] GDPR Art.5(1)(d) accuracy**: atomic SETNX + 5-variant `EnqueueResult` + `limbo_forced` silent-no-op prevention maintained per ADR-0018 L1050
    - **[EU] GDPR Art.30 audit trail**: ADR-0016 / ADR-0018 LCC cross-sign chains preserved; PR-D-6 2 audit actions (`embedding_backfill_collision_resolved` + `page_analyze_collision_resolved`) continue 365-day retention
    - **[EU] GDPR Art.32(1)(b) integrity**: `sanitizeErrorMessage` SSOT continuity strengthened across 5 new enforcement sites + 9 `start-workers.ts` catch blocks
    - **[EU] EU CRA 2024/2847 (2026-09-11 in force)**: CycloneDX 1.6 SBOM drift 0 (no new dependency added); CWE-209 state-of-the-art evidence enumerated across 5 sites
    - **[JP] APPI 第23条 (安全管理措置) / Article 23 (Security control measures)**: technical measures strengthened via CWE-209 eradication + audit trail continuity
    - **[US] CCPA §1798.150 reasonable security**: proactive security measures reinforced (sanitize pass-through + AST regression guard)
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — PR-D-7 は既存 `sanitizeErrorMessage` / `extractPrismaCode` (新 helper、同一 file 内) / `bullmq` / `zod` / `@prisma/client` の existing API のみ使用。外部パッケージ追加ゼロ。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ No new dependency; PR-D-7 uses only existing `sanitizeErrorMessage` / `extractPrismaCode` (new helper in the same file) / `bullmq` / `zod` / `@prisma/client` APIs. Zero external package additions. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.

- **feat(queue): PR-D-6 RC-A BullMQ jobId collision 根治 + atomic SETNX Lua + 6-variant EnqueueResult + both-queue QueueEvents listener wiring + 2 new audit actions (ADR-0018 Amendment 6) (v0.4.0 PR-D-6) [EU GDPR Art.30/5(1)(c)/33][JP APPI 第23条][US CCPA §1798.150]** / **feat(queue): PR-D-6 RC-A BullMQ jobId collision eradication + atomic SETNX Lua + 6-variant EnqueueResult + both-queue QueueEvents listener wiring + 2 new audit actions (ADR-0018 Amendment 6) (v0.4.0 PR-D-6)**:
  - **JP**: stripe.com 実証 (`019da991`, 2026-04-20) で 7/7 category に 16-21h Redis/DB drift が観測された **RC-A (BullMQ jobId 衝突による silent no-op)** を根治。ADR-0018 §Decision 4 の 3 state-branching 契約 (completed/failed → re-enqueue / active/waiting/delayed → trust existing / unknown → ADR-0017 limbo) を runtime 実装 + atomic SETNX Lua pattern (WorkerActiveLockService precedent 流用) で TOCTOU race を根絶。SSOT helper `addEmbeddingBackfillJobWithGuard` (`apps/mcp-server/src/queues/embedding-backfill-queue.ts` L810-847) + `addPageAnalyzeJobWithGuard` (`apps/mcp-server/src/queues/page-analyze-queue.ts` L729-749) を新設し、generic `enqueueWithCollisionGuard` (`enqueue-with-collision-guard.ts`, 5 sub-handler dispatch + 6-variant `EnqueueResult` discriminated union) に委譲。`EnqueueResult` 6-variant = `enqueued_new` / `reused_active` / `enqueued_retry` / `limbo_forced` / `enqueued_fail_open` / `race_lost_atomic` (§Decision 4 case (c) `state === "unknown"` → `limbo_forced` emit contract alignment, FIND-TPA-IMPL-01 closure)。両 queue の `QueueEvents "duplicated"` listener を `start-workers.ts` で boot 時に wire + `shutdownWorkers` lifecycle で detach (`registerEmbeddingBackfillDuplicatedListener` embedding-backfill-queue.ts L872 + `registerPageAnalyzeDuplicatedListener` page-analyze-queue.ts L767、FIND-TPA-IMPL-02 RESOLVED + FIND-SEC-04 CLOSED-COCLOSE)。2 new audit actions を `audit_logs.action` 列挙集に append: (a) `embedding_backfill_collision_resolved` (actor `system:embedding-backfill-queue`, primary emit via `emitCollisionAudit`, Zod `.strict()` `CollisionAuditPayloadSchema` 5-field contract: `webPageId` 11-char / `originalJobId` regex-pinned / `retryJobId` regex-pinned / `originalState` enum / `timestamp` ISO-8601) (b) `page_analyze_collision_resolved` (actor `system:page-analyze-queue`, observability-only emit via `emitPageAnalyzeCollisionAudit`, Registry v3 §3 FIND-TPA-02 binding per observability-only scope)。両 action とも既存 365 日 retention + `truncateId` SSOT PII minimisation (utils/truncate-id.ts L17) + `system:<role>` actor naming convention を継承 (GDPR Art.30 audit trail continuity、domain-specific actor naming は T1 SoT Hierarchy resolution、FIND-LCC-IMPL-01 closure)。Retry jobId suffix は Node.js 20.19+ stdlib `crypto.randomUUID({version:7})` 採用 — **新規 npm 依存 0 (CycloneDX 1.6 SBOM drift 0、EU CRA 2026-09-11 compliance 保持)**。SLO tier mapping: 両 new action は L0/L1/L2 source として既存 5-tier framework に参加 (L3 CRIT breach notification gate は `embedding_parity_schema_drift` sentinel 専用で不変)。Standing regression +17 tests を 2 new file で landing: `worker-lifecycle/inv-worker-lock-003-jobid-collision.test.ts` (12 tests = Block A AST source-pin 4 + Block B fixture behavior 4 + Block C real Prisma + real Redis integration 4, INV-WORKER-LOCK-003 expansion) + `large-page/inv-page-queue-001-e-jobid-concurrent.test.ts` (5 tests = Block A 1 + Block B 2 + Block C 2, INV-PAGE-QUEUE-001-E expansion)。Dispatcher `handleCollisionEnqueue` cyclomatic complexity 実測 = 7、ESLint `complexity: ["error", 10]` threshold 内 (FIND-TDA-IMPL-01 closure)。`race_lost_atomic` variant の production emit site は 0 件の pre-existing design gap として **PR-D-7 carryover** (FIND-IO-PR-D-6-IMPL-04 Option Z: 型定義削除 or 実 emit 経路追加 + ADR Amendment 7 authoring を PR-D-7 で joint 判断、deadline 2026-05-30)。ADR-0018 Amendment 6 新設 (§Decision 4 case(c) emit contract + §Privacy Considerations Update 2 new audit actions + §Test Coverage + §Implementation Notes + §Notes + LCC cross-sign footer、PR-D-5 Amendment 5 anchor `019db52e` 継承 cross-sign chain)、DATA_RETENTION.md §Embedding Integrity SLO subsection に 2 row append + Changelog v2.14.0 entry 追加 (FIND-LCC-02 M binding)。
  - **EN**: Eradicates **RC-A (BullMQ jobId collision silent no-op)** — empirically confirmed on stripe.com (`019da991`, 2026-04-20) with 16–21h Redis/DB drift across all 7 categories. Runtime-lands ADR-0018 §Decision 4's 3 state-branching contract (completed/failed → re-enqueue / active/waiting/delayed → trust existing / unknown → ADR-0017 limbo) AND adds an atomic SETNX Lua pattern (reusing the WorkerActiveLockService precedent) to eradicate the TOCTOU race surface. Introduces SSOT helpers `addEmbeddingBackfillJobWithGuard` (`apps/mcp-server/src/queues/embedding-backfill-queue.ts` L810-847) + `addPageAnalyzeJobWithGuard` (`apps/mcp-server/src/queues/page-analyze-queue.ts` L729-749) delegating to generic `enqueueWithCollisionGuard` (`enqueue-with-collision-guard.ts`, 5 sub-handler dispatch + 6-variant `EnqueueResult` discriminated union). The `EnqueueResult` 6-variant = `enqueued_new` / `reused_active` / `enqueued_retry` / `limbo_forced` / `enqueued_fail_open` / `race_lost_atomic` (§Decision 4 case (c) `state === "unknown"` → `limbo_forced` emit contract alignment, FIND-TPA-IMPL-01 closure). Both queues' `QueueEvents "duplicated"` listeners are wired in `start-workers.ts` at boot time with lifecycle detach in `shutdownWorkers` (`registerEmbeddingBackfillDuplicatedListener` embedding-backfill-queue.ts L872 + `registerPageAnalyzeDuplicatedListener` page-analyze-queue.ts L767; FIND-TPA-IMPL-02 RESOLVED + FIND-SEC-04 CLOSED-COCLOSE). 2 new audit actions are appended to the `audit_logs.action` enumeration: (a) `embedding_backfill_collision_resolved` (actor `system:embedding-backfill-queue`, primary emit via `emitCollisionAudit`, Zod `.strict()` `CollisionAuditPayloadSchema` 5-field contract: `webPageId` 11-char / `originalJobId` regex-pinned / `retryJobId` regex-pinned / `originalState` enum / `timestamp` ISO-8601) and (b) `page_analyze_collision_resolved` (actor `system:page-analyze-queue`, observability-only emit via `emitPageAnalyzeCollisionAudit`, Registry v3 §3 FIND-TPA-02 binding per observability-only scope). Both inherit the existing 365-day retention + `truncateId` SSOT PII minimisation (utils/truncate-id.ts L17) + `system:<role>` actor naming convention (GDPR Art.30 audit trail continuity; domain-specific actor naming reflects T1 SoT Hierarchy resolution, FIND-LCC-IMPL-01 closure). Retry jobId suffix uses the Node.js 20.19+ stdlib `crypto.randomUUID({version:7})` — **zero new npm dependency (CycloneDX 1.6 SBOM drift zero, EU CRA 2026-09-11 compliance preserved)**. SLO tier mapping: both new actions participate in the existing 5-tier framework as L0/L1/L2 sources (the L3 CRIT breach-notification gate remains exclusively triggered by the `embedding_parity_schema_drift` sentinel). Standing regression +17 tests landed across 2 new files: `worker-lifecycle/inv-worker-lock-003-jobid-collision.test.ts` (12 tests = Block A AST source-pin 4 + Block B fixture behavior 4 + Block C real Prisma + real Redis integration 4, INV-WORKER-LOCK-003 expansion) + `large-page/inv-page-queue-001-e-jobid-concurrent.test.ts` (5 tests = Block A 1 + Block B 2 + Block C 2, INV-PAGE-QUEUE-001-E expansion). Measured cyclomatic complexity of `handleCollisionEnqueue` dispatcher = 7, within the ESLint `complexity: ["error", 10]` threshold (FIND-TDA-IMPL-01 closure). The `race_lost_atomic` variant has zero production emit sites as a pre-existing design gap, **carried over to PR-D-7** (FIND-IO-PR-D-6-IMPL-04 Option Z: joint decision between removing the type-union variant vs. adding an explicit emit branch + ADR Amendment 7 authoring, PR-D-7 deadline 2026-05-30). New ADR-0018 Amendment 6 (§Decision 4 case(c) emit contract + §Privacy Considerations Update for 2 new audit actions + §Test Coverage + §Implementation Notes + §Notes + LCC cross-sign footer, inheriting the PR-D-5 Amendment 5 anchor `019db52e` cross-sign chain); DATA_RETENTION.md §Embedding Integrity SLO subsection appended with 2 new rows + Changelog v2.14.0 entry (FIND-LCC-02 M binding).
  - **Related contracts / 関連契約**: ADR-0018 Amendment 6 (§Context / §Decision 4 case (c) emit contract alignment / §Privacy Considerations Update for 2 new audit actions / §Test Coverage Block A/B/C distribution / §Implementation Notes dispatcher CC=7 + `race_lost_atomic` Option Z carryover / §Notes both-queue listener wiring / §Footer LCC cross-sign); INV-WORKER-LOCK-003 (expansion, +12 tests); INV-PAGE-QUEUE-001-E (NEW, +5 tests); `audit_logs` 2 new action values (`embedding_backfill_collision_resolved` L0/L1/L2 + `page_analyze_collision_resolved` L0/L1/L2).
  - **IO anchors / IO anchor**: Plan Decision v1 CONDITIONAL `019db568-990f-73f4-8d04-cbf0d18f5266` → v2 CONDITIONAL `019db584-d1da-7d5a-b108-57ee9c14875d` → v3 APPROVE `019db599-627e-70d1-8c4b-ad6ea39631bc` / Impl Decision v1 Round 2 Impl CONDITIONAL `019db677-79fc-7f53-a57f-353fdf115666` → v2 Round 3 Impl APPROVE (Registry v5 §16 Phase 3 Docs-Sync Dispatch Binding authoring, post-Round-3 targeted TPA re-audit APPROVE Arch 91 → 94 `019db694-a486-7f4a-a130-59e5121d9201`) / Phase 3 docs-sync binding: ADR-0018 Amendment 6 + DATA_RETENTION.md §Embedding Integrity SLO (2 row append + v2.14.0 entry) + Plan v1.3 wording patch + page-analyze-queue.ts SPDX header + race_lost_atomic Option Z note.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — UUIDv7 via Node.js 20.19+ stdlib `crypto.randomUUID({version:7})`、既存 `@prisma/client` / `audit-log.service` / `bullmq` / `zod` / `ioredis` API のみ使用、外部パッケージ追加なし。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ No new dependency; UUIDv7 uses the Node.js 20.19+ stdlib `crypto.randomUUID({version:7})`; only existing `@prisma/client` / `audit-log.service` / `bullmq` / `zod` / `ioredis` APIs used. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.

- **feat(embedding): PR-D-5 INV-EMBEDDING-INTEGRITY-001 Partial → Full landing + audit_logs DB write + 5-tier SLO + 9 loc sanitize (ADR-0018 Amendment 5) (v0.4.0 PR-D-5)** / **feat(embedding): PR-D-5 INV-001 Partial → Full landing + audit_logs DB write + 5-tier SLO + 9 loc sanitize (ADR-0018 Amendment 5) (v0.4.0 PR-D-5)**:
  - **JP**: `INV-EMBEDDING-INTEGRITY-001` が **Partial → Full** に昇格。新設 dedicated test file `apps/mcp-server/tests/regression/standing/embedding-integrity/inv-embedding-integrity-001-parity.test.ts` (12 tests = Block A AST precondition 4 + Block B fixture-based returnvalue parity 4 + Block C integration-level runtime parity 4) で per-job `returnvalue.generatedCount` vs DB `SELECT COUNT(*)` strict equality を source-pin + fixture propagation + real Prisma DB-state の 3-layer defense で verify。Block C は Option C trade-off により raw SQL UPDATE helpers (`writeTextEmbedding` / `writeVisualEmbedding`) で DB-write side を exercise し、ONNX Runtime / BullMQ / Playwright infrastructure surface への露出を回避 (ADR-0018 Amendment 5 §Block C Implementation Trade-off に rationale + 3-layer defense + carryover path 記録)。standing regression `embedding-integrity` ドメイン: 21 → 33 tests (+12 net)。`audit_logs.embedding_parity_check_failed` (primary emit) + `audit_logs.embedding_parity_schema_drift` (sentinel emit) の 2 action を DB 書込で landing し、PR-D-4 Amendment 4 §Privacy Considerations accepted-risk (deadline 2026-05-09) を formal closure (GDPR Art.30 Record of Processing preserved、`logger.warn` 5-field contract は dual-emit として継続)。Runtime Set-equality enforcement を `detectCategoryDrift` helper + `pickKnownKeys` filter として `apps/mcp-server/src/utils/pick-known-keys.ts` に landing — `EMBEDDING_BACKFILL_CATEGORIES` SSOT と `pendingSnapshot` keys の missing / unexpected 双方向 drift を write-time で再検証 (PR-D-4 `collectCategoryPendingSnapshot` collect-time guard との co-existence は ADR-0018 Amendment 5 §Drift Detection Functional Delta に matrix 化)。5-tier SLO monitoring: L0 OK / L1 WARN (1 ≤ count < 10) / **L1.5 SLO_MARKER (NEW, log-based metric)** / L2 ALERT (count ≥ 10) / L3 CRIT (sentinel drift ≥ 1、GDPR Art.33 72h breach notification gate trigger)。L1.5 は primary emit fail-open tension を `[SLO_MARKER] audit_log_emit_failed` log line aggregation で補完 (observability blind spot 解消)。`embedding-backfill.service.ts` の 9 loc (L834 / L880 / L941 / L996 / L1031 / L1129 / L1164 / L1439 / L1472) + reconcile & repair path (`reconcile-backfill.ts:212` + FIND-IMPL-IO-15) に `sanitizeErrorMessage` を適用し CWE-209 Information Exposure Through an Error Message hardening (SEC-S-01 deadline 2026-05-04 strict resolved、11/11 sanitize points 達成)。ESLint complexity 実測: `detectCategoryDrift` = 3 / `emitParityCheckFailedIfEnabled` = 5 / `emitCategoryDriftSentinel` = 2 / `pickKnownKeys` = 3 — 全て < 10 threshold (Plan estimate より tight)。`DATA_RETENTION.md` §Embedding Integrity SLO subsection + §変更履歴 v2.13.0 entry 新設 (FIND-LCC-02 M deadline 2026-05-04 binding)、ADR-0018 Amendment 5 新設 (9 subsection、LCC cross-sign footer、PR-D-4 Amendment 4 先例踏襲)。
  - **EN**: `INV-EMBEDDING-INTEGRITY-001` promoted **Partial → Full**. Introduces a dedicated test file `apps/mcp-server/tests/regression/standing/embedding-integrity/inv-embedding-integrity-001-parity.test.ts` (12 tests = Block A AST precondition 4 + Block B fixture-based returnvalue parity 4 + Block C integration-level runtime parity 4) verifying per-job `returnvalue.generatedCount` vs DB `SELECT COUNT(*)` strict equality via a 3-layer defense (source-pin + fixture propagation + real Prisma DB-state). Under the Option C trade-off, Block C exercises the DB-write side via raw SQL UPDATE helpers (`writeTextEmbedding` / `writeVisualEmbedding`) to avoid exposing the ONNX Runtime / BullMQ / Playwright infrastructure surface (rationale + 3-layer defense + carryover path recorded in ADR-0018 Amendment 5 §Block C Implementation Trade-off). `embedding-integrity` standing regression domain: 21 → 33 tests (+12 net). Lands `audit_logs.embedding_parity_check_failed` (primary emit) + `audit_logs.embedding_parity_schema_drift` (sentinel emit) as real DB writes, formally closing the PR-D-4 Amendment 4 §Privacy Considerations accepted-risk (deadline 2026-05-09) while preserving GDPR Art.30 Records of Processing (the `logger.warn` 5-field contract continues as a dual-emit). Adds runtime Set-equality enforcement via `detectCategoryDrift` helper + `pickKnownKeys` filter in `apps/mcp-server/src/utils/pick-known-keys.ts`, revalidating at write-time any missing / unexpected drift between the `EMBEDDING_BACKFILL_CATEGORIES` SSOT and `pendingSnapshot` keys (co-existence with the PR-D-4 collect-time guard `collectCategoryPendingSnapshot` is formalized as a matrix in ADR-0018 Amendment 5 §Drift Detection Functional Delta). 5-tier SLO monitoring: L0 OK / L1 WARN (1 ≤ count < 10) / **L1.5 SLO_MARKER (NEW, log-based metric)** / L2 ALERT (count ≥ 10) / L3 CRIT (sentinel drift ≥ 1, triggers the GDPR Art.33 72h breach-notification gate). L1.5 compensates the primary-emit fail-open tension via `[SLO_MARKER] audit_log_emit_failed` log-line aggregation, eliminating the observability blind spot. Applies `sanitizeErrorMessage` to 9 loc in `embedding-backfill.service.ts` (L834 / L880 / L941 / L996 / L1031 / L1129 / L1164 / L1439 / L1472) + the reconcile & repair path (`reconcile-backfill.ts:212` + FIND-IMPL-IO-15), hardening CWE-209 Information Exposure Through an Error Message (SEC-S-01 deadline 2026-05-04 strict resolved; 11/11 sanitize points landed). Measured ESLint complexity: `detectCategoryDrift` = 3 / `emitParityCheckFailedIfEnabled` = 5 / `emitCategoryDriftSentinel` = 2 / `pickKnownKeys` = 3 — all under the < 10 threshold and tighter than the Plan estimate. New `DATA_RETENTION.md` §Embedding Integrity SLO subsection + §Changelog v2.13.0 entry (FIND-LCC-02 M deadline 2026-05-04 binding); new ADR-0018 Amendment 5 (9 subsections, LCC cross-sign footer, following the PR-D-4 Amendment 4 precedent).
  - **Block C Implementation Trade-off (documented via ADR Amendment 5)**: TPA targeted re-audit (2026-04-22, 91/100) が Option C を recommend、IO Impl Decision v3.1 で CONDITIONAL → APPROVE 昇格。Block C pseudocode element (5) real `processBackfillJob` invocation を raw SQL UPDATE helper に narrow、3-layer defense (Block A AST source-pin + Block B fixture propagation + Block C real Prisma DB-state) で INV-001 contract semantic equivalence を証明。Full invocation integration fidelity は FIND-TPA-REAUDIT-01 (L, PR-D-6 candidate) として carryover tracked. / The TPA targeted re-audit (2026-04-22, 91/100) recommended Option C and the IO Impl Decision v3.1 upgraded CONDITIONAL → APPROVE accordingly. Block C pseudocode element (5) real `processBackfillJob` invocation was narrowed to raw SQL UPDATE helpers; the 3-layer defense proves INV-001 contract semantic equivalence. Full `processBackfillJob` integration fidelity is tracked as FIND-TPA-REAUDIT-01 (L, PR-D-6 candidate).
  - **Related contracts / 関連契約**: ADR-0018 Amendment 5 (§Context / §Decision 3 DI prerequisite / §Privacy Considerations Update (a) fail-open tension (b) duplicate details / §Drift Detection Functional Delta / §Breach Notification Gate (Art.33) / §Block C Implementation Trade-off / §Accepted-risk start-workers.ts 9 loc / §Footer LCC cross-sign); INV-EMBEDDING-INTEGRITY-001 (Full); INV-EMBEDDING-INTEGRITY-003 (co-existence unchanged); INV-SEC-SANITIZE (CWE-209 coverage 11/11 sanitize points).
  - **IO anchors / IO anchor**: Plan Decision (Finding Registry v2.0, 2026-04-22) CONDITIONAL → documentation-only re-audit APPROVE 昇格 / Impl Decision v3.1 APPROVE (TPA 91 / SEC 98 / TDA 98 / LCC 94 post-targeted-reaudit) / Phase 3 docs-sync binding: ADR-0018 Amendment 5 + DATA_RETENTION.md §Embedding Integrity SLO + Plan §2.3 / §3.2 footnotes + Plan §12 Step 6 CC pinned expectations + CHANGELOG + current-architecture.md +
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — 本 PR は既存 `@prisma/client` / `audit-log.service` API のみ使用、外部パッケージ追加なし。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ No new dependency; only existing `@prisma/client` / `audit-log.service` APIs used. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.

- **feat(embedding): PR-D-4 INV-EMBEDDING-INTEGRITY-003 full terminal parity check + standing regression domain 22 → 31 tests (+9 net) (ADR-0018 Amendment 4) (v0.4.0 PR-D-4)** / **feat(embedding): PR-D-4 INV-003 full terminal parity runtime check + standing regression 22 → 31 tests (+9 net) (ADR-0018 Amendment 4) (v0.4.0 PR-D-4)**:
  - **JP**: Standing regression `embedding-integrity` ドメイン: 22 → 31 tests (+9 net)。INV-EMBEDDING-INTEGRITY-003 full terminal parity runtime check + single-query refactor 完了 (PR-D-4)。`verifyCategoryParity(webPageId, prisma)` を `apps/mcp-server/src/services/backfill-status.helper.ts` に新設し、`EMBEDDING_BACKFILL_CATEGORIES` T1 SSOT (7 categories: `part_text` / `part_visual` / `section_visual` / `motion` / `background` / `js_animation` / `responsive`) の全カテゴリで `pending count = 0` を terminal transition 直前に検証。不一致時は `skipReason='parity_check_failed'` → `skipped_fork_error` (retry bucket) 経由で reconciliation cron に委譲。`collectCategoryPendingSnapshot` shared helper + `computeRemainingStatusWithPrisma` single-query refactor により TOCTOU phantom-read surface を原理的に排除。`handleTerminalParityGate` sub-function 抽出で `initiateBackfillJob` cyclomatic complexity ≤ 10 維持 (TDA Health 97/100)。Partial test (`inv-embedding-integrity-003-status-parity-partial.test.ts`) retired + full test (`inv-embedding-integrity-003-status-parity-full.test.ts`, 16 tests, 573 LOC) 新設 (Block A AST precondition 4 + Block B mapping integrity 4 incl. Set-equality + 負定 + Block C runtime parity 8 incl. DB-failure fail-closed + 並行 INSERT 下 TOCTOU)。ADR-0018 Amendment 4 で wording を 9→7 category に align (T1 SSOT 正典化) + LCC-01 H carve-out paragraph (`section_text` / `narrative` accountability, GDPR Art.5(1)(d), LCC cross-signed) + APPI 第23条 技術的安全管理措置 sentence + §Privacy Considerations Amendment (audit_logs accepted-risk ≤ 2026-05-09 + logger.warn 5-field contract, LCC cross-signed)。INV-EMBEDDING-INTEGRITY-001 dedicated test は IO Plan Decision §2.2 Option B 拘束により PR-D-5 に defer。
  - **EN**: Standing regression `embedding-integrity` domain: 22 → 31 tests (+9 net). INV-EMBEDDING-INTEGRITY-003 full terminal parity runtime check + single-query refactor landed (PR-D-4). Introduces `verifyCategoryParity(webPageId, prisma)` in `apps/mcp-server/src/services/backfill-status.helper.ts` to verify `pending count = 0` across all categories in `EMBEDDING_BACKFILL_CATEGORIES` T1 SSOT (7 categories: `part_text` / `part_visual` / `section_visual` / `motion` / `background` / `js_animation` / `responsive`) immediately before terminal transition. On mismatch, routes via `skipReason='parity_check_failed'` → `skipped_fork_error` (retry bucket) so the reconciliation cron can handle it. `collectCategoryPendingSnapshot` shared helper + `computeRemainingStatusWithPrisma` single-query refactor structurally eliminate the TOCTOU phantom-read surface. `handleTerminalParityGate` sub-function extraction maintains `initiateBackfillJob` cyclomatic complexity ≤ 10 (TDA Health 97/100 baseline). Retires partial test (`inv-embedding-integrity-003-status-parity-partial.test.ts`) and introduces the full test (`inv-embedding-integrity-003-status-parity-full.test.ts`, 16 tests, 573 LOC) covering Block A AST precondition (4) + Block B mapping integrity (4 incl. Set-equality + negative assertion) + Block C runtime parity (8 incl. DB-failure fail-closed + integration-level TOCTOU with real Prisma + concurrent INSERT). ADR-0018 Amendment 4 aligns the wording 9→7 categories (T1 SSOT canonicalisation) + adds LCC-01 H carve-out paragraph (`section_text` / `narrative` accountability, GDPR Art.5(1)(d), LCC cross-signed) + APPI Article 23 technical security measure sentence + §Privacy Considerations Amendment (audit_logs accepted-risk ≤ 2026-05-09 + logger.warn 5-field contract, LCC cross-signed). INV-EMBEDDING-INTEGRITY-001 dedicated test is deferred to PR-D-5 per IO Plan Decision §2.2 Option B binding.
  - **Audit action strings scope clarification (FIND-PLAN-IO-11)**: `embedding_parity_check_failed` のような audit action 文字列は **free-form identifier** であり `INV-SCHEMA-ENUM-004` scope 対象外。`INV-SCHEMA-ENUM-004` は Prisma ↔ TypeScript ↔ Zod ↔ OpenAPI/MCP spec の **pipeline enum 同期** (`EmbeddingSkipReason` / `EmbeddingBackfillStatus` 等) のみを対象とする。/ Audit action strings such as `embedding_parity_check_failed` are free-form identifiers outside `INV-SCHEMA-ENUM-004` scope. `INV-SCHEMA-ENUM-004` covers pipeline enum sync between Prisma ↔ TypeScript ↔ Zod ↔ OpenAPI/MCP spec only (`EmbeddingSkipReason` / `EmbeddingBackfillStatus` etc.).
  - **Related contracts / 関連契約**: ADR-0018 Amendment 4 (§INV-003 wording 9→7 align + LCC-01 carve-out + APPI 第23条 + §Decision 1 Landing addendum + §Privacy Considerations Amendment); INV-EMBEDDING-INTEGRITY-003 (full landed, PR-D-4); INV-EMBEDDING-INTEGRITY-001 (Landing status updated to **Partial** — supporting helper PR-D-4 / primary test PR-D-5).
  - **IO anchors / IO anchor**: Plan Decision `019db00f-d8b9-7fa3-8174-13a690b98a0c` / docs-only re-audit PASS `019db024-dcc4-792d-a65f-141d88a04be2` / Impl Decision `019db068-241b-78a6-8d44-baaf9c0de174` / Phase 2 code commit `2090eee5` / Plan revision commit `e65e73a2`.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — 本 PR は既存 `@prisma/client` API のみ使用、外部パッケージ追加なし。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ No new dependency; only existing `@prisma/client` APIs used. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.

### Added / 追加

- **test(embedding): PR-D-3 `savePartEmbeddings` catch block sanitize + INV-SEC-SANITIZE standing regression (ADR-0018 §3.9 landed) (v0.4.0 PR-D-3)** / **test(embedding): PR-D-3 catch-block sanitize + INV-SEC-SANITIZE standing regression (v0.4.0 PR-D-3)**:
  - **New CI-failing regression test / 新規 CI-failing 回帰テスト**: `apps/mcp-server/tests/services/part/part-embedding-db-error-sanitize.test.ts` (INV-SEC-SANITIZE standing regression、7 CI-failing cases)。UC-D3-03 "whitelist positive match" (Prisma P2002 → "A record with this value already exists" 等の test-assertion 粒度 7 種 subset) + Test #7 "server-log preservation" (raw `error.message` + error.code が `logger.warn` で server-side に保持される契約) を enforce。 / New standing regression test with 7 CI-failing cases; UC-D3-03 whitelist positive match + Test #7 server-log preservation enforcement.
  - **Whitelist / SSOT relationship (documentation-only clarification)**: `apps/mcp-server/src/utils/sanitize-error.ts` L17-31 が 17 種 error-code → client-safe message マッピングの **SSOT (Single Source of Truth)** であることを ADR-0018 §3.9 に明記。Test whitelist 7 種は SSOT 17 種の **test-assertion 粒度 subset** であり、SSOT の代替ではない (whitelist 7 ⊂ SSOT 17)。SSOT 拡張は security-engineer 判断、whitelist 拡張は test-qa-engineer 判断。 / Documentation clarifies `sanitize-error.ts` L17-31 (17 entries) is the SSOT; test whitelist (7 entries) is a test-assertion-grained subset. SSOT expansion: security-engineer; whitelist expansion: test-qa-engineer.

- **feat(embedding): PR-D-2 `savePartEmbeddings` atomic 2-step write + `bbox_invalid` skip reason (ADR-0018 §Decision 3 + §Decision 2 tail-append) (v0.4.0 PR-D-2)** / **feat(embedding): PR-D-2 `savePartEmbeddings` atomic 2-step transaction + `bbox_invalid` skip reason (ADR-0018 §Decision 3 + §Decision 2 tail-append) (v0.4.0 PR-D-2)**:
  - (a) **Atomic 2-step transaction / atomic 2 段書込**: `savePartEmbeddings` を単一 `prisma.$transaction` 境界でラップし、(1) `createMany` による非ベクトル column 挿入 → (2) `$executeRawUnsafe` による `vector(768)` column UPDATE の 2 step を transaction-boundary atomic に実行。partial failure 時は rollback により "base row exists but vector is NULL" の半書込状態を絶対に残さない (INV-EMBEDDING-INTEGRITY-002-A)。NaN/Infinity pre-filter を transaction 境界前段で実施 (CWE-20 / SEC-02 対応)。/ Wraps `savePartEmbeddings` in a single `prisma.$transaction` boundary, executing (1) `createMany` for non-vector columns and (2) `$executeRawUnsafe` `UPDATE` for `vector(768)` columns as transaction-boundary atomic. Partial failure rollback eliminates the "base row exists but vector is NULL" half-written state (INV-EMBEDDING-INTEGRITY-002-A). NaN/Infinity pre-filter applied before the transaction boundary (CWE-20 / SEC-02).
  - (b) **`EMBEDDING_SKIP_REASONS` 14→15 additive tail-append (`bbox_invalid`)**: 既存 14 値の order / value / semantic は完全不変、新規 1 値 `bbox_invalid` を enum 末尾に append (GDPR Art.5(1)(d) accuracy principle に従い "bbox invalid → skipped, retryable via next capture cycle" を明示)。`INV-SCHEMA-ENUM-004` で Prisma ↔ TS ↔ Zod ↔ OpenAPI/MCP spec の 4-way exhaustive mapping を 15 値対応に更新。GDPR Art.13/14: `bbox_invalid` は operator-facing technical identifier のみ、data subject には返却されない。/ Existing 14-value order / value / semantic fully preserved; new value `bbox_invalid` appended at the enum tail. `INV-SCHEMA-ENUM-004` updated to 15 values for 4-way exhaustive Prisma ↔ TS ↔ Zod ↔ OpenAPI/MCP spec mapping. GDPR Art.13/14: operator-facing only.
  - (c) **New standing regression (Severity H)**: `tests/regression/standing/embedding-integrity/inv-embedding-integrity-002a-part-atomic-write.test.ts` (INV-002-A) + `inv-embedding-integrity-003-status-parity-partial.test.ts` (INV-003 partial — transaction atomicity sub-invariant; `-partial` suffix で完全 landing は PR-D-4 であることを明示) + `inv-embedding-integrity-005-bbox-zero-observability.test.ts` (INV-005 bbox=0 observability). / Three new CI-failing standing regression tests landed in `tests/regression/standing/embedding-integrity/`.
  - (d) **Related contracts / 関連契約**: ADR-0018 §Decision 3 (formal merge of §3.1-§3.9 Amendment); ADR-0018 §3.9 `result.errors[]` sanitize policy (new, with documented risk + FIND-IMPL-SEC-01 deadline 2026-05-04); INV-EMBEDDING-INTEGRITY-002-A (new, atomic dual-phase write); INV-EMBEDDING-INTEGRITY-003 (partial landing); INV-EMBEDDING-INTEGRITY-005 (bbox=0 observability).
  - (e) **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — 本 PR は `@prisma/client` 既存 API (`$transaction` + `$executeRawUnsafe`) のみ使用、外部パッケージ追加なし。`pnpm sbom` で生成される CycloneDX 1.6 JSON は drift ゼロ。EU CRA (2026-09-11 vulnerability reporting obligation) 対応。/ No new dependency; only existing `@prisma/client` APIs used. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.

### Changed (BREAKING) / 破壊的変更

- **chore(api): `PartEmbeddingSaveResult.savedCount` field 完全削除 (v0.4.0 PR-D-3) [JP][US][EU]** / **chore(api): remove `PartEmbeddingSaveResult.savedCount` field entirely (v0.4.0 PR-D-3) [JP][US][EU]**:
  - **Removal / 削除**: PR-D-2 で `savedCount` は deprecated alias として 48 時間 window で併存していたが、本 PR-D-3 で **完全削除**。`PartEmbeddingSaveResult` interface には `generatedCount` のみが残存。 / In PR-D-2, `savedCount` was retained as a deprecated alias during a 48-hour window; PR-D-3 now **removes it entirely**. Only `generatedCount` remains on the `PartEmbeddingSaveResult` interface.
  - **Migration / 移行**: Consumer コードで `result.savedCount` を使用している箇所を `result.generatedCount` に置換すること (PR-D-2 migration guide と同一)。本 PR-D-3 以降 `result.savedCount` は **TypeScript compile error** となる。 / Replace `result.savedCount` with `result.generatedCount` in consumer code (identical to PR-D-2 migration guide). From PR-D-3 onwards, `result.savedCount` is a **TypeScript compile error**.
  - **Deprecation notice / 事前告知**: PR-D-2 merge 時点で 48-hour window + 2026-04-27 hard deadline が CHANGELOG L26-28 に事前告知されていた (本 PR-D-3 は **2026-04-22 merge**、hard deadline から 5 日早期遵守)。/ PR-D-2 merge announced the 48-hour window + 2026-04-27 hard deadline (CHANGELOG L26-28); PR-D-3 merged **2026-04-22** — 5 days ahead of the hard deadline.
  - **Rationale / 理由**: SSOT 二重化解消 (FIND-TDA-01 H、PR-D-2 で発行)。`generatedCount` は INV-EMBEDDING-INTEGRITY-002 "DB round-trip-derived generatedCount" の wording と一致する正典名称。 / Eliminates SSOT duplication (FIND-TDA-01 H, issued in PR-D-2). `generatedCount` aligns with the INV-EMBEDDING-INTEGRITY-002 canonical wording.
  - **Scope**: `apps/mcp-server/src/services/part/part-embedding-db.service.ts` の `PartEmbeddingSaveResult` interface。MCP 公開 API schema (tool response) には **影響なし** (internal service type のみ)。/ Internal service type only; no impact on MCP public API schemas (tool responses).

- **chore(api): `PartEmbeddingSaveResult.savedCount` → `generatedCount` 名称変更 (v0.4.0 PR-D-2)** / **chore(api): rename `PartEmbeddingSaveResult.savedCount` → `generatedCount` (v0.4.0 PR-D-2)**:
  - **Rationale / 理由**: ADR-0018 INV-EMBEDDING-INTEGRITY-002 "DB round-trip-derived generatedCount" に整合。`savedCount` は "DB に保存された件数" を示唆するが実際には "transaction commit 成功後 DB round-trip で確認された件数" であり、INV-002 の wording (`generatedCount`) との不一致を解消。/ Aligns with ADR-0018 INV-EMBEDDING-INTEGRITY-002 wording (`generatedCount`). The prior name `savedCount` ambiguously suggested "saved to DB" whereas the value is "count confirmed via DB round-trip after transaction commit".
  - **Migration guide / 移行手順**: Consumer コードで `result.savedCount` を使用している箇所を `result.generatedCount` に置換すること。`savedCount` は **deprecated alias として 48 時間併存** (PR-D-2 merge 後)、その後 **2026-04-27 (PR-D-3 cycle) に完全削除**。 / Replace `result.savedCount` with `result.generatedCount` in consumer code. `savedCount` remains as a **deprecated alias for a 48-hour window** post-PR-D-2 merge; full removal at **2026-04-27 (PR-D-3 cycle)**.
  - **Scope**: `apps/mcp-server/src/services/part/part-embedding-db.service.ts` の `PartEmbeddingSaveResult` interface。MCP 公開 API schema (tool response) には **影響なし** (internal service type のみ)。/ Internal service type only; no impact on MCP public API schemas (tool responses).

### Security / セキュリティ

- **fix(security): CWE-209 Information Exposure 修復 — `savePartEmbeddings` catch block sanitize (v0.4.0 PR-D-3) [JP][US][EU]** / **fix(security): CWE-209 Information Exposure remediation — `savePartEmbeddings` catch-block sanitize (v0.4.0 PR-D-3) [JP][US][EU]**:
  - **Remediation / 修復**: `apps/mcp-server/src/services/part/part-embedding-db.service.ts` の `savePartEmbeddings` catch block で、従来 `result.errors[]` に push していた生 `error.message` を `sanitizeErrorMessage(error)` 経由化。Prisma error code (P2002 unique violation / P2025 record not found 等) の内部テーブル名・カラム名の client response 漏洩を遮断 (CWE-209 Information Exposure Through an Error Message 防御)。/ Replaced raw `error.message` push to `result.errors[]` with `sanitizeErrorMessage(error)` in the `savePartEmbeddings` catch block of `apps/mcp-server/src/services/part/part-embedding-db.service.ts`. Blocks leakage of internal Prisma table/column names from error codes (P2002 unique violation / P2025 record not found, etc.) to client responses (CWE-209 Information Exposure Through an Error Message defense).
  - **Server-side log preservation (Test #7 enforced) / サーバーログ保持 (Test #7 で enforce)**: Server-side `logger.warn` では raw `error.message` + error.code (P2002 等) を継続保持 (debug 性能維持)。本契約は `part-embedding-db-error-sanitize.test.ts` Test #7 で CI-failing regression として enforce。/ Server-side `logger.warn` retains raw `error.message` + error.code (e.g. P2002) for debugging. This contract is enforced as a CI-failing regression in Test #7.
  - **Early landing / 早期 landing**: FIND-IMPL-SEC-01 (M severity, deadline **2026-05-04**, PR-D-2 で発行) を **2026-04-22 (PR-D-3) に landing** — deadline から **12 日早期遵守**。ADR-0018 §3.9 "Landing deadline" の status を Planned → Landed に更新。/ FIND-IMPL-SEC-01 (M severity, deadline **2026-05-04**, issued in PR-D-2) landed in PR-D-3 on **2026-04-22** — **12 days ahead of deadline**. ADR-0018 §3.9 "Landing deadline" updated from Planned → Landed.
  - **Regulatory alignment [JP][US][EU] / 法域アラインメント**:
    - **[EU] GDPR Art.32(1)(b) "integrity"**: state-of-the-art error message sanitization to preserve integrity of processing. / 処理の integrity を保つための state-of-the-art エラーメッセージサニタイズ。
    - **[JP] APPI 第23条 (安全管理措置) / Article 23 (Security control measures)**: 個人データに関する漏えい・滅失・毀損を防止するための組織的・技術的措置。error message sanitize は技術的措置として CWE-209 情報漏洩を防止。 / Technical measure preventing CWE-209 information leakage to protect personal data.
    - **[US] CCPA §1798.150 (breach surface reduction)**: breach notification 対象となる "unauthorized disclosure" の surface 削減。 / Reduces the breach surface subject to `unauthorized disclosure` under §1798.150.
    - **[EU] EU CRA 2024/2847 compliance readiness (2026-09-11 in force)**: state-of-the-art vulnerability handling の一環として CWE-209 latent risk を事前除去。 / Pre-removes CWE-209 latent risk as part of state-of-the-art vulnerability handling.
  - **Standing regression landing / standing regression landing**: `apps/mcp-server/tests/services/part/part-embedding-db-error-sanitize.test.ts` を INV-SEC-SANITIZE standing regression として追加 (CI-failing 7 cases + whitelist positive match UC-D3-03 + server-log preservation Test #7)。CI `regression-standing` job で常時実行。 / Added as INV-SEC-SANITIZE standing regression (CI-failing 7 cases, UC-D3-03 whitelist positive match, Test #7 server-log preservation). Runs continuously in the CI `regression-standing` job.

- **fix(embedding): Prisma transaction isolation + NaN/Infinity pre-filter defense (v0.4.0 PR-D-2)** / **fix(embedding): Prisma transaction isolation + NaN/Infinity pre-filter defense (v0.4.0 PR-D-2)**:
  - **UC-03 (transaction isolation)**: Prisma `$transaction` interactive transaction の rollback 挙動を CI-failing test で verify (`apps/mcp-server/tests/services/part/part-embedding-db-transaction-isolation.test.ts`)。Step 2 `$executeRawUnsafe` 失敗時に Step 1 `createMany` 挿入 row が rollback で撤回され、"half-written state" が DB に残存しないことを保証。 / Interactive `$transaction` rollback verified via CI-failing test (`part-embedding-db-transaction-isolation.test.ts`). Ensures Step 1 `createMany` rows are withdrawn on Step 2 `$executeRawUnsafe` failure.
  - **UC-04 (NaN/Infinity defense)**: transaction 境界前段で `Number.isFinite()` による pre-filter を実装 (`apps/mcp-server/tests/services/part/part-embedding-db-nan-infinity.test.ts`)。`NaN` / `Infinity` を含む embedding による pgvector query 失敗を防止 (CWE-20 Improper Input Validation 対策)。/ `Number.isFinite()` pre-filter implemented at the transaction boundary prologue. Prevents pgvector query failures from `NaN` / `Infinity` embeddings (CWE-20 Improper Input Validation defense).
  - **Documented risk (FIND-IMPL-SEC-01, M severity, deadline 2026-05-04)**: 現行 PR-D-2 実装の `savePartEmbeddings` catch block で生 `error.message` が `result.errors[]` に push されている CWE-209 latent risk を ADR-0018 §3.9 + `に documented risk として記載。PR-D-3 cycle (deadline 2026-05-04) で`sanitizeErrorMessage(error)`経由化 +`part-embedding-db-error-sanitize.test.ts`landing 予定。/ CWE-209 latent risk (raw`error.message`pushed to`result.errors[]`in the catch block) is documented in ADR-0018 §3.9 and` Will be routed through `sanitizeErrorMessage(error)` + CI-failing test added in PR-D-3 (deadline 2026-05-04).

### Compliance / 法務・コンプライアンス

- **chore(compliance): PR-D-2 GDPR / AGPL / CRA compliance verification (v0.4.0)** / **chore(compliance): PR-D-2 GDPR / AGPL / CRA compliance verification (v0.4.0)**:
  - **GDPR Art.5(1)(d) accuracy**: `bbox_invalid` skip reason の semantic は "bounding box が無効なため skip、次回 capture cycle で retry 可能" であり、Option D (recovery bucket: `bbox_invalid` → `skipped_fork_error` retry bucket 経由で次回 cycle で再試行) に分類。"inaccurate permanent loss" ではない旨を ADR-0018 §Decision 2 tail-append rationale + CHANGELOG §(b) に明記。 / `bbox_invalid` semantic is "skipped due to invalid bounding box, retryable in next capture cycle" (Option D: retry bucket), explicitly distinguished from "inaccurate permanent loss" in ADR-0018 and CHANGELOG.
  - **GDPR Art.13/14 transparency**: `bbox_invalid` は operator-facing technical identifier のみ、`audit_logs` metadata / MCP server logger にのみ出力され、`data.export` (Art.20) / `preference.get` で data subject に verbatim 返却されない。 / Operator-facing only; never returned verbatim to data subjects via `data.export` / `preference.get`.
  - **EU CRA (2024/2847) SBOM**: 新規依存ゼロ、CycloneDX 1.6 SBOM drift ゼロ。`pnpm sbom` 生成物不変 (SEC M-3 根拠)。EU CRA 2026-09-11 vulnerability reporting obligation 対応継続。 / Zero new dependencies; CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.
  - **AGPL-3.0 transparency**: `savedCount` → `generatedCount` rename は internal service type のみで MCP public API schema には影響なし。ただし AGPL-3.0 §5 "modify the Program" 観点で CHANGELOG に rename + migration guide + deadline を明記することで transparency 要件を充足。 / Internal type rename documented in CHANGELOG with migration guide and deprecation deadline, satisfying AGPL-3.0 §5 transparency requirements.

- **feat(embedding): `EmbeddingSkipReason` 12→14 additive expansion (ADR-0018 §Decision 2) (v0.4.0 PR-D-1)** / **feat(embedding): additive `EmbeddingSkipReason` expansion from 12 to 14 values (ADR-0018 §Decision 2) (v0.4.0 PR-D-1)**:
  - (a) **Additive policy / 追加ポリシー**: 既存 12 値 (`v8_heap_headroom_low` / `system_memavailable_low` / `text_fork_failed` / `text_child_error` / `text_child_abnormal_exit` / `text_ipc_race` / `visual_fork_failed` / `visual_child_error` / `visual_child_abnormal_exit` / `visual_ipc_race` / `no_embeddable_items` / `dispatch_phase_failed`) の **order / value / semantic は一切不変**。新規 2 値 (`fork_terminated_before_done` + `parity_check_failed`) は enum 末尾に append する additive 拡張。`EMBEDDING_SKIP_REASONS` の 12→14 拡張は `apps/mcp-server/src/workers/phases/types.ts` に反映済、`INV-SCHEMA-ENUM-004` (`tests/regression/standing/schema-enum-sync/inv-schema-enum-004.test.ts` + 新規 `inv-schema-enum-004-embedding-v2.test.ts`) で Prisma ↔ TS ↔ Zod ↔ OpenAPI/MCP spec の 4-way exhaustive mapping を CI 検証。/ Order, value, and semantics of the existing 12 values are fully preserved; the two new values (`fork_terminated_before_done` + `parity_check_failed`) are appended at the enum tail as an additive extension. `EMBEDDING_SKIP_REASONS` now holds 14 entries in `apps/mcp-server/src/workers/phases/types.ts`; CI verifies the 4-way exhaustive Prisma ↔ TS ↔ Zod ↔ OpenAPI/MCP spec mapping via `INV-SCHEMA-ENUM-004` (`inv-schema-enum-004.test.ts` + new `inv-schema-enum-004-embedding-v2.test.ts`).
  - (b) **TypeScript consumer migration note / TypeScript consumer 移行注記**: `EmbeddingSkipReason` に対して TypeScript の **exhaustive `switch`** や discriminated union (`never` type 到達チェック) を使う consumer は、本拡張で compile-time fail するため、**新値 2 件 (`fork_terminated_before_done` / `parity_check_failed`) の handler 追加が必要**。`result.skipReason` を単純に log / persist するのみの consumer は変更不要 (backward-compatible additive extension)。/ Consumers using TypeScript exhaustive `switch` or discriminated unions over `EmbeddingSkipReason` will see compile-time `never`-type failures and MUST add handlers for the two new values (`fork_terminated_before_done` / `parity_check_failed`). Consumers that simply log or persist `result.skipReason` require no change (backward-compatible additive extension).
  - (c) **GDPR display notice / GDPR 表示通知**: These enum values are **technical identifiers for processing classification**, not intended for direct display to data subjects (GDPR Art.13/14). They are surfaced in operator-facing logs (`audit_logs` action metadata, MCP server logger) only and are never returned verbatim to data subjects as part of `data.export` (GDPR Art.20) or `preference.get` payloads. / これらの enum 値は **処理分類のための技術識別子** であり、data subject (GDPR Art.13/14) への直接表示を意図していない。運用者向けログ (`audit_logs` metadata、MCP server logger) にのみ出力され、`data.export` (GDPR Art.20) や `preference.get` の payload で data subject に verbatim で返却されることはない。
  - (d) **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — Enum 拡張は `apps/mcp-server/src/workers/phases/types.ts` の TypeScript リテラル配列追加のみで、外部パッケージ追加 / version bump なし。`pnpm sbom` で生成される CycloneDX 1.6 JSON には影響なし (SEC M-3 根拠)。EU CRA (2026/9/11 vulnerability reporting obligation) に対する SBOM drift リスクゼロ。/ The enum extension adds TypeScript literal array entries only in `apps/mcp-server/src/workers/phases/types.ts`; no external package is added and no version is bumped. The CycloneDX 1.6 JSON generated by `pnpm sbom` is unaffected (per SEC M-3). Zero SBOM drift risk against EU CRA vulnerability reporting obligations (in force 2026-09-11).
  - **Related contracts / 関連契約**: ADR-0018 §Decision 2 (本 enum 拡張の T1 Canonical 化、Amendment for `parity_check_failed`); INV-EMBEDDING-INTEGRITY-001 (terminal transition parity check → `skipReason='parity_check_failed'` 遷移); INV-EMBEDDING-INTEGRITY-004 (fork child abnormal exit → `skipReason='fork_terminated_before_done'` 遷移); INV-SCHEMA-ENUM-004 (4-way enum exhaustive mapping CI 検証) / ADR-0018 §Decision 2 (T1 Canonical of this enum extension; Amendment covering `parity_check_failed`); INV-EMBEDDING-INTEGRITY-001 (terminal transition parity → `parity_check_failed`); INV-EMBEDDING-INTEGRITY-004 (fork abnormal exit → `fork_terminated_before_done`); INV-SCHEMA-ENUM-004 (4-way enum exhaustive mapping).
  - **Files changed (6 / Phase 2 implementation)**: `apps/mcp-server/src/workers/phases/types.ts`, `apps/mcp-server/src/workers/page-analyze-worker.ts`, `apps/mcp-server/tests/regression/standing/schema-enum-sync/inv-schema-enum-004.test.ts`, `apps/mcp-server/tests/regression/standing/schema-enum-sync/inv-schema-enum-004-embedding-v2.test.ts` (new), `apps/mcp-server/tests/workers/page-analyze-silent-skip-fix.test.ts`, `apps/mcp-server/scripts/force-reconcile-backfill.ts`.
  - **Quality gates / 品質ゲート**: `pnpm lint` 0 errors / `pnpm typecheck` 0 errors / `pnpm test` 14,107 passed / `pnpm test:regression:standing` 63 passed / `pnpm docs:verify` 46 PASS 0 FAIL.

### Fixed / 修正

- **fix(mcp-progress): MCP `notifications/progress` async-path race over closed transport — `page.analyze` async mode crash 修復 (INV-MCP-PROGRESS-ASYNC-001) [JP][US][EU]** / **fix(mcp-progress): MCP `notifications/progress` async-path race over closed transport — `page.analyze` async mode crash repair (INV-MCP-PROGRESS-ASYNC-001) [JP][US][EU]**:
  - **JP**: `page.analyze` async mode 呼び出し時、jobId return 後 ~42 秒で MCP server + worker child 両方が crash していた race condition を修復。`apps/mcp-server/src/tools/page/analyze.tool.ts:498-501` で async-return path 直前に fire-and-forget で送信していた `void progressService.notifyPhaseStart("ingest")` (4 行: コメント 2 + コード 1 + 空行 1) を削除。**真因 (CWE-662 Improper Synchronization, primary / CWE-755 Improper Handling of Exceptional Conditions, secondary)**: response が return される前に notification を fire-and-forget するため、(1) MCP SDK client が response 受信時に `_progressHandlers.delete(messageId)` を実行 → (2) その後到着する progress notification は handler 不在で `_onerror` 発火 → (3) STDIO transport drop → MCP server stdin close → server exit + worker child cascade kill。**MCP spec 違反 (dual-cite)**: MCP **2025-06-18 + 2025-11-25** spec 両方で identical clause "Progress notifications **MUST** stop after completion" + "Progress notifications **MUST** only reference tokens that ... Are associated with an in-progress operation" — `page.analyze` async mode は `{async: true, jobId, status: "queued"}` を return した瞬間に request lifecycle 完了とみなされるため、後続の progress notification は contract violation。**正規経路**: 進捗取得は `page.getJobStatus(job_id, intervalSeconds: 10)` polling のみ (analyze.tool.ts L510-515 で client に明示済)。**INV-MCP-PROGRESS-ASYNC-001 standing test**: hybrid 設計 (Block A AST source-pin via `addMcpServerSourceFile` + `createAstProject` + `assertInvName` standing helper precedent / Block B runtime integration via `vi.spyOn(sendNotification)` + 100ms microtask drain wait) で `apps/mcp-server/tests/tools/page/analyze-async-no-progress.test.ts` に landing。Affected versions: **commit `be62a059` (v0.3.0 Tier 2 development cycle) 以降の全 build** — 公式 semver tag リリースは未発行 (`git tag --contains be62a059` 結果空、現状 tag は `archive/feature-v0.2.0-sprint` / `pre-webui-removal` / `v-pre-svg-removal` のみ)、現在 v0.4.0 WIP 期間中の `main` / `feat/*` branch 全 build が影響対象。
  - **EN**: Repairs the race condition that crashed both the MCP server and worker child ~42 s after `jobId` return whenever `page.analyze` was invoked in async mode. Removes the fire-and-forget `void progressService.notifyPhaseStart("ingest")` (4 lines: 2 comments + 1 code + 1 blank) at `apps/mcp-server/src/tools/page/analyze.tool.ts:498-501`, immediately preceding the async-return path. **Root cause (CWE-662 Improper Synchronization, primary / CWE-755 Improper Handling of Exceptional Conditions, secondary)**: the notification is fired-and-forgotten before the response returns, so (1) the MCP SDK client deletes `_progressHandlers[messageId]` upon receiving the response → (2) the later-arriving progress notification has no handler and triggers `_onerror` → (3) STDIO transport drops → MCP server stdin closes → server exits + worker child cascades to kill. **MCP spec violation (dual-cite)**: both MCP **2025-06-18 + 2025-11-25** specs share the identical clause "Progress notifications **MUST** stop after completion" + "Progress notifications **MUST** only reference tokens that ... Are associated with an in-progress operation" — `page.analyze` async mode marks the request lifecycle as completed at the moment `{async: true, jobId, status: "queued"}` is returned, so any subsequent progress notification is a contract violation. **Canonical channel**: progress polling via `page.getJobStatus(job_id, intervalSeconds: 10)` only (already documented to clients at analyze.tool.ts L510-515). **INV-MCP-PROGRESS-ASYNC-001 standing test** lands in `apps/mcp-server/tests/tools/page/analyze-async-no-progress.test.ts` with a hybrid design (Block A AST source-pin using `addMcpServerSourceFile` + `createAstProject` + `assertInvName` standing helper precedent / Block B runtime integration via `vi.spyOn(sendNotification)` + 100 ms microtask drain wait). Affected versions: **all builds since commit `be62a059` (v0.3.0 Tier 2 development cycle)** — no official semver tag releases yet (`git tag --contains be62a059` returns empty; current tags are only `archive/feature-v0.2.0-sprint` / `pre-webui-removal` / `v-pre-svg-removal`); all `main` / `feat/*` branch builds during the current v0.4.0 WIP period are affected.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — 4 行 deletion + 1 new test file のみ、外部パッケージ追加ゼロ。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ 4-line deletion + 1 new test file only; zero new dependencies; CycloneDX 1.6 SBOM drift zero; EU CRA 2026-09-11 compliance preserved.
  - **GHSA disclosure**: GHSA disclosure not required — internal STDIO race, AV:L+UI:R, CVSS Low/Medium estimate (~3.3-4.0), no external attacker triggers (MCP server runs locally over STDIO transport only). Detailed rationale in Finding Registry §8 (multi-axis affirmation: AV/AC/PR/UI/S/CIA + no external trigger + internal trusted-process race + PR-D-8 prior-art alignment). / GHSA disclosure intentionally skipped per multi-axis assessment in Finding Registry §8.
  - **Carryover / 期限付き継続課題 (5 items)**: CO-MCP-01 (M, 2026-05-31) Defense-in-depth `ProgressNotificationService.markCompleted()` API / CO-MCP-02 (M, 2026-05-31, **L→M elevated**) sync-processing.ts **10 occurrences** (L147/173/471/608/669/704/724/725/736/790, IO grep 確定) await 化 / CO-MCP-03 (L, Q3 2026) Redis orphan recovery + queue-cleanup audit_logs / CO-MCP-04 (L, Q3 2026) `isDevelopment()` URL log review / CO-MCP-05 (M, 2026-05-31) MCP notification lifecycle ADR.
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.5(1)(d) accuracy**: MCP server crash → 部分的 / 不完全な分析結果が client へ返却される race を排除、page.analyze 結果 accuracy を保全
    - **[EU] GDPR Art.32(1)(b) ongoing resilience of processing systems**: MCP transport stability + worker child lifecycle 健全性を回復
    - **[JP] APPI 第23条 (安全管理措置) / Article 23 (Security control measures)**: 技術的安全管理措置として MCP protocol contract violation を排除
    - **[US] CCPA §1798.150 reasonable security**: proactive crash race repair + INV-\* CI-failing test landing
    - **[EU] EU CRA 2024/2847 (2026-09-11 in force)**: CycloneDX 1.6 SBOM drift 0 (no new dependency added); MCP spec 2025-06-18 + 2025-11-25 dual-cite compliance evidence
  - **Related contracts / 関連契約**: INV-MCP-PROGRESS-ASYNC-001 (NEW, hybrid Block A AST source-pin + Block B runtime integration); MCP spec 2025-06-18 + 2025-11-25 (`MUST stop after completion` clause dual-cite); ADR-0011 (cross-link for cascade kill behavior context); CWE-662 (primary) + CWE-755 (secondary).
  - **IO anchors / IO anchor**: Plan Decision v1 CONDITIONAL (Finding Registry v1 §0-§10, 2026-04-26) → Impl Decision v1 CONDITIONAL (Finding Registry v2 §11-§17, 2026-04-26, TDA H FIND-IMPL-TDA-01 false positive resolved by IO via 3-source evidence + 3-audit cross-confirmation) → Phase 3 docs-sync (this CHANGELOG entry + Plan v1.1 + Registry §7 stale path cleanup + PR-D-8 §32 cross-link, per Registry v2 §15 C-IMPL-01..C-IMPL-08) → Phase 4 IO Final Consistency Check + final commit. Finding Registry: `

- **PR7e-β3 hotfix: 4 面実装監査で検出された 3 件の指摘を統合解消 (v0.4.0)** / **PR7e-β3 hotfix: resolve 3 audit findings from 4-face implementation audit (v0.4.0)**:
  - **TDA NO GO → GO**: `phase-5-text-embedding-child.ts:setupDI()` の TS2741 型エラー解消。`setMotionPersistenceEmbeddingServiceFactory(() => sharedService)` は `motion-persistence.service.ts` の `IEmbeddingService.generateEmbedding(text, type)` を要求するが `LayoutEmbeddingService` は `generateFromText()` のみ公開で型不適合だった。sync path (`service-registrar-analysis.ts:236`) と同じ `mlEmbeddingService` (@reftrixmcp/ml) を渡すよう変更。turbo cache 越しの pre-commit typecheck では false PASS していたため、cache 迂回 `pnpm --filter @reftrixmcp/mcp-server exec tsc --noEmit` で再検証し 0 errors 確定。/ Fix TS2741 type error. Switched to `mlEmbeddingService` (matches the sync path instance and satisfies the `IEmbeddingService.generateEmbedding(text, type)` contract). Turbo cache masked this in pre-commit; verified via direct `tsc --noEmit` invocation.
  - **SEC-β3-M1 (CWE-209)**: `embedding-handler.ts:generateMotionEmbeddings()` の silent-skip 防御で `result.errors` に push していた error 文字列から内部関数名 (`setMotionPersistencePrismaClientFactory` / `setMotionPersistenceEmbeddingServiceFactory`) を除去し、`"Motion embedding DI configuration missing"` に汎用化。詳細は `logger.error` 側にのみ残し、MCP クライアント露出経路での CWE-209 情報漏洩リスクを遮断。/ Removed internal function names from the `result.errors` entry surfaced via `generateMotionEmbeddings()`'s silent-skip defense; diagnostic details remain only in `logger.error`. Closes a latent CWE-209 exposure if `result.errors` is ever returned via MCP.
  - **LCC MEDIUM-A**: `DATA_RETENTION.md` の Changelog テーブルに 2.11.0 行を追加 (2.10.0 と 2.9.0 の間)。ヘッダーのみ 2.11.0 で Changelog テーブルに行が未追加だった整合性違反を解消。GDPR Art.30 (処理活動記録) の履歴追跡性を確保。/ Added the 2.11.0 row to the DATA_RETENTION.md Changelog table (previously only the header was bumped). Restores document integrity for GDPR Art.30 traceability.
  - **テスト / Tests**: 全 15,361 passed / 0 failed を維持。pre-commit hook で docs-impact が canonical doc 更新を要求したため ` Embedding Backfill Pipeline セクションに PR7e-β3 追記 (既にメインコミットで対応済)。

- **PR7e-β3: motion_embeddings 全サイト 0 件問題を fork child DI 修復で解消 (v0.4.0)** / **PR7e-β3: resolve motion_embeddings zero-count across all sites via fork child DI repair (v0.4.0)**:
  - **真因 / Root cause**: `apps/mcp-server/src/workers/phases/phase-5-text-embedding-child.ts` の `setupDI()` が `setMotionPersistenceEmbeddingServiceFactory` / `setMotionPersistencePrismaClientFactory` を呼び出していなかった。その結果 fork child モジュールスコープで `motion-persistence.service.ts` の `prismaClientFactory` が `null` のままとなり、`getMotionPersistenceService().isAvailable()` が false を返し、`embedding-handler.ts:generateMotionEmbeddings()` で全件 silent skip されていた。sync 経路 (`service-registrar-analysis.ts:236-243`) は正しく factory 登録済だが fork child は DI 網が独立していた。DB 観測事実: reftrix.io 552 motion_patterns / 0 motion_embeddings、Stripe 216 / 0、全サイト motion_embeddings = 0 (js_animation_embeddings は 187/187 正常 — 直接 `ctx.prisma` 使用のため DI gate 迂回)。/ `setupDI()` in `phase-5-text-embedding-child.ts` was not calling `setMotionPersistenceEmbeddingServiceFactory` / `setMotionPersistencePrismaClientFactory`. The fork child's module-scoped `prismaClientFactory` in `motion-persistence.service.ts` remained `null`, causing `getMotionPersistenceService().isAvailable()` to return false and `embedding-handler.ts:generateMotionEmbeddings()` to silently skip every record. The sync path (`service-registrar-analysis.ts:236-243`) registered the factories correctly, but the fork child has an independent DI network. DB evidence: reftrix.io 552 motion_patterns / 0 motion_embeddings, Stripe 216 / 0, motion_embeddings = 0 across every page (js_animation_embeddings 187/187 healthy because that path uses `ctx.prisma` directly and bypasses the DI gate).
  - **修正 / Fix**: `setupDI()` に `setMotionPersistenceEmbeddingServiceFactory(() => sharedService)` と `setMotionPersistencePrismaClientFactory(() => prisma as never)` の 2 行を追加 (Background / Layout factory と同パターン)。これにより Phase 5 text embedding child で motion embedding 生成が正常動作する。Vision-detected motion も同じ `generateMotionEmbeddings` 経路を通るため同時救済。/ Added 2 lines to `setupDI()`: `setMotionPersistenceEmbeddingServiceFactory(() => sharedService)` and `setMotionPersistencePrismaClientFactory(() => prisma as never)`, mirroring the Background / Layout factory pattern. Motion embedding generation is now functional in the Phase 5 text embedding child. Vision-detected motion takes the same `generateMotionEmbeddings` path and is simultaneously rescued.
  - **backfill worker 側の DI 補強 / Backfill worker DI reinforcement**: `apps/mcp-server/src/workers/embedding-backfill-worker.ts` にも `setFramePrismaClientFactory(() => prisma as never)` を追加。将来 motion backfill が enqueue された際に `saveMotionEmbedding` が throw しないよう予防。/ Added `setFramePrismaClientFactory(() => prisma as never)` to `embedding-backfill-worker.ts` as defensive DI for future motion backfill jobs (prevents `saveMotionEmbedding` from throwing "PrismaClient not initialized").
  - **silent-skip 防御 (TDA P2) / silent-skip defense (TDA P2)**: `embedding-handler.ts:generateMotionEmbeddings()` の `isAvailable() === false` 分岐で従来 `logger.warn` だったものを `logger.error` に格上げし、`result.errors.push({ patternId: "__di_unavailable__", error: ... })` で discriminated error を追加。本番ログで DI 欠落を即座に検知可能。/ Upgraded the `isAvailable() === false` branch in `generateMotionEmbeddings()` from `logger.warn` to `logger.error` and added a discriminated `result.errors` entry (`patternId: "__di_unavailable__"`) to surface DI misconfiguration immediately in production logs.
  - **既知ギャップ (data.delete motion カバレッジ) / Known gap (data.delete motion coverage)**: `motion_patterns` は `onDelete: SetNull` で web_page に紐付くため、`data.delete` (GDPR Art.17) が web_page を削除しても motion_patterns は残存する。MCP tool `data.delete` は現時点で motion_patterns / motion_embeddings を削除対象に含まない (`apps/mcp-server/src/tools/data/data.tool.ts` に motion 参照なし)。別 PR で data.delete に motion カバレッジを追加予定 (`DATA_RETENTION.md` 2.11.0 に明記)。/ Known gap documented in `DATA_RETENTION.md` 2.11.0: `motion_patterns` use `onDelete: SetNull` (not CASCADE), so `data.delete` (GDPR Art.17) does not remove them; the MCP tool `data.delete` does not yet list motion_patterns / motion_embeddings for deletion (`apps/mcp-server/src/tools/data/data.tool.ts` has no motion references). A separate PR will add motion coverage to `data.delete`.
  - **テスト / Tests**: `apps/mcp-server/tests/workers/phases/phase-5-text-embedding-child-di.test.ts` (新規 17 tests) — fork child `setupDI()` に全ての必須 factory 呼び出しが含まれることをソースレベルで検証、backfill worker の `setFramePrismaClientFactory` module-scope 呼び出しを検証、`generateMotionEmbeddings` の silent-skip 防御 (logger.error + result.errors) を検証。mcp-server スイート全体 15,361 passed / 0 failed。/ New test file with 17 cases; full mcp-server suite: 15,361 passed / 0 failed.
  - **既存 768 件の復旧 / Recovery of existing 768 records**: reftrix.io 552 件 + Stripe 216 件の既存 motion_patterns は修正後に該当ページを page.analyze で再実行することで motion_embeddings が自動生成される (別 CLI 不要)。本番環境では DB 状態に応じて個別対応。/ The existing 552 (reftrix.io) + 216 (Stripe) motion_patterns can be recovered by re-running `page.analyze` on those pages; no dedicated CLI is needed. Production recovery is situational.

- **backfill-reconciliation SSOT 統一で false-failed バグ根本解決 (v0.4.0, commit `7ad4e448`)** / **SSOT unification in backfill-reconciliation fixes false-failed bug (v0.4.0, commit `7ad4e448`)**:
  - **真因 / Root cause**: `backfill-reconciliation.service.ts` の `countRemainingGaps` が **part_text / part_visual 2 カテゴリのみ**を backfill-eligible 絞り込みなしで数えていたため、blank image skip や section カバー外で legitimate にスキップされた parts を「pending」としてカウントし `embeddingBackfillStatus = 'failed'` に false-failed pin していた。一方 backfill worker 側 `computeRemainingStatus` は 7 カテゴリ + `countPartVisualBackfillTargets()` で正しく絞り込み済 (SSOT drift)。Stripe 実例: 254 parts のうち visual 30 件 (224 legitimate skip) → 旧 reconciliation が `visualPending=224` と判定 → false-failed pin。/ `countRemainingGaps` counted only 2 categories without backfill-eligible filtering, incorrectly flagging legitimately-skipped parts as pending and false-failed pinning. Stripe case: 254 parts / 30 visual (224 legit skip) → `visualPending=224` → false-failed.
  - **修正 / Fix**: 新規 SSOT helper `apps/mcp-server/src/services/backfill-status.helper.ts` を導入し (循環 import 回避)、`computeRemainingStatusWithPrisma(webPageId, prisma)` が 7 カテゴリ全件を backfill-eligible 絞り込み済で判定する。backfill-worker の `computeRemainingStatus` は helper wrapper に delegate し公開 API 契約維持。reconciliation は `countRemainingGaps` を完全削除し helper を直接呼出、`'in_progress'` → `'failed'` マッピングで stale pin 責務維持。/ New SSOT helper `backfill-status.helper.ts` avoids circular imports; `computeRemainingStatusWithPrisma` handles all 7 categories with proper filtering. Worker delegates via wrapper (public API preserved); reconciliation deletes `countRemainingGaps` and maps `'in_progress'` → `'failed'` for stale pin semantics.
  - **既存 false-failed 行の補正 / Repair for existing false-failed rows**: 新規スクリプト `apps/mcp-server/src/scripts/repair-false-failed-backfill.ts`。`embedding_backfill_status = 'failed'` かつ `retry_count < 5` の行を SSOT 再判定で補正、`--dry-run` (デフォルト) / `--confirm` で production 安全運用、`NODE_ENV=production` 時は明示指定必須。補正成功時に `audit_logs.action = 'backfill_status_corrected_after_ssot_fix'` で GDPR Art.30 処理活動記録。/ New script `repair-false-failed-backfill.ts` recompletes eligible false-failed rows with `--dry-run` (default) / `--confirm`, production guard, and GDPR Art.30 audit_logs entries.
  - **テスト / Tests**: `backfill-status.helper.test.ts` (新規 14 tests、7 カテゴリ + NaN 防御 + Stripe 回帰)、`backfill-reconciliation.service.test.ts` (+3 tests = 20)、`embedding-backfill-worker.test.ts` (+2 tests = 38)。全体 15,344 passed / 0 failed。/ 14 new tests + 5 additions to existing suites; full suite 15,344 passed / 0 failed.
  - **Scope 外 / Out of scope**: motion_embeddings 0/216 件問題 (Stripe) は別 PR で調査 (TODO コメント残置)。SSOT 統一後は motion_embeddings 欠損が正しく `failed` に pin される。/ motion_embeddings 0/216 issue tracked separately (TODO comment).

### Security / セキュリティ (SSOT commit で同時対応 / handled with SSOT commit)

- **`pnpm.overrides`: `protobufjs >= 7.5.5` を強制 (GHSA-xq3m-2v4x-88gg)** / **`pnpm.overrides`: pin `protobufjs >= 7.5.5` (GHSA-xq3m-2v4x-88gg)**:
  - `@huggingface/transformers > onnxruntime-web > protobufjs` 経由で `protobufjs < 7.5.5` (Critical CVE) が解決されていた問題を `pnpm.overrides` で強制解消。`pnpm audit --audit-level=high` 0 件、Critical 0 件を維持。/ Resolved a transient `protobufjs < 7.5.5` (Critical CVE, GHSA-xq3m-2v4x-88gg) pulled via `@huggingface/transformers > onnxruntime-web` by forcing `protobufjs >= 7.5.5` via `pnpm.overrides`. `pnpm audit --audit-level=high`: 0 findings, 0 Critical.

### Changed / 変更

- **PR7e-β2 P0 (partial): Phase 5 entry blocker 緩和 + Phase 4→5 選択的メモリ解放 (v0.4.0, commit `4c15ef47`)** / **PR7e-β2 P0 (partial): mitigate Phase 5 entry blocker + selective Phase 4→5 memory disposal (v0.4.0, commit `4c15ef47`)**:
  - **背景 / Context**: β1 で `PHASE5_PARENT_RSS_MAX_MB` を 3072→4096 に緩和したが、reftrix.io (実測 RSS 5528MB) および Stripe (実測 RSS 5027MB) では Phase 5 entry 時点で 4096 を超え early-skip → 全 embedding 0 件が継続していた。β2 P0 は真因 A (RSS guard 不足) と F (Phase 4→5 中間データの非選択的維持) を緩和し、Phase 5 fork が成立する条件を整える。本 PR は **partial success** であり、Phase 5 entry 通過と fork 成立は達成したが `success=true` は別途 P1 で対応する。/ β1 relaxed `PHASE5_PARENT_RSS_MAX_MB` from 3072→4096 but reftrix.io (RSS 5528MB) and Stripe (RSS 5027MB) still exceeded 4096 at Phase 5 entry, triggering early-skip and zero embeddings DB-wide. β2 P0 addresses root causes A (insufficient RSS guard) and F (non-selective preservation of Phase 4→5 intermediate data) so Phase 5 fork can proceed. This PR is a **partial success**: Phase 5 entry and fork are now reachable, but `success=true` requires follow-up work in P1.
  - **P0-1: `PHASE5_PARENT_RSS_MAX_MB` デフォルト 4096 → 7168 (7 GB) に段階緩和** / **P0-1: tier relaxation 4096 → 7168 (7 GB)**: `apps/mcp-server/src/config/phase5-config.ts` の `DEFAULT_PARENT_RSS_MAX_MB` を 4096 から 7168 に引き上げ。計画段階では 6144 を暫定値としたが再検証で reftrix.io 実測 6363/6480 MB が依然超過したため、観測最大 6480MB に約 10% のマージンを加えて 7168 に確定。16GB RAM 環境では parent 7168 + Phase 5 子プロセス (DINOv2 ~800MB + e5-base ~500MB ≈ 1300MB) = 8468MB で RAM 総量の 50% 強に収まる single-tenant 安全圏。ADR-0013 で決定根拠を正式化し、ADR-0012 §2.3 の β3 上限 8192 との境界逸脱を Amendment で記述。運用影響: env var `PHASE5_PARENT_RSS_MAX_MB` でデプロイごとに override 可。/ Raised `DEFAULT_PARENT_RSS_MAX_MB` in `phase5-config.ts` from 4096 to 7168. The β2 plan-time provisional value of 6144 was insufficient on re-verification (reftrix.io measured 6363/6480 MB), so the final value is set to the observed maximum 6480 MB + ~10% margin = 7168. On a 16 GB RAM single-tenant node, parent 7168 + Phase 5 children (~1300 MB) = 8468 MB stays well under 50% of RAM. ADR-0013 formalises the rationale; ADR-0012 §2.3 is amended to document that β2 picks 7168 against the β3 ceiling of 8192. Operational impact: override per-deployment via the `PHASE5_PARENT_RSS_MAX_MB` env var.
  - **P0-2: `disposePhase4Memory` の選択的中間データ解放 (非破壊) / Selective intermediate disposal at Phase 4→5 handoff (non-destructive)**: `apps/mcp-server/src/workers/page-analyze-worker.ts` の `disposePhase4Memory` を拡張し、Phase 5 が **使う** reference (sections / patterns / animations / save results) は完全保持しつつ、**使わない** 中間フィールドのみを選択解放したうえで `tryGarbageCollect()` を発火。`MotionServiceResult` からは `frame_capture` / `frame_analysis` (5-30 MB) / `webgl_animations` / `video_info` / `runtime_info` / `warnings` / `js_animation_summary` / `js_animations` (embedding 用 `jsAnimationsForEmbedding` は別フィールドで保持) を破棄。`ScrollVisionResult` からは per-capture `analyses` (Ollama Vision raw response) のみ破棄し `scrollTriggeredAnimations` は保持。過去 PR7b の `state = null` 強制撤回 (PR7e-α / ADR-0012 §2.1) とは異なり、Phase 5 入力 reference は 1 つも壊さない。保持/破棄の境界は新規 `tests/workers/page-analyze-worker-phase4-dispose.test.ts` の 7 ケースで担保。実測効果: reftrix.io で beforeRssMb=6484 → afterRssMb=6480 (reclaimed 4 MB)、現状は小幅だが安定化。/ Extended `disposePhase4Memory` in `page-analyze-worker.ts` to keep every reference Phase 5 **uses** (sections / patterns / animations / save results) while selectively dropping intermediate fields it does **not**, then calls `tryGarbageCollect()`. `MotionServiceResult` drops `frame_capture` / `frame_analysis` (5-30 MB) / `webgl_animations` / `video_info` / `runtime_info` / `warnings` / `js_animation_summary` / `js_animations` (the embedding-bound `jsAnimationsForEmbedding` lives in a separate field and is preserved). `ScrollVisionResult` drops per-capture `analyses` (raw Ollama Vision response) while keeping `scrollTriggeredAnimations`. Unlike the PR7b `state = null` forced null-out reverted by PR7e-α (ADR-0012 §2.1), this preserves every reference Phase 5 actually consumes. The keep/drop boundary is enforced by 7 new cases in `tests/workers/page-analyze-worker-phase4-dispose.test.ts`. Measured effect on reftrix.io: beforeRssMb=6484 → afterRssMb=6480 (reclaimed 4 MB) — modest but stabilising.
  - **P0-3 (revert): `onnxruntime-node` 1.21.0 → 1.24.3 upgrade 試行後に revert、1.21.0 を維持** / **P0-3 (reverted): `onnxruntime-node` 1.21.0 → 1.24.3 upgrade attempted then reverted; keeping 1.21.0**: β2 計画では packages/ml/package.json の `optionalDependencies.onnxruntime-node` を 1.21.0 → 1.24.3 に upgrade する予定だったが、検証段階で 1.24.3 に切り替えると in-process embedding 経路が CUDA library missing で全 fail する regression が判明したため revert。packages/ml/package.json は 1.21.0 のまま据え置き。`packages/ml/node_modules/onnxruntime-node` の 1.24 系バイナリが Node.js `worker_threads` 内の CUDA 初期化で provider library を解決できない事象と整合 (ADR-0014 §Context 参照)。再評価トリガと upstream issue link は ADR-0014 に記録。/ The β2 plan called for bumping `optionalDependencies.onnxruntime-node` in `packages/ml/package.json` from 1.21.0 to 1.24.3. Verification revealed a regression where 1.24.3 caused in-process embedding paths to fail universally with CUDA library missing errors, so the bump was reverted and 1.21.0 was kept. Consistent with a known class of failures where the 1.24 binary cannot resolve CUDA provider libraries inside Node.js `worker_threads` (see ADR-0014 Context). Reevaluation triggers and the upstream issue link are tracked in ADR-0014.
  - **影響範囲 / Scope**: 5 files changed, +296 / -64 (net ~232 lines). 破壊的変更なし。DB migration / API 変更なし。env var `PHASE5_PARENT_RSS_MAX_MB` デフォルト値のみ変更 (4096 → 7168、override 可)。運用者は RAM 総量 < 16 GB のホストでは override 推奨 (例: 8 GB ホストでは `PHASE5_PARENT_RSS_MAX_MB=3584` 等)。/ 5 files, +296/-64 (net ~232 lines). No breaking change; no DB migration; no API change. Only the `PHASE5_PARENT_RSS_MAX_MB` env-var default changes (4096 → 7168, still overridable). Operators on hosts with < 16 GB RAM should override (e.g. `PHASE5_PARENT_RSS_MAX_MB=3584` on an 8 GB host).
  - **受け入れ基準 — partial success / Acceptance criteria — partial success**: reftrix.io `page.analyze` (jobId `019d9764-591a-738f-8704-7f76f59bfa40`) 検証では **success=true は未達**。達成項目: (1) Parent RSS Guard 通過 (6480 < 7168、β1 の 4096 なら skip 確定)、(2) Phase 5 fork 成立、(3) Visual Embedding (DINOv2 in-process) initialization 成功。阻害要因 (P1 範囲): (a) Phase 5 text fork child が abnormal exit (skipReason=`text_child_abnormal_exit`、exitCode=signal)、(b) LayoutEmbedding in-process が CUDA library missing で全 fail。これら 2 件は onnxruntime-node 1.21 / 1.24 共通の課題で P0-3 の scope 外、別途 P1 で対応予定。/ Verified on reftrix.io `page.analyze` (jobId `019d9764-591a-738f-8704-7f76f59bfa40`): **`success=true` not achieved**. Achieved: (1) Parent RSS Guard passes (6480 < 7168; β1's 4096 would skip), (2) Phase 5 fork proceeds, (3) Visual Embedding (DINOv2 in-process) initialisation succeeds. Remaining blockers (P1 scope): (a) Phase 5 text fork child exits abnormally (skipReason=`text_child_abnormal_exit`, exitCode=signal), (b) LayoutEmbedding in-process fails universally with CUDA library missing. Both exist on onnxruntime-node 1.21 _and_ 1.24, so they are out of scope for P0-3 and tracked to P1.
  - **品質ゲート / Quality gates**: `pnpm typecheck` 0 errors / `pnpm lint` 0 errors / `pnpm format:check` PASS / `pnpm docs:verify` 45 PASS 0 FAIL / mcp-server config + phase4-dispose + skip-recovery suites 45/45 PASS / @reftrixmcp/ml 544/544 PASS (23 skipped on 1.21.0).
  - **関連 commit / Related commit**: hotfix 1 `3ce924a6` (BullMQ 5.66.5 の `resume()` race を `pause/resume` 削除で回避)。/ hotfix 1 `3ce924a6` (removed `pause/resume` to avoid the BullMQ 5.66.5 `resume()` race).
  - **関連 ADR / Related ADRs**: ADR-0013 (Parent RSS ceiling 7168 decision) 新規起票、ADR-0012 §2.3 Amendment (β3 ceiling 8192 との境界を明記)、ADR-0014 (onnxruntime-node upgrade deferred) 新規起票。/ New ADR-0013 (Parent RSS ceiling 7168 decision), Amendment to ADR-0012 §2.3 (boundary vs. β3 ceiling 8192), new ADR-0014 (onnxruntime-node upgrade deferred).

- **PR7e-β2 P1: fork child CUDA 伝搬修正 + RSS delta kill 閾値緩和で `success=true` 達成 (v0.4.0, commit `f749e0f4`)** / **PR7e-β2 P1: fix fork child CUDA propagation + raise RSS delta kill threshold, achieving `success=true` (v0.4.0, commit `f749e0f4`)**:
  - **真因 / Root causes**: (1) `.env.local` の `ONNX_EXECUTION_PROVIDER=cuda` が `buildChildEnv()` 経由で fork child に伝搬 → CUDA provider 不在で child 異常終了。(2) e5-base CPU 推論の glibc sbrk ヒープが delta 3121MB → kill threshold 3072MB 超過。/ (1) `.env.local` `ONNX_EXECUTION_PROVIDER=cuda` propagated to fork child via `buildChildEnv()`, causing crash without CUDA provider. (2) e5-base CPU inference glibc sbrk heap reached delta 3121MB, exceeding kill threshold 3072MB.
  - **修正 / Fixes**: `phase-5-fork-orchestrator.ts` の `buildChildEnv()` に `ONNX_EXECUTION_PROVIDER = "cpu"` 強制。`phase-5-child-ipc.ts` の RSS delta 閾値を warn 2048→2560, kill 3072→4096 に緩和。/ Force `ONNX_EXECUTION_PROVIDER = "cpu"` in `buildChildEnv()`. Raise RSS delta thresholds: warn 2048→2560, kill 3072→4096.
  - **検証 / Verification**: reftrix.io `page.analyze` で **success=true**, failedPhases=[], 全 7 フェーズ完走。Section 8 text + 8 vision / Part 98 text + 42 visual。/ reftrix.io `page.analyze`: **success=true**, failedPhases=[], all 7 phases completed. Section 8 text + 8 vision / Part 98 text + 42 visual.

### Security / セキュリティ

- **`chore(deps)`: example/reftrix4 Next.js を 16.2.3 に bump + test/test2 を workspace から除外 (GHSA-q4gf-8mx6-v5v3)** / **`chore(deps)`: bump example/reftrix4 Next.js to 16.2.3 + exclude test/test2 from workspace (GHSA-q4gf-8mx6-v5v3)**:
  - Next.js Denial of Service with Server Components (High CVE, CVE-2026-23869) を解消。`example/reftrix4/package.json` の `next` / `eslint-config-next` を `16.1.7` → `16.2.3` に bump。破壊的変更なし（16.2 はパフォーマンス改善・新機能のみで、reftrix4 の空 `next.config.ts` には影響なし）。/ Resolves Next.js DoS with Server Components (High CVE, CVE-2026-23869). Bumps `next` / `eslint-config-next` in `example/reftrix4/package.json` from `16.1.7` → `16.2.3`. No breaking changes (16.2 ships performance improvements and new features only; reftrix4's empty `next.config.ts` is unaffected).
  - `pnpm-workspace.yaml`: 実験プロトタイプ `example/test` (reftrix-the-prism) と `example/test2` (lattice) を workspace から除外（`!example/test` / `!example/test2` negate パターン）。`pnpm audit --audit-level=high` のスコープから外すことで release gate「High 脆弱性 0 件」を満たす。ファイル自体は削除せず保持（LP 資産のため、削除判断は別途）。/ Excludes experimental prototypes `example/test` (reftrix-the-prism) and `example/test2` (lattice) from the workspace via `!example/test` / `!example/test2` negate patterns, removing them from `pnpm audit --audit-level=high` scope to satisfy the release gate ("zero High vulnerabilities"). Files are preserved on disk (LP assets; deletion decision is separate).
  - **注記 / Note**: `example/test/` および `example/test2/` の `package.json` には脆弱バージョン（`next@16.1.7`）が残存するが、`pnpm-workspace.yaml` の `!example/test` / `!example/test2` negate パターンにより `pnpm audit` スコープ外。実験プロトタイプでありプロダクションビルドされないため、attack surface への露出なし。ファイル実削除はユーザー判断で将来対応予定。/ `example/test/` and `example/test2/` still reference the vulnerable version (`next@16.1.7`) in their `package.json`, but are excluded from `pnpm audit` scope via `pnpm-workspace.yaml` negate patterns. These are experimental prototypes not built for production; no attack surface exposure. File-level deletion is deferred to user decision.
  - PR7c (Pre-Return Pause resume + Phase 5 Screenshot 削除統一) のマージ前提条件として先行対応。`<!-- gen:ver-nextjs -->` マーカーは PR7c 本体コミットで既に 16.2.3 に同期済み、追加更新なし。/ Prerequisite fix for PR7c (Pre-Return Pause resume + Phase 5 Screenshot deletion unification) merge. The `<!-- gen:ver-nextjs -->` marker was already synced to 16.2.3 in the PR7c main commit.
  - 影響範囲 / Scope: `example/reftrix4/package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml` のみ。MCP サーバー本体は Next.js 非依存のため影響なし。OSS sync への影響なし（`example/` は `.ossfilter` で除外）。/ Only `example/reftrix4/package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml` are touched. MCP server proper does not depend on Next.js; no OSS sync impact (`example/` is excluded via `.ossfilter`).

### Added / 追加

- **PR7e-β1 (BLOCKER fix): Visual embedding 根本原因修復 — Worker resourceLimits + Phase 5 RSS tier + sections cap + loadEnvLocal 統一 + audit_logs bootstrap (v0.4.0)** / **PR7e-β1 (BLOCKER fix): Visual embedding root-cause remediation — Worker resourceLimits + Phase 5 RSS tier + sections cap + loadEnvLocal unification + audit_logs bootstrap (v0.4.0)**: PR7e-α で stale backfill を救出しても Stripe 697 parts 再 analyze で再度 visual embedding 0 件化する根本原因 (ADR-0012 §3 BLOCKER 4) を 6 commit で修復。Commit 1: `本体を正式に landing (205 行、日英バイリンガル、ADR-0008 §4 supersedes、β1/β2/β3 分割プラン)。Commit 2: ONNX`EmbeddingService`/`DINOv2Service`の Worker spawn に`resourceLimits.maxOldGenerationSizeMb=4096`を明示指定 (env`ML_WORKER_MAX_OLD_SPACE_MB`、range 512..8192)、CPU fallback OOM を解消、`packages/ml/src/config/worker-resource-limits.ts`新設 + Zod + 15 unit tests。Commit 3:`PHASE5_PARENT_RSS_MAX_MB`default 3072→**4096** 段階緩和 (Stripe 相当 5027MB で early skip しなくなる)、新`PHASE5_MAX_SECTIONS_INPUT=50`で Phase 4→5 handoff の`sections.slice(0, maxSectionsInput)`を適用 (GDPR Art. 5(1)(c) data minimisation)、phase5-config tests 6→13。Commit 4:`packages/core/src/config/env-local.ts` 新規、5 script (`start-workers`/`check-embedding-coverage`/`backfill-embeddings`/`repair-page-analyze`/`repair-orphaned-backfill-records`) の自前実装を shared helper に統一 (SEC-β-01 maxDepth=5 / SEC-β-07 verbose=false で DATABASE_URL password 漏洩防止 / 既存 `process.env`絶対非上書き)、16 unit tests で password-leak-proofness 検証。Commit 5:`audit-log.service.ts`に`bootstrapAuditLogServiceForScript(prisma)`追加、2 repair script で呼び出して`getAuditLogService().log()` がサイレント warn だけ出して DB に書かない状態を解消 (GDPR Art.30)、実 DB integration test 2 シナリオで bootstrap 有無を検証 (`describe.skipIf(!DATABASE_URL)`CI セーフ)。Commit 6:`DATA_RETENTION.md`を 2.8.0 → **2.10.0** に bump、β1 5 変更の retention 影響 (なし) / SEC-β-02 運用ガイド (coredump 抑止`ulimit -c 0`推奨) を追加、DATABASE_URL sanitize regression test 7 cases (Prisma P1000 / ECONNREFUSED / timeout / stack trace / P2002 / string throw / 3 bootstrap paths 反復) を`tests/services/sanitize-error.regression.test.ts`に追加。SBOM は`.gitignore`ポリシーに従い`pnpm sbom`で再生成のみ実行。全 6 commit で`pnpm lint`/`pnpm typecheck`/`pnpm format:check`/`pnpm docs:verify`/ mcp-server 15319 pass / ml 544 pass / core 399 pass。4 面計画監査 (TPA/SEC/TDA/LCC) CONDITIONAL APPROVE 済。β2 Stage A (Phase 5 Worker Pool / Idle Timer) と β3 (観測性 + 再測定) は別 PR 予定。/ Root-cause fix in 6 commits for the zero-visual-embedding regression on Stripe 697 parts re-analyze (ADR-0012 §3 BLOCKER 4). Commit 1 lands the formal`ADR-0012`file (205 lines bilingual, supersedes ADR-0008 §4). Commit 2 passes explicit`resourceLimits.maxOldGenerationSizeMb=4096`(env`ML_WORKER_MAX_OLD_SPACE_MB`) to ONNX Worker spawn to stop CPU-fallback OOM; adds `packages/ml/src/config/worker-resource-limits.ts`+ Zod + 15 unit tests. Commit 3 relaxes`PHASE5_PARENT_RSS_MAX_MB`default 3072→**4096** and introduces`PHASE5_MAX_SECTIONS_INPUT=50`(GDPR Art. 5(1)(c) data minimisation); 6→13 tests. Commit 4 extracts`loadEnvLocal()`into`packages/core/src/config/env-local.ts`(SEC-β-01 maxDepth=5, SEC-β-07 verbose=false, preserve-existing-env), unifying 5 scripts; 16 unit tests. Commit 5 adds`bootstrapAuditLogServiceForScript()`so repair scripts persist audit_logs (GDPR Art.30); 2 real-DB integration scenarios. Commit 6 bumps`DATA_RETENTION.md`to **2.10.0**, adds SEC-β-02 operational guidance (coredump disablement), and 7 sanitize regression tests across 3 bootstrap paths. SBOM regenerated (not committed, per`.gitignore`). mcp-server 15319 / ml 544 / core 399 pass; 4-way plan audit CONDITIONAL APPROVE.
- **PR7e-α (Rescue): Visual embedding 5 種 zero + stale backfill 救出 (v0.4.0)** / **PR7e-alpha (Rescue): Visual-embedding 5-category zero + stale backfill rescue (v0.4.0)**: Stripe.com `page.analyze` で判明した 7 バグクラスタ (①〜⑦) を 6 commit で解消。根本は α' 診断 AI-1〜AI-4 で確定した `disposePhase4Memory` の 4 行 `null` 代入 (Commit 1 で撤回、ADR-0012 参照)。Commit 2: `PartVisualProcessor.requiresBboxResolution` 処理実装 + `bbox-resolution.helper` 本体 (SEC HIGH-1 SSRF 再検証 + SEC HIGH-3 `partBboxLaunchSemaphore` max=1)、SSRF block 時に `skipReason='ssrf_blocked_on_backfill'` 露出 (バグ② + バグ⑦)。Commit 3: `PhasedDbHandler` 統合で `analysis_phase_status` / `analysis_started_at` / `last_analyzed_phase` を success / partial / failure 全経路で遷移 (バグ④)。Commit 4: `dispatchBackfillJobsForPage` に `section_visual` 追加 + `backfill-pending.builder` に `sectionVisualPending` field + 最小 observability ログ常時出力 (バグ⑥)。Commit 5: 遡及修復スクリプト `repair-page-analyze.ts` 新設 (SEC 9 ガードレール: env gate / max pages 100 / `--confirm --yes` / `--operator` / CAS / User-Agent / `WorkerActiveLockService` probe / `sha256` idempotency key / non-fatal audit、LCC A-1 / A-2 スキーマで `_repair` suffix 必須)、バグ⑤救出。Commit 6: `DATA_RETENTION.md` に遡及処理の GDPR Art.5(1)(e) 整合性と audit_logs (Art.30) 記述を追加。全 commit で `pnpm lint` / `pnpm typecheck` / `pnpm format:check` / `pnpm docs:verify` / 該当テスト PASS。新規テスト 44 件。ADR-0012 "Visual Embedding 5-Category Zero + Stale Backfill Remediation" で 4 面 CONDITIONAL APPROVE (TPA ADR 統合 / SEC 9 ガードレール / TDA complexity + idempotency / LCC suffix + DATA_RETENTION) を記録。**注 / Note**: ADR-0012 本文は `への書込が現行ローカル harness 制約で不可のため、コミットメッセージ + 本 CHANGELOG +`DATA_RETENTION.md`追記に分散記録。PR7e-β で` を正式配置予定。/ Consolidated fix for the 7-bug cluster uncovered by Stripe.com `page.analyze` diagnostics (α-prime AI-1〜5). Commit 1 reverts `disposePhase4Memory`'s 4 `state.* = null` assignments (root cause of 5-embedding zero bug). Commit 2 implements `PartVisualProcessor.requiresBboxResolution` handling via `bbox-resolution.helper` (SEC HIGH-1 SSRF re-validation + SEC HIGH-3 Chromium launch semaphore; `skipReason='ssrf_blocked_on_backfill'` surface). Commit 3 integrates `PhasedDbHandler` so `analysis_phase_status` / `analysis_started_at` / `last_analyzed_phase` advance on all paths. Commit 4 extends `dispatchBackfillJobsForPage` with `section_visual` + `sectionVisualPending` + unconditional dispatch log. Commit 5 adds the retrospective `repair-page-analyze.ts` with 9 SEC guardrails and LCC A-1/A-2 audit schema (`_repair` suffix). Commit 6 documents Art. 5(1)(e) alignment and Art. 30 audit trail in `DATA_RETENTION.md`. All commits pass `pnpm lint` / `pnpm typecheck` / `pnpm format:check` / `pnpm docs:verify` and relevant tests (+44 new tests). ADR-0012 "Visual Embedding 5-Category Zero + Stale Backfill Remediation" captures the 4-way CONDITIONAL APPROVE (TPA single-ADR / SEC 9 guardrails / TDA complexity + idempotency / LCC suffix + DATA_RETENTION). ADR-0012 body is distributed across commit messages + this CHANGELOG entry + `DATA_RETENTION.md` due to a local-harness write restriction on ` a formal file will be landed in PR7e-beta.
- **PR7e-α0: helper 先行抽出 — bbox-clipping / sanitize-selector / bbox-resolution.helper 骨格 (v0.4.0)** / **PR7e-α0: Helper extraction — bbox-clipping / sanitize-selector / bbox-resolution.helper skeleton (v0.4.0)**: PR7e-Ω (Stripe.com page.analyze で判明した 7 バグ統合修正) を 5 PR (α0→α'→α→β→γ) に分割する先行リファクタ段階。`apps/mcp-server/src/utils/bbox-clipping.ts` (`clampBboxToImage()` — Sharp extract_area 境界値エラー防止、負値→0 / オーバーフロー→imgW-left-1 余白確保 / NaN/Infinity→null)、`apps/mcp-server/src/utils/sanitize-selector.ts` (`sanitizeSelector()` / `sanitizeCssClasses()` — CSS-in-JS hash / email / UUID / 汎用 hash-suffix の `[REDACTED]` 置換 + 80/32 char truncate + 先頭 3 件制限、GDPR Art.5(1)(c) データ最小化 / SEC HIGH / LCC B-1)、`apps/mcp-server/src/workers/phases/shared/bbox-resolution.helper.ts` (`BboxResolutionParams` / `BboxResolutionResult` 型 + throw-only skeleton、α で PartBboxPlaywrightService の thin wrapper として Phase 5 / backfill / repair script 3 経路に統合予定) を新設。call site 統合なし (α で実装)。テスト 22 件 (bbox-clipping 10 件 / sanitize-selector 12 件) 追加、pass^3 確認済み。4 面計画監査 (TPA/SEC/TDA/LCC) CONDITIONAL APPROVE。/ Pre-refactor stage that splits PR7e-Ω (unified fix for 7 bugs uncovered by Stripe.com page.analyze) into 5 PRs (α0→α'→α→β→γ). Adds `bbox-clipping.ts` (`clampBboxToImage()` for Sharp.extract boundary-error prevention), `sanitize-selector.ts` (`sanitizeSelector()` / `sanitizeCssClasses()` for CSS-in-JS hash / email / UUID / generic hash-suffix redaction with 80/32-char truncation and top-3 cap, per GDPR Art.5(1)(c) data minimisation / SEC HIGH / LCC B-1), and `bbox-resolution.helper.ts` (type definitions + throw-only skeleton; integrated across Phase 5 / backfill / repair script in α). No call-site integration (deferred to α). 22 new tests (10 bbox-clipping + 12 sanitize-selector) with pass^3 verified. 4-way plan audit (TPA/SEC/TDA/LCC) CONDITIONAL APPROVE.

### Changed / 変更

- **PR7d-2: Worker 起動経路の統一 — `spawnPageAnalyzeWorkerChild()` ヘルパー化 (v0.4.0)** / **PR7d-2: Unified Worker spawn path via `spawnPageAnalyzeWorkerChild()` helper (v0.4.0)**: `analyze-50-award-sites.ts` / `reanalyze-serial.ts` / `reanalyze-large-html.ts` の 3 バッチスクリプトは `apps/mcp-server/scripts/_worker-spawn-helper.ts::spawnPageAnalyzeWorkerChild()` 経由で統一起動。旧 `spawn(...)` 直書きは削除。`REFTRIX_ALLOW_MANUAL_WORKER=true` が自動注入され、起動前に BullMQ `queue.getWorkers()` で既存 Worker を probe し warning を出す。`package.json` の dead script `worker:start:quality` (Phase Quality Worker 廃止 v0.3.0 以来参照元なし) も同時削除。/ The 3 batch scripts now spawn through `_worker-spawn-helper.ts::spawnPageAnalyzeWorkerChild()`. Direct `spawn(...)` calls removed. The helper auto-injects `REFTRIX_ALLOW_MANUAL_WORKER=true` and pre-probes BullMQ `queue.getWorkers()` with a warning. Dead script `worker:start:quality` (no referents since v0.3.0) also removed.

### Bug Fixes / バグ修正

- **PR7d-3: ドキュメント同期 + 監査要件対応（audit_logs / OSS 公開 / fail-open 整合）(v0.4.0)** / **PR7d-3: docs sync + audit compliance (audit_logs / OSS exposure / fail-open alignment) (v0.4.0)** (`CVE-not-assigned (internal reliability improvements)`):
  - **背景 / Context**: PR7d-1 (d98fece4) + PR7d-2 (a1b6c4e0) の計 8 回の CONDITIONAL PASS 監査で deferred された LOW/MEDIUM 指摘を集約する最終 PR。リリース可能状態を実現する。/ Final PR aggregating all LOW/MEDIUM deferred items from the 8 CONDITIONAL PASS audits of PR7d-1 (d98fece4) and PR7d-2 (a1b6c4e0), bringing the branch to release-ready state.
  - **SEC M-1 (Redis fail-closed → fail-open 整合)**: `start-workers.ts` の `evaluateDualRunGuard()` が ADR-0011 記載の「Redis 障害時は fail-open」設計意図と不整合だった。`WorkerActiveLockService.checkExistingLock()` / `acquireLock()` が内部で catch して両ケース (Redis 不可到達 / race lost) を null/false に潰していたため、呼び出し側は Redis 障害でも fail-closed で `process.exit(1)` せざるを得なかった。`tryAcquireLock()` / `probeExistingLock()` の discriminated union API を追加し、fail-open vs fail-closed を明示的に区別。既存 `acquireLock` / `checkExistingLock` は legacy wrapper として残置 (後方互換)。/ `start-workers.ts`'s `evaluateDualRunGuard()` was fail-closed even on Redis outage, contradicting ADR-0011's documented fail-open intent. Added discriminated union APIs `tryAcquireLock()` / `probeExistingLock()`; legacy methods kept as wrappers.
  - **LCC MEDIUM-1 (`.ossfilter` 更新)**: `repair-orphaned-backfill-records.ts` を OSS 公開対象に追加 (`.ossfilter` に `+ /apps/mcp-server/scripts/repair-orphaned-backfill-records.ts` include exception 追加)。operator が OSS 利用時に破損レコード修復できるよう透明性向上 (AGPL-3.0)。`_worker-spawn-helper.ts` は内部バッチ専用のため除外継続。/ Added `repair-orphaned-backfill-records.ts` as an OSS-exposed script (include exception in `.ossfilter`) so OSS operators can repair orphaned records. `_worker-spawn-helper.ts` remains excluded (internal batch-only).
  - **LCC MEDIUM-2 (TTL cron + repair script audit_logs 記録)**: `ScreenshotPersistenceService.cleanupExpired()` が削除件数 > 0 時に `audit_logs` へ `action=screenshot_ttl_cleanup` エントリ追加 (GDPR Art.30 処理活動記録)。`repair-orphaned-backfill-records.ts` は dry-run / confirm 両モードで `action=backfill_orphaned_repaired` エントリ追加 (dry-run でも検出件数は運用証跡として重要)。両経路とも `webPageId` は `truncateId` で 8 文字 + `...` に truncate (PII 最小化 GDPR Art.5(1)(c))。/ `cleanupExpired()` writes an `audit_logs` entry when `deletedCount > 0`. The repair script writes an entry in both dry-run and confirm modes. Both paths truncate `webPageId` to 8 chars (PII minimisation).
  - **SEC L-1 (sanitizeErrorMessage 適用)**: `start-workers.ts` の fatal error handler (3 箇所) / `_worker-spawn-helper.ts` の probe error (1 箇所) / `repair-orphaned-backfill-records.ts` の fatal handler (1 箇所) の生 `error.message` を全て `sanitizeErrorMessage()` に置換。CWE-209 (Information Exposure Through Error Message) 対応。/ Replaced raw `error.message` in 5 locations with `sanitizeErrorMessage()` (CWE-209).
  - **SEC L-2 (heartbeat extendLock timeout)**: `start-workers.ts` の standalone heartbeat の `extendLock()` 呼び出しを `Promise.race` で 10s タイムアウト保護。Redis hang による heartbeat cadence の詰まりを防止。/ Wrapped heartbeat `extendLock()` in `Promise.race` with 10s timeout; prevents heartbeat cadence stall on hung Redis calls.
  - **TPA LOW-1 (`acquireRedisLockBestEffort` 再入防止)**: `WorkerSupervisor` に `lockAcquireInflight` フラグを追加。連続 `ensureWorkerRunning()` 呼び出しで `acquireLock()` が多重 Redis round-trip を発生させる race を防止。/ Added `lockAcquireInflight` flag to prevent concurrent `acquireLock()` round-trips on repeated `ensureWorkerRunning()` calls.
  - **TPA LOW-3 (fatal path での lock release)**: `start-workers.ts` の `process.on('exit', ...)` ハンドラに standalone lock の best-effort release を追加。crash 時に 60s TTL まで lock がリークするのを短縮。/ Added best-effort `releaseLock` in `process.on('exit', ...)`; reduces leaked-lock window on crash from 60s TTL to near-zero.
  - **TDA NEW-1 (dead script 削除)**: `apps/mcp-server/README.md:952` と `docs/README.ja.md:453` から `worker:start:quality` (Phase Quality Worker 廃止で v0.3.0 以来 dead script) を削除。/ Removed `worker:start:quality` references from README files.
  - **ドキュメント同期 / Documentation sync**:
    - `CLAUDE.md`: Worker 設定表に新 env var 3 件追加 (`REFTRIX_ALLOW_MANUAL_WORKER` / `REFTRIX_WORKER_IS_CHILD` / `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN`)、Pre-Return Pause 記述に PR7d-2 Redis lock 追記、Screenshot Persistence セクションに PR7d-1/7d-2 更新を反映。
    - ` 同 env var 3 件を Worker 設定表に追加、v0.4.0 PR7d 新コンポーネントセクションを新設 (`WorkerActiveLockService`/`spawnPageAnalyzeWorkerChild()`/`cleanupPhase5TempDir` 3 段 whitelist / TTL cron audit_logs / repair script audit_logs)。
    - ` WorkerSupervisor セクションに「Dual-Run Prevention (v0.4.0 PR7d-2 / PR7d-3)」サブセクションを新設。4 ケース early-return 順序 + fail-open vs fail-closed 判別 + audit trail を明文化。
    - `apps/mcp-server/DATA_RETENTION.md`: 削除経路マトリクスを PR7d-2 / PR7d-3 列追加、§6.1「運用手順: Worker 二重稼働防止」セクション追加 (AGPL-3.0 透明性)、バージョン 2.7.0 → 2.8.0 bump。
    - `README.md` / `apps/mcp-server/README.md` / `docs/users-guide/01-getting-started.md` / `docs/users-guide/04-troubleshooting.md` / `docs/README.ja.md` / `scripts/templates/oss-readme.md`: 手動 Worker 起動を「開発者向け」に降格し、`REFTRIX_ALLOW_MANUAL_WORKER=true` opt-out の説明を追加。
  - **ADR 追記 / ADR supplements**:
    - ADR-0011 §K 脅威モデル追加 (SEC M-2): 同一ホスト内誤設定防止を目的、マルチテナント隔離・env var 偽装・`REFTRIX_WORKER_IS_CHILD=1` 偽装は脅威モデル外。
    - ADR-0011 §L PR サイズ超過 approval 記録 (TPA M-1): PR7d-2 (1456 行) の 800 行超過理由を明文化。
    - ADR-0010 PR7e 継続検証項目 (TPA M-1 PR7d-1): `cleanupPhase5TempDir` のデッドコード化検証を PR7e へ deferred。
  - **テスト追加 / New tests**: `worker-active-lock.service.test.ts` に discriminated union API テスト + legacy API 互換性テスト 9 ケース追加、`screenshot-persistence.service.test.ts` に TTL cron audit_logs 記録 2 ケース追加、`repair-orphaned-backfill-records.test.ts` に audit_logs 記録 3 ケース追加、`start-workers-dual-run.test.ts` に SEC M-1 fail-open/fail-closed 判別テスト 1 ケース追加。
  - **影響範囲 / Scope**: 破壊的変更なし (`WorkerActiveLockService` API 拡張は後方互換、既存呼び出しを更新済)。DB Migration / Config 追加なし (env var は PR7d-2 で済)。integration test は別 tracking issue へ deferred。
- **PR7d-2: Worker 二重稼働防止 + RAW decode 永続化ディレクトリ汚染修正 (v0.4.0)** / **PR7d-2: Worker dual-run prevention + RAW decode target directory fix (v0.4.0)** (`CVE-not-assigned (internal concurrency bug, not externally exploitable)`):
  - **背景 / Context**: PR7d-1 (ADR-0010) で永続化 Screenshot 削除経路を完全修復した直後、別種の障害として MCP server の `WorkerSupervisor` fork 子プロセスと、手動 `pnpm worker:start:page` で起動した tsx プロセスが同じ `page-analyze` BullMQ Queue を並列消費し、jobId race / `embeddingBackfillStatus` の race-condition 上書き / RSS 倍増を引き起こしていた。加えて PR7d-1 の TDA 監査で `phase-5-embedding.ts:2279` の RAW decode 書き込み先が永続化ディレクトリを指し、例外時にゴミ RAW ファイルが `<REFTRIX_SCREENSHOT_ROOT>/phase5/` に残存する LOW 指摘が未解消のまま残っていた。/ Immediately after PR7d-1, a new failure mode emerged: the MCP server's `WorkerSupervisor` fork child and a manual `pnpm worker:start:page` tsx process simultaneously consumed the same `page-analyze` BullMQ Queue, causing jobId races, `embeddingBackfillStatus` race-condition overwrites, and doubled RSS. PR7d-1's TDA audit also left a LOW finding unresolved: `phase-5-embedding.ts:2279` wrote RAW decode output into the persisted directory.
  - **A. WorkerActiveLockService 新規追加 / New service**: `apps/mcp-server/src/services/worker-active-lock.service.ts` — Redis キー `reftrix:worker:active:<workerType>` (UUID nonce, 60s TTL, 30s heartbeat) により単一ホスト上の二重 Worker を検出。`acquireLock` は `SET ... EX 60 NX` atomic、`releaseLock` は Lua script で自 nonce 一致時のみ削除 (他者の lock を誤削除しない)。/ Redis-based active-worker lock. `acquireLock` is atomic (`SET ... EX 60 NX`); `releaseLock` uses a Lua script that only deletes when the current value matches own nonce.
  - **B. WorkerSupervisor fork env 拡張 / Fork env expansion**: 構築時に `randomUUID()` で boot token 生成 → 初回 `ensureWorkerRunning()` で Redis best-effort publish。fork 子プロセスには `REFTRIX_WORKER_IS_CHILD=1` + `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN=<UUID>` を env 注入。Redis 失敗時は warn のみで supervisor 起動継続 (fail-open)。/ Supervisor publishes its boot-token to Redis best-effort and injects `REFTRIX_WORKER_IS_CHILD=1` + `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN=<UUID>` into every fork child. Redis-unreachable falls back to warn-and-continue.
  - **C. start-workers.ts 二重起動検出 / Dual-run detection**: `main()` で 4 ケース評価: (1) `NODE_ENV=test` skip、(2) `REFTRIX_WORKER_IS_CHILD=1` skip (fork 子の自己検出ループ回避)、(3) `REFTRIX_ALLOW_MANUAL_WORKER=true` warn + 続行 (opt-out)、(4) 既定経路は Redis lock 確認 → 検出時 `process.exit(1)`、未検出時は lock 取得 + 30 秒 heartbeat。Redis 障害時は fail-open。/ `main()` evaluates 4 cases: test-mode skip, fork-child skip, manual opt-out warn, and Redis-lock probe that `process.exit(1)` on detection or acquires with 30s heartbeat. Fail-open on Redis failure.
  - **D. spawnPageAnalyzeWorkerChild() ヘルパー統一 / Unified spawn helper**: 詳細は「Changed」節参照。/ See "Changed" section above.
  - **E. RAW decode 書き込み先修正 (TDA LOW-1) / RAW decode target directory fix (TDA LOW-1)**: `phase-5-embedding.ts:2279` の `phase5TmpDir = path.dirname(screenshotPngPath)` を `createPhase5TempDir()` に置換。RAW 出力先が `<os.tmpdir()>/reftrix-phase5-raw-*/screenshot.raw` に移動、outer `finally` で `cleanupPhase5TempDir()` を呼び例外パスでも確実回収。副次変更: `decodeToRawFile()` の Path Traversal 防御を「tmpDir が `os.tmpdir()/reftrix-phase5-raw-*` 配下」に切り替え (呼び出し側で既に `isAllowedScreenshotPath()` 検証済み)。/ Replaced `path.dirname(screenshotPngPath)` with `createPhase5TempDir()` — RAW output moves to `<os.tmpdir()>/reftrix-phase5-raw-*/screenshot.raw`, cleanup guaranteed via outer `finally`. Side change: `decodeToRawFile()`'s path-traversal guard switches to "tmpDir under `os.tmpdir()` with `reftrix-phase5-raw-` prefix".
  - **ADR-0011 起票 (Accepted)**: ` で PR7d-2 の Root Cause、代替案 4 件、GDPR Art. 32(1)(b) / APPI 第 23 条整合性、deferred items を記録。/ Filed ADR-0011 with root-cause analysis, 4 alternatives, GDPR/APPI alignment, and deferred items.
  - **テスト追加 / Tests added**: `worker-active-lock.service.test.ts` (14 件、in-memory Redis stub で acquire/extend/release/checkExistingLock の atomic semantics 検証) + `start-workers-dual-run.test.ts` (11 件、source inspection で 4 ケース評価と lifecycle integration を担保)。/ `worker-active-lock.service.test.ts` (14 cases) + `start-workers-dual-run.test.ts` (11 cases).
  - **Deferred to PR7d-3 (800 行制約遵守のため / to stay within the 800-line PR budget)**: CLAUDE.md / current-architecture.md / README Worker 設定表更新 (`REFTRIX_ALLOW_MANUAL_WORKER` 等の公式記載)、TTL cron audit_logs 記録、`.ossfilter` 更新 (LCC MEDIUM-1)、実 BullMQ + 実 DB + 実 DINOv2 integration test (spec C-1、DINOv2 ~800MB モデルロードのため個別 PR 化)、`cleanupPhase5TempDir` → `cleanupPhase5RawDecodeTmpDir` 改名 (ADR-0010 からの継続 deferred)。/ Docs sync, TTL cron audit_logs, `.ossfilter` update, real-stack integration test, and `cleanupPhase5TempDir` rename — deferred to PR7d-3.
  - **注記 / Note**: 本バグは内部プロセス間競合に起因し、外部から攻撃可能な脆弱性ではない。CVE 未採番。GDPR Art. 32(1)(b) の「処理の完全性」要件と APPI 第 23 条「安全管理措置」に対する構造的強化。/ Internal inter-process concurrency bug, not externally exploitable. No CVE. Structurally reinforces GDPR Art. 32(1)(b) and APPI Art. 23.

- **PR7d-1: Phase 5 Screenshot 削除経路完全修正 + 破損レコード修復 (v0.4.0)** / **PR7d-1: Phase 5 Screenshot deletion path completion + orphaned record repair (v0.4.0)** (`CVE-not-assigned (internal bug, not externally exploitable)`):
  - **背景 / Context**: PR7c (ADR-0009) で `phase-5-fork-orchestrator.ts` の即時 `deleteScreenshot()` 呼び出しは解除したが、`page-analyze-worker.ts` の Phase 5 後処理 (L1294-1299) と finally ブロック (L2107-2116) に残っていた `cleanupPhase5TempDir(path.dirname(state.screenshotPngPath))` 呼び出し 2 箇所を見落としていた。v0.4.0 PR1 で `state.screenshotPngPath` が永続化パス `<REFTRIX_SCREENSHOT_ROOT>/phase5/<webPageId>.png` を指すようになった結果、これらの呼び出しが永続化ディレクトリ全体を `rmSync(recursive:true)` で破壊するようになり、Queue-based Backfill (`part_visual` / `section_visual`) の visual embedding が 0 件に戻る retention-over-deletion bug が残存していた。/ PR7c (ADR-0009) removed the immediate `deleteScreenshot()` call in `phase-5-fork-orchestrator.ts` but overlooked two `cleanupPhase5TempDir(path.dirname(state.screenshotPngPath))` call sites in `page-analyze-worker.ts` (Phase 5 post-processing at L1294-1299 and finally block at L2107-2116). After v0.4.0 PR1 repointed `state.screenshotPngPath` at the persisted path `<REFTRIX_SCREENSHOT_ROOT>/phase5/<webPageId>.png`, these calls started destroying the persisted directory via `rmSync(recursive:true)`, reintroducing the zero-visual-embedding bug.
  - **A. Worker cleanup 除去 / Worker cleanup removal**: `page-analyze-worker.ts` の 2 箇所から `cleanupPhase5TempDir` 呼び出しを削除。`delete state.screenshotPngPath` による in-memory null 化のみ残す。`delete` は throw しないため finally 内 try/catch を縮退。/ Removed the 2 `cleanupPhase5TempDir` calls in `page-analyze-worker.ts`; only the in-memory null-out via `delete state.screenshotPngPath` remains. Since `delete` cannot throw, the finally-block try/catch was simplified.
  - **B. 3 段 whitelist 防御 / 3-stage whitelist defense (SEC A-1)**: `cleanupPhase5TempDir` に 3 段検証を追加 — (1) 入力正規化 + null byte 防御、(2) `fs.realpathSync` で symlink 解決（ENOENT 時は silent return）、(3) `realTmp.startsWith(realOsTmp + path.sep)` + `path.basename(realTmp).startsWith("reftrix-phase5-raw-")` の 2 条件 AND。いずれか失敗時は `logger.warn` + silent return（throw しない）。ログ出力パスは `.slice(0, 80)` で truncate、`sanitizeErrorMessage` で catch 時のエラー整形。/ Added a 3-stage check to `cleanupPhase5TempDir`: (1) input normalization + null-byte defense, (2) symlink resolution via `fs.realpathSync` (silent return on ENOENT), (3) `realTmp.startsWith(realOsTmp + path.sep)` AND `path.basename(realTmp).startsWith("reftrix-phase5-raw-")`. On any failure: `logger.warn` + silent return. Logged paths truncated to 80 chars; catch errors formatted via `sanitizeErrorMessage`.
  - **C. DRY 統合 / DRY consolidation (TDA)**: `phase-5-fork-orchestrator.ts` の `cleanupPhase5TmpDirOnly` を削除し、`cleanupPhase5TempDir(path.dirname(screenshotPngPath))` に委譲。B の 3 段防御が備わったため冗長なロジックを排除。/ Removed `cleanupPhase5TmpDirOnly` from `phase-5-fork-orchestrator.ts` and delegated to `cleanupPhase5TempDir`.
  - **D. 破損レコード修復スクリプト / Repair script**: `apps/mcp-server/scripts/repair-orphaned-backfill-records.ts` を新規追加。`--dry-run`（デフォルト）/ `--confirm` ガードで、`screenshotStoragePath IS NOT NULL` かつ `embeddingBackfillStatus IN ('queued','in_progress')` かつ実ファイル不在のページを検出し、`embeddingBackfillStatus='skipped_screenshot_missing'` に CAS 遷移する。/ New `repair-orphaned-backfill-records.ts`. Detects pages where `screenshotStoragePath IS NOT NULL`, `embeddingBackfillStatus IN ('queued','in_progress')`, and the file is missing; transitions them to `skipped_screenshot_missing` via `updateMany` CAS. `--dry-run` default, `--confirm` required.
  - **E. Enum 拡張 / Enum expansion**: `EmbeddingBackfillStatus` に `skipped_screenshot_missing` を追加。Prisma migration `20260415000000_add_embedding_backfill_skipped_screenshot_missing` で `ALTER TYPE ... ADD VALUE IF NOT EXISTS` を使用（非破壊的）。/ Added `skipped_screenshot_missing` to `EmbeddingBackfillStatus` via non-destructive Prisma migration.
  - **ADR-0010 起票 (Accepted)**: ` で PR7c の見落としと本修正の方針、GDPR Art.17/30/32(1)(b)/5(1)(a) 影響、7 日 TTL の定量根拠（BullMQ retry horizon から導出）、ADR-0009 との継承関係を記録。/ Filed ADR-0010 documenting the PR7c oversight, the PR7d-1 fix plan, GDPR impact (Art.17/30/32(1)(b)/5(1)(a)), the 7-day TTL quantitative rationale derived from the BullMQ retry horizon, and the succession relationship with ADR-0009.
  - **ドキュメント同期 / Doc sync**: `DATA_RETENTION.md` §9 を v2.7.0 に更新（削除経路マトリクスに PR7d-1 列追加 + "Historical Bug Transparency" セクション — PR7a-PR7c 期間の retention-over-deletion bug を GDPR Art.5(1)(a) 公正・透明性観点で開示）。/ Bumped `DATA_RETENTION.md` §9 to v2.7.0 with a PR7d-1 column in the deletion-path matrix and a new "Historical Bug Transparency" section disclosing the PR7a-PR7c retention-over-deletion bug per GDPR Art.5(1)(a).
  - テスト +18 件以上（whitelist 7 + worker regression guard 5 + repair script 6）、0 regression 目標。CLAUDE.md Worker 設定表、current-architecture、README 等の Worker-lock 関連ドキュメント同期は **PR7d-3 に延期**。Worker 二重稼働防止は **PR7d-2 に延期**。TTL cron `audit_logs` 記録実装も **PR7d-3 に延期**。/ 18+ new tests (whitelist 7 + worker regression guard 5 + repair script 6), zero regressions targeted. Docs sync for CLAUDE.md Worker config table, current-architecture, and README is **deferred to PR7d-3**. Worker double-start prevention is **deferred to PR7d-2**. TTL-cron `audit_logs` implementation is also **deferred to PR7d-3**.
  - **注記 / Note**: 本バグは内部コードパスの見落としで、外部から攻撃可能な脆弱性ではない（ユーザー入力で trigger できない、ネットワーク経由でも発動しない）。CVE 未採番。CWE-209 / CWE-770 / CWE-22 の「類似パターン」としてセキュリティ観点を担保。/ This bug is an internal code-path oversight, not an externally exploitable vulnerability (cannot be triggered by user input, cannot be invoked over the network). No CVE assigned. Security posture is preserved via CWE-209 / CWE-770 / CWE-22 "related pattern" defenses.

### Fixed / 修正

- **PR7c: Pre-Return Pause resume 補完 + Phase 5 Screenshot 削除タイミング統一 (v0.4.0)** / **PR7c: Pre-Return Pause resume patch + Phase 5 Screenshot deletion unification (v0.4.0)**:
  - **バグ 1 (Pre-Return Pause 永続化)**: `WORKER_MAX_JOBS_BEFORE_RESTART=1`（default）下で RSS 軽量 Worker (page-analyze / embedding-backfill) が 1 ジョブ完了後にジョブを取得しなくなる問題を修正。新 helper `applyPreReturnPauseAndMemoryGate()` で `pause(true)` → RSS 判定 → 閾値超過なら `process.exit(0)`、未満なら `worker.resume()` の 3 段階にまとめ、RSS 軽量 Worker でも次ジョブ取得を継続できるようにした。/ **Bug 1 (Pre-Return Pause becomes permanent)**: fixes the issue where RSS-light workers (page-analyze / embedding-backfill) stopped acquiring new jobs after a single completion under `WORKER_MAX_JOBS_BEFORE_RESTART=1` (default). The new `applyPreReturnPauseAndMemoryGate()` helper consolidates pause → RSS check → exit-or-resume, restoring job acquisition for RSS-light workers.
  - **バグ 2 (Phase 5 Screenshot 即時削除)**: `phase-5-fork-orchestrator.ts` の `cleanupScreenshotAndTmp()` が Visual child 完了直後に `deleteScreenshot()` を呼び、後続の Queue-based Backfill (`part_visual` / `section_visual`) が screenshot を参照できず visual embedding が 0 件しか生成されないバグを修正。関数名を `cleanupPhase5TmpDirOnly()` に改名し、RAW decode tmp dir (`reftrix-phase5-raw-*`) のみを best-effort 削除する。永続化された screenshot (`<REFTRIX_SCREENSHOT_ROOT>/phase5/<webPageId>.png`) の削除は (1) GDPR `data.delete` (Art. 17) + (2) PR6 TTL cron (7d) の 2 経路に統一。/ **Bug 2 (Phase 5 immediate screenshot deletion)**: fixes the issue where `phase-5-fork-orchestrator.ts`'s `cleanupScreenshotAndTmp()` called `deleteScreenshot()` immediately after the visual child completed, leaving nothing for the downstream Queue-based Backfill (`part_visual` / `section_visual`) to read — which in turn caused zero visual embeddings. Renamed to `cleanupPhase5TmpDirOnly()` — only best-effort removes the RAW decode tmp dir (`reftrix-phase5-raw-*`). Persisted screenshot deletion (`<REFTRIX_SCREENSHOT_ROOT>/phase5/<webPageId>.png`) is now consolidated into two paths: (1) GDPR `data.delete` (Art. 17) + (2) PR6 TTL cron (7d).
  - **M1 共通 helper 抽出**: `apps/mcp-server/src/workers/shared/post-job-lifecycle.ts` に `applyPreReturnPauseAndMemoryGate()` を新規追加。page-analyze-worker / embedding-backfill-worker 両方の success path が同じ lifecycle を共有する（DRY）。/ New shared helper `applyPreReturnPauseAndMemoryGate()` in `apps/mcp-server/src/workers/shared/post-job-lifecycle.ts`, used by both page-analyze-worker and embedding-backfill-worker success paths (DRY).
  - **M2 sanitizeErrorMessage 適用 (CWE-209)**: pause() / resume() の例外ログを `sanitizeErrorMessage()` 経由に統一。BullMQ 内部例外（Redis コマンド、jobId）がそのまま warn ログに出ないよう保護。/ pause() / resume() exceptions are now sanitized via `sanitizeErrorMessage()` (CWE-209), preventing BullMQ-internal Redis commands / jobIds from leaking into warn logs.
  - **M4 `maxJobsBeforeRestart` セマンティクス明記**: `WORKER_MAX_JOBS_BEFORE_RESTART=0` 時は helper が full no-op（pause / resume / memory-check すべてスキップ）。「再起動なし・永続 Worker モード」を明示サポート。/ When `WORKER_MAX_JOBS_BEFORE_RESTART=0`, the helper is a full no-op (skipping pause / resume / memory check) — explicit support for "no-restart persistent worker" mode.
  - **M6 DI 残骸クリーンアップ**: Phase 5 dispatch から `ScreenshotPersistenceService` の注入を削除。fork orchestrator の `deps.screenshotPersistenceService` 引数も除去。GDPR `data.delete` 経路は `service-registrar-search.ts` 経由の DI を維持。/ Removed `ScreenshotPersistenceService` injection from Phase 5 dispatch; removed `deps.screenshotPersistenceService` from the fork orchestrator. The GDPR `data.delete` path retains its DI via `service-registrar-search.ts`.
  - **M10 Art.17 回帰テスト**: `tests/services/gdpr-deletion-pr7c-regression.test.ts` に 4 ケース追加（data.delete 後の即時 unlink、in_progress / queued 中の削除、TTL cron 先行削除との冪等性）+ 1 regression ケース。/ Added 4 Art.17 regression cases + 1 regression case: immediate unlink after data.delete, deletion during in_progress / queued backfill, and idempotency vs. TTL cron prior deletion.
  - **ADR-0009 起票 (Accepted)**: ` Root Cause、3 頂点検証 (A=Product, B=Data/DB, C=Platform/Security)、Race Condition 対応方針 (M9) を記録。/ Filed ADR-0009 (Accepted) with root-cause analysis, A/B/C triangulation, and the M9 race-condition matrix.
  - **ドキュメント同期**: ` `apps/mcp-server/DATA_RETENTION.md`§9 (削除経路マトリクス),`CLAUDE.md`(Screenshot Persistence 節) を PR7c 方針に同期。/ Synced` `DATA_RETENTION.md` §9 (deletion-path matrix), and `CLAUDE.md` with PR7c's new policy.
  - テスト +12 件以上（helper 7 + GDPR 回帰 5）、0 regression。ADR-0009 Status: Accepted、4 面監査（TPA/SEC/TDA/LCC）計画段階で全 CONDITIONAL PASS。/ 12+ new tests (helper 7 + GDPR regression 5), zero regressions. ADR-0009 Accepted, all 4 audits CONDITIONAL PASS at plan stage.

### Added / 追加

- **PR7b: Phase 5 Skip Recovery 全活性化 + 収束修正 (v0.4.0)** / **PR7b: Phase 5 Skip Recovery full activation + convergence fix (v0.4.0)**:
  - Worker 即時 enqueue (`dispatchSkipRecoveryBackfill`): Phase 5 全体 skip (`skipped_fork_error` / `skipped_memory_pressure`) 時に全 7 カテゴリを embedding-backfill Queue へ同期 enqueue、`embeddingBackfillStatus` を `queued` に CAS 遷移。`updateMany WHERE status IN ('skipped_*', 'in_progress')` で Worker/Cron race を防御。/ Worker immediate enqueue on full Phase 5 skip, atomic CAS transition, protects against Worker/Cron races.
  - Retry cap 5 + back-pressure (waiting > 10,000): 無限ループ防御 (SEC HIGH-1) + DoS 増幅防御 (SEC HIGH-2)。超過時 `audit_logs.action = "backfill_retry_exhausted"` 記録。/ Infinite-loop + DoS amplification defense with audit log.
  - Exponential backoff + full jitter (SEC MEDIUM-1): 5s/10s/20s に ±50% full jitter 適用、Thundering Herd 回避。/ Full jitter for exponential backoff.
  - 親 RSS upstream guard (`PHASE5_PARENT_RSS_MAX_MB=3072` default) + Phase 4 dispose (in-memory reference null 化 + GC + RSS 3 サンプル平均)。/ Parent RSS guard + Phase 4 dispose.
  - Cron 拡張 + 7d TTL: WHERE を `IN ('in_progress', 'skipped_fork_error', 'skipped_memory_pressure')` に拡張、7 日超過で `failed` + `skip_recovery_expired` audit log。/ Cron expanded with 7d TTL.
  - `backfillPending.source` discriminated union (Zod `discriminatedUnion("source", [...])`): `sync_overflow` (既存) と `skip_recovery` (新規) を型レベル区別。両方同時 present は invariant violation として `logger.warn` 記録。/ Type-level discrimination for MCP clients.
  - SectionVisualProcessor DINOv2 統合: `requiresScreenshot()` → true、section vision embedding 再生成、PII フィルタ維持。/ SectionVisualProcessor regenerates vision embeddings.
  - 収束修正 (TPA CRITICAL / SEC HIGH / TDA H-1/H-2/M-1/M-2/M-3): `embeddingBackfillSkippedAt` 書き込み追加で cron dead code を解消、`SKIP_RECOVERY_RETRY_CAP` SSOT 化、`enqueueAllCategoriesForSkipRecovery` ヘルパー抽出で重複 67→0 行・複雑度 14→6/8、Zod max SSOT 化、`enqueuedAt` をループ開始前に記録。/ Convergence fix: `embeddingBackfillSkippedAt` writes unblock cron, SSOT consolidation, helper extraction reduces duplication 67 → 0 lines and complexity 14 → 6/8.
  - テスト +100 件以上、15,139 passed / regression ゼロ。ADR-0008 Status: Accepted、4 面監査 (TPA/SEC/TDA/LCC) 全 PASS（収束修正後）。/ 100+ new tests, zero regressions, ADR-0008 Accepted, all audits PASS.

- **ADR-0008 起票 — Phase 5 Skip Recovery 全カテゴリ Backfill 拡張 (Proposed)** / **ADR-0008 filed — Phase 5 Skip Recovery all-category Backfill extension (Proposed)**:
  - 新規 `を起票（Status: Proposed、Extends: ADR-0007）。Stripe 697 件ケース（jobId`019d8261-4f90-7708-922b-276a9dd22ee5`、親 RSS 5027MB → `text_child_abnormal_exit`→ 665 件 embedding 恒久欠落）を契機に、ADR-0007 の Part 限定前提が Phase 5 全体 skip 時に崩れる問題を解決する設計判断を記録。/ Filed new ADR-0008 (Status: Proposed, Extends: ADR-0007). Triggered by the Stripe 697-part case (jobId`019d8261-4f90-7708-922b-276a9dd22ee5`, parent RSS 5027MB → `text_child_abnormal_exit` → 665 embeddings permanently lost); records the design decision to resolve the failure mode where ADR-0007's Part-only scope breaks when the entire Phase 5 is skipped.
  - 11 項目の Decision: (1) `EmbeddingBackfillCategory` 2→7 値化（`section_visual` / `motion` / `background` / `js_animation` / `responsive` 追加）、(2) Phase 5 全体 skip 時の Worker 即時 enqueue、(3) exponential backoff + jitter（固定 60s 廃止）、(4) 親 RSS upstream guard `PHASE5_PARENT_RSS_MAX_MB`（デフォルト 3072MB）、(5) Phase 4 後の in-memory reference null 化（screenshot/sharedBrowser は物理維持）、(6) cron `reconcileStaleBackfillJobs` を `skipped_*` 対象化 + 7 日 TTL、(7) `backfillPending.source` discriminated union（`sync_overflow` / `skip_recovery`）、(8) retry cap 5 回（`embeddingBackfillRetryCount` 超過で `failed` 固定）、(9) `updateMany` CAS ガード拡張、(10) Strategy Pattern `BackfillCategoryProcessor` + `Record<EmbeddingBackfillCategory, ...>` exhaustiveness 型保証、(11) `data.delete` で全 7 カテゴリ jobId を BullMQ から削除。/ 11-item Decision set covering enum expansion, Worker immediate enqueue, exponential backoff, parent RSS guard, Phase 4 dispose, cron extension, source field, retry cap, CAS guard, Strategy Pattern, and `data.delete` queue removal.
  - Semantics Table（TPA H-3 必須）で `backfillPending.source` の `sync_overflow` / `skip_recovery` / 両方同時 present 3 状態の MCP クライアント推奨アクションを明文化。/ Semantics Table (TPA H-3) clarifies MCP client recommended actions for the three states of `backfillPending.source`.
  - 4 代替案（A: Part のみ / B: synchronous retry / C: 全カテゴリ + Strategy Pattern — 採用 / D: 完全 async）を比較検討、Privacy Considerations 6 項目（GDPR Art. 5/17/30/13/14、CCPA §1798.105、APPI 要配慮個人情報）、Migration Path を PR7a（基盤: schema + enum + 型 — 実装済み）と PR7b（skip recovery 実装 — 予定）に分離。/ Compares 4 alternatives (A/B/C-adopted/D), 6 privacy considerations, and splits migration into PR7a (scaffolding, implemented) + PR7b (skip recovery implementation, planned).
  - ADR-0007 は **Accepted のまま維持**（過去の設計根拠保持）。ADR-0008 は Extends 関係で、Part sync 成功時の 100 件閾値は維持し、Phase 5 全体 skip 時のみ all-or-nothing で全 7 カテゴリを enqueue する。/ ADR-0007 remains **Accepted** (preserved as historical rationale). ADR-0008 extends it: the 100-item threshold is preserved for successful Part sync, while all 7 categories are enqueued all-or-nothing only on full Phase 5 skip.

- **PR6 最終統合 — TTL cron + 自動 reconciliation + Stripe E2E + PR6 引継ぎ事項完全回収 (v0.4.0)** / **PR6 final integration — TTL cron + automatic reconciliation + Stripe E2E + PR6 follow-up full closure (v0.4.0)**:
  - 新規 `apps/mcp-server/src/cron/screenshot-cleanup-cron.ts`: PR1 で永続化した screenshot を日次 (24h 間隔、7 日保持、バッチ 1000 件上限) で削除。overlap 防止 + NaN ガード + `.unref()` timer。/ New cron for daily (24h interval, 7d retention, 1000-file batch cap) cleanup of PR1-persisted screenshots. Overlap prevention + NaN guards + `.unref()` timer.
  - 新規 `apps/mcp-server/src/cron/backfill-reconciliation-cron.ts`: `embedding_backfill_status = 'in_progress'` の stale 行を 1h 間隔で自動補正。`start-workers.ts` 起動時に両 cron を自動セットアップし shutdown で stop。/ New cron auto-reconciles stale `embedding_backfill_status = 'in_progress'` rows every 1h. Both crons auto-start with `start-workers.ts` and stop on shutdown.
  - `reconcileStaleBackfillJobs` を `updateMany` CAS 化 (PR6 TPA #1): WHERE 句に `embeddingBackfillStatus = 'in_progress'` ガードを含めて worker 側の先行 status 遷移との race を検出し、`BackfillReconciliationResult.concurrentUpdatesSkipped` カウンタで surface。/ `reconcileStaleBackfillJobs` uses `updateMany` CAS (PR6 TPA #1): WHERE-clause guard `embeddingBackfillStatus = 'in_progress'` detects worker races; skipped updates surfaced via `concurrentUpdatesSkipped`.
  - DB migration `20260412120000_add_embedding_backfill_started_at` で `web_pages.embedding_backfill_started_at TIMESTAMPTZ NULL` 列 + partial index を追加 (PR6 TPA #2)。stale 判定を `updatedAt` ではなく専用列ベースに切り替え、他カラム更新との衝突を排除。/ New migration adds `web_pages.embedding_backfill_started_at` column + partial index (PR6 TPA #2). Stale detection now keyed on the dedicated column, decoupled from `updatedAt` fluctuations.
  - CLI `reconcile-backfill.ts` に `--confirm` / `--dry-run` フラグ追加 (PR6 SEC LOW-2)。production では明示必須化で誤発火防止、`--dry-run` で DB 書き込みをスキップし対象ページをログ出力のみ。/ CLI adds `--confirm` / `--dry-run` flags (PR6 SEC LOW-2). Production requires explicit flag; `--dry-run` skips DB writes and logs targets only.
  - ユーティリティ抽出 (PR6 TDA TD-1 / TD-3): `utils/prisma-raw-count.ts` に `countNonNullVector` (table/column allowlist + webPageId パラメータ化)、`services/backfill-pending.builder.ts` に `buildBackfillPending` pure function を抽出。worker 本体の複雑度を削減しユニットテスト可能に。/ Extracted utilities (PR6 TDA TD-1 / TD-3): `countNonNullVector` (allowlist + parameterized webPageId) and pure `buildBackfillPending`. Worker complexity reduced, unit-testable.
  - 新規テスト: `tests/cron/{screenshot-cleanup-cron,backfill-reconciliation-cron}.test.ts` (fake timers、overlap 防止、NaN ガード)、`tests/services/backfill-pending.builder.test.ts` (8 件、NaN/Infinity 防御)、`tests/utils/prisma-raw-count.test.ts` (9 件、allowlist 検証)、`tests/scripts/reconcile-backfill-cli.test.ts` (8 件、CLI 引数パーサ)、`tests/e2e/stripe-697-regression.test.ts` (Stripe 697 件回帰防止、`REFTRIX_RUN_STRIPE_E2E=1` で有効化)。/ New tests for crons, pure builder, raw-count util, CLI parser, and Stripe 697-item regression (env-gated).
  - `apps/mcp-server/DATA_RETENTION.md` を v2.4.0 に更新 (LCC Low 1): TTL cron の実装状況、overlap 防止、env var（`SCREENSHOT_CLEANUP_*` / `BACKFILL_RECONCILIATION_*`）、CAS 記述、`--confirm` / `--dry-run` 運用を記載。/ Bumped `DATA_RETENTION.md` to v2.4.0 with cron implementation details, overlap prevention, env vars, CAS note, and `--confirm` / `--dry-run` operations.
  - ADR-0007 "Follow-up Work (PR6)" を "Completed in PR6" に変更し、実装済みの 8 項目を明示。/ ADR-0007 renames "Follow-up Work (PR6)" to "Completed in PR6" and enumerates the 8 shipped items.

- **Counter Reconciliation 9カテゴリ化 + MCP response `backfillPending` (PR5)** / **Counter Reconciliation extended to 9 categories + MCP response `backfillPending` (PR5)**:
  - Post-Phase 5 Counter Reconciliation を 4 カテゴリ (`section/part/motion/bg`) から 9 カテゴリ
    (`section`, `section_visual`, `part_text`, `part_visual`, `motion`, `bg`,
    `js_animation`, `responsive`) へ拡張。`part_text`/`part_visual` は `text_embedding IS NOT NULL` /
    `visual_embedding IS NOT NULL` の raw SQL で集計し、PR4 で非同期 backfill に分割された
    生成数を DB self-discovery で厳密に再計上する。/
    Extended Post-Phase 5 Counter Reconciliation from 4 categories to 9
    (`section`, `section_visual`, `part_text`, `part_visual`, `motion`, `bg`,
    `js_animation`, `responsive`). `part_text`/`part_visual` use raw SQL over
    `text_embedding IS NOT NULL` / `visual_embedding IS NOT NULL` so sync-phase
    counts reflect the DB truth after PR4's async backfill split.
  - MCP response `results.embedding.backfillPending` を追加（`partTextPending`,
    `partVisualPending`, `jobIds`, `estimatedCompletionAt`）。100 件閾値を超えて
    backfill に回された場合のみ設定される。/
    Added `results.embedding.backfillPending` to the MCP response with fields
    `partTextPending`, `partVisualPending`, `jobIds`, `estimatedCompletionAt`.
    Populated only when the 100-item threshold triggered backfill enqueue.
  - 新環境変数 `EMBEDDING_BACKFILL_AVG_MS_PER_ITEM`（デフォルト 5000ms、100–60000ms で
    clamp）で `estimatedCompletionAt` の簡易ヒューリスティックを制御。/
    New env var `EMBEDDING_BACKFILL_AVG_MS_PER_ITEM` (default 5000ms, clamped to
    100–60000ms) tunes the `estimatedCompletionAt` heuristic.
  - 新 service `backfill-reconciliation.service.ts` と CLI `reconcile-backfill.ts` を追加。
    `embeddingBackfillStatus='in_progress'` のまま Queue から該当ジョブが消えたページを
    検出し、DB 完全性で `completed` / `failed` に補正する stale job reconciler。
    cron 統合は PR6 で予定。/
    Added new service `backfill-reconciliation.service.ts` and CLI
    `reconcile-backfill.ts` to detect pages stuck in
    `embeddingBackfillStatus='in_progress'` without a matching queue job and
    correct status to `completed` / `failed` from DB truth. Cron integration
    deferred to PR6.
  - ADR-0007 記録: Phase 5 Queue-based Backfill 100 件閾値の設計判断。/
    ADR-0007 records the design rationale for the 100-item Phase 5 backfill threshold.

- **Queue-based Embedding Backfill (PR4)** / **Queue ベース Embedding バックフィル (PR4)**:
  - New BullMQ Queue `embedding-backfill` + dedicated Worker for Part text / visual
    embedding overflow. When `page.analyze` Phase 5 has more than 100 Parts, only the
    first 100 are processed synchronously; the remainder is enqueued and backfilled
    asynchronously. Prevents Phase 5 timeout / RSS spikes on content-heavy pages. /
    `page.analyze` Phase 5 で Part が 100 件を超える場合、先頭 100 件のみ同期処理し残余を
    非同期バックフィルする新 BullMQ Queue `embedding-backfill` と専用 Worker を追加。
  - New service entry points in `embedding-backfill.service.ts`:
    `backfillPartTextForPage()`, `countPartVisualBackfillTargets()`. /
    `embedding-backfill.service.ts` に `backfillPartTextForPage()` と
    `countPartVisualBackfillTargets()` を追加。
  - New `partsLimit` plumbed through `EmbeddingPhaseParams` →
    `TextEmbeddingSubPhaseParams` / `VisualEmbeddingSubPhaseParams` → IPC Zod schemas →
    children. DB-level cap via Prisma `take` + deterministic `orderBy: { id: "asc" }`. /
    `partsLimit` を `EmbeddingPhaseParams` → `TextEmbeddingSubPhaseParams` /
    `VisualEmbeddingSubPhaseParams` → IPC Zod スキーマ → 子プロセスに伝搬。
  - New env var `EMBEDDING_BACKFILL_CONCURRENCY` (default 1, OOM-safe). /
    新環境変数 `EMBEDDING_BACKFILL_CONCURRENCY` (デフォルト 1、OOM 防御)。
  - Bull Board UI now shows both `page-analyze` and `embedding-backfill` Queues. /
    Bull Board UI は `page-analyze` と `embedding-backfill` の両 Queue を可視化する。
  - `start-workers.ts` gains `--backfill` / `-b` flag to start only the backfill Worker. /
    `start-workers.ts` に `--backfill` / `-b` フラグを追加。
  - `web_pages.embeddingBackfillStatus` transitions to `queued` when backfill jobs are
    enqueued and to `completed` when the Worker finishes. /
    backfill 投入時に `web_pages.embeddingBackfillStatus` を `queued` に、Worker 完了時に
    `completed` に遷移させる。

### BREAKING CHANGES / 破壊的変更

- **Phase 5 child process RSS monitoring — delta-based (PR3)** / **Phase 5 子プロセス RSS 監視を delta ベースに変更 (PR3)**:
  - Removed env vars / 削除した環境変数: `PHASE5_CHILD_RSS_WARN_MB`, `PHASE5_CHILD_RSS_KILL_MB`
    (absolute RSS thresholds, no longer functional due to fork() COW / 絶対値 RSS 閾値、fork() COW により機能しないため削除)
  - Added env vars / 追加した環境変数: `PHASE5_CHILD_RSS_WARN_DELTA_MB` (default / デフォルト 2048),
    `PHASE5_CHILD_RSS_KILL_DELTA_MB` (default / デフォルト 3072)
  - Migration / 移行方法: Remove old env vars from deployment config. Default delta values
    work for most workloads; tune only if you previously needed to override absolute thresholds. /
    デプロイ設定から旧環境変数を削除すること。delta のデフォルト値はほとんどのワークロードで動作する。
    以前に絶対値閾値を調整していた場合のみチューニングが必要。
  - Rationale / 背景: Linux fork() Copy-on-Write causes the child to inherit parent RSS,
    making absolute thresholds misfire (e.g., Stripe 697 件 0 件保存 bug). /
    Linux fork() の Copy-on-Write により子プロセスが親の RSS を継承するため、
    絶対値閾値は偽陽性を生む (Stripe 697 件 0 件保存バグ)。

- **Phase 5 atomic deployment requirement** / **Phase 5 アトミックデプロイ要件**:
  - IPC schema `childHeartbeatSchema` now requires `rssDeltaMb` field. /
    IPC スキーマ `childHeartbeatSchema` に `rssDeltaMb` 必須フィールドを追加。
  - Rolling deployment with mixed parent/child versions WILL FAIL (Zod validation). /
    ローリングデプロイで親子が異なるバージョンになると Zod バリデーションで失敗する。
  - Restart the entire MCP server (BullMQ drain + all workers) atomically. /
    MCP サーバー全体（BullMQ drain 後に全 worker プロセス）を一括再起動すること。

## [0.4.0] - 2026-04-11

### Added / 追加

- **`page.batch_analyze` ツール**: 複数URL一括分析（BullMQバッチジョブ） / **`page.batch_analyze` tool**: batch analysis for multiple URLs (BullMQ batch jobs)
- **`page.getBatchStatus` ツール**: バッチジョブ進捗確認 / **`page.getBatchStatus` tool**: batch job progress check
- **`design.regression_test` ツール**: Pixelmatchによるビジュアル回帰テスト（Option Bアーキテクチャ: DesignSnapshot metadata JSONB） / **`design.regression_test` tool**: Pixelmatch visual regression test (Option B: DesignSnapshot metadata JSONB)
- **`report.generate` ツール**: Handlebarsテンプレートによるレポート生成 / **`report.generate` tool**: report generation via Handlebars templates
- **Phase 5 child_process.fork()プロセス分離**: ONNX Runtime（e5-base/DINOv2）のglibc malloc sbrk断片化によるRSS 36-58GB OOM Killを根本解決。embedding推論を2子プロセスに分離し、exit(0)でOS全メモリ回収。ピークRSS 82%+削減。IPC Zod双方向バリデーション、CWE-209対策、Path Traversal防御、3層タイムアウト保護 / **Phase 5 child_process.fork() process isolation**: fundamentally resolves RSS 36-58GB OOM Kill caused by ONNX Runtime (e5-base/DINOv2) glibc malloc sbrk fragmentation. Isolates embedding inference into 2 child processes with OS full memory reclamation on exit(0). Peak RSS reduced 82%+. IPC Zod bidirectional validation, CWE-209 mitigation, Path Traversal defense, 3-layer timeout protection
- **Bull Boardジョブログ表示**: 全6フェーズに`job.log()`追加でBull Board UIのLogsタブにフェーズ進捗を表示 / **Bull Board job log display**: added `job.log()` to all 6 phases for Bull Board UI Logs tab

### Changed / 変更

- **MCPツール数**: 35 → 39（新規4ツール追加: `page.batch_analyze`, `page.getBatchStatus`, `design.regression_test`, `report.generate`） / **MCP tool count**: 35 → 39 (4 new tools added: `page.batch_analyze`, `page.getBatchStatus`, `design.regression_test`, `report.generate`)
- **onnxruntime-node を optionalDependencies に移動**: `packages/ml/package.json` で `onnxruntime-node` を `dependencies` から `optionalDependencies` に変更。`npm install` / `pnpm install` は CUDA 検出失敗やプラットフォーム非互換でも成功するようになり、フリクションレスなインストールを実現。ML機能（Embedding生成、DINOv2 visual similarity）は未インストール時に `OnnxRuntimeUnavailableError` でgraceful degrade。非ML機能（layout解析、quality評価、コード生成等）は onnxruntime-node なしで動作 / **onnxruntime-node moved to optionalDependencies**: changed `onnxruntime-node` from `dependencies` to `optionalDependencies` in `packages/ml/package.json`. `npm install` / `pnpm install` now succeeds even on CUDA detection failure or platform incompatibility, enabling frictionless installation. ML features (embedding generation, DINOv2 visual similarity) gracefully degrade with `OnnxRuntimeUnavailableError` when unavailable. Non-ML features (layout analysis, quality evaluation, code generation, etc.) work without onnxruntime-node

### Fixed / 修正

- **OllamaVisionClient リトライ修正 (OV-1〜4)**: タイムアウト/リトライ/クリーンアップの4件修正 / **OllamaVisionClient retry fix (OV-1–4)**: 4 fixes for timeout, retry, and cleanup
- **Responsive FK違反+ブラウザリーク修正 (RS-1, RS-3)**: FK制約違反解消、ブラウザインスタンスリーク修正 / **Responsive FK violation + browser leak fix (RS-1, RS-3)**: FK constraint violation fix, browser instance leak fix
- **SectionDetector contact検出改善 (SD-1)**: contactセクションの検出精度向上 / **SectionDetector contact detection improvement (SD-1)**: improved contact section detection accuracy
- **Worker analysisStatus未更新修正**: 成功/失敗パスでanalysisStatusが正しく更新されるよう修正（analysisErrorサニタイズ付き） / **Worker analysisStatus update fix**: analysisStatus now correctly updated on success/failure paths (with analysisError sanitization)
- **onnxruntime-node CUDA誤検出修正**: `.npmrc`に`onnxruntime-node-install-cuda=skip`を追加し、CUDA 12環境でCUDA 11と誤検出されるインストールエラーを解消。OSS同期（prepare-oss.sh）にも反映。GPU利用者向けに手動CUDA設定手順をドキュメント化 / **onnxruntime-node CUDA misdetection fix**: added `onnxruntime-node-install-cuda=skip` to `.npmrc` to resolve installation error where CUDA 12 is misdetected as CUDA 11. Also reflected in OSS sync (prepare-oss.sh). Documented manual CUDA setup for GPU users
- **sync-oss.sh 初回フル公開ブロッカー修正**: Step 6.6の一括pre-publish validation（全パッケージを一斉検証→一斉publish）を、パッケージ単位の逐次フロー（validate→publish→registry反映待ち→次パッケージ）に変更。初回フル公開時に上位Tier（webdesign-core, mcp-server）が未公開の下位Tier依存（core@0.3.0等）を解決できない問題を解消。関数外`local`キーワードも修正 / **sync-oss.sh initial full publish blocker fix**: changed Step 6.6 bulk pre-publish validation (validate-all → publish-all) to per-package sequential flow (validate → publish → registry wait → next package). Resolves the issue where upper-tier packages (webdesign-core, mcp-server) fail to resolve unpublished lower-tier dependencies (core@0.3.0, etc.) during initial full publish. Also fixed `local` keyword outside function scope
- **Phase 5 IPC競合修正 4層防御**: fork()子プロセスのIPC結果メッセージ消失を4層防御で修正（setImmediate、サイレントドロップ検出、ONNX dispose削除、Counter Reconciliation全タイプ拡張） / **Phase 5 IPC race fix — 4-layer defense**: fixes IPC result message loss via setImmediate, silent drop detection, ONNX dispose removal, Counter Reconciliation expansion
- **IPC共有化**: text/visual子プロセス間の71行IPC重複コードを`phase-5-child-ipc.ts`に6関数として集約 / **IPC deduplication**: consolidated 71 lines of duplicated IPC code into 6 shared functions in `phase-5-child-ipc.ts`
- **Bull Board進捗バー修正**: `updateProgress(オブジェクト)` → `updateProgress(数値)` で進捗バーが正しく表示されるよう修正 / **Bull Board progress bar fix**: `updateProgress(object)` → `updateProgress(number)` for correct progress bar display
- **バージョン表記統一**: CLI VERSION（0.2.0→package.json SSoT）、User-Agent 8箇所（0.1.0→0.3.0）、SECURITY.md（0.3.x追加） / **Version string unification**: CLI VERSION (SSoT from package.json), User-Agent 8 files (0.1.0→0.3.0), SECURITY.md (add 0.3.x)

### Security / セキュリティ

- **vite override (defense-in-depth)**: GHSA-v2wj-q39q-566r 対応 / **vite override (defense-in-depth)**: GHSA-v2wj-q39q-566r mitigation
- **middleware/auth 鍵長40文字検証 (CWE-521)**: 認証キーの最小長を強制 / **middleware/auth key length 40-char validation (CWE-521)**: enforce minimum authentication key length
- **Phase 4 analysisError PIIサニタイズ (CWE-209, GDPR Art.5(1)(c))**: エラーメッセージからのPII漏洩防止 / **Phase 4 analysisError PII sanitization (CWE-209, GDPR Art.5(1)(c))**: prevent PII leakage from error messages

### Documentation / ドキュメント

- **MCPツールガイド全面修正**: `02-mcp-tools-guide.md`にv0.3.0新規11ツールの使用ガイドを追加。ツール数表記を`gen:tool-count`マーカーで統一し日英不整合（37/35混在）を解消。TOC再構成 / **MCP tools guide comprehensive revision**: added usage guides for 11 new v0.3.0 tools to `02-mcp-tools-guide.md`. Unified tool count notation with `gen:tool-count` markers, fixing JP/EN inconsistency (37/35 mismatch). Restructured TOC
- **OSS READMEツールテーブル完全化**: `oss-readme.md`にv0.3.0新規11ツール（`responsive.capture`, `search.facets`, `design.similar_site`, `design.compare`, `design.track_changes`, `data.delete`, `data.export`, `audit.query`, `embedding.quality`, `accessibility.audit`, `performance.evaluate`）を追加 / **OSS README tool table completion**: added 11 v0.3.0 tools to `oss-readme.md`
- **.env.example 環境変数追加**: Phase 5 fork()関連5件（`PHASE5_FORK_ENABLED`等）、DINOv2関連3件（`DINOV2_RECYCLE_ENABLED`等）、Worker関連1件（`WORKER_RESTART_DELAY_MS`）の計9環境変数を追加。`ONNX_EXECUTION_PROVIDER`の重複定義を「ML/Embedding」セクションに集約 / **.env.example environment variables**: added 9 env vars for Phase 5 fork() (5), DINOv2 (3), Worker (1). Consolidated duplicate `ONNX_EXECUTION_PROVIDER` into "ML/Embedding" section
- **docs-verify.sh Section 14追加**: OSSツールテーブル網羅性の自動検証（`check_oss_tool_table()`）を追加。`docs-verify-extract.mjs`に`--tool-names`サブコマンド新設（ts-morph AST抽出） / **docs-verify.sh Section 14**: added OSS tool table completeness auto-verification (`check_oss_tool_table()`). New `--tool-names` subcommand in `docs-verify-extract.mjs` (ts-morph AST extraction)
- **`next-gen-roadmap.md` 更新**: Tier 3以降の未実装計画を削除（ブランチ失敗に伴い） / **`next-gen-roadmap.md` update**: removed unimplemented Tier 3+ plans (branch failure)

## [0.3.0] - 2026-03-27

### Added / 追加

- **GDPR/APPIデータ削除対応 `data.delete`, `data.export` ツール**: GDPR Art.17削除権 + APPI Art.33利用停止請求に準拠。カスケード削除対応 / **GDPR/APPI data deletion `data.delete`, `data.export` tools**: compliant with GDPR Art.17 right to erasure + APPI Art.33. Cascade deletion support
- **監査ログサービス `audit.query` ツール**: CWE-778対策、GDPR Art.30処理活動記録、Append-only PostgreSQLテーブル / **Audit log service `audit.query` tool**: CWE-778 mitigation, GDPR Art.30 records of processing, append-only PostgreSQL table
- **セマンティック検索高度化**: LLM Query Understanding + Cross-Encoder Reranking で検索精度 +30% 改善 / **Semantic search enhancement**: LLM Query Understanding + Cross-Encoder Reranking for +30% precision improvement
- **Embedding品質モニタリング `embedding.quality` ツール**: Embedding分布統計 + 検索安定性モニタリング / **Embedding quality monitoring `embedding.quality` tool**: embedding distribution statistics + search stability monitoring
- **WCAG監査+コントラストチェック `accessibility.audit` ツール**: axe-core統合、WCAG 2.1 AA準拠チェック / **WCAG audit + contrast check `accessibility.audit` tool**: axe-core integration, WCAG 2.1 AA compliance check
- **類似サイト検索 `design.similar_site` ツール**: URL→mean pooling→類似デザインサイト検索 / **Similar site search `design.similar_site` tool**: URL → mean pooling → similar design site search
- **Core Web Vitals `performance.evaluate` ツール**: CDP PerformanceObserver + Lighthouse統合 / **Core Web Vitals `performance.evaluate` tool**: CDP PerformanceObserver + Lighthouse integration
- **ファセット検索+検索ログ `search.facets` ツール**: 動的ファセット生成、検索行動記録・分析 / **Facet search + search logs `search.facets` tool**: dynamic facet generation, search behavior recording and analysis
- **マルチデバイスキャプチャ `responsive.capture` ツール**: 3 viewport同時キャプチャ（mobile/tablet/desktop） / **Multi-device capture `responsive.capture` tool**: 3 viewport simultaneous capture (mobile/tablet/desktop)
- **ストリーミング進捗**: MCPプロトコル通知で長時間ジョブの進捗リアルタイム送信 / **Streaming progress**: real-time progress notifications for long-running jobs via MCP protocol notifications
- **デザイン比較 `design.compare` ツール**: 多次元比較（レイアウト、カラー、タイポグラフィ、モーション） / **Design comparison `design.compare` tool**: multi-dimensional comparison (layout, color, typography, motion)
- **マルチフレームワークコード生成拡張**: Svelte/Astro出力追加（既存React/Vue/HTMLに加えて） / **Multi-framework code generation extension**: added Svelte/Astro output (in addition to existing React/Vue/HTML)
- **デザイン変更追跡 `design.track_changes` ツール**: 時系列デザイン変更の記録・差分分析 / **Design change tracker `design.track_changes` tool**: temporal design change recording and diff analysis

### Performance / パフォーマンス

- **Phase 5メモリ制御: 6ステップOOM防止**: Sharp RAWシングルデコード、DINOv2セッションリサイクル、動的Fallback時dispose/re-init、Phase 1/3逐次実行による全分析完走保証 / **Phase 5 memory control: 6-step OOM prevention**: Sharp RAW single-decode, DINOv2 session recycle, dynamic fallback dispose/re-init, Phase 1/3 sequential execution for guaranteed full analysis completion

### Changed / 変更

- **MCPツール数**: 28 → 35（新規11ツール追加、2ツール削除） / **MCP tool count**: 28 → 35 (11 new tools added, 2 tools removed)
- **検索アーキテクチャ**: Cross-Encoder Reranking + Query Understanding 追加 / **Search architecture**: added Cross-Encoder Reranking + Query Understanding
- **レート制限ティア拡張**: 新ツール11件を適切なティアに分類 / **Rate limiting tier expansion**: 11 new tools classified into appropriate tiers

### Removed / 削除

- **quality.batch_evaluate ツール削除**（v0.1.0で非推奨化済み、2マイナーバージョン経過）。代替: `quality.evaluate` を個別URLに対して繰り返し実行 / **Removed quality.batch_evaluate tool** (deprecated since v0.1.0, 2 minor versions elapsed). Migration: use `quality.evaluate` for individual URLs
- **quality.getJobStatus ツール削除**（quality.batch_evaluate専用のため同時削除）/ **Removed quality.getJobStatus tool** (quality.batch_evaluate-exclusive, removed together)

### Database / データベース

- **3テーブル追加**: `audit_logs`, `search_logs`, `design_snapshots` + `design_snapshot_sections` / **3 tables added**: `audit_logs`, `search_logs`, `design_snapshots` + `design_snapshot_sections`

### Tests / テスト

- **Phase 5メモリ制御テスト57件追加**: Sharp RAWデコード、DINOv2セッションリサイクル、動的Fallback dispose/re-init、Phase 1/3逐次化の各ステップを網羅 / **57 Phase 5 memory control tests added**: covering Sharp RAW decode, DINOv2 session recycle, dynamic fallback dispose/re-init, Phase 1/3 sequential execution

## [0.2.1] - 2026-03-27

### Fixed / 修正

- **npm パッケージに `dist/` ビルド成果物を含める**: v0.2.0 は `src/` のみで `dist/index.js` が欠落していたため、Glama 等のランタイム実行環境で `MODULE_NOT_FOUND` エラーが発生していた / **Include `dist/` build artifacts in npm package**: v0.2.0 only contained `src/`, missing `dist/index.js`, causing `MODULE_NOT_FOUND` errors in runtime environments like Glama

## [0.2.0] - 2026-03-25

### Added / 追加

- **検索結果キャッシュ（LRU）**: lru-cache v11によるインメモリキャッシュ。P95レイテンシ500ms→50ms目標 / **Search result cache (LRU)**: in-memory cache via lru-cache v11. P95 latency target 500ms → 50ms
- **コンポーネント横断検索 `search.unified` ツール**: 5サービス並列検索+similarity統合 / **Cross-component search `search.unified` tool**: 5-service parallel search with similarity aggregation
- **マルチモーダル検索 `design.search_by_image` ツール**: 画像→DINOv2→HNSW vision search、RRF 3-source (40/30/30) / **Multimodal search `design.search_by_image` tool**: image → DINOv2 → HNSW vision search, RRF 3-source (40/30/30)
- **sanitizeErrorMessage グローバルユーティリティ**: CWE-209対策として47ファイルに適用 / **sanitizeErrorMessage global utility**: applied to 47 files for CWE-209 mitigation
- **SBOM自動生成**: CycloneDX 1.6フォーマット、`pnpm sbom`コマンド、CI統合 / **Automated SBOM generation**: CycloneDX 1.6 format, `pnpm sbom` command, CI integration
- **BullMQジョブ管理UI**: @bull-board/express、Basic Auth認証、ポート21080 / **BullMQ job management UI**: @bull-board/express, Basic Auth, port 21080
- **マイグレーション自動化（db-migrate-safe.sh）**: 自動バックアップ + 自動ロールバック / **Migration automation (db-migrate-safe.sh)**: auto-backup + auto-rollback
- **Phase 1/3並列化**: Promise.allによる約40%高速化 / **Phase 1/3 parallelization**: ~40% speedup via Promise.all
- **スタンドアロンCLI `apps/cli/`**: `reftrix analyze <url>`コマンド、MCP非依存 / **Standalone CLI `apps/cli/`**: `reftrix analyze <url>` command, MCP-independent
- **pgvector 0.8 iterative scan有効化**: ALTER ROLE SET + アプリ層SETで有効化 / **pgvector 0.8 iterative scan enabled**: via ALTER ROLE SET + application-layer SET
- **レート制限ミドルウェア**: Token Bucket + Redis Lua、3ティア（analysis 10RPM / search 120RPM / default 60RPM） / **Rate limiting middleware**: Token Bucket + Redis Lua, 3 tiers (analysis 10RPM / search 120RPM / default 60RPM)
- **フィルタリング統一**: industry/audience/tags共通スキーマ、6検索ツールに適用 / **Filtering unification**: industry/audience/tags common schema, applied to 6 search tools
- **sync-oss.sh npm publish統合**: Step 6でnpm publish実行、`--skip-publish`オプション対応 / **sync-oss.sh npm publish integration**: npm publish in Step 6, `--skip-publish` option supported

### Changed / 変更

- **巨大ファイル4件を責務分割（motion/search.tool.ts, layout/search.tool.ts, sync-processing.ts, service-initializer.ts）** / **Split 4 large files by responsibility (motion/search.tool.ts, layout/search.tool.ts, sync-processing.ts, service-initializer.ts)**
- **MCPツール数の表記統一**: 26→28に統一（37ファイル、約75箇所） / **MCP tool count unification**: unified from 26 to 28 across 37 files (~75 locations)
- **`.gitignore` 更新**: `captured-frames/` と `*.tsbuildinfo` を追加、Git追跡中の生成物7ファイルを除去 / **`.gitignore` update**: added `captured-frames/` and `*.tsbuildinfo`, removed 7 tracked generated files
- **QA一本化**: E2Eランナー統一、CI全ファイル実行、typecheck拡張 / **QA unification**: E2E runner alignment, CI full file coverage, typecheck expansion

### Security / セキュリティ

- **isDevelopment() ガード全是正**: 本番エラーサイレント吸収を防止 / **Remove all isDevelopment() guards in error paths**: prevent silent error absorption in production

### Fixed / 修正

- **narrative.search フィルタ転送修正**: フィルタパラメータが正しく転送されない問題を修正 / **narrative.search filter forwarding fix**: fixed filter parameters not being forwarded correctly
- **.ossfilter に db-migrate-safe.sh を追加**: OSSテスト失敗を修正 / **Added db-migrate-safe.sh to .ossfilter**: fixed OSS test failure

### Tests / テスト

- **SEC/TDA/LCC監査テスト91件追加** / **Add 91 SEC/TDA/LCC audit tests**

## [0.1.8] - 2026-03-22

### Added / 追加

- **npm公開・MCP Registry登録対応**: package.jsonバージョン整合、OSS同期変換（名前空間・スコープ変換）、server.json生成、運用ルール追加 / **npm publish & MCP Registry support**: package.json version alignment, OSS sync transforms (namespace/scope conversion), server.json generation, operation rules

### Fixed / 修正

- **OSS同期パイプライン安定化**: prepare-oss.sh Phase 3末尾とsync-oss.sh rsync後にPrettier自動整形を追加。Step 3.5でPrettier実行前に`pnpm install`を追加しCI Format Check失敗を解消 / **OSS sync pipeline stabilization**: added Prettier auto-formatting after prepare-oss.sh Phase 3 and sync-oss.sh rsync. Added `pnpm install` before Prettier in Step 3.5 to fix CI format check failure

### Tests / テスト

- CIパフォーマンステスト閾値調整: frame-image-analysis、motion-vectorの閾値をCI環境に合わせて緩和 / CI performance test threshold adjustments: relaxed frame-image-analysis and motion-vector thresholds for CI runner
- layout-searchテストでhtml-sanitizerをモック化しCIタイムアウトを解消 / Mocked html-sanitizer in layout-search test to fix CI timeout

### CI/CD

- GitHub Actions CI結果のSlack通知を追加 / Added Slack notification for CI results
- Prettierフォーマット修正・OSS同期検証FAIL 2件解消 / Fixed Prettier format and resolved 2 OSS sync verification failures

## [0.1.7] - 2026-03-21

### Refactored / リファクタリング

- **God Function分割**: page-analyze-worker→7フェーズモジュール（phase-0〜phase-5 + common）に分割。analyze.tool→sync-processing抽出、evaluate.tool→evaluate-engine抽出 / **God Function splits**: page-analyze-worker → 7 phase modules (phase-0–phase-5 + common). analyze.tool → sync-processing, evaluate.tool → evaluate-engine
- **スキーマ分割**: motion/schemas→3ファイル、page/schemas→3ファイル（re-export hub方式で後方互換性維持） / **Schema splits**: motion/schemas → 3 files, page/schemas → 3 files (re-export hub pattern for backward compatibility)
- **llama-vision.adapter→prompts+types分割**: LLMプロンプトと型定義を分離 / **llama-vision.adapter split**: separated LLM prompts and type definitions
- Dead code 13件削除、narrative-search重複統合、ESLint warning 27件修正 / Removed 13 dead code items, consolidated narrative-search duplicates, fixed 27 ESLint warnings

### Security / セキュリティ

- **SSRF・XSSサニタイズ・DOMPurifyバイパス修正**: 複数のセキュリティ脆弱性を解消 / **SSRF, XSS sanitization, and DOMPurify bypass fixes**: resolved multiple security vulnerabilities
- **isDevelopment()ガード違反25箇所修正**: catchブロック内でのエラーサイレント吸収を防止。全環境で`logger.warn`/`logger.error`を出力するよう統一 / **Fixed 25 isDevelopment() guard violations**: prevented silent error absorption in catch blocks. Unified to output `logger.warn`/`logger.error` in all environments
- **脆弱性解消**: flatted, effect (pnpm overrides)、undici 7.18.2→7.24.3 (High×6件)、flatted >=3.4.0 (High×1件) / **Vulnerability fixes**: flatted, effect (pnpm overrides), undici 7.18.2→7.24.3 (6 High), flatted >=3.4.0 (1 High)

### Tests / テスト

- デッドテスト11件削除（4,367行削減）、セキュリティテスト3件追加 / Removed 11 dead tests (4,367 lines reduced), added 3 security tests
- テストカバレッジ拡充（Worker系、スコアリング、グラデーション検出） / Test coverage expansion (Worker, scoring, gradient detection)

### Fixed / 修正

- **Phase 2.5 Progress範囲逆行修正**: 35-45%→60-63%に修正。進捗バーの逆行を解消 / **Phase 2.5 progress range regression fix**: corrected from 35-45% to 60-63%, resolving progress bar reversal
- **resolveMemoryConfig重複初期化を遅延初期化に変更**: 起動時の不要なメモリ設定計算を排除 / **resolveMemoryConfig lazy initialization**: eliminated redundant memory config computation at startup

### CI/CD

- **E2Eテストジョブ追加**: PostgreSQL+pgvector+Playwright+Redisを含む統合テスト環境 / **E2E test job added**: integrated test environment with PostgreSQL+pgvector+Playwright+Redis
- `format:check`有効化、`.git-blame-ignore-revs`作成 / Enabled `format:check`, created `.git-blame-ignore-revs`

### Code Style / コードスタイル

- コードベース全体（1,261ファイル）にPrettier整形適用 / Applied Prettier formatting across entire codebase (1,261 files)

## [0.1.6] - 2026-03-15

### Added / 追加

- **Section Visual Embedding (v0.1.5)**: Phase 5 (Embedding) でセクション単位のDINOv2 ViT-B/14 visual embedding生成を統合。screenshotBase64からセクションbounding boxでcrop→DINOv2で768D L2正規化ベクトルを生成し`section_embeddings.vision_embedding`に保存 / Section-level DINOv2 ViT-B/14 visual embedding generation integrated in Phase 5 (Embedding). Crops sections from screenshotBase64 using bounding boxes → generates 768D L2-normalized vectors via DINOv2, stored in `section_embeddings.vision_embedding`
  - DINOv2 init/disposeはPart Visual Embedding（v0.1.4）と共有しメモリ効率を最適化 / DINOv2 init/dispose shared with Part Visual Embedding (v0.1.4) for memory efficiency
  - PII保護: piiRiskLevel='high'パーツを含むセクションはスキップ / PII protection: skips sections containing piiRiskLevel='high' parts
  - Graceful Degradation: screenshot無し/DINOv2失敗/セクション高さ<10px時はtext_embeddingのみ / Graceful Degradation: text_embedding only when screenshot unavailable, DINOv2 fails, or section height < 10px
- **Section Screenshot Fallback (v0.1.6)**: Phase 5でscreenshotBase64範囲外のセクションに対し、`SectionScreenshotFallbackService`がPlaywrightで個別スクリーンショットを取得→DINOv2 visual embedding生成 / Section Screenshot Fallback captures individual section screenshots via Playwright for sections outside screenshotBase64 range in Phase 5 → DINOv2 visual embedding generation
  - バッチ処理化: workerがフォールバック対象セクションを事前一括収集し、1回の`captureSectionScreenshots`呼び出しで全セクション処理（page.goto N回→1回に削減） / Batch processing: worker pre-collects fallback target sections, processes all in a single `captureSectionScreenshots` call (reduces page.goto from N to 1)
  - Type-aware重複ベクトル検出: DINOv2生成後に同一sectionType内でのみコサイン類似度>0.995の重複をスキップ（スライディングウィンドウ10件、環境変数`DUPLICATE_VECTOR_THRESHOLD`で調整可能） / Type-aware duplicate vector detection: skips vision embeddings with cosine similarity > 0.995 within same sectionType (sliding window 10, configurable via `DUPLICATE_VECTOR_THRESHOLD` env var)
  - CTA小セクション（height<=200px）はdedup対象外 / CTA small sections (height <= 200px) exempt from dedup
  - scrollTo後にrequestAnimationFrame 2フレーム完了待ち（2秒タイムアウト付き）でLazy Rendering描画改善 / Post-scrollTo requestAnimationFrame 2-frame wait (2s timeout) improves lazy rendering paint
  - Feature flag `ENABLE_SECTION_SCREENSHOT_FALLBACK`（デフォルト`true`、opt-out） / Feature flag `ENABLE_SECTION_SCREENSHOT_FALLBACK` (default `true`, opt-out)
  - 安全装置: SSRF検証、NaN/Infinity防御、checkMemoryPressure毎セクション、累積300sタイムアウト、上限50セクション / Safety: SSRF validation, NaN/Infinity defense, checkMemoryPressure per section, 300s cumulative timeout, max 50 sections
- **Section Merge Post-Processor (v0.1.7-v0.1.8)**: Phase 1 (Layout Analysis) 内でmergeVisionDetectedSections()後に実行されるポストプロセッサ / Post-processor executed after mergeVisionDetectedSections() within Phase 1 (Layout Analysis)
  - Rule 1: 同一タイプ3+連続セクションをマージ（MERGEABLE_TYPES 11タイプ: unknown/feature/testimonial/gallery/partners/portfolio/team/stories/stats/faq/cta） / Rule 1: merge 3+ consecutive sections of same type (11 MERGEABLE_TYPES including cta)
  - Rule 2: コンテンツ空unknownセクション吸収（HTMLタグ除去後textContent<20文字） / Rule 2: absorb content-empty unknown sections (textContent < 20 chars after HTML tag stripping)
  - Rule 3: 同名隣接マージ（同一タイプ+同一heading+2件以上連続→1件にマージ） / Rule 3: same-heading adjacent merge (same type + same heading + 2+ consecutive → 1)
  - Feature flag `ENABLE_SECTION_MERGE_POSTPROCESSOR`（デフォルト`true`、opt-out） / Feature flag `ENABLE_SECTION_MERGE_POSTPROCESSOR` (default `true`, opt-out)
  - 安全装置: MAX_INPUT_SECTIONS=500、NaN/Infinity防御、Graceful Degradation / Safety: MAX_INPUT_SECTIONS=500, NaN/Infinity defense, Graceful Degradation
- **Section Split Post-Processor Rule 4 (v0.1.9)**: height > MAX_SECTION_HEIGHT (10,000px)の巨大セクション再分割 / Rule 4: oversized section re-splitting for sections with height > 10,000px
  - 3戦略: (A) HTML子セクション検出（`<section>`, `<article>`, `<h1>`-`<h3>`分割点）、(B) 等分割フォールバック、(C) 分割不可（MIN_SPLIT_SECTION_HEIGHT未満） / 3 strategies: (A) HTML child section detection, (B) equal-split fallback, (C) no-split (below MIN_SPLIT_SECTION_HEIGHT)
  - 実行順序: Rule 4 → Rule 1 → Rule 3 → Rule 2（分割→マージの順） / Execution order: Rule 4 → Rule 1 → Rule 3 → Rule 2 (split before merge)
  - Rule 4分割セクションはexcludeIdsでRule 1再マージ防止 / Rule 4 split sections use excludeIds to prevent Rule 1 re-merging
  - Feature flag `ENABLE_SECTION_SPLIT_POSTPROCESSOR`（デフォルト`true`、opt-out） / Feature flag (default `true`, opt-out)
- **Blank Image Detection + Dynamic Fallback (v0.1.9)**: Phase 5でfullPage screenshotのLazy Loading未描画セクション（白画像）を`isBlankImage()`で検出し、動的にFallback再取得→DINOv2 visual embedding生成 / Detects lazy-loading unrendered sections (blank images) in fullPage screenshots via `isBlankImage()`, dynamically re-captures via Section Screenshot Fallback → DINOv2 visual embedding
  - `acquireSectionCropBuffer()`でcropパス・白画像検出・Fallback取得を一元管理 / `acquireSectionCropBuffer()` unifies crop path, blank image detection, and fallback acquisition
  - 安全装置: MAX_DYNAMIC_FALLBACK_SECTIONS=20、位置ベース+動的合計50件上限、checkMemoryPressure / Safety: MAX_DYNAMIC_FALLBACK_SECTIONS=20, position-based + dynamic total capped at 50, checkMemoryPressure
  - 環境変数`BLANK_IMAGE_STDDEV_THRESHOLD`（デフォルト5.0、0-255範囲） / Env var `BLANK_IMAGE_STDDEV_THRESHOLD` (default 5.0, range 0-255)
- **Lazy Loading Scroll (v0.1.9)**: Phase 0でfullPage screenshot撮影前にページ全体をスクロールし、IntersectionObserverベースのLazy Loadingを発火。白画像問題を根本解決 / Scrolls entire page in Phase 0 before fullPage screenshot to trigger IntersectionObserver-based lazy loading, fundamentally resolving blank images
  - WebGLサイト(fullPage=false)では自動スキップ / Auto-skipped for WebGL sites (fullPage=false)
  - 安全装置: LAZY_SCROLL_MAX_ITERATIONS=50、rAF+2sタイムアウト、Graceful Degradation / Safety: MAX_ITERATIONS=50, rAF+2s timeout, Graceful Degradation
- **Section Screenshot Fallback Multi-Tile Capture (v0.1.10)**: section.height > viewportHeightの場合、セクションを動的に複数タイルに分割してPlaywrightで個別キャプチャし、Sharp compositeで垂直結合して完全なセクション画像を生成 / Multi-tile capture: dynamically splits sections exceeding viewport height into tiles, captures each via Playwright, vertically composites with Sharp
  - デフォルト上限20タイル（環境変数`MAX_TILES_PER_SECTION`でオーバーライド可能、絶対上限100） / Default cap 20 tiles (override via `MAX_TILES_PER_SECTION`, absolute limit 100)
  - Viewport統一: Fallback viewportを1920x1080に変更（ingest viewportと統一） / Viewport unification: 1920x1080 (unified with ingest viewport)
  - scrollY実測値によるclipY計算（sticky header対策、viewportHeight/2以上のズレ時は期待値にフォールバック） / scrollY actual measurement for clipY calculation (sticky header compensation)
  - 診断ログ: セクション単位のpath追跡ログ（in_range/fallback/dynamic/dedup/skipped）とサマリー出力 / Diagnostic logging: per-section path tracking (in_range/fallback/dynamic/dedup/skipped) with summary
  - SEC修正: viewport NaN/Infinity/0/負数防御+上限4096px、timeoutMs NaN防御、env var絶対上限100（SEC-TILES-01） / SEC fixes: viewport NaN defense + 4096px upper limit, absolute env var limit 100
- **Type-aware dedupヘルパー関数抽出**: `shouldSkipDuplicateVision()`として共通ヘルパーに抽出し、テスト7件追加 / Extracted type-aware dedup logic into `shouldSkipDuplicateVision()` shared helper with 7 tests
- **MAX_TILES_PER_SECTION動的化**: 環境変数によるランタイム設定でVision coverageボトルネックを解消 / Dynamic `MAX_TILES_PER_SECTION` via env var resolves Vision coverage bottleneck

### Fixed / 修正

- **onnxruntime-node ABIミスマッチ解消**: onnxruntime-nodeバージョン固定でNode.js ABIバージョン不整合によるクラッシュを修正 / Pin onnxruntime-node version to fix Node.js ABI version mismatch crash
- **isBlankImage()ダークテーマ誤検出修正**: stddev単独判定からstddev + mean輝度の2条件判定に変更（avgStddev < 5.0 AND (avgMean > 245 OR avgMean < 10)）。ダークテーマサイトが白画像と誤検出される問題を解消 / Fix isBlankImage() dark theme false positive: changed from stddev-only to dual-condition check (stddev + mean brightness). Prevents dark theme sites from being incorrectly flagged as blank
- **actualScrollY clipY計算フォールバック追加**: clip_height_zeroリグレッションを修正 / Add fallback to actualScrollY clipY calculation, fixing clip_height_zero regression
- **Dynamic Fallbackバグ修正3件**: (1) acquireSectionCropBufferの構造的バグ修正（isBlank判定の論理反転: false→true）、(2) メモリ圧力回避のscreenshotBuffer明示的解放、(3) Rule 3にexcludeIds適用（Rule 4分割セクションの再マージ防止） / 3 Dynamic Fallback bug fixes: (1) isBlank logic inversion fix, (2) explicit screenshotBuffer release, (3) excludeIds applied to Rule 3
- **Pre-Return Pauseレースコンディション解消**: Worker計画的再起動時のPre-Return Pauseのレースコンディションを修正。startup recoveryのfetchNext呼び出しも修正 / Fix Pre-Return Pause race condition during planned worker restart + fix startup recovery fetchNext call
- **Worker path postProcessSections統合**: page.analyzeの非同期Worker処理にpostProcessSections(Rule 1-4)呼び出しを追加。saveSectionPatterns前に実行し、分割/マージされたセクションのtext_embeddingも正しく生成 / Added postProcessSections (Rule 1-4) to Worker path before saveSectionPatterns, ensuring correct text_embeddings for split/merged sections

### Changed / 変更

- Fallback viewport: 1280x800 → 1920x1080（ingest viewportと統一） / Fallback viewport unified to 1920x1080

### Documentation / ドキュメント

- Section Visual Embedding（v0.1.5）のドキュメント包括的アップデート / Comprehensive docs update for Section Visual Embedding (v0.1.5)
- Section Merge Post-Processor（v0.1.7-v0.1.8）のドキュメント追記 / Section Merge Post-Processor (v0.1.7-v0.1.8) docs added
- v0.1.9 Blank Image Detection + Dynamic Fallback / Section Split Rule 4のドキュメント追記 / v0.1.9 docs for Blank Image Detection, Dynamic Fallback, and Section Split Rule 4
- v0.1.9 Lazy Loading Scroll + Worker postProcessSections統合 + Dynamic Fallbackバグ修正のドキュメント追記 / v0.1.9 docs for Lazy Loading Scroll, Worker postProcessSections integration, and Dynamic Fallback bug fixes
- v0.1.10 Section Screenshot Fallback Multi-Tile + isBlankImage改善のドキュメント追記 / v0.1.10 docs for Multi-Tile Capture and isBlankImage improvement

## [0.1.5] - 2026-03-12

### Added / 追加

- **Part-Level Analysis機能**（3つの新規MCPツール: `part.search`, `part.inspect`, `part.compare`） / **Part-Level Analysis** (3 new MCP tools: `part.search`, `part.inspect`, `part.compare`)
  - `part.search`: セマンティックUIパーツ検索（ハイブリッド: ベクトル + 全文検索 RRF）。16パーツタイプ対応、searchMode（visual/text/hybrid）、partType/webPageIdフィルタ / Semantic UI part search (hybrid: vector + fulltext RRF). 16 part types, searchMode (visual/text/hybrid), partType/webPageId filters
  - `part.inspect`: パーツ詳細取得（computedStyles, boundingBox, interactionInfo, cssClasses, attributes, piiRiskLevel）。オプション: includeHtml, includeEmbedding / Get detailed part info by ID with computed styles, bounding box, interaction info. Opt-in: includeHtml, includeEmbedding
  - `part.compare`: 2-5パーツの比較（スタイル、レイアウト、インタラクション、アクセシビリティ） / Compare 2-5 parts side by side on styles, layout, interaction, and accessibility
  - 16パーツタイプ: heading, button, link, image, icon, input, form, card, navigation, footer, badge, avatar, divider, modal, toast, tooltip / 16 part types
  - PII検出（piiRiskLevel: low/medium/high）で個人情報を含むパーツの自動分類 / PII detection with automatic classification of parts containing personal information
- **DINOv2 ViT-B/14 visual embedding**: パーツの視覚的特徴を768次元L2正規化ベクトルで表現。`part.search` のsearchMode `visual` / `hybrid` で利用可能 / DINOv2 ViT-B/14 visual embedding: 768-dim L2-normalized vectors representing visual features of parts. Available via `part.search` searchMode `visual` / `hybrid`
  - Phase 5 Embedding末尾で自動生成（e5-base dispose後にDINOv2ロード ~800MB、完了後dispose） / Auto-generated at end of Phase 5 Embedding (loads DINOv2 ~800MB after e5-base disposal, disposes after completion)
  - Graceful Degradation: screenshot無し/DINOv2失敗/bbox未解決時はtext_embeddingのみ / Graceful Degradation: text_embedding only when screenshot unavailable, DINOv2 fails, or bbox unresolved
  - piiRiskLevel='high'パーツはvisual embeddingスキップ / Skips visual embedding for piiRiskLevel='high' parts
  - 環境変数 `DINOV2_MODEL_PATH` でモデルパス指定可能 / Model path configurable via `DINOV2_MODEL_PATH` env var

### Fixed / 修正

- **JSDOM bounding box常時ゼロ問題を修正**: Part ExtractionがJSDOMベースのため`getBoundingClientRect()`が常に`{0,0,0,0}`を返す問題を、Phase 5冒頭でPlaywright（`PartBboxPlaywrightService`）により実bounding boxを後付け取得することで解決 / Fix JSDOM bounding box always zero: Part Extraction uses JSDOM where `getBoundingClientRect()` always returns `{0,0,0,0}`. Resolved by retroactively obtaining real bounding boxes via Playwright (`PartBboxPlaywrightService`) at the start of Phase 5
  - sharedBrowserの`isConnected()`チェック + 切断時はフォールバックとして独自Chromiumインスタンスを起動 / Checks sharedBrowser `isConnected()` and falls back to launching standalone Chromium if disconnected
  - SSRF対策（`validateExternalUrl()`）+ CSSインジェクション防御（`escapeCssIdentifier()`） / SSRF prevention (`validateExternalUrl()`) + CSS injection defense (`escapeCssIdentifier()`)
  - 39テスト追加（正常系、早期リターン、Graceful Degradation、リソースクリーンアップ、SSRF、CSSセレクタ） / 39 tests added (normal flow, early return, graceful degradation, resource cleanup, SSRF, CSS selectors)

### Changed / 変更

- MCPツール数: 23 → **26**（`part.search`, `part.inspect`, `part.compare`追加） / MCP tool count: 23 → **26** (added `part.search`, `part.inspect`, `part.compare`)
- Worker Pipeline Phase 5にStep 0（Playwright Bounding Box Resolution）を追加 / Added Step 0 (Playwright Bounding Box Resolution) to Worker Pipeline Phase 5

### Documentation / ドキュメント

- Part-Level Analysisのドキュメントを更新 / Updated Part-Level Analysis documentation

## [0.1.4] - 2026-03-11

### Fixed / 修正

- **WorkerパスでCSSスニペットがDB保存されない問題を修正**: worker-db-save → section_patternsに5 CSSフィールド（cssSnippet, externalCssContent, externalCssMeta, cssFramework, cssFrameworkMeta）を追加。page-analyze-workerでページレベルCSSをセクション単位に配布 / Fix CSS snippet data not saved in Worker path: added 5 CSS fields to section_patterns via worker-db-save, distributing page-level CSS to sections in page-analyze-worker
- **BullMQ obliterateを完全除去しwaiting/completedジョブを保護**: obliterate()がwaiting/completedジョブを破壊していた問題を修正 / Remove BullMQ obliterate entirely to protect waiting/completed jobs
- **コードレビュー指摘5件を修正**: 認証チェック、キュー管理、ポーリング間隔、ドキュメント整合性 / Fix 5 code review findings (auth checks, queue management, polling interval, docs consistency)
- **テスト修正3件**: layout-first-mode並列テストタイムアウト延長(120s)、system-health detectGpuMismatchモック追加、パフォーマンステスト閾値緩和 / Fix 3 test issues: extend parallel test timeout, add GPU mismatch mock, relax perf test threshold

### Security / セキュリティ

- **CVE-2026-0540**: DOMPurify 3.3.2に更新しXSS脆弱性を修正 / Update DOMPurify to 3.3.2 to fix XSS vulnerability
- **GHSA-qffp-2rhf-9h96**: tar overrideを>=7.5.10に更新しhardlinkパストラバーサルを修正 / Update tar override to fix hardlink path traversal
- **hono/node-server脆弱性4件を解消** / Fix 4 Dependabot vulnerabilities in hono/node-server

### Changed / 変更

- Glama MCPサーバーディレクトリ登録（`glama.json`追加、READMEにバッジ追加） / Register on Glama MCP server directory (add `glama.json` and badge to README)
- OSS版mcp-serverパッケージから`private:true`を自動除去 / Auto-remove `private:true` from OSS mcp-server package.json

### Documentation / ドキュメント

- README冒頭にペルソナ文とQuickstart誘導文を追加 / Add persona line and quickstart hook to README

## [0.1.3] - 2026-03-08

### Added / 追加

- **User preference profiling system** (3 new MCP tools: `preference.hear`, `preference.get`, `preference.reset`) / **ユーザー嗜好プロファイリングシステム**（3つの新規MCPツール: `preference.hear`, `preference.get`, `preference.reset`）
  - `preference.hear`: Stateless hearing session (Mode A: sample presentation with progress tracking, Mode B: feedback recording with profile update) / ステートレスヒアリングセッション（Mode A: サンプル提示＋進捗トラッキング、Mode B: フィードバック記録＋プロファイル更新）
  - `preference.get`: Profile retrieval with GDPR data portability (Art. 20) via `include_signals` / プロファイル取得（GDPRデータポータビリティ Art. 20 対応、`include_signals`オプション）
  - `preference.reset`: Soft reset and hard delete (GDPR Art. 17 Right to Erasure) / ソフトリセット＋完全削除（GDPR Art. 17 忘れられる権利）
  - 2-factor confidence model (MoodCategory coverage 0.6 + interaction sufficiency 0.4, threshold 0.8, max 15 hearings) / 2因子信頼度モデル（MoodCategoryカバレッジ 0.6 + インタラクション充足度 0.4、閾値 0.8、最大15回ヒアリング）
  - GDPR Art. 13/14 compliant `profiling_notice` on new profile creation / 新規プロファイル作成時にGDPR Art. 13/14準拠の`profiling_notice`を返却
- **Preference-aware search reranking** across all 5 search tools (layout, motion, background, narrative, responsive) / **全5検索ツールにpreference rerankingを統合**（layout, motion, background, narrative, responsive）
  - Cosine similarity reranking with configurable `rerank_weight` (default 0.3) / コサイン類似度リランキング（設定可能な`rerank_weight`、デフォルト0.3）
  - `applyPreferenceReranking()` shared helper replacing ~35-line inline code per tool / 共通ヘルパー`applyPreferenceReranking()`で各ツール~35行のインラインコードを1行に集約
- **Database schema**: `preference_profiles`, `preference_signals` tables with Prisma migration / **データベーススキーマ追加**: `preference_profiles`, `preference_signals`テーブル（Prismaマイグレーション）
- **PRIVACY.md**: Profiling privacy policy (GDPR Art. 6/13/14/22, 8 sections, bilingual) / **PRIVACY.md**: プロファイリングプライバシーポリシー（GDPR Art. 6/13/14/22、8セクション、日英バイリンガル）
- **DATA_RETENTION.md**: Data retention policy (soft reset, hard delete, data export specifications) / **DATA_RETENTION.md**: データ保持ポリシー（ソフトリセット、完全削除、データエクスポート仕様）
- 138 new tests (29 service + 13 security + 96 tool/schema tests) / テスト138件追加（サービス29 + セキュリティ13 + ツール/スキーマ96）
- SEC/TDA/LCC 3-agent audit passed (12 audits across 4 phases, all PASS) / SEC/TDA/LCC 3エージェント監査全4Phase全PASS（12監査すべて合格）
- **Embedding Idle Timer**: MCPサーバー本体のEmbeddingService ONNX Worker ThreadがCUDA VRAMを30秒アイドル後に自動解放。後続のOllama Vision解析（page.analyze）がGPUで実行可能に / Auto-release CUDA VRAM from MCP server's EmbeddingService ONNX Worker Thread after 30s idle, enabling GPU acceleration for subsequent Ollama Vision analysis (page.analyze)
  - 環境変数 `EMBEDDING_IDLE_TIMEOUT_MS` で設定可能（デフォルト30秒、0で無効化） / Configurable via `EMBEDDING_IDLE_TIMEOUT_MS` env var (default 30s, 0 to disable)
- **Ollama GPU不整合検出** (`system.health`): nvidia-smiでGPU検出 + Ollama `size_vram=0` の不整合をクロスチェックし、actionable warningを表示。不整合時はステータスを `degraded` に降格 / **Ollama GPU mismatch detection** (`system.health`): Cross-checks nvidia-smi GPU detection against Ollama `size_vram=0`, shows actionable warning with fix steps. Downgrades status to `degraded` on mismatch (REFTRIX-GPU-MISMATCH-01)

### Fixed / 修正

- **RRF hybrid search missing `id`** -- search results lacked `id` field required for preference reranking / **RRFハイブリッド検索の`id`欠落** -- 検索結果にリランキングに必要な`id`フィールドが欠落していた
- **Responsive search reranking ID mismatch** -- `responsive_analysis_id` vs top-level `id` inconsistency / **レスポンシブ検索のリランキングID不一致** -- `responsive_analysis_id`とトップレベル`id`の不整合
- **DI factory registration** -- `PrismaClient` and `EmbeddingService` factories were missing at MCP server startup / **DIファクトリ登録欠落** -- MCPサーバー起動時に`PrismaClient`と`EmbeddingService`のファクトリが未登録
- **GPU VRAM競合修正**: 検索ツール（layout.search等）実行後にMCPサーバー本体のONNX Embeddingが~1,406MiB VRAMを占有し続け、Ollama Vision (11.7GB)がCPUフォールバックする問題を修正 / Fix GPU VRAM contention where MCP server's ONNX Embedding held ~1,406MiB VRAM after search tool execution, causing Ollama Vision (11.7GB) to fall back to CPU

### Security / セキュリティ

- **SEC-M-2**: `truncateId()` PII-safe ID truncation utility (5 files, 21 locations) / `truncateId()` PII安全なID切り詰めユーティリティ（5ファイル、21箇所）
- **SEC-L-1**: `parseVectorString()` NaN/Infinity defense with `Number.isFinite()` validation / `parseVectorString()`のNaN/Infinity防御（`Number.isFinite()`バリデーション）
- **SEC-L-2**: Embedding vector pre-generation NaN/Infinity validation / Embeddingベクトル生成前のNaN/Infinityバリデーション
- **LCC-5**: `sanitizeErrorMessage()` preventing internal DB structure leakage / `sanitizeErrorMessage()`で内部DB構造の漏洩を防止

### Changed / 変更

- MCP tool count: 20 → **23** (added `preference.hear`, `preference.get`, `preference.reset`) / MCPツール数: 20 → **23**（`preference.hear`, `preference.get`, `preference.reset`追加）
- All 5 search tool schemas now include optional `profile_id` parameter / 全5検索ツールスキーマにオプションの`profile_id`パラメータを追加

### Documentation / ドキュメント

- Updated `README.md`, `apps/mcp-server/README.md` with preference profiling info / `README.md`、`apps/mcp-server/README.md`にpreference profiling情報を追加
- Updated `docs/users-guide/02-mcp-tools-guide.md` with Preference tools section (Chapter 14) / `docs/users-guide/02-mcp-tools-guide.md`にPreferenceツールセクション（第14章）を追加
- Updated `docs/legal/PRIVACY_POLICY.md` v0.1.0 → v0.1.1 (profiling contradiction fix, Art. 22 explanation) / `docs/legal/PRIVACY_POLICY.md` v0.1.0 → v0.1.1（プロファイリング矛盾修正、Art. 22説明追加）
- Updated `docs/legal/TERMS_OF_SERVICE.md` with preference profiling in feature list / `docs/legal/TERMS_OF_SERVICE.md`の機能リストにpreference profilingを追加

## [0.1.2] - 2026-03-05

### Added / 追加

- **Apple Silicon Metal GPU detection** / **Apple Silicon Metal GPU検出**
  - `GpuVendor` enum (`NVIDIA | APPLE_METAL | UNKNOWN`) for GPU vendor identification / GPUベンダー識別用の`GpuVendor`列挙型
  - `HardwareDetector.isAppleSilicon()` static method (`process.platform + process.arch`, no external commands) / 外部コマンド不使用の静的メソッド
  - Ollama `/api/ps` reports Apple Silicon Unified Memory as `size_vram > 0`, so existing GPU detection works correctly / Ollama `/api/ps`がUnified Memoryを`size_vram > 0`として報告するため既存GPU判定で正しく動作
  - OllamaReadinessProbe log improved: "Apple Silicon detected: Metal GPU manages memory natively" (was "assuming CPU mode") / ReadinessProbeログ改善
  - 10 new tests (8 hardware-detector + 2 readiness-probe) / テスト10件追加
  - SEC/TDA/LCC 3-agent audit passed / SEC/TDA/LCC 3エージェント監査通過
- **NOTICE file** with Apple trademark attribution (per LCC audit recommendation) / Apple商標帰属表示のNOTICEファイル（LCC監査推奨）

### Fixed / 修正

- **Docker Compose backup authentication failure** / **Docker Composeバックアップ認証失敗**
  - Root cause: Docker Compose only reads `.env` by default, not `.env.local`, causing `POSTGRES_PASSWORD` to always resolve to the default `change_me` / 根本原因: Docker Composeは`.env`のみ読み込み、`.env.local`を読まないため`POSTGRES_PASSWORD`が常にデフォルト値`change_me`に展開される
  - Fix: `env_file` two-stage loading (`.env.example` defaults → `.env.local` overrides) / 修正: `env_file` 2段階読み込み（`.env.example`デフォルト → `.env.local`上書き）
  - Added auth pre-flight check (`pg_isready` + `psql SELECT 1`) to `db-backup.sh` and `db-restore.sh` / `db-backup.sh`と`db-restore.sh`に認証事前チェック（`pg_isready` + `psql SELECT 1`）追加
  - Added container-mode support to `db-restore.sh` (`REFTRIX_BACKUP_INSIDE_CONTAINER=true`) / `db-restore.sh`にコンテナ内モード対応追加（`REFTRIX_BACKUP_INSIDE_CONTAINER=true`）
  - `POSTGRES_PASSWORD` added to `.env.example` for Docker Compose consumption / Docker Compose用に`.env.example`に`POSTGRES_PASSWORD`追加
- **Quickstart setup completeness** / **Quickstartセットアップの完全性**
  - Added `pnpm exec playwright install chromium` for page crawling browser dependency / ページクロール用ブラウザ依存の`pnpm exec playwright install chromium`追加
  - Integrated Ollama setup into main Setup section (was optional, now required) / Ollamaセットアップをメインセットアップセクションに統合（オプション→必須）
  - Added `NODE_ENV` and `OLLAMA_BASE_URL` to MCP config example / MCP設定例に`NODE_ENV`と`OLLAMA_BASE_URL`追加
  - Corrected worker auto-start documentation (WorkerSupervisor auto-starts, no manual launch needed) / Worker自動起動ドキュメントの修正（WorkerSupervisorが自動起動、手動起動不要）
- **Ollama Vision OOM prevention (3-point unload)** / **Ollama Vision OOM防止（3箇所アンロード）**
  - Unloads Vision model via `keep_alive: "0"` at 3 points: (1) after Phase 1 Layout Analysis, (2) after Phase 2.5 Scroll Vision, (3) after Phase 4 Narrative / 3箇所でVisionモデルをアンロード
  - Frees ~10.6 GB on CPU-only (16 GB RAM) to prevent embedding OOM / CPU-only環境で~10.6GB解放しembedding OOM防止
  - Phase 1 unload also fixes Phase 2.5 skip on GPU environments (VRAM threshold was not cleared) / Phase 1アンロードでGPU環境のPhase 2.5スキップも解消
  - Idempotent (no-op when Vision not loaded); SSRF-safe via `validateOllamaLocalhostUrl()` / 冪等; SSRF対策済み
- **Default mode `visionUsed: false`** -- was incorrectly set even when Vision was used / デフォルトモードでVision使用時もfalseになっていた
- **CPU-only scroll vision timeout** -- scroll vision analysis timed out prematurely on CPU environments / CPU環境でスクロールVision分析が早期タイムアウト

### Changed / 変更

- Ollama (`llama3.2-vision`) promoted from optional to required prerequisite / Ollama（`llama3.2-vision`）をオプションから必須の前提条件に昇格
- Backup/restore scripts now show clear bilingual error messages on auth failure / バックアップ/リストアスクリプトが認証失敗時に明確な日英エラーメッセージを表示

### Documentation / ドキュメント

- Updated `current-architecture.md` HardwareDetector section with GpuVendor enum, `isAppleSilicon()`, Apple Silicon Unified Memory explanation / `current-architecture.md`のHardwareDetectorセクションにGpuVendor enum・isAppleSilicon()・Unified Memory説明を追加
- Updated `current-architecture.md` Database Backup & Restore section (bilingual, env_file two-stage loading, auth pre-flight check) / `current-architecture.md`のDatabase Backup & Restoreセクション更新（日英バイリンガル、env_file 2段階読み込み、認証事前チェック）
- Added troubleshooting section 2.6: Backup/Restore Authentication Failure / トラブルシューティングセクション2.6追加: バックアップ/リストア認証失敗
- Updated FAQ Q8 to use `pnpm db:backup` / FAQ Q8を`pnpm db:backup`に更新
- Updated development rules with Ollama Vision Unload + Apple Metal Support info / 開発ルールにOllama Vision Unload＋Apple Metal Support情報を追加

## [0.1.1] - 2026-03-03

### Added / 追加

- **Responsive design analysis** (`responsive.search` -- 20th MCP tool) / **レスポンシブデザイン分析**（`responsive.search` -- 20番目のMCPツール）
  - Multi-viewport capture (mobile 375px / tablet 768px / desktop 1440px) with Playwright / Playwrightによるマルチビューポートキャプチャ
  - Difference detection engine v2: computedStyle, BoundingRect, external CSS resolution / 検出エンジンv2: computedStyle・BoundingRect・外部CSS解決
  - Screenshot diff via Pixelmatch with configurable threshold / Pixelmatchによるスクリーンショット差分（閾値設定可能）
  - 8 diff categories: layout, typography, spacing, visibility, navigation, image, interaction, animation / 8つの差異カテゴリ
  - Semantic search over responsive analysis results via pgvector HNSW + JSONB filters / pgvector HNSW + JSONBフィルタによるレスポンシブ分析セマンティック検索
  - Embedding generation integrated into Worker Phase 5 pipeline / Worker Phase 5パイプラインにEmbedding生成を統合
  - Clean-slate pattern: re-analysis overwrites previous results per URL / Clean-slateパターン: 同一URL再分析時に旧データを上書き
  - SEC/TDA/LCC 3-agent audit passed (2 rounds) / SEC/TDA/LCC 3エージェント監査通過（2ラウンド）
- Full Japanese README at `docs/README.ja.md` / 日本語フルREADME（`docs/README.ja.md`）
- Restructured English-main `README.md` (~150 lines, concise and action-oriented) / 英語メインREADME再構築（約150行、簡潔・行動指向）

### Fixed / 修正

- ONNX Worker Thread `execArgv` propagation causing zero embeddings / ONNX Worker ThreadのexecArgv伝播によるEmbedding生成0件問題
- Missing `setEmbeddingServiceFactory` in Worker process DI initialization / WorkerプロセスDI初期化でのsetEmbeddingServiceFactory未設定
- `screenshot_diffs` design bug: separated internal capture from response payload / screenshot_diffs設計バグ: 内部キャプチャとレスポンス返却の分離
- `viewportsAnalyzed` missing `width`/`height` fields / viewportsAnalyzedのwidth/heightフィールド欠落
- Offset schema missing `.int()` constraint (SEC W-1) / offsetスキーマの.int()制約欠落（SEC W-1）

### Changed / 変更

- MCP tool count: 19 → **20** (added `responsive.search`) / MCPツール数: 19 → **20**（`responsive.search`追加）
- Updated all documentation to reflect 20 tools / 全ドキュメントを20ツールに更新
  - `README.md`, `apps/mcp-server/README.md`, `docs/users-guide/02-mcp-tools-guide.md`, `docs/users-guide/03-page-analyze-deep-dive.md`

## [0.1.0] - 2026-03-01

### Added / 追加

- Initial OSS release / 初回OSS公開
- MCP Server with 19 tools (layout, motion, quality, page analysis, search) / 19ツール搭載のMCPサーバー（レイアウト、モーション、品質、ページ分析、検索）
- Layout analysis and section detection / レイアウト分析とセクション検出
- Motion/animation detection (CSS + JavaScript) / モーション・アニメーション検出（CSS + JavaScript）
- Design quality evaluation / デザイン品質評価
- Semantic search with multilingual-e5-base embeddings (768 dimensions) / multilingual-e5-baseエンベディングによるセマンティック検索（768次元）
- Hybrid search (vector + full-text with RRF) / ハイブリッド検索（ベクトル + 全文検索、RRF統合）
- Background design detection / 背景デザイン検出
- Narrative analysis with vector + full-text + hybrid search / ベクトル・全文・ハイブリッド検索対応のナラティブ分析
- Frame image analysis with Worker thread parallelization, CLS calculation, and color change detection / Worker Thread並列処理・CLS計算・色変化検出対応のフレーム画像分析
- Page analysis pipeline with WorkerSupervisor / WorkerSupervisorによるページ分析パイプライン
- GPU Resource Manager for ONNX/Ollama coordination / ONNX/Ollama連携のためのGPUリソースマネージャー
- PostgreSQL 18 + pgvector 0.8 with HNSW indexing / PostgreSQL 18 + pgvector 0.8（HNSWインデックス）
- Browser automation with Playwright / Playwrightによるブラウザ自動化
- Comprehensive pre-release security audit / 包括的リリース前セキュリティ監査
- `pnpm audit --audit-level=high` enforced in CI / CIでHigh以上の脆弱性チェック強制
- `REFTRIX_RESPECT_ROBOTS_TXT` environment variable for robots.txt compliance / robots.txt準拠のための環境変数
- Coverage thresholds for all 5 packages / 全5パッケージにカバレッジ閾値を設定
- Prisma `postinstall` generate hook / Prisma postinstallでの自動generate
