# @reftrixmcp/mcp-server Changelog / 変更履歴

All notable changes to the `@reftrixmcp/mcp-server` package will be documented in this file.

`@reftrixmcp/mcp-server` パッケージに対する注目すべき変更点はすべてこのファイルに記載されます。

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

形式は [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) に基づき、
[セマンティックバージョニング](https://semver.org/spec/v2.0.0.html) に準拠しています。

This package-level CHANGELOG was added in v0.4.0 PR-D-9 per Phase 3 docs-sync OBS-4 / FIND-IMPL-LCC-03 (Finding Registry §11.6, mandatory landing). It complements the root `CHANGELOG.md` (OSS-facing) and `(internal full) per the 3-CHANGELOG policy in`

このパッケージレベル CHANGELOG は v0.4.0 PR-D-9 で Phase 3 docs-sync OBS-4 / FIND-IMPL-LCC-03（Finding Registry §11.6、必須 landing）として追加されました。`の 3-CHANGELOG ポリシーに従い、ルート`CHANGELOG.md`（OSS公開用）および `

---

## [0.5.1] — 2026-05-07 (target) / [0.5.1] — 2026-05-07 (target)

### Changed / 変更

### Fixed / 修正

- **Per-file LoC deltas (within `apps/mcp-server/`) for Amendment 1 + 2**:
  - `apps/mcp-server/vitest.base.config.mts` — Amendment 1 F2 revert: **−1 LoC net** (1 line removed; SSOT propagates to 3 derived configs without per-config edits per ADR-0016 inheritance contract).
  - `apps/mcp-server/tests/regression/standing/schema-enum-sync/inv-release-version-coherence-001.test.ts` — Amendment 2 Layer 1: **NEW file**; 1 positive PASS test asserting `git tag` ↔ `package.json.version` equality across 5 publishable packages; negative mutation correctly fails (test-qa-engineer owner; landing under `schema-enum-sync` standing regression domain).

- **CI Flaky Systemic Remediation (v0.5.1 patch release, ADR-0020 Decisions 1-5)** / **CI flaky 構造的修復 (v0.5.1 パッチリリース、ADR-0020 Decisions 1-5)**:
  - **EN**: CI flaky structural remediation across 5 fixes per ADR-0020. F1 (Vitest `maxWorkers: 3 → 2`, SSOT propagation to 3 derived configs) + F2 (top-level `fileParallelism: false`, suppresses intra-worker async I/O race) + F3 (`actions/cache@v4` for Playwright browser at 3 callsites in `.github/workflows/ci.yml`, lines 154/224/382, cuts cold-install ~60s → cache-hit ~5s) + F4 (`LocalStorageProvider` mock-replace in Phase 1 integration test via `vi.mock`, removes storage I/O timing from test path; new compensation unit test lands in same commit per R-11 mandate; **note**: Plan/ADR previously referred to this provider as `MemoryStorageProvider` for historical reasons — production code (`apps/mcp-server/src/services/storage/local-storage.provider.ts:85` `export class LocalStorageProvider`) and 3-CHANGELOG entries now uniformly use `LocalStorageProvider` per CLAUDE.md SoT Hierarchy T1 wins, RENAME-PROVIDER-01 landed in Phase 3) + CO-PR21-REVIEW-01 (CSS-animation parser ReDoS threshold replaced with input-size-proportional formula `expectedMaxMs = input.length / throughputCharsPerMs + 200ms` with env var `REDOS_TEST_THROUGHPUT_CHARS_PER_MS` override; local default 100 chars/ms, CI 80 chars/ms via `.github/workflows/ci.yml` env injection per TPA-04⚯TDA-02 unified). CWE-770 mitigation completeness preserved as **bounded-growth detection** (PR #21 fixed-1000ms hotfix superseded).
  - **JP**: ADR-0020 に従う 5 件の CI flaky 構造的修復。F1 (Vitest `maxWorkers: 3 → 2`、3 derived config に SSOT 伝播) + F2 (top-level `fileParallelism: false`、worker 内 async I/O 競合抑制) + F3 (`.github/workflows/ci.yml` の 3 callsite (154/224/382 行) に `actions/cache@v4` for Playwright browser、cold install ~60秒 → cache hit ~5秒) + F4 (Phase 1 integration test で `LocalStorageProvider` を `vi.mock` で置換、storage I/O timing を test path から構造的撤去、R-11 mandate に従い同一 commit で新規補填 unit test も landing。**注**: Plan/ADR は historical reasons から `MemoryStorageProvider` と表記していたが、production code (`apps/mcp-server/src/services/storage/local-storage.provider.ts:85` `export class LocalStorageProvider`) と 3-CHANGELOG entry はいずれも CLAUDE.md SoT Hierarchy (T1 wins) に従い `LocalStorageProvider` を使用、RENAME-PROVIDER-01 は Phase 3 で landing) + CO-PR21-REVIEW-01 (CSS-animation parser ReDoS test threshold を input-size 比例式 `expectedMaxMs = input.length / throughputCharsPerMs + 200ms` に置換、env var `REDOS_TEST_THROUGHPUT_CHARS_PER_MS` で override 可能。Local default 100 chars/ms、CI は `.github/workflows/ci.yml` env injection で 80 chars/ms、TPA-04⚯TDA-02 unified per)。CWE-770 mitigation completeness は **bounded-growth detection** で維持 (PR #21 固定 1000 ms hotfix を supersede)。

  - **Per-file LoC deltas (within `apps/mcp-server/`) / per-file LoC delta (`apps/mcp-server/` scope)**:
    - `apps/mcp-server/vitest.base.config.mts` — F1 (`maxWorkers: 2`) + F2 (`fileParallelism: false`) — **+2 LoC net** (1 line modified for F1, 1 line added for F2). SSOT propagates automatically to 3 derived configs (`vitest.config.mts` / `vitest.smoke.config.ts` / `vitest.regression-standing.config.ts`) via existing ADR-0016 inheritance contract; no derived-config edits needed.
    - `apps/mcp-server/tests/integration/phase1-integration.test.ts` — F4 (`vi.mock("@/services/storage/local-storage.provider")` + mock signature). Replaces actual `LocalStorageProvider` I/O construction with `vi.mock` factory + `MockLocalStorageProvider` in-memory fake. Storage I/O timing removed from test path; Phase 1 logic verification preserved.
    - `apps/mcp-server/tests/services/page/css-animation-parser.service.test.ts` — CO-PR21-REVIEW-01. PR #21 fixed-1000ms threshold replaced with `expectedMaxMs = input.length / throughputCharsPerMs + 200ms`; env var `REDOS_TEST_THROUGHPUT_CHARS_PER_MS` override (default 100, CI fallback 80 per TPA-04⚯TDA-02 measurement).
    - `apps/mcp-server/tests/services/storage/memory-storage-provider.test.ts` — **NEW file (Planned)**. F4 coverage compensation per RENAME-PROVIDER-01 + ADR-0020 R-11 mandate (H severity, TDA-01 reinforcement). Physical file lands in same commit as F4 in Phase 2 implementation per Plan §2.2 Step 2.3. Maintains `LocalStorageProvider` coverage 80%+ absent from integration test path post-mock.

  - **Test counts / テスト件数**:
    - F4 mock replacement: removes storage I/O timing dependency from `tests/integration/phase1-integration.test.ts`; logic-verifying assertions retained, mock-based assertions added (`mockPersist` call count + payload equality).
    - F4 compensation file: new unit tests cover `LocalStorageProvider.persist` / `read` / path traversal / size cap / TTL behaviour; coverage threshold ≥ 80% verified via `pnpm test:coverage`.
    - CO-PR21-REVIEW-01: 3 input sizes (small ≤ 500 chars / medium 500-2000 / large 2000-5000) × 3-run consistency (per Plan §2.3.5 CI baseline measurement TPA-04⚯TDA-02 unified).
    - Skip / todo check: 0 lines added.

  - **Quality Gate / 品質ゲート**: `pnpm lint` + `pnpm typecheck` + `pnpm test` + `pnpm format:check` + `pnpm docs:verify` (47/47 PASS) all PASS. Standing regression 4 domains (large-page / gdpr-delete / worker-lifecycle / schema-enum-sync) all PASS pass^3 (3 consecutive). `pnpm audit --audit-level=high` 0 findings preserved.

  - **Environment variables / 環境変数**:
    - `REDOS_TEST_THROUGHPUT_CHARS_PER_MS` (NEW, test fixture only) — default 100 chars/ms (local), CI override 80 chars/ms (per TPA-04⚯TDA-02 unified review post-F1+F2 measurement). Configured via `.github/workflows/ci.yml` env injection at Phase 2.4. env var unset 時は default 100 chars/ms で fallback、後方互換 / Defaults to 100 chars/ms when unset (backwards-compatible).
    - No production env var added in `apps/mcp-server/`. F1-F4 are test-infra only.

  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-05-07 に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` および `にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per` `apps/mcp-server/vitest.base.config.mts` (F1+F2) + `apps/mcp-server/tests/integration/phase1-integration.test.ts` (F4) + `apps/mcp-server/tests/services/page/css-animation-parser.service.test.ts` (CO-PR21-REVIEW-01) + `apps/mcp-server/tests/services/storage/memory-storage-provider.test.ts` (NEW、F4 coverage compensation per RENAME-PROVIDER-01)。改変日付: 2026-05-07。
    - **EN**: The following files in this package were modified on 2026-05-07 under AGPL-3.0-only §5(a)/(b) (Modified Versions). Primary modification notice with date is in root `CHANGELOG.md` and `this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per` Modified files: `apps/mcp-server/vitest.base.config.mts` (F1+F2) + `apps/mcp-server/tests/integration/phase1-integration.test.ts` (F4) + `apps/mcp-server/tests/services/page/css-animation-parser.service.test.ts` (CO-PR21-REVIEW-01) + `apps/mcp-server/tests/services/storage/memory-storage-provider.test.ts` (NEW, F4 coverage compensation per RENAME-PROVIDER-01). Modification date: 2026-05-07.

- **`@reftrixmcp/ml` DINOv2 mock test isolation — full structural closure under Vitest `pool: forks`** (CO-DINOV2-MOCK-ISOLATION-01 **CLOSED in this cycle**, deferred-to-v0.5.2 から本 cycle 内 closure に升格) — **NB: out-of-package scope reference** (this fix lives in the sibling `packages/ml/` package; recorded here for 3-CHANGELOG sync rule per `and to keep the v0.5.1 audit trail discoverable from the`apps/mcp-server`package boundary): 4-attempt cumulative resolution で zero-flake を達成 — IO Supplementary Decision #1 → #2 → #3 → IO Decision #3 → User Path ii direct sequential。**Per-package LoC delta**:`packages/ml/`内 (Stage 1) +1 LoC`vi.resetModules()`+ (Stage 2) 既存 1 test を 2 it() に split (test count 21 → 22) + (Stage 3) 新規 isolated file`packages/ml/tests/dinov2/service-mocked-onnx.test.ts` を分離 + (Stage 4) Option A'' で同 file を 7 changes rewrite (7 tests preserved); **`apps/mcp-server/` 内は 0 LoC delta\*\* (production runtime / dependency tree / SBOM CycloneDX 1.6 driver entries 不変)。

  **Final solution**: Path B' (file split) + Option A'' (`vi.mock` + `vi.hoisted`) の合成で **inter-file (process boundary) + intra-file (static hoisting) の double structural guarantee** を確立。Path B' alone で intra-file race ~10% 残存、Option A'' alone で inter-file leak 防げず、両者の合成で zero-flake を達成。

  **Attempts breakdown**:
  - **Stage 1 (Supplementary #1, anchor `019de32d-3a6a-73fa-9803-42336c19a671`)**: `describe("DINOv2Service with mocked ONNX")` block の `beforeEach` 先頭に `vi.resetModules()` を追加 (sibling files `packages/ml/tests/dinov2/service-worker-mock.test.ts` / `packages/ml/tests/dinov2/worker-thread.test.ts` と整合)、describe-block-level mock leak を closure。Verification: 17/20 PASS。
  - **Stage 2 (Supplementary #2, anchor `019de345-b62e-747d-a429-20df65cc94ea`、ADR-0020 Amendment 3)**: `should handle dispose and re-initialize` test を **2 つの `it()` に分離** ("1 test = 1 mock cycle" 契約)。Test count delta: 21 → 22。Verification: 19/30 PASS (~37% fail、reshape 対象外 test も flaky)。
  - **Stage 3 (Supplementary #3, anchor `019de376-...`)**: Path B' file split — mocked-ONNX 専用 test 群を新規 isolated file `packages/ml/tests/dinov2/service-mocked-onnx.test.ts` に分離。Vitest `pool: forks` の inter-file process boundary 活用。Verification: 9/10 PASS (~10% intra-file flake 残存)。
  - **Stage 4 (User Path ii direct, ADR-0020 Amendment 4)**: User が IO escalation を bypass し Option A'' (`vi.doMock` → `vi.mock` + `vi.hoisted`) を直接指示。`packages/ml/tests/dinov2/service-mocked-onnx.test.ts` を **7 changes で rewrite**: vi.doMock→vi.mock / vi.hoisted introduction / vi.resetModules deletion / vi.doUnmock deletion / restoreAllMocks→clearAllMocks / 動的 await import → static top-level import / mockReset for per-test state clean。Production code (`packages/ml/src/dinov2/`) untouched、7 tests preserved。Verification: **30/30 PASS** ✓ (Wilson 95% CI lower bound 88.4%)。

  **Stage 5 Scope Expansion (DRIFT-U20-01, ADR-0020 Amendment 4 §5.5)**: IO U-20 Final Consistency Check (`019de395-8c12-723e-b4af-3368c8984a8d`) で発見された DRIFT-U20-01 [H severity] により、`packages/ml/tests/dinov2/service-coverage.test.ts` (v0.2.0 sprint で作成、本 v0.5.1 cycle 全体で untouched) も同じ `vi.doMock` async hoisting race を持つことが判明。Directory pass^5 stress 2/5 fail (40%)、isolation pass^5 stress 1/5 fail (20%)。Option A'' canonical pattern (Pattern (a): single `vi.mock` factory + per-test `mockReset` state) を適用、`vi.doMock` × 5 + `vi.doUnmock` × 4 → 1 `vi.mock` + 1 `vi.hoisted`、9 dynamic imports → static top-level imports。**Per-package LoC delta**: `packages/ml/` 内のみ (Stage 5 で `service-coverage.test.ts` を Pattern (a) Option A'' に rewrite、14 tests preserved); **`apps/mcp-server/` 内は 0 LoC delta** (production runtime / dependency tree / SBOM CycloneDX 1.6 driver entries 不変)。Verification: isolation pass^10 + directory pass^10 = **20/20 PASS** (zero flake)。CO-DINOV2-MOCK-ISOLATION-01 closure を **"Path B' + Option A'' + DRIFT-U20-01 application expansion 三段合成"** に再 phrasing。

  / 4-attempt cumulative resolution achieving zero-flake — sequential landing of IO Supplementary Decision #1 → #2 → #3 → IO Decision #3 → User Path ii direct. **Per-package LoC delta**: within `packages/ml/` (Stage 1) +1 LoC `vi.resetModules()` + (Stage 2) existing test split into 2 it() (test count 21 → 22) + (Stage 3) new isolated file `packages/ml/tests/dinov2/service-mocked-onnx.test.ts` separated + (Stage 4) same file rewritten with 7 changes under Option A'' (7 tests preserved); **0 LoC delta within `apps/mcp-server/`** (production runtime / dependency tree / SBOM CycloneDX 1.6 driver entries unchanged).

  **Final solution**: Path B' (file split) + Option A'' (`vi.mock` + `vi.hoisted`) composition establishes a **double structural guarantee — inter-file (process boundary) + intra-file (static hoisting)**. Path B' alone leaves ~10% intra-file race, Option A'' alone cannot prevent inter-file leak; their composition achieves zero-flake.

  **Attempts breakdown**:
  - **Stage 1 (Supplementary #1, anchor `019de32d-...`)**: added `vi.resetModules()` to `beforeEach` head in `describe("DINOv2Service with mocked ONNX")` block (aligned with sibling-file pattern in `service-worker-mock.test.ts` / `worker-thread.test.ts`); closed describe-block-level mock leak. Verification: 17/20 PASS.
  - **Stage 2 (Supplementary #2, anchor `019de345-...`, ADR-0020 Amendment 3)**: split `should handle dispose and re-initialize` into **2 `it()` blocks** ("1 test = 1 mock cycle" contract). Test count delta: 21 → 22. Verification: 19/30 PASS (~37% fail; tests outside the reshape scope were also revealed flaky).
  - **Stage 3 (Supplementary #3, anchor `019de376-...`)**: Path B' file split — separated mocked-ONNX-dedicated tests into new isolated file `packages/ml/tests/dinov2/service-mocked-onnx.test.ts`. Leveraged Vitest `pool: forks` inter-file process boundary. Verification: 9/10 PASS (~10% intra-file flake persisted).
  - **Stage 4 (User Path ii direct, ADR-0020 Amendment 4)**: User bypassed IO escalation and directly instructed Option A'' (`vi.doMock` → `vi.mock` + `vi.hoisted`). Rewrote `packages/ml/tests/dinov2/service-mocked-onnx.test.ts` with **7 changes**: vi.doMock→vi.mock / vi.hoisted introduction / vi.resetModules deletion / vi.doUnmock deletion / restoreAllMocks→clearAllMocks / dynamic await import → static top-level import / mockReset for per-test state clean. Production code (`packages/ml/src/dinov2/`) untouched; 7 tests preserved. Verification: **30/30 PASS** ✓ (Wilson 95% CI lower bound 88.4%).

  **Stage 5 Scope Expansion (DRIFT-U20-01, ADR-0020 Amendment 4 §5.5)**: IO U-20 Final Consistency Check (`019de395-8c12-723e-b4af-3368c8984a8d`) revealed DRIFT-U20-01 [H severity]: `packages/ml/tests/dinov2/service-coverage.test.ts` (created in v0.2.0 sprint, untouched throughout the v0.5.1 cycle) carries the same `vi.doMock` async hoisting race. Directory pass^5 stress 2/5 fail (40%); isolation pass^5 stress 1/5 fail (20%). Applied Option A'' canonical pattern (Pattern (a): single `vi.mock` factory + per-test `mockReset` state); converted `vi.doMock` × 5 + `vi.doUnmock` × 4 to 1 `vi.mock` + 1 `vi.hoisted`; replaced 9 dynamic imports with static top-level imports. **Per-package LoC delta**: changes confined to `packages/ml/` (Stage 5 rewrites `service-coverage.test.ts` to Pattern (a) Option A'', 14 tests preserved); **0 LoC delta within `apps/mcp-server/`** (production runtime / dependency tree / SBOM CycloneDX 1.6 driver entries unchanged). Verification: isolation pass^10 + directory pass^10 = **20/20 PASS** (zero flake). CO-DINOV2-MOCK-ISOLATION-01 closure re-phrased as **"three-stage composition: Path B' + Option A'' + DRIFT-U20-01 scope expansion"**.

### Security / セキュリティ

- **Security content inheritance from v0.5.0 / v0.5.0 SEC content の継承**:
  - **EN**: "v0.5.1 includes all v0.5.0 SEC content: PR-0 protobufjs Critical / PR-0 vite / PR-0 MCP_API_KEYS CWE-521 鍵長検証 / PR-0.5 LP Next.js 16.1.7→16.2.3 GHSA-q4gf-8mx6-v5v3 (DoS with Server Components) / SEC pre-release hotfix CodeQL CWE-829 SRI / Dependabot hono + @hono/node-server + DOMPurify"
  - **JP**: "v0.5.1 は v0.5.0 の SEC content をすべて継承: PR-0 protobufjs Critical / PR-0 vite / PR-0 MCP_API_KEYS CWE-521 鍵長検証 / PR-0.5 LP Next.js 16.1.7→16.2.3 GHSA-q4gf-8mx6-v5v3 (Server Components による DoS) / SEC pre-release hotfix CodeQL CWE-829 SRI / Dependabot hono + @hono/node-server + DOMPurify"
  - **Active exploit watch**: SEC sign-off APPROVE WITH CONDITIONS (`019de070-65f5-77bc-b3b9-6017b35d85c3`) condition 2 inherited; security-engineer reassesses CISA KEV / NVD / GHSA over the 2026-05-01 〜 2026-05-07 publish window per Plan §2.4 Step 4.2.5; publish BLOCKs and escalates to IO if any new advisory matches dependency tree / SEC condition 2 を継承、publish 直前に CISA KEV / NVD / GHSA を 6-day window で reassess、新規 advisory が dependency tree に該当する場合は publish BLOCK + IO escalate.

### Compliance / 法務・コンプライアンス

- **SBOM Invariance / SBOM 不変性 attestation**:
  - **EN**: "SBOM unchanged from v0.5.0 (CycloneDX 1.6, last generated 2026-04-29 with 1300 components). v0.5.1 introduces no production dependency changes; pnpm-lock.yaml is bit-identical to v0.5.0 baseline."
  - **JP**: "SBOM は v0.5.0 から変更なし (CycloneDX 1.6、最終生成日 2026-04-29、1300 コンポーネント)。v0.5.1 は production 依存関係を変更せず、pnpm-lock.yaml は v0.5.0 baseline と bit-identical。"
  - EU CRA Art.13(8)/14(2) transparency obligation 履行証拠として機能。F1-F4 + CO-PR21-REVIEW-01 はいずれも test infrastructure / CI workflow のみの変更で `apps/mcp-server/` package の production runtime / dependency tree は不変 / Functions as EU CRA Art.13(8)/14(2) transparency evidence; F1-F4 + CO-PR21 are test-infra / CI-workflow only with no production runtime / dependency tree change in `apps/mcp-server/` package.
- **CRA SBOM impact / CRA SBOM 影響**: **No new direct dependency added; no transitive dep version change**. `pnpm-lock.yaml` bit-identical to v0.5.0 baseline. SBOM CycloneDX 1.6 driver entries unchanged on next `pnpm sbom` invocation. EU CRA 2026-09-11 compliance preserved.
- **GDPR Art.32(1)(b) processing integrity**: CI green path stabilisation contributes to "ongoing confidentiality, integrity, availability and resilience" / CI green path 安定化により Art.32(1)(b) "ongoing resilience" に貢献.
- **APPI 第23条 安全管理措置**: Technical safeguarding measures (CI green path 安定化による継続的脆弱性低減・technical reliability 向上) / Technical safeguarding measures (continuous vulnerability reduction + technical reliability via CI stabilisation).

### Cross-references / 相互参照

- ADR-0020 (`../../ — v0.5.1 architectural contract (Decisions 1-5)
- Plan v1.0 (`../../ — 4-phase 13-step implementation plan
- IO BLOCK Decision `019de06a-5e6e-73d8-9d0b-c58eb55375d3` — v0.5.0 publish gate
- SEC sign-off APPROVE WITH CONDITIONS `019de070-65f5-77bc-b3b9-6017b35d85c3` — 3 conditions inherited per Plan §6
- root `CHANGELOG.md` `[0.5.1]` Fixed section + ` `[0.5.1]`full audit trail (3-CHANGELOG sync rule per`

---

## [0.5.0] — 2026-04-29 / [0.5.0] — 2026-04-29

### Security / セキュリティ

- **fix(security): SEC pre-release hotfix — CodeQL CWE-829 SRI integrity (test fixtures) + Dependabot hono / @hono/node-server / DOMPurify pnpm.overrides 拡張 (v0.5.0 release/v0.5.0)** / **fix(security): SEC pre-release hotfix — CodeQL CWE-829 SRI integrity (test fixtures) + Dependabot hono / @hono/node-server / DOMPurify pnpm.overrides expansion (v0.5.0 release/v0.5.0)**:
  - **JP**: OSS リポジトリ (TKMD/ReftrixMCP) の Security 監査で 2026-04-29 時点で報告された **2 CodeQL alerts (CWE-829 `js/functionality-from-untrusted-source`)** + **8 Dependabot moderate alerts (hono ×7 + @hono/node-server ×1)** を `release/v0.5.0` ブランチで pre-release 解消。`apps/mcp-server/` 内の test fixture HTML 2 ファイル (`tests/fixtures/js-animations/{gsap-test,anime-js-test}.html`) の cdnjs CDN script 参照に **SRI (Subresource Integrity) `integrity="sha384-..."` + `crossorigin="anonymous"` 属性**を付与し CDN 改竄時の untrusted source 実行を構造的に防止。SHA-384 hash は CDN 配信ファイルから `openssl dgst -sha384 -binary | openssl base64 -A` で算出 (gsap@3.12.2 / anime.js@3.2.1 配信ファイルと bit-exact 一致)。Dependabot は root `package.json` の `pnpm.overrides` を拡張: `hono: ">=4.12.7"` → `">=4.12.16"` (closes 7 GHSA: GHSA-26pp-8wgv-hjvm cookie name validation + GHSA-r5rp-j6wh-rvv4 NBSP prefix bypass + GHSA-458j-xx4x-4375 toSSG path traversal + GHSA-xpcf-pg52-r92g serveStatic middleware bypass + GHSA-wmmm-f939-6g9c JSX HTML injection + GHSA-xf4j-xp2r-rqqx ipRestriction IPv4-mapped + 関連 1 件)、`@hono/node-server: ">=1.19.10"` → `">=1.19.14"` (closes GHSA-92pp-h63x-v22m middleware bypass)、新規 `dompurify: ">=3.4.1"` (defensive override; 主依存は既に 3.4.0 で advisory 対象外だが OSS Dependabot が transitive nested 旧版を検知する可能性に対する深層防御)。Lockfile 解決結果: hono@4.12.7 → 4.12.16、@hono/node-server@1.19.10 → 2.0.1 (latest)、dompurify@3.4.0 → 3.4.1。`pnpm install --lockfile-only` で binary download なしで lockfile 更新。
  - **EN**: Resolves **2 CodeQL alerts (CWE-829 `js/functionality-from-untrusted-source`)** + **8 Dependabot moderate alerts (hono ×7 + @hono/node-server ×1)** reported on the OSS repo (TKMD/ReftrixMCP) at pre-release on the `release/v0.5.0` branch. Added SRI attributes (`integrity="sha384-..."` + `crossorigin="anonymous"`) to the cdnjs CDN script references in 2 `apps/mcp-server/` test fixture HTML files (`tests/fixtures/js-animations/{gsap-test,anime-js-test}.html`), structurally preventing untrusted-source execution if the CDN is tampered with. SHA-384 hashes were computed from CDN-served files via `openssl dgst -sha384 -binary | openssl base64 -A` for bit-exact match against gsap@3.12.2 / anime.js@3.2.1. Dependabot: expanded root `package.json` `pnpm.overrides` — `hono: ">=4.12.7"` → `">=4.12.16"` (closes 7 GHSAs), `@hono/node-server: ">=1.19.10"` → `">=1.19.14"` (closes GHSA-92pp-h63x-v22m), new `dompurify: ">=3.4.1"` (defensive). Lockfile resolution: hono@4.12.7 → 4.12.16, @hono/node-server@1.19.10 → 2.0.1, dompurify@3.4.0 → 3.4.1. Lockfile updated via `pnpm install --lockfile-only`.
  - **Per-file LoC deltas / per-file LoC delta**:
    - `apps/mcp-server/tests/fixtures/js-animations/gsap-test.html` **+0/-0 LoC net** (1 line modified) — added `integrity="sha384-d+vyQ0dYcymoP8ndq2hW7FGC50nqGdXUEgoOUGxbbkAJwZqL7h+jKN0GGgn9hFDS"` and `crossorigin="anonymous"` to the existing `<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js">` element. Closes CodeQL alert (CWE-829, alert 1 of 2).
    - `apps/mcp-server/tests/fixtures/js-animations/anime-js-test.html` **+0/-0 LoC net** (1 line modified) — added `integrity="sha384-fXdIufVbE9aU7STmdk/DWK0imNOozId9fTwzM/gi0NfPjphEIC3gq0M760UnsKVy"` and `crossorigin="anonymous"` to the existing `<script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/3.2.1/anime.min.js">` element. Closes CodeQL alert (CWE-829, alert 2 of 2).
  - **Test counts / テスト件数**: Test fixtures only; no unit/integration test changes in `apps/mcp-server/` for the SRI fix. Dependency override fix is build-time only (no runtime API contract change). Skip/todo check: 0 lines added.
  - **Quality Gate / 品質ゲート**: `pnpm lint` (mcp-server) + `pnpm typecheck` (mcp-server) + `pnpm format:check` (root) + `pnpm docs:verify` (47/47 PASS). `pnpm audit --audit-level=high` 0 findings preserved. `pnpm audit --audit-level=moderate` reduced **10 → 3** (8 advisories closed: 7 hono + 1 @hono/node-server).
  - **Environment variables / 環境変数**: **No new env var added in `apps/mcp-server/`**. Build-time `pnpm.overrides` adjustments only.
  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-04-29 に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` および `にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per` `apps/mcp-server/tests/fixtures/js-animations/gsap-test.html` (SRI integrity 属性付与) + `apps/mcp-server/tests/fixtures/js-animations/anime-js-test.html` (SRI integrity 属性付与)。改変日付: 2026-04-29。
    - **EN**: The following files in this package were modified on 2026-04-29 under AGPL-3.0-only §5(a)/(b) (Modified Versions). The primary modification notice with date is carried in the root `CHANGELOG.md` and `this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per` Modified files: `apps/mcp-server/tests/fixtures/js-animations/gsap-test.html` (SRI integrity attribute added) + `apps/mcp-server/tests/fixtures/js-animations/anime-js-test.html` (SRI integrity attribute added). Modification date: 2026-04-29.
  - **Compliance / 法務・コンプライアンス**:
    - **[CWE-829 Inclusion of Functionality from Untrusted Control Sphere]**: SRI integrity hash + `crossorigin="anonymous"` on cdnjs CDN script references prevents tampered or replaced CDN content from executing in test fixture HTML. Defense-in-depth at the test scaffolding layer; the production MCP server does not load remote scripts at runtime, but the fixtures exist in the published package and OSS repo, so the fix removes the latent supply-chain attack surface.
    - **[EU] GDPR Art.32(1)(b) processing integrity**: Strengthens "ongoing confidentiality, integrity, availability and resilience" via dependency vulnerability remediation across the hono cookie/IP/path-traversal/middleware-bypass class.
    - **[JP] APPI 第23条 安全管理措置**: Technical safeguarding measures (継続的な脆弱性低減 / continuous vulnerability reduction) at the dependency boundary.
    - **[EU] EU CRA 2024/2847 Article 14**: Vulnerability handling within published products. SBOM (CycloneDX 1.6) regeneration: dependency version metadata only refreshed (hono / @hono/node-server / dompurify), no new components added. EU CRA 2026-09-11 compliance preserved.
  - **CRA SBOM impact / CRA SBOM 影響**: **No new direct dependency added; existing transitive deps re-pinned via root `pnpm.overrides`**. SBOM CycloneDX 1.6 driver entries refresh on next `pnpm sbom` invocation (hono → 4.12.16, @hono/node-server → 2.0.1, dompurify → 3.4.1).
  - **Refs / 参照**: GHSA-26pp-8wgv-hjvm + GHSA-r5rp-j6wh-rvv4 + GHSA-458j-xx4x-4375 + GHSA-xpcf-pg52-r92g + GHSA-wmmm-f939-6g9c + GHSA-xf4j-xp2r-rqqx (hono, 6 GHSAs) + GHSA-92pp-h63x-v22m (@hono/node-server) + CWE-829 (CodeQL `js/functionality-from-untrusted-source`) + root `CHANGELOG.md` `[0.5.0]` Security section + ` `[0.5.0]` SEC pre-release hotfix sub-section.

- **fix(security): PR-0 SEC hotfix — protobufjs Critical + vite + MCP_API_KEYS 鍵長検証 (CWE-521) (v0.5.0 PR-0) [GHSA-xq3m-2v4x-88gg][GHSA-v2wj-q39q-566r][CWE-521]** / **fix(security): PR-0 SEC hotfix — protobufjs Critical + vite + MCP_API_KEYS key-length check (v0.5.0 PR-0)**:
  - **JP**: PR7e-consolidated 全体 (85 commits, 223 files) を 6 PR に分割する戦略の最優先 PR-0 として、3 件の SEC 防御を `apps/mcp-server/` に landing。
    1. **SEC Critical (GHSA-xq3m-2v4x-88gg)**: `@huggingface/transformers > onnxruntime-web > protobufjs < 7.5.5` の transient Critical 脆弱性をルート `pnpm.overrides` の `protobufjs: >=7.5.5` で強制解消。protobufjs@8.0.1 に pin、`pnpm audit --audit-level=critical` 0 件達成 (本パッケージは依存解決経路の終端としてビルド成果物に影響、SBOM CycloneDX 1.6 driver entry が protobufjs 8.0.1 に sync)。
    2. **SEC High defense-in-depth (GHSA-v2wj-q39q-566r)**: `pnpm.overrides` の `vite: ">=7.3.2 <9"` で vite@8.0.8 に pin。dev-only で exploitable ではないが defense-in-depth として適用、main High 3 件中 2 件解消。
    3. **SEC CWE-521 (M-4a) — `apps/mcp-server/` 直接コード変更**: `apps/mcp-server/src/middleware/auth.ts` に `MIN_ENV_KEY_LENGTH = 40` filter を追加し `MCP_API_KEYS` env 由来のキー長不足を runtime 拒否 (rate-limited warn 出力)。`reftrix_` prefix (8 chars) + 32 chars secret = 40 文字最低要件、190-bit entropy (base62) で 80-bit 安全閾値を大幅超過。`TEST_API_KEY_MAPPINGS` (開発用) は filter 対象外として温存、prod-test 経路の互換性維持。
  - **EN**: As the top-priority PR-0 of the 6-PR split of feat/pr7e-consolidated (85 commits, 223 files), lands 3 SEC defenses into `apps/mcp-server/`.
    1. **SEC Critical (GHSA-xq3m-2v4x-88gg)**: Resolved transient Critical `protobufjs < 7.5.5` (via `@huggingface/transformers > onnxruntime-web > protobufjs`) by pinning `protobufjs: >=7.5.5` (resolved to 8.0.1) in root `pnpm.overrides`. `pnpm audit --audit-level=critical` reports 0 (this package is the leaf consumer; build artefact + CycloneDX 1.6 SBOM driver entry sync to protobufjs@8.0.1).
    2. **SEC High defense-in-depth (GHSA-v2wj-q39q-566r)**: Applied `vite: ">=7.3.2 <9"` (resolved to 8.0.8) as defense-in-depth (not exploitable due to dev-only, no `--host`); resolved 2/3 High findings on main.
    3. **SEC CWE-521 (M-4a) — direct `apps/mcp-server/` code change**: Added `MIN_ENV_KEY_LENGTH = 40` filter in `apps/mcp-server/src/middleware/auth.ts` to runtime-reject under-length `MCP_API_KEYS` env-derived keys (rate-limited warn). `reftrix_` prefix (8 chars) + 32-char secret = 40-char minimum; 190-bit entropy (base62) far exceeds the 80-bit baseline. `TEST_API_KEY_MAPPINGS` (dev keys) is excluded from the filter to preserve prod-test path compatibility.
  - **Per-file LoC deltas / per-file LoC delta**:
    - `apps/mcp-server/src/middleware/auth.ts` **+17/-2 LoC** — `MIN_ENV_KEY_LENGTH = 40` constant + env-key length validation branch + rate-limited warn emission for rejected keys (CWE-521 weak password requirement enforcement at config-load time).
    - `apps/mcp-server/tests/middleware/auth.test.ts` **+80 LoC** — NEW unit cases covering `MIN_ENV_KEY_LENGTH` filter behaviour: env keys ≥40 chars accepted / env keys <40 chars rejected with rate-limited warn / `TEST_API_KEY_MAPPINGS` exempt path / boundary cases (39 chars / 40 chars / 41 chars). 0 `.skip` / `.todo`.
    - `apps/mcp-server/tests/tools/motion/detect-lighthouse.test.ts` **+1/-1 LoC** — collateral test fixture sync (test-only, no production behaviour change).
  - **Test counts / テスト件数**: `apps/mcp-server/` test suite +5+ new auth.test.ts unit cases for MIN_ENV_KEY_LENGTH filter. No regression in existing auth tests. Skip/todo check: 0 lines on new cases.
  - **Quality Gate / 品質ゲート**: `pnpm audit --audit-level=critical` 0 findings (was 1 Critical pre-PR-0) + `pnpm audit --audit-level=high` 0 findings on main scope (was 3 High pre-PR-0; remaining 1 High on `example/reftrix4>next` resolved by PR-0.5 outside `apps/mcp-server/` scope). `pnpm lint` + `pnpm typecheck` PASS post-landing.
  - **Environment variables / 環境変数**: **No new env var added in `apps/mcp-server/`**. The MIN*ENV_KEY_LENGTH constant is a compile-time SSOT (40 chars, derived from `reftrix*` prefix + 32-char secret entropy budget); not env-tunable per Plan v0.1 §3.1 (intentional non-tunability for security baseline immutability — operator override would lower entropy floor below the 80-bit safety threshold).
  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-04-18 (commit `b2de9b7a`) に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per `hotfix 後追い landing per FIND-IMPL-LCC-03 / OBS-4)。改変対象:`apps/mcp-server/src/middleware/auth.ts`(MIN_ENV_KEY_LENGTH = 40 filter + rate-limited warn) +`apps/mcp-server/tests/middleware/auth.test.ts`(NEW 80 LoC unit cases) +`apps/mcp-server/tests/tools/motion/detect-lighthouse.test.ts` (collateral fixture sync 1 LoC)。改変日付: 2026-04-18。
    - **EN**: The following files in this package were modified on 2026-04-18 (commit `b2de9b7a`) under AGPL-3.0-only §5(a)/(b) (Modified Versions). The primary modification notice with date is carried in the root `CHANGELOG.md`; this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per `SEC hotfix retroactive landing per FIND-IMPL-LCC-03 / OBS-4). Modified files:`apps/mcp-server/src/middleware/auth.ts`(MIN_ENV_KEY_LENGTH = 40 filter + rate-limited warn) +`apps/mcp-server/tests/middleware/auth.test.ts`(NEW 80 LoC unit cases) +`apps/mcp-server/tests/tools/motion/detect-lighthouse.test.ts` (collateral fixture sync 1 LoC). Modification date: 2026-04-18.
  - **Compliance / 法務・コンプライアンス**:
    - **[CWE-521 Weak Password Requirements]**: API key length floor (40 chars / 190-bit entropy base62) far exceeds the OWASP / NIST 80-bit safety baseline; eliminates the runtime acceptance path for short / weak operator-provided keys via env (CWE-521 mitigation).
    - **[EU] GDPR Art.32(1)(b) processing integrity**: API key entropy floor preserves "ongoing availability and resilience of processing systems" by structurally rejecting weak credentials at boot, preventing brute-force resistance erosion.
    - **[JP] APPI 第23条 安全管理措置**: Technical safeguarding measures extended to API key strength enforcement at the MCP middleware boundary.
    - **[EU] EU CRA 2024/2847 Article 14**: **Genuine vulnerability scope item** (CRA Annex I §1 essential cybersecurity requirements: protection from unauthorized access). protobufjs Critical (GHSA-xq3m-2v4x-88gg) is the primary CRA disclosure-eligible vulnerability; this `apps/mcp-server/` AGPL §5(a)/(b) modification notice records the operator-facing mitigation contract (40-char minimum) at the package boundary. SBOM CycloneDX 1.6 driver entry updated to protobufjs@8.0.1 + vite@8.0.8 (no new direct dep added; existing transitive deps re-pinned via root `pnpm.overrides`). EU CRA 2026-09-11 compliance preserved.
  - **CRA SBOM impact / CRA SBOM 影響**: **SBOM driver entries refreshed (no new direct dependency)**. `pnpm.overrides` forces protobufjs → 8.0.1 + vite → 8.0.8 across all transitive dep paths; SBOM CycloneDX 1.6 generation updated accordingly (`pnpm sbom`). EU CRA 2026-09-11 compliance preserved with strengthened audit trail for transitive Critical vulnerability remediation.
  - **Refs / 参照**: GHSA-xq3m-2v4x-88gg (protobufjs Critical) + GHSA-v2wj-q39q-566r (vite High defense-in-depth) + CWE-521 (Weak Password Requirements, MIN_ENV_KEY_LENGTH=40) + commit `b2de9b7a` (PR #12) + cherry-pick predecessor commit `7e037bf4` (vite override + middleware/auth 鍵長検証) + root `CHANGELOG.md` `[0.5.0]` Security section + ` for full rationale + IO Decision anchors.

### Fixed / 修正

- **fix(worker): Item 3 / CO-31 closure — `acquireRedisLockBestEffort` fail-open recovery via `tryAcquireLock` retry-with-backoff (v0.4.0 Item 3) [EU GDPR Art.32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(worker): Item 3 / CO-31 closure — `acquireRedisLockBestEffort` fail-open recovery を `tryAcquireLock` retry-with-backoff で復旧 (v0.4.0 Item 3)**:
  - **JP**: PR-E-1 §1.3 carryover **CO-31 (L, deadline 2026-Q4, pipeline-engineer)** を Item 3 PR で **deadline 前倒し closure**。`apps/mcp-server/src/services/worker-supervisor.service.ts:1260 acquireRedisLockBestEffort` の fail-open path に **retry-with-backoff (100/200/400ms、max 3 attempts、total 700ms bounded)** helper (`tryAcquireLockWithRetry` + `runAcquireLockWithRetryOrchestrator`) を導入。`worker-active-lock.service.ts:236 tryAcquireLock` の **discriminated union API** (PR7d-3 SEC M-1 lineage) を consume し、`reason === "redis_unavailable"` を retry trigger / `reason === "already_held"` を no-retry path として構造的に separate。CO-31 観測 transient Redis failure (production trace 14 分超 page-lock 不在) の degradation window を **14 分超 → ≤700ms に縮小** (NOT elimination per ADR-0011 Amendment 5 §A5.5)。詳細はルート `CHANGELOG.md` および ` 参照。
  - **EN**: Closes the PR-E-1 §1.3 carryover **CO-31 (L, deadline 2026-Q4, pipeline-engineer)** **earlier than deadline** via Item 3 PR. Adds **retry-with-backoff (100/200/400ms, max 3 attempts, 700ms total bounded)** helper (`tryAcquireLockWithRetry` + `runAcquireLockWithRetryOrchestrator`) to the fail-open path of `apps/mcp-server/src/services/worker-supervisor.service.ts:1260 acquireRedisLockBestEffort`. Consumes `worker-active-lock.service.ts:236 tryAcquireLock` **discriminated union API** (PR7d-3 SEC M-1 lineage), structurally separating `reason === "redis_unavailable"` (retry trigger) vs `reason === "already_held"` (no-retry path). For the CO-31 observed transient Redis failure (production trace: 14+ minute page-lock absence), the degradation window is **reduced from 14+ minutes to ≤700ms** — NOT elimination per ADR-0011 Amendment 5 §A5.5. See root `CHANGELOG.md` and ` for full details.
  - **Per-file LoC deltas / per-file LoC delta**:
    - `apps/mcp-server/src/services/worker-supervisor.service.ts` **LoC 1494/1500 (max-lines hard cap maintained, 6 LoC margin)** — `acquireRedisLockBestEffort` を thin caller に refactor、`runAcquireLockWithRetryOrchestrator` に callback object `{ onAcquired / onHeldByOther / onExhausted / onError / onSettled }` を渡す形へ移行 (CO-PRDD9-01 / OBS-7 closure landing 状態継続)。
    - `apps/mcp-server/src/services/worker-supervisor-helpers.ts` **+~80 LoC** — NEW: `tryAcquireLockWithRetry(workerType, nonce): Promise<{ outcome: "acquired" | "held_by_other" | "exhausted"; nonce: string }>` helper consuming `AcquireLockResult` discriminated union; 100/200/400ms backoff curve with `Math.pow(2, attempt - 1)` (intentional shape unification with PR-D-8 §3.2.5 `tryReleaseLockWithRetry` per TDA-02 L); `sanitizeErrorMessage` rail for `logger.warn` error field per CWE-209 SEC-04 L. NEW: `runAcquireLockWithRetryOrchestrator` with `switch + assertNever` exhaustive narrowing for `.then` / `.catch` / `.finally` chain delegation from Supervisor (TPA-03 + SEC-02 joint landing).
    - `apps/mcp-server/tests/services/worker-supervisor.test.ts` **+~120 LoC** — 3 new unit cases under `describe("Item 3 / CO-31: tryAcquireLockWithRetry retry-with-backoff")`. Real timer scope (no `vi.useFakeTimers()` for backoff curve). Stub-based fault injection per Plan §4.4 SEC-03 L. Cases: (1) `redis_unavailable` 3 times → `exhausted` with 100/200/400ms cumulative wait / (2) `redis_unavailable` once then `ok: true` → `acquired` / (3) `already_held` → no-retry, immediate `held_by_other`.
    - `apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-lock-003-acq.test.ts` **NEW** — INV-WORKER-LOCK-003 standing regression. 2 cases: page worker fail-open recovery via `tryAcquireLockWithRetry` on transient Redis failure + `held_by_other` no-retry race-lost path. Per-test `// INV-WORKER-LOCK-003` comment + `assertInvName` runtime validation. 0 `.skip` / `.todo` / `describe.skip`.
  - **Test counts / テスト件数**: Standing regression worker-lifecycle 91/91 PASS (was 89/89 with PR-E-1 baseline; +2 INV-WORKER-LOCK-003 acq cases for Item 3). Unit tests +3 cases (`tryAcquireLockWithRetry` describe block). Skip/todo check: 0 lines.
  - **Quality Gate / 品質ゲート**: `pnpm format:check` 0 violations 維持 + `pnpm docs:verify` 47 PASS / 0 FAIL 維持 (Phase 3 baseline both exit 0 confirmed).
  - **Environment variables / 環境変数**: **None added**. Retry parameters (100/200/400ms, max 3 attempts) are constants per Plan v0.2 §3.2.1 / ADR-0011 Amendment 5 §A5.2 (intentional non-tunability for production reliability — operator override 不要、env-var override 不要).
  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-04-29 に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per ` `apps/mcp-server/src/services/worker-supervisor.service.ts` (`acquireRedisLockBestEffort`thin caller refactor + callback object pattern) +`apps/mcp-server/src/services/worker-supervisor-helpers.ts` (`tryAcquireLockWithRetry`+`runAcquireLockWithRetryOrchestrator`2 helpers,`sanitizeErrorMessage`rail,`switch + assertNever`exhaustive narrowing) +`apps/mcp-server/tests/services/worker-supervisor.test.ts`(3 new unit cases) +`apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-lock-003-acq.test.ts` (NEW INV-WORKER-LOCK-003 standing regression file, 2 cases)。改変日付: 2026-04-29。
    - **EN**: The following files in this package were modified on 2026-04-29 under AGPL-3.0-only §5(a)/(b) (Modified Versions). The primary modification notice with date is carried in the root `CHANGELOG.md`; this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per `Modified files:`apps/mcp-server/src/services/worker-supervisor.service.ts` (`acquireRedisLockBestEffort`thin caller refactor + callback object pattern) +`apps/mcp-server/src/services/worker-supervisor-helpers.ts` (`tryAcquireLockWithRetry`+`runAcquireLockWithRetryOrchestrator`2 helpers,`sanitizeErrorMessage`rail,`switch + assertNever`exhaustive narrowing) +`apps/mcp-server/tests/services/worker-supervisor.test.ts`(3 new unit cases) +`apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-lock-003-acq.test.ts` (NEW INV-WORKER-LOCK-003 standing regression, 2 cases). Modification date: 2026-04-29.
  - **IO anchors / IO anchor**: §1.0 IO Plan Decision Initial CONDITIONAL `019dd66c-3c20-7024-949f-b62a9d8474d2` (SUPERSEDED, 2026-04-28) → §1.0 IO Plan Decision Final **APPROVE** `019dd689-77a8-724d-9290-b7998c327b57` (2026-04-29, post targeted re-audit, 4 face APPROVE consensus, 1 M finding RESOLVED via Plan v0.2 §3.2.1 helper rewrite to `tryAcquireLock` discriminated union API) + §2.0 IO Impl Decision **APPROVE** `019dd6bb-980b-772b-82e8-c3bd980a340f` (2026-04-29, Phase 2 Step 8, 0 H / 0 M / 1 L acceptable: FIND-IMPL-LCC-ITEM3-01 = 3-CHANGELOG bilingual JP/EN Item 3 closure entries Phase 3 forward landing per Plan v0.2 §4.9 / G7) + §3 IO Final Consistency Check (Phase 3 docs-sync separate IO session, scheduled post-Phase 3).
  - **Phase 3 docs-sync anchors / Phase 3 docs-sync アンカー**: 3-CHANGELOG bilingual JP/EN sync (root + `+`apps/mcp-server/`, all 3 grep return Item 3 entry) + ADR-0011 Amendment 5 §A5.1..§A5.7 NEW landing (Status: **Accepted**, 2026-04-29 Phase 3 docs-sync target) + PR-E-1 §1.3 CO-31 row status `OPEN`→`CLOSED (Item 3, commit <NEW_SHA>)`.
  - **Compliance / 法務・コンプライアンス**: GDPR Art.32(1)(b) processing integrity (Reduces operational reliability gap on `reftrix:worker:active:page` Redis lock acquire path during transient Redis failure — natural extension of ADR-0011 Amendment 1 §A.2 already-decided dual-run guarantee contract), APPI 第23条 安全管理措置 (technical safeguarding measures extended to acquire-side fail-open recovery), CRA 2024/2847 Article 14 (NOT a vulnerability scope item — operational reliability blocker, not attacker-exploitable, no Article 14 disclosure obligation triggered, no GDPR Art.33 72-hour breach notification gate triggered; 4-axis disclaimer per ADR-0011 Amendment 5 §A5.5: no external attacker surface / no privilege escalation / no information disclosure (CWE-209 `sanitizeErrorMessage` rail per SEC-04 L) / no DoS amplification). CRA SBOM drift zero (no new dependency).
  - **Related contracts / 関連契約**: ADR-0011 Amendment 5 (NEW, Acquire-side fail-open recovery via `tryAcquireLock` discriminated union, **Status: Accepted in Phase 3 docs-sync 2026-04-29**, includes §A5.4 cross-ref to PR-D-8 release-side shape `tryReleaseLockWithRetry` documenting intentional ~50% jscpd token-level overlap per TDA-02 L) + ADR-0011 Amendment 4 (cross-ref, PR-E-1 lock nonce contract — distinct concern, no regression) + PR7d-3 SEC M-1 (`tryAcquireLock` discriminated union API lineage source) + INV-WORKER-LOCK-003 (cross-binding standing test, worker-lifecycle domain, 91/91 PASS) + PR-D-8 §3.2.5 Layer 1 `tryReleaseLockWithRetry` (intentional pattern unification) + Plan v0.2 + Finding Registry §1.0 (Plan Decision Initial CONDITIONAL + Final APPROVE) + §2.0 (IO Impl Decision Phase 2 APPROVE) + **PR-E-1 §1.3 CO-31 (CLOSED Item 3, deadline 2026-Q4 met early)**.

### Changed / 変更

- **fix(cron): Item 2 / CO-30 closure — BackfillReconciliationCron polling cadence 1h → 5min (v0.4.0 Item 2) [EU GDPR Art.5(1)(d)/30/32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(cron): Item 2 / CO-30 closure — BackfillReconciliationCron ポーリング間隔 1h → 5min (v0.4.0 Item 2)**:
  - **JP**: PR-E-1 §1.3 carryover **CO-30 (M, deadline 2026-Q3, pipeline-engineer)** を Item 2 PR で closure。`apps/mcp-server/src/cron/backfill-reconciliation-cron.ts` の `DEFAULT_INTERVAL_MS` を 1h → 5min に短縮。`embedding-backfill-worker.ts:661 await updateEmbeddingBackfillStatus(webPageId, remainingStatus)` per-job aggregate UPDATE late-arrive / drop の reconciliation polling 頻度を高めた。Root cause re-attribution per TPA-01 (`safeUpdateBackfillStatus` 0 hits → embedding-backfill-worker per-job UPDATE path)、SLO claim correction per TPA-02 path b (実 reconciliation upper bound = `max(staleThresholdMs=1h, intervalMs=5min) = 1h+5min worst-tail`、~12% reduction in worst-tail lag、true 12x SLO は CO-30-FOLLOWUP M 2026-Q4 へ繰越)。詳細はルート `CHANGELOG.md` および ` 参照。
  - **EN**: Closes the PR-E-1 §1.3 carryover **CO-30 (M, deadline 2026-Q3, pipeline-engineer)** via Item 2 PR. Reduces `apps/mcp-server/src/cron/backfill-reconciliation-cron.ts` `DEFAULT_INTERVAL_MS` from 1h to 5min, increasing reconciliation polling frequency for `embedding-backfill-worker.ts:661 await updateEmbeddingBackfillStatus(webPageId, remainingStatus)` per-job aggregate UPDATE late-arrive / drop scenarios. Root cause re-attribution per TPA-01 (`safeUpdateBackfillStatus` 0 hits → embedding-backfill-worker per-job UPDATE path); SLO claim correction per TPA-02 path b (effective reconciliation upper bound = `max(staleThresholdMs=1h, intervalMs=5min) = 1h+5min worst-tail`, ~12% reduction in worst-tail lag; true 12x SLO improvement deferred to CO-30-FOLLOWUP M 2026-Q4). See root `CHANGELOG.md` and ` for full details.
  - **Per-file LoC deltas / per-file LoC delta**:
    - `apps/mcp-server/src/cron/backfill-reconciliation-cron.ts` **+39/-3 LoC** — `DEFAULT_INTERVAL_MS` constant 1h → 5min (1 line) + bilingual JP/EN JSDoc 拡充 (cadence rationale + staleThresholdMs orthogonality + CO-30-FOLLOWUP cross-ref + `inFlight` skip-tick backpressure attribution per TPA-03 advisory).
    - `apps/mcp-server/src/scripts/start-workers.ts:650-655` **+12 LoC** — TPA-01 L advisory Phase 3 landing: bilingual JP/EN comment at env-var override site (5min default + 1h+5min upper bound + CO-30-FOLLOWUP M 2026-Q4 deferral). Discoverability supplement to cron-internal JSDoc.
    - `apps/mcp-server/tests/regression/standing/gdpr-delete/inv-data-delete-002-co-30-reconciliation.test.ts` **NEW (312 LoC, 4 cases)** — INV-DATA-DELETE-002 cases: (1) in_progress→completed CAS happy path / (2) concurrentUpdatesSkipped CAS race guard / (3) in_progress→failed pin on residual gaps / (4) defer when active/waiting/delayed BullMQ job present. Per-test `// INV-DATA-DELETE-002` comment + `assertInvName` runtime validation in `beforeEach`. 0 `.skip` / `.todo` / `describe.skip`.
    - `apps/mcp-server/DATA_RETENTION.md` §11.9.6.bis (NEW sub-section) — Backfill in_progress reconciliation SLO orthogonality docs from §11.9.4 24h zombie-worker SLO + 5min cadence rationale + CO-30-FOLLOWUP deferral.
  - **Test counts / テスト件数**: Standing regression gdpr-delete 4 new cases PASS (8ms total). Skip/todo check: 0 lines. No regression in existing INV-DATA-DELETE-002 files.
  - **Quality Gate / 品質ゲート**: `pnpm format:check` 0 violations 維持 + `pnpm docs:verify` 47 PASS / 0 FAIL 維持.
  - **Environment variables / 環境変数**: **None added**. `BACKFILL_RECONCILIATION_INTERVAL_MS` env-var override preserved for ops tuning / emergency rollback (`start-workers.ts:650`). `BACKFILL_RECONCILIATION_STALE_THRESHOLD_MS` env-var override preserved for `staleThresholdMs` (default 1h, immutable per IO Plan Decision option (b)).
  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-04-28 に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per ` `apps/mcp-server/src/cron/backfill-reconciliation-cron.ts`(DEFAULT_INTERVAL_MS + bilingual JSDoc) +`apps/mcp-server/src/scripts/start-workers.ts:650-655`(TPA-01 L advisory bilingual comment) +`apps/mcp-server/tests/regression/standing/gdpr-delete/inv-data-delete-002-co-30-reconciliation.test.ts`(NEW INV-DATA-DELETE-002 standing test) +`apps/mcp-server/DATA_RETENTION.md` (§11.9.6.bis NEW)。改変日付: 2026-04-28。
    - **EN**: The following files in this package were modified on 2026-04-28 under AGPL-3.0-only §5(a)/(b) (Modified Versions). The primary modification notice with date is carried in the root `CHANGELOG.md`; this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per `Modified files:`apps/mcp-server/src/cron/backfill-reconciliation-cron.ts`(DEFAULT_INTERVAL_MS + bilingual JSDoc) +`apps/mcp-server/src/scripts/start-workers.ts:650-655`(TPA-01 L advisory bilingual comment) +`apps/mcp-server/tests/regression/standing/gdpr-delete/inv-data-delete-002-co-30-reconciliation.test.ts`(NEW INV-DATA-DELETE-002 standing test) +`apps/mcp-server/DATA_RETENTION.md` (§11.9.6.bis NEW). Modification date: 2026-04-28.
  - **IO anchors / IO anchor**: §1.0 IO Plan Decision Final **APPROVE** `019dd60c-bc74-772c-aa8e-8aad2e64f86a` (post targeted re-audit, 4 face APPROVE consensus; supersedes Initial CONDITIONAL `019dd5f2-ed05-77ec-adc2-4b5e08780123`) + §2.0 IO Impl Decision **APPROVE** `019dd63a-e247-763b-bebf-23004582b27e` (Phase 2 Step 8, 0 H / 0 M / 2 L advisory carryover acceptable: TPA-01 + SEC-01) + §3 IO Final Consistency Check (Phase 3 docs-sync separate IO session, scheduled post-Phase 3).
  - **Phase 3 docs-sync anchors / Phase 3 docs-sync アンカー**: TPA-01 L advisory Phase 3 landing (`start-workers.ts:650-655` bilingual JP/EN comment) + SEC-01 L advisory Phase 4 directive (commit body observability runbook step `tail -f apps/mcp-server/logs/worker.log | grep BackfillReconciliation`, deploy 後 2 cycles で 5min cadence observability 確認義務化 per Plan §5 R7) + 3-CHANGELOG bilingual JP/EN sync (root + `+`apps/mcp-server/`, all 3 grep return Item 2 entry) + DATA_RETENTION §11.9.6.bis NEW sub-section landing (orthogonality from §11.9.4) + PR-E-1 §1.3 CO-30 row root cause re-attribution + CO-30-FOLLOWUP M 2026-Q4 dual-registry registration.
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.5(1)(d) accuracy**: Reduces stale `embedding_backfill_status='in_progress'` window from `[1h, 2h]` to `[1h, 1h+5min]` worst-tail (~12% reduction), strengthening data accuracy invariant for downstream consumers gating on `status='completed'`.
    - **[EU] GDPR Art.30 records of processing activities**: No new audit_logs action; reconciliation Section A path (`reconcileInProgressRows`) remains audit_logs-silent per existing contract (CO-30 closure scope orthogonal to Section B terminal-pin audit emission).
    - **[EU] GDPR Art.32(1)(b) processing integrity**: Reduces operational reliability gap for backfill terminal-state reconciliation, strengthening "ongoing resilience of processing systems".
    - **[JP] APPI 第23条 安全管理措置**: Technical safeguarding measures extended to backfill reconciliation cadence accuracy.
    - **[EU] EU CRA 2024/2847 Article 14**: **NOT a CRA Article 14 vulnerability scope item** — operational reliability blocker, not attacker-exploitable. No Article 14 disclosure obligation; no GDPR Art.33 72-hour breach notification gate triggered.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged**. CycloneDX 1.6 SBOM drift zero.
  - **Related contracts / 関連契約**: ADR-0008 (Skip Recovery / 7d TTL, cross-ref) + ADR-0011 (Worker Dual-run Lock, natural extension claim VALID, no new ADR) + INV-DATA-DELETE-002 (cross-binding standing test, gdpr-delete domain) + DATA_RETENTION.md §11.9.6.bis (NEW) + PR-E-1 §1.3 CO-30 (CLOSED) + PR-E-1 §1.3 CO-30-FOLLOWUP (NEW M 2026-Q4) + Item 2 §1.3.2 (dual-registry mirror) + Plan v0.2 + Finding Registry §1.0 + §2.0.

### Fixed / 修正

- **fix(worker): WorkerSupervisor lock nonce contract mismatch 恒久対応 (v0.4.0 PR-E-1) [EU GDPR Art.5(1)(d)/30/32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(worker): permanent fix for WorkerSupervisor lock nonce contract mismatch (v0.4.0 PR-E-1)**:
  - **JP**: PR-E-1 (Severity H, current-release-cycle deadline) として WorkerSupervisor 内の lock nonce contract mismatch を Option A (lockNonce を bootToken に統一、1 line modification at line 687) で恒久対応。NF-6 として `state="crashed"` セット両経路で `clearLockHeartbeatTimer` helper 呼出を landing し CWE-770 heartbeat 永久 refresh を防止。INV-WORKER-RESPAWN-LOCK-NONCE-007 standing test 8 cases (worker-lifecycle domain) + worker-supervisor unit test U-1..U-5 5 cases 追加。`audit-log.service.ts` SSOT 拡張 (LCC-04 `worker_zombie_recovered` action contract: 365d retention / `operator:<email>` actor / raw workerType target_id)。詳細はルート `CHANGELOG.md` および ` 参照。
  - **EN**: PR-E-1 (Severity H, current-release-cycle deadline) permanently resolves the WorkerSupervisor lock-nonce contract mismatch via Option A (unify `lockNonce` on `bootToken`, 1-line modification at line 687). NF-6 lands `clearLockHeartbeatTimer` helper invocation at both `state="crashed"` entry paths preventing CWE-770 heartbeat permanent refresh. New INV-WORKER-RESPAWN-LOCK-NONCE-007 standing test (8 cases, worker-lifecycle domain) + worker-supervisor unit test U-1..U-5 (5 cases). `audit-log.service.ts` SSOT extended (LCC-04 `worker_zombie_recovered` action contract: 365d retention / `operator:<email>` actor / raw workerType target_id). See root `CHANGELOG.md` and ` for full details.
  - **Per-file LoC deltas / per-file LoC delta**:
    - `apps/mcp-server/src/services/worker-supervisor.service.ts` **LoC 1500 → 1500 (unchanged at max-lines hard cap)** — Option A line 687 `lockNonce = this.bootTokens[workerType]` (was `randomUUID()`) + NF-6 `clearLockHeartbeatTimer(workerType, this.lockHeartbeatTimers)` invocation at both crash-entry paths (`crash_max_attempts` AND `foreign_lock`) + helpers import 拡張 (`buildBootTokens` + `clearLockHeartbeatTimer`) + `randomUUID` import 削除 + `bootTokens` constructor initialization extracted to `buildBootTokens()` helper for max-lines compliance per Plan v1.1 §6.1.1 + Phase 2 commit gate.
    - `apps/mcp-server/src/services/worker-supervisor-helpers.ts` **+57 LoC (496 → 553)** — NEW `clearLockHeartbeatTimer(workerType, lockHeartbeatTimers): boolean` helper (NF-6 / CWE-770 prevention, both crashed-entry paths) + NEW `buildBootTokens(): Record<WorkerType, string>` helper (per-WorkerType independent randomUUID, supervisor max-lines compliance) + EXPORT `tryReleaseLockWithRetry` (TDA-02 direct unit test enablement) + `randomUUID` import追加.
    - `apps/mcp-server/src/services/audit-log.service.ts` **+25 LoC (405 → ~430)** — `AUDIT_LOG_CONSTANTS` SSOT extended per LCC-04: `WORKER_ZOMBIE_RECOVERED_ACTION` (`worker_zombie_recovered`) + `WORKER_ZOMBIE_RECOVERED_ACTOR_PREFIX` (`operator:`) + `WORKER_ZOMBIE_RECOVERY_METHODS` (`['force_release_redis_lock']` as const).
    - `apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-respawn-lock-nonce-007.test.ts` **NEW (~395 LoC, 8 cases)** — full INV-WORKER-RESPAWN-LOCK-NONCE-007 standing regression. Cases (1) `unexpected_exit` Layer 1 retry budget 0 + (2) `handlePlannedRestart` bootToken nonce forward + (3) lockNonce alias enforcement + (4) static-source-grep `lockNonce = randomUUID()` 0 hits + (5) WorkerType-isolation per-WorkerType independence + (6) Fix-1 ordering CO-22 supplement + (7) NF-6 clearInterval both crash-entry paths + (8) AST/static-grep verification.
    - `apps/mcp-server/tests/services/worker-supervisor.test.ts` **+~150 LoC (1154 → ~1305)** — U-1..U-5 5-case unit test extension under `describe("PR-E-1 lock nonce contract (Plan v1.1 §5.5)")`.
    - ` **+~100 LoC** — Amendment 4 (renumbered from Amendment 2 in Plan v1.0 per LCC-PLAN-PRE1-01 H to avoid collision with PR-D-9 Vision Unload Amendment 2 + PR-D-9-patch CHANGELOG line 66 reserved Amendment 3). Sub-sections §A4.1..§A4.9.
    - `apps/mcp-server/DATA_RETENTION.md` **+~150 LoC** — NEW §11.9 "Worker respawn lock nonce contract runbook" (placed between §11.8 motion SIGABRT and §12 Search Log). Sub-sections §11.9.1..§11.9.7.
  - **Test counts / テスト件数**: Standing regression worker-lifecycle 89/89 PASS pass^1 (was 81/81; +8 INV-007 cases). worker-supervisor unit tests 38/38 PASS (was 33; +5 U-1..U-5 cases). audit-log.service tests 19/19 PASS (no regression). Skip/todo check: 0 lines.
  - **Quality Gate (Phase 2 in progress) / 品質ゲート (Phase 2 進行中)**: `pnpm lint` (EXIT=0) + `pnpm typecheck` (EXIT=0) PASS post-Deliverable 1-2-5. Pass^3 verification scheduled in Phase 2 completion.
  - **Environment variables / 環境変数**: **none added in this PR** — PR-E-1 は contract fix のみで env-var 影響なし. Worker 設定表 (` 不変.
  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-04-28 に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per ` `apps/mcp-server/src/services/worker-supervisor.service.ts`(Option A + NF-6 helper invocation + import 整理) +`apps/mcp-server/src/services/worker-supervisor-helpers.ts` (`clearLockHeartbeatTimer`+`buildBootTokens`+`tryReleaseLockWithRetry`export) +`apps/mcp-server/src/services/audit-log.service.ts`(LCC-04 SSOT extension) + standing/unit test files +`apps/mcp-server/DATA_RETENTION.md` (§11.9)。改変日付: 2026-04-28。
    - **EN**: The following files in this package were modified on 2026-04-28 under AGPL-3.0-only §5(a)/(b) (Modified Versions). The primary modification notice with date is carried in the root `CHANGELOG.md`; this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per `Modified files:`apps/mcp-server/src/services/worker-supervisor.service.ts`(Option A + NF-6 helper invocation + import refactor) +`apps/mcp-server/src/services/worker-supervisor-helpers.ts` (`clearLockHeartbeatTimer`+`buildBootTokens`+`tryReleaseLockWithRetry`export) +`apps/mcp-server/src/services/audit-log.service.ts`(LCC-04 SSOT extension) + standing/unit test files +`apps/mcp-server/DATA_RETENTION.md` (§11.9). Modification date: 2026-04-28.
  - **IO anchors / IO anchor**: §1 IO Plan Decision (Phase 1 Step 3, CONDITIONAL, 2026-04-28) + §1.8 IO Plan Decision v2 (Phase 1 Step 6, APPROVE, 2026-04-28, 3-face SEC + TDA + LCC re-audit unanimously APPROVE) + §2 IO Impl Decision (Phase 2 Step 8, APPROVE with Option (c) accepted-risk on FIND-IMPL-SEC-PRE1-01 M finding via ADR-0011 Amendment 4 §A4.4 rationale 補記 + CO-29 tracked carryover deadline 2026-06-28) + §3 IO Final Consistency Check (Phase 3 docs-sync separate IO session, scheduled post-Phase 3).
  - **Compliance / 法務・コンプライアンス**: GDPR Art.32(1)(b) processing integrity (lock nonce contract violation による embedding-backfill worker permanent zombie 化を恒久対応), Art.5(1)(d) accuracy (§11.9-4 24h Stale state SLO + accuracy invariant 運用化), Art.30 records of processing activities (1 new `worker_zombie_recovered` audit_logs action, 365-day retention + raw workerType target_id (no truncation, fixed enum) + `operator:<email>` actor naming convention, LCC-04 explicit decision), APPI 第23条 安全管理措置 (technical safeguarding measures extended to WorkerSupervisor lock nonce contract integrity + zombie worker recovery audit trail), CRA 2024/2847 Article 14 (NOT a vulnerability scope item — operational reliability blocker, not attacker-exploitable, no Article 14 disclosure obligation triggered, no GDPR Art.33 72-hour breach notification gate triggered per ADR-0011 Amendment 4 §A4.5 + DATA_RETENTION §11.9.5 4-axis disclaimer mirroring ADR-0019:196-211). CRA SBOM drift zero (no new dependency).
  - **Related contracts / 関連契約**: ADR-0011 Amendment 4 (NEW, Lock Nonce Contract Clarification, **Status: Accepted in Phase 3 docs-sync 2026-04-28**, includes §A4.4 rationale 補記 documenting 3-callsite topology + NF-6 scope intentional limitation) + ADR-0019:196-211 (CRA Article 14 disclaimer source) + INV-WORKER-RESPAWN-LOCK-NONCE-007 (NEW standing test, 8 cases; 9th case `(9)_spoofing_suppress_timer_cleanup` tracked under CO-29 deadline 2026-06-28) + INV-WORKER-LOCK-003 (cross-ref, 89/89 PASS) + DATA_RETENTION.md §11.9.1..§11.9.7 (NEW, 4-artifact coherence verified Phase 3) + Plan v1.1 + Finding Registry §1 (Plan Decision v1 CONDITIONAL) + §1.8 (Plan Decision v2 APPROVE) + §2 (IO Impl Decision Phase 2 APPROVE Option (c)) + §2.4 (CO-27/CO-28/CO-29 NEW carryovers) + §2.5 (Phase 3 docs-sync hand-off contract) + **CO-22 / CO-PRDD9-01 / OBS-7 (CLOSED PR-E-1)** + CO-25 / CO-26 (NEW carryovers from §1, deadline 2026-Q4 / 2026-Q3) + **CO-27 (NEW L deadline 2026-Q4, `WorkerChildState.lockNonce` JSDoc enforcement clause; carryover 維持 due to max-lines 1500 exact cap)** + **CO-28 (NEW L deadline operator-tooling PR landing, `truncateTargetId` action-specific bypass for `worker_zombie_recovered`)** + **CO-29 (NEW M deadline 2026-06-28, `shouldSuppressRespawn` 3rd `state="crashed"` callsite NF-6 enforcement + INV-007 9th case + ADR §A wording 3-path rewrite; CO-26 cycle alignment, IO Option (c) consequence per Registry §2.3)**.
  - **Post-Landing Runtime Verification Amendment (2026-04-28) / 着地後 runtime verification 改訂**:
    - **JP**: stripe.com `019dd42e-b962-7138-ba6b-47e8c56c59eb` runtime verification (14 分、5/5 core assertions PASS) で 2 件の orthogonal carryover を Finding Registry §1.3 + §3.9 + Plan Appendix C に追加。**CO-30 (M, 2026-Q3, pipeline-engineer)**: backfill terminal-state reconciliation gap (`page-analyze-worker.ts:1064-1111` `safeUpdateBackfillStatus` aggregate UPDATE 未到達 on page worker restart, BackfillReconciliationCron は workaround)。**CO-31 (L, 2026-Q4, pipeline-engineer)**: `worker-supervisor.service.ts:1260 acquireRedisLockBestEffort` fail-open recovery 不足 (pre-existing, PR-E-1 fix と無関係)。新規 IO Decision なし、§3 Final Consistency PASS verdict 不変。
    - **EN**: stripe.com runtime verification (14 minutes, 5/5 core assertions PASS, jobId `019dd42e-b962-7138-ba6b-47e8c56c59eb`) added 2 orthogonal carryovers to Finding Registry §1.3 + §3.9 + Plan Appendix C. **CO-30 (M, 2026-Q3, pipeline-engineer)**: backfill terminal-state reconciliation gap (`page-analyze-worker.ts:1064-1111` `safeUpdateBackfillStatus` aggregate UPDATE not delivered after page worker restart; BackfillReconciliationCron is a workaround). **CO-31 (L, 2026-Q4, pipeline-engineer)**: missing `acquireRedisLockBestEffort` fail-open recovery in `worker-supervisor.service.ts:1260` (pre-existing, unrelated to PR-E-1 fix). No new IO Decision; §3 Final Consistency PASS verdict unchanged.

- **fix(infra+worker): PR-D-9-patch Batch D-1 — INFRA-RLS-ADMIN-GRANT-001 (M) + INFRA-EMBEDDING-MOTION-SIGABRT-001 (M) (v0.4.0 PR-D-9-patch Batch D-1) [EU GDPR Art.5(1)(d)/17/30/32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(infra+worker): PR-D-9-patch Batch D-1 — INFRA-RLS-ADMIN-GRANT-001 (M) + INFRA-EMBEDDING-MOTION-SIGABRT-001 (M)**:
  - **JP**: PR-D-9-patch Batch D-1 として 2 件の M severity 永続化 finding を統合 landing (deadline 2026-05-25)。**D-1a**: `reftrix_admin` role canonical GRANT を新 Prisma migration として永続化 + INV-RLS-ADMIN-GRANT-005 standing test (schema-enum-sync M1 placement) + DATA_RETENTION §11.7 runbook。**D-1b**: motion category SIGABRT race condition を 3 階層 (Fix-1 close→dispose / Fix-2 pre-fork dispose / Fix-3 SIGABRT detection + suppression) で恒久対応 + ADR-0019 (NEW) + INV-EMBEDDING-MOTION-WORKER-SIGABRT-006 standing test (worker-lifecycle placement) + DATA_RETENTION §11.8 motion SIGABRT recovery runbook with CRA Article 14 disclaimer。詳細はルート `CHANGELOG.md` および ` 参照。
  - **EN**: PR-D-9-patch Batch D-1 lands two M-severity persistence findings together (deadline 2026-05-25). **D-1a**: persists canonical `reftrix_admin` GRANT as new Prisma migration + INV-RLS-ADMIN-GRANT-005 standing test (schema-enum-sync M1 placement) + DATA_RETENTION §11.7 runbook. **D-1b**: permanently resolves motion-category SIGABRT race condition via 3 layered fixes (Fix-1 close→dispose / Fix-2 pre-fork dispose / Fix-3 SIGABRT detection + suppression) + ADR-0019 (NEW) + INV-EMBEDDING-MOTION-WORKER-SIGABRT-006 standing test (worker-lifecycle placement) + DATA_RETENTION §11.8 motion SIGABRT recovery runbook with CRA Article 14 disclaimer. See root `CHANGELOG.md` and ` for full details.
  - **Per-file LoC deltas / per-file LoC delta**:
    - **D-1a (Plan 1)**:
      - `packages/database/prisma/migrations/20260427120000_restore_admin_role_grants/migration.sql` **NEW (~94 LoC)** (idempotent + audit_logs SSOT entry with WHERE NOT EXISTS 4-tuple guard + 13-line GRANT ALL justification block per FIND-PLAN-SEC-D1a-01 + Phase 3 audit_logs `result='success'` constraint comment per FIND-IMPL-TPA-D1a-02 L)
      - `apps/mcp-server/tests/regression/standing/schema-enum-sync/inv-rls-admin-grant-005.test.ts` **NEW** (4 cases: canonical privilege presence + ALL-tables coverage + default privileges contract + audit_logs SSOT entry verification + Phase 3 M2 5th-domain promotion criteria carryover anchor per FIND-IMPL-TPA-D1a-01 L)
      - `apps/mcp-server/DATA_RETENTION.md` §11.7 (NEW DDL/permission-change runbook with GDPR Art.32(1)(b) + APPI Art.23 + AGPL §5 bilingual legal-basis table + PII contract assertion per FIND-PLAN-LCC-D1A-01)
    - **D-1b (Plan 2)**:
      - `apps/mcp-server/src/workers/embedding-backfill-worker.ts` **+~15 LoC at 950-973** (Fix-1 close → dispose ordering reversal, FIND-PLAN-SEC-D1b-01 M closed)
      - `apps/mcp-server/src/queues/embedding-backfill-processors.ts` **+~30 LoC at 264-** (Fix-2 pre-fork dispose with `pre_fork_dispose_duration_ms` metric per TPA-MOTION-02 M)
      - `apps/mcp-server/src/services/worker-supervisor.service.ts` **+1 LoC** (Phase 3 max-lines guard comment per FIND-IMPL-TDA-D1b-01 L; file at exact 1500 cap, must extract new logic to helpers before adding)
      - `apps/mcp-server/src/services/worker-supervisor-helpers.ts` **+~70 LoC at 361/371/379/422-435/456-472** (Fix-3 SIGABRT branch + 1/min rate-limited audit emit per FIND-PLAN-SEC-D1b-03 L `SIGABRT_AUDIT_RATE_LIMIT_MS = 60_000` + `emitWorkerRestartAudit` consolidation +36 LoC win per FIND-IMPL-TDA-D1b-03 L acceptable refactor)
      - `apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-lock-003-motion-sigabrt.test.ts` **NEW (257 LoC, 6 test cases)** (static-source-grep based per FIND-IMPL-TPA-D1b-03 M acknowledgement / CO-22 deadline 2026-Q3, supplemental runtime test at `tests/workers/embedding-backfill-worker.test.ts:411-430` mitigates residual risk)
      - `apps/mcp-server/tests/workers/embedding-backfill-worker.test.ts` **+~20 LoC at 411-431** (TDA-D-1b-01 H severity closure: ordering + forensic anchor assertion red→green CI cycle, 0 skip/todo per H severity mandate)
      - ` **NEW (266 LoC, AGPL-3.0-only SPDX header, 11 H2 sections)** (architectural contract for close-before-dispose ordering + CRA Article 14 disclaimer at lines 196-209 + Phase 3 §Decision 3/4 status amended from "out of scope" to "co-landed in PR-D-9-patch Batch D-1b Phase 2" per FIND-IMPL-TPA-D1b-01 L)
      - `apps/mcp-server/DATA_RETENTION.md` §11.8 (NEW motion SIGABRT recovery runbook + Phase 3 CRA Article 14 disclaimer per FIND-IMPL-LCC-D1b-01 M + production env hygiene clarification per FIND-IMPL-SEC-D1b-01 L + FORK_ENABLED env-var distinction footnote per FIND-IMPL-LCC-D1b-02 L)
  - **Test counts / テスト件数**: Standing regression 174/174 PASS pass^3 3 consecutive (includes new INV-005 4 cases + INV-006 6 cases). Skip/todo check: 0 lines across `inv-rls-admin-grant-005.test.ts` + `inv-worker-lock-003-motion-sigabrt.test.ts` + `embedding-backfill-worker.test.ts:411-431` (H severity mandate satisfied). Unit/integration: 14246 passed / 51 skipped / 40 todo / 0 failed across 436 files.
  - **Quality Gate 6/6 PASS / 品質ゲート 6/6 PASS**: `pnpm lint` (EXIT=0) + `pnpm typecheck` (EXIT=0) + `pnpm format:check` (EXIT=0) + `pnpm docs:verify` (EXIT=0) + `pnpm test:regression:standing` (174/174 PASS) + `pnpm test:unit` (14246 passed)
  - **Environment variables / 環境変数**: **none added in this Batch D-1**, but production env hygiene clarification: `EMBEDDING_BACKFILL_FORK_ENABLED=true` MUST be explicitly set in production env file for Fix-2 (pre-fork dispose) to be active. Default fallback (env unset or `!= "true"`) bypasses Fix-2 and uses in-process fallback only (Fix-1 close-first ordering protects regardless). `EMBEDDING_BACKFILL_FORK_ENABLED` is **distinct** from `PHASE5_FORK_ENABLED` (orthogonal flags with separate scopes; the former controls embedding-backfill worker fork, the latter controls Phase 5 child_process.fork).
  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-04-27 に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per `(D-1a + D-1b 統合):`apps/mcp-server/src/workers/embedding-backfill-worker.ts`(Fix-1) +`apps/mcp-server/src/queues/embedding-backfill-processors.ts`(Fix-2) +`apps/mcp-server/src/services/worker-supervisor.service.ts`(Phase 3 max-lines guard) +`apps/mcp-server/src/services/worker-supervisor-helpers.ts`(Fix-3) + standing test files +`apps/mcp-server/DATA_RETENTION.md` (§11.7 + §11.8)。改変日付: 2026-04-27。
    - **EN**: The following files in this package were modified on 2026-04-27 under AGPL-3.0-only §5(a)/(b) (Modified Versions). The primary modification notice with date is carried in the root `CHANGELOG.md`; this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per `Modified files (D-1a + D-1b combined):`apps/mcp-server/src/workers/embedding-backfill-worker.ts`(Fix-1) +`apps/mcp-server/src/queues/embedding-backfill-processors.ts`(Fix-2) +`apps/mcp-server/src/services/worker-supervisor.service.ts`(Phase 3 max-lines guard) +`apps/mcp-server/src/services/worker-supervisor-helpers.ts`(Fix-3) + standing test files +`apps/mcp-server/DATA_RETENTION.md` (§11.7 + §11.8). Modification date: 2026-04-27.
  - **IO anchors / IO anchor**: §13.16 IO Plan Decision (Batch D Phase 1, Plan 1 APPROVE-CONDITIONAL → APPROVE; Plan 2 CONDITIONAL `full re-audit` → CONDITIONAL maintained) → §13.17 IO Impl Decision (Batch D Phase 2, Plan 1 APPROVE direct + Plan 2 CONDITIONAL `documentation-only check` → APPROVE auto-promotion gated on Phase 3 docs landings completed in this entry).
  - **Compliance / 法務・コンプライアンス**: GDPR Art.32(1)(b) processing integrity (D-1a + D-1b restored), Art.5(1)(d) accuracy (D-1b close-first ordering preserves embeddingBackfillStatus), Art.30 records of processing activities (2 new audit_logs actions, 365-day retention + `truncateTargetId` PII minimisation), APPI 第23条 安全管理措置 (technical safeguarding extended to admin role lifecycle + worker shutdown ordering), CRA 2024/2847 Article 14 (NOT a vulnerability scope item — operational reliability blockers, not attacker-exploitable, no Article 14 disclosure obligation triggered, no GDPR Art.33 72-hour breach notification gate triggered per ADR-0019:196-211 + DATA_RETENTION §11.8 CRA Article 14 disclaimer). CRA SBOM drift zero (no new dependency).
  - **Related contracts / 関連契約**: ADR-0019 (NEW), ADR-0011 (cross-ref), ADR-0016 §M1, INV-RLS-ADMIN-GRANT-005 (NEW), INV-EMBEDDING-MOTION-WORKER-SIGABRT-006 (NEW), DATA_RETENTION.md §11.7 + §11.8 (both NEW), Registry §13.16 + §13.17, CO-22/CO-23/CO-24 (new carryovers, deadline 2026-Q3).

- **fix(worker): PR-D-9-patch post-merge runtime silent stall regression — IPC schema jobId composite-key acceptance via `BACKFILL_JOB_ID_REGEX` SSOT z.union (v0.4.0 PR-D-9-patch) [EU GDPR Art.5(1)(d)/32(1)(b)][JP APPI 第23条][EU CRA 2024/2847]** / **fix(worker): PR-D-9-patch post-merge runtime silent stall regression — IPC schema jobId composite-key acceptance via `BACKFILL_JOB_ID_REGEX` SSOT z.union (v0.4.0 PR-D-9-patch)**:
  - **JP**: PR-D-9 main 着地後 (stripe.com page.analyze, 2026-04-26-27 時間枠) に backfill drain が再び silent stall する事象を運用観測。原因は PR-D-8 で導入された IPC schema (`worker-ipc.schema.ts`) の `jobId: z.string().uuid().optional()` strict UUID 制約と、PR-D-9 の `EmbeddingBackfillWorker` 経路で `buildBackfillJobId(webPageId, category)` が生成する `<UUID>__<category>` 複合キー間の shape mismatch。Worker child → parent IPC message が `jobId` Zod parse fail → message reject → backfill drain handshake が成立せず production rows が `embedding_backfill_status='pending'` で固定。本 patch は **Option B (z.union + SSOT regex)** で landing。詳細はルート `CHANGELOG.md` を参照。
  - **EN**: After PR-D-9 main merge, backfill drain silent stall regression observed (stripe.com page.analyze, 2026-04-26-27 time window). Root cause: shape mismatch between PR-D-8's IPC schema (`worker-ipc.schema.ts`) `jobId: z.string().uuid().optional()` strict UUID constraint and the `<UUID>__<category>` composite key generated by `buildBackfillJobId(webPageId, category)` in PR-D-9's `EmbeddingBackfillWorker` path. Worker child→parent IPC messages failed Zod parse on `jobId` → message reject → backfill drain handshake never completed → production rows stuck in `embedding_backfill_status='pending'`. This patch lands **Option B (z.union + SSOT regex)**. See root `CHANGELOG.md` for details.
  - **Per-file LoC deltas / per-file LoC delta**:
    - `apps/mcp-server/src/queues/embedding-backfill-queue.ts` **+40 LoC** (`BACKFILL_JOB_ID_REGEX` SSOT export L376-398, RFC 4122 strict-hyphen lowercase + dynamic `EMBEDDING_BACKFILL_CATEGORIES` join, 3-regex doc-comment block L676-691)
    - `apps/mcp-server/src/schemas/worker-ipc.schema.ts` **+22/-1 LoC** (L25 import BACKFILL_JOB_ID_REGEX, L55-72 `jobId` field → `z.union([z.string().uuid(), z.string().regex(BACKFILL_JOB_ID_REGEX)]).optional()` + JSDoc, L110-114 `parseWorkerIpcStrict` cyclomatic delta = 0 inline doc-comment)
    - `apps/mcp-server/src/services/worker-supervisor.service.ts` **0 LoC delta** (1497 unchanged, CO-PRDD9-01 contract STRENGTHENED to absolute neutrality)
    - `apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-lock-003-embedding-backfill-supervisor.test.ts` **+416 LoC net** (Wave 1+2 cases #15-#18 +418 → Wave 4 prettier collapse -2 → Wave 5 case renumber #15-#18 → #20-#23 + 2 pre-existing test bug fixes (vi.doUnmock cross-contamination + hardcoded `"embeddin..."` truncation literal) +57/-23)
  - **Test counts / テスト件数**: Standing regression `worker-lifecycle` 167/167 PASS pass^3 3 consecutive (was 165/167 pre-Wave-5 due to 2 pre-existing test bugs surfaced by case renumber). INV-WORKER-LOCK-003 4 new cases #20-#23 (post-renumber) cross-binding INV-DATA-ACCURACY-001 (GDPR Art.5(1)(d) accuracy invariant literal × 17 occurrences) and INV-WORKER-IPC-SPOOFING (worker-IPC composite-key acceptance verified end-to-end through `parseWorkerIpcStrict` Zod re-validation, preventing IPC payload spoofing on the child→parent boundary; tracked per IO §13 PR-D-9-patch blocking invariant set per TPA-PHASE3-02 M attribution closure).
  - **Quality Gate Pass^3 (corrected) / 品質ゲート Pass^3 (修正版)**: `pnpm lint && pnpm typecheck && pnpm test && pnpm format:check && pnpm docs:verify` — `pnpm test` + `pnpm format:check` を Plan v1.1 §8 enumeration から正式追補 (Plan §13.6 corrected enumeration, FIND-IMPL-TPA-PATCH-W5-01 M / C-IMPL-PATCH-W5-02 resolution per Registry §13.11.5)。
  - **Environment variables / 環境変数**: **none added in this patch** — PR-D-9-patch は schema-side z.union extension のみで、`BACKFILL_JOB_ID_REGEX` は internal SSOT export (env-var configurable ではない、enum SSOT 動的 join により automatic drift tracking)。
  - **6 commits / 6 コミット**: `0d5de99d` (Wave 1+2: SSOT regex + standing test) + `c4c0d3ef` (Wave 3: schema z.union) + `e753400c` (Wave 4: prettier collapse) + `a1d5830d` (chore: docs README format) + `8e040d29` (Wave 5 test: case renumber + 2 bug fixes) + `7337d21b` (Wave 5 doc: Registry §13 prettier fix + §13.11 IO Decision delta).
  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-04-27 に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per `M attribution closure)。改変対象:`apps/mcp-server/src/queues/embedding-backfill-queue.ts` (`BACKFILL_JOB_ID_REGEX`SSOT export, commit`0d5de99d`) + `apps/mcp-server/src/schemas/worker-ipc.schema.ts` (`jobId`z.union 拡張, commit`c4c0d3ef`)。改変日付: 2026-04-27。
    - **EN**: The following files in this package were modified on 2026-04-27 under AGPL-3.0-only §5(a)/(b) (Modified Versions). The primary modification notice with date is carried in the root `CHANGELOG.md`; this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per `SEC-PHASE3-M-1 M attribution closure). Modified files:`apps/mcp-server/src/queues/embedding-backfill-queue.ts` (`BACKFILL_JOB_ID_REGEX`SSOT export, commit`0d5de99d`) + `apps/mcp-server/src/schemas/worker-ipc.schema.ts` (`jobId`z.union extension, commit`c4c0d3ef`). Modification date: 2026-04-27.
  - **IO anchors / IO anchor**: §13 IO Impl Decision CONDITIONAL (commit `797de86a`, TPA `019dca89-b4f8` / SEC `019dca8b-6024` / TDA `019dca8c-4fb2` / LCC `019dca8c-09c3`) → §13.11 IO Impl Decision delta APPROVE for Wave 5 (commit `7337d21b`, TPA `019dcabd-47c9` / SEC `019dcabf-becd` Path B sign-off / LCC `019dcabf-1202`).
  - **Related contracts / 関連契約**: ADR-0011 Amendment 3 (PR-D-9-patch post-merge runtime regression fix), INV-WORKER-LOCK-003 (cases #20-#23 post-renumber), INV-DATA-ACCURACY-001 (NEW cross-binding), Plan §13 (canonical Implementation Plan), Registry §13 + §13.11 (Finding Registry + Wave 5 IO Decision delta), DATA_RETENTION.md §Embedding Integrity SLO changelog.

### Added / 追加

- **feat(worker-supervisor): PR-D-8 extend WorkerSupervisor scope to EmbeddingBackfillWorker (per-type Map + 9 T1 must-fix contracts + 4 audit_logs actions) (v0.4.0 PR-D-8) [EU GDPR Art.30/32(1)(b)][JP APPI 第23条][US CCPA §1798.150][EU CRA 2024/2847]** / **feat(worker-supervisor): PR-D-8 extend WorkerSupervisor scope to EmbeddingBackfillWorker (v0.4.0 PR-D-8)**:
  - **JP**: PR-D-7 empirical verification (linear.app, 2026-04-24) と stripe.com 再現観測 (2026-04-25) で `EmbeddingBackfillWorker` が `WorkerSupervisor` scope 外で稼働している設計 gap を確認。`workerArgs: ["--page"]` ハードコード + `WorkerType="page"` 単一 union により RSS 閾値超過時 self-exit 後の OS-level restart 経路欠落 → production backfill drain が手動介入依存 (GDPR Art.32(1)(b) "ongoing resilience of processing systems" 劣化、APPI 第23条 安全管理措置 scope)。本 PR で `WorkerSupervisor` を **per-type Map 設計 (Option C)** に拡張し、9 T1 Canonical must-fix contracts (Registry v3 §20.1) を landing: per-type child Map + IPC binding table dispatch + 5-rule boot token contract (per-type isolation: `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN_PAGE` / `_BACKFILL`) + self-chained respawn 3-layer protocol + 4 audit_logs actions wiring + ESLint complexity gate + `WorkerType` SSOT + IPC Zod schema SSOT + `WorkerChildState` struct + helper module 抽出。Per-type Redis lock key 拡張: `reftrix:worker:active:page` (PageAnalyzeWorker) + `reftrix:worker:active:embedding-backfill` (EmbeddingBackfillWorker)。Staggered scheduling (primary `page` → heartbeat 確立 → secondary `embedding-backfill`) で 32GB tier RSS budget 健全性を担保。MF-V12-01 unified fix で SEC-V12-01 H 🚫 (worker IPC spoofing audit emit) + LCC-V12-01 M 🚫 (Art.30 audit trail completeness) 同時 RESOLVED。MF-V12-02 で standing test 2 cases #13/#14 を新設。**8/8 H findings RESOLVED, 1/1 M finding RESOLVED, accepted-risk 0** (Severity → Landing rule compliant)。LoC re-baseline: Plan v1.0 estimate 350-450 → 1849 measured (1499 supervisor + 350 helpers, +302%) — 9 T1 contracts 同 PR 内 absorption により justified、PR-D-9 class split deferred per CO-20。Legacy `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN` deprecated (1-cycle backward compat preserved via fallback、`[DEPRECATION]` warn hard landing は CO-21 deadline 2026-05-31)。詳細はルート `CHANGELOG.md` および ` 参照。
  - **EN**: PR-D-7 empirical verification (linear.app, 2026-04-24) and stripe.com re-reproduction (2026-04-25) confirmed a design gap in which `EmbeddingBackfillWorker` operated **outside `WorkerSupervisor` scope**. Hardcoded `workerArgs: ["--page"]` + single-value `WorkerType="page"` union meant no OS-level restart path post-self-exit, so production backfill drain depended on manual intervention (GDPR Art.32(1)(b) "ongoing resilience of processing systems" degradation; in scope of APPI Article 23 安全管理措置 / safeguarding measures). This PR extends `WorkerSupervisor` to a **per-type Map design (Option C)** and lands 9 T1 Canonical must-fix contracts (Registry v3 §20.1): per-type child Map + IPC binding-table dispatch + 5-rule boot token contract (per-type isolation: `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN_PAGE` / `_BACKFILL`) + self-chained respawn 3-layer protocol + 4 `audit_logs` action wiring + ESLint complexity gate + `WorkerType` SSOT + IPC Zod schema SSOT + `WorkerChildState` struct + helper module extraction. Per-type Redis lock keys: `reftrix:worker:active:page` (PageAnalyzeWorker) + `reftrix:worker:active:embedding-backfill` (EmbeddingBackfillWorker). Staggered scheduling (primary `page` start → heartbeat established → secondary `embedding-backfill` start) preserves 32GB tier RSS-budget health. MF-V12-01 unified fix simultaneously resolves SEC-V12-01 H 🚫 (worker IPC spoofing audit emit) + LCC-V12-01 M 🚫 (Art.30 audit-trail completeness). MF-V12-02 lands 2 new standing test cases #13/#14. **8/8 H findings RESOLVED, 1/1 M finding RESOLVED, accepted-risk count 0** (Severity → Landing rule compliant). LoC re-baseline: Plan v1.0 estimate 350-450 → 1849 measured (1499 supervisor + 350 helpers, +302%) — justified by 9 T1 contracts absorbed in-PR; PR-D-9 class split deferred per CO-20 (deadline 2026-06-30). Legacy `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN` is deprecated (1-cycle backward compat preserved via fallback; `[DEPRECATION]` warn hard landing tracked in CO-21, deadline 2026-05-31). See root `CHANGELOG.md` and ` for full details.
  - **Per-file LoC deltas / per-file LoC delta** (commit `fc2f733d`, +5291 / -685 across `apps/mcp-server/`):
    - `apps/mcp-server/src/services/worker-supervisor.service.ts` **+1455 / -... LoC** — Per-type `Map<WorkerType, WorkerChildState>` migration (MF-01) + IPC binding-table dispatch (MF-02) + per-type independent UUIDs (MF-03) + self-chained respawn 3-layer protocol wired in `handleWorkerExit` / `scheduleRestart` paths (MF-04) + per-type `evaluateDualRunGuard` + `standaloneLockWorkerType` resolution (MF-06) + staggered scheduling (primary → first heartbeat 10s → secondary, MF-07) + 4 `audit_logs` action lifecycle wiring (MF-08) + boot token log-prohibition (MF-09) + `dispatchVerifiedIpc` → `escalateSpoofing` (SIGTERM + 60s respawn suppress) for MF-V12-01 unified fix.
    - `apps/mcp-server/src/services/worker-supervisor-helpers.ts` **NEW 349 LoC** — `verifyWorkerIpcMessage` + `emitSupervisorAuditLog` + binding-table dispatch helpers; `worker_ipc_spoofing_detected` audit emit at lines 308-317 (`emitSupervisorAuditLog('worker_ipc_spoofing_detected', expectedWorkerType, { pid, reason }, 'denied')`).
    - `apps/mcp-server/src/types/worker-type.ts` **NEW 82 LoC** — WorkerType SSOT (Plan v1.1 §3.2.3, 3-way collision resolved); single source of truth for `'page' | 'embedding-backfill'` discriminator, exhaustive switch enforced by INV-SCHEMA-ENUM-004.
    - `apps/mcp-server/src/schemas/worker-ipc.schema.ts` **NEW 117 LoC** — IPC Zod schema SSOT + `parseWorkerIpcStrict` strict parser; payload SSOT `{ type, workerType, jobId, timestamp }` (PR-D-9-patch later extends `jobId` to z.union for composite-key acceptance — see separate entry below).
    - `apps/mcp-server/src/scripts/start-workers.ts` **+237 / -... LoC** — Per-type dual-run guard refactor (MF-06) + staggered launch helper integration.
    - `apps/mcp-server/src/services/worker-active-lock.service.ts` **+34 / -... LoC** — Per-type Redis lock key isolation (`reftrix:worker:active:<workerType>`).
    - `apps/mcp-server/src/workers/embedding-backfill-worker.ts` **+14 / -... LoC** — IPC payload migration to `{type, workerType, jobId, timestamp}` SSOT (MF-02).
    - `apps/mcp-server/src/workers/page-analyze-worker.ts` **+18 / -... LoC** — IPC payload migration mirroring embedding-backfill-worker.
    - `apps/mcp-server/scripts/_worker-spawn-helper.ts` **+153 / -... LoC** — Spawn helper extension for per-type child registration + boot token per-type injection.
    - `apps/mcp-server/scripts/analyze-50-award-sites.ts` / `reanalyze-large-html.ts` / `reanalyze-serial.ts` **+4 each** — Boot token per-type wiring at batch-script entry points.
    - `apps/mcp-server/.env.example` **+23 LoC** — Per-type boot token env-var documentation (`REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN_PAGE` / `_BACKFILL`) + legacy single-token deprecation notice + `EMBEDDING_BACKFILL_MAX_JOBS_BEFORE_RESTART` (default 3) + `WORKER_RELEASE_RETRY_BUDGET` (default 3) + `WORKER_RELEASE_VERIFY_TIMEOUT_MS` (default 3000ms).
    - `apps/mcp-server/tests/regression/standing/worker-lifecycle/inv-worker-lock-003-embedding-backfill-supervisor.test.ts` **NEW 1683 LoC, 14 cases** — INV-WORKER-LOCK-003 expanded to embedding-backfill domain (cases #1-#14 including v12 MF-V12-02 #13/#14 unknown-workerType + schema-invalid IPC scenarios). Per-test `// INV-WORKER-LOCK-003` comment + `assertInvName` runtime validation.
    - `apps/mcp-server/tests/regression/standing/schema-enum-sync/inv-schema-enum-004-worker-type.test.ts` **NEW 198 LoC** — INV-SCHEMA-ENUM-004 WorkerType SSOT exhaustive switch verification.
    - `apps/mcp-server/tests/regression/standing/gdpr-delete/inv-data-delete-002-worker-supervisor-audit.test.ts` **NEW 182 LoC** — INV-DATA-DELETE-002 cross-binding for 4 new audit_logs actions (gdpr-delete domain coverage).
    - `apps/mcp-server/tests/workers/multi-child-supervisor.test.ts` **NEW 728 LoC** — Multi-child supervision regression suite.
    - `apps/mcp-server/tests/services/worker-supervisor-sanitize.test.ts` **NEW 219 LoC** — `sanitizeErrorMessage` rail per CWE-209 across supervisor surface.
    - `apps/mcp-server/tests/e2e/worker-supervisor-queue-drain.e2e.test.ts` **NEW 204 LoC** — End-to-end drain test for both worker types.
    - `apps/mcp-server/tests/services/worker-supervisor.test.ts` **+42 / -... LoC** — Existing test extension for per-type Map cases.
    - `apps/mcp-server/tests/scripts/start-workers-dual-run.test.ts` **+27 / -... LoC** — Per-type dual-run guard test extension.
    - `apps/mcp-server/tests/workers/sprint1-p0p1-changes.test.ts` **+187 / -... LoC** — v0.1.0 / P0-P1 changes test extension.
  - **Test counts / テスト件数**: Standing regression INV-WORKER-LOCK-003 14 cases + INV-SCHEMA-ENUM-004 1 case + INV-DATA-DELETE-002 audit cross-ref — all PASS pass^3 3 consecutive. Multi-child supervisor 728 LoC PASS. E2E drain test 204 LoC PASS. Worker-supervisor sanitize 219 LoC PASS. Skip/todo check: 0 lines on all new files.
  - **Quality Gate / 品質ゲート**: Pass^3 verification PASS at landing. ESLint complexity gate (threshold 10) enforced supervisor-wide.
  - **Environment variables added / 追加された環境変数** (12 new + 1 legacy deprecated):
    - `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN_PAGE` (auto-injected by WorkerSupervisor per-type, MF-03 isolation; manual setting prohibited)
    - `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN_BACKFILL` (auto-injected by WorkerSupervisor per-type, MF-03 isolation; manual setting prohibited)
    - `REFTRIX_WORKER_CHILD_TYPE` (auto-injected, identifies child workerType; argv `--page` / `--backfill` integrity via Zod re-validation in `verifyWorkerIpcMessage`)
    - `WORKER_RELEASE_RETRY_BUDGET` (default 3, self-chained respawn protocol Layer 1 retry count)
    - `WORKER_RELEASE_VERIFY_TIMEOUT_MS` (default 3000ms, `probeExistingLock` fail-closed verification timeout)
    - `EMBEDDING_BACKFILL_MAX_JOBS_BEFORE_RESTART` (default 3, per-type override of `WORKER_MAX_JOBS_BEFORE_RESTART`)
    - **DEPRECATED (1-cycle backward compat)**: `REFTRIX_WORKER_SUPERVISOR_BOOT_TOKEN` (legacy single token; superseded by per-type tokens; `[DEPRECATION]` warn hard landing tracked in CO-21 deadline 2026-05-31)
  - **AGPL §5(a)/(b) supplementary modification notice / AGPL §5(a)/(b) 改変通知 (補足)**:
    - **JP**: 本パッケージの以下のファイルは AGPL-3.0-only ライセンス §5(a)/(b) (Modified Versions) に従い 2026-04-25 (commit `fc2f733d`) に改変されました。改変内容と日付の主要表示はルート `CHANGELOG.md` にあり、本パッケージレベル CHANGELOG はパッケージ境界での補足表示として記録します (3-CHANGELOG sync rule per `retroactive package-level landing per FIND-IMPL-LCC-03 / OBS-4)。改変対象 (24 files、+5291 / -685):`worker-supervisor.service.ts`+`worker-supervisor-helpers.ts`+`types/worker-type.ts`(NEW SSOT) +`schemas/worker-ipc.schema.ts`(NEW SSOT) +`start-workers.ts`+`worker-active-lock.service.ts`+`embedding-backfill-worker.ts`+`page-analyze-worker.ts`+`scripts/\_worker-spawn-helper.ts`+ 3 batch scripts +`.env.example` + standing/unit/e2e test files。改変日付: 2026-04-25。
    - **EN**: The following files in this package were modified on 2026-04-25 (commit `fc2f733d`) under AGPL-3.0-only §5(a)/(b) (Modified Versions). The primary modification notice with date is carried in the root `CHANGELOG.md`; this package-level CHANGELOG records the supplementary attribution at the package boundary (3-CHANGELOG sync rule per `PR-D-8 retroactive package-level landing per FIND-IMPL-LCC-03 / OBS-4). Modified files (24 files, +5291 / -685):`worker-supervisor.service.ts`+`worker-supervisor-helpers.ts`+`types/worker-type.ts`(NEW SSOT) +`schemas/worker-ipc.schema.ts`(NEW SSOT) +`start-workers.ts`+`worker-active-lock.service.ts`+`embedding-backfill-worker.ts`+`page-analyze-worker.ts`+`scripts/\_worker-spawn-helper.ts`+ 3 batch scripts +`.env.example` + standing/unit/e2e test files. Modification date: 2026-04-25.
  - **IO anchors / IO anchor**: Plan Decision v1 CONDITIONAL → Impl Decision v1 CONDITIONAL → re-impl re-audit v4 CONDITIONAL targeted → MF-V12-01 + MF-V12-02 targeted re-fix → SEC v2 PASS 96 + LCC v2 PASS 96 → **IO Impl Decision v5 APPROVE** (Registry v5 §33, 2026-04-25) → Phase 3 docs-sync (Registry v5 §34.1 D-01..D-09) → Phase 4 Final Consistency Check + commit. 4-auditor evolution: TPA 88→42→92, SEC 90→42→78→**96** (+18), TDA 89→87→84 (CONDITIONAL absorbed via Phase 3 D-01), LCC 82→78→80→**96** (+16).
  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.30 records of processing activities**: 4 new `audit_logs` actions wired (`worker_supervisor_restart` / `worker_type_spoofing_detected` / `worker_lock_ttl_fallback` / `worker_ipc_spoofing_detected`; Worker lifecycle 100% audit trail completeness); `actor="system:worker-supervisor"` + 365-day retention + `targetId` `truncateId` PII-truncate + `details` raw payload excluded (CWE-209 + SEC-V11-01 PII minimisation preserved) — inherits PR7d-3 precedent.
    - **[EU] GDPR Art.32(1)(b) ongoing resilience**: Per-type WorkerSupervisor scope expansion completes processing-integrity contract for both `page` and `embedding-backfill` worker-types.
    - **[EU] GDPR Art.5(1)(d) accuracy (downstream)**: Backfill drain reliability now supervised — eliminates stale window until BackfillReconciliationCron 1h sweep.
    - **[JP] APPI 第23条 (安全管理措置) / Article 23**: Technical safeguarding measures (Worker dual-run prevention + boot-token authentication + IPC schema validation) extended to per-type Worker supervision.
    - **[US] CCPA §1798.150 reasonable security**: Proactive Worker-lifecycle audit trail + IPC spoofing fail-closed (SIGTERM + 60s suppress).
    - **[EU] EU CRA 2024/2847 (2026-09-11 in force)**: CycloneDX 1.6 SBOM drift 0 (no new dependency added); Worker supervision hardening as state-of-the-art evidence.
  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — PR-D-8 は既存 `@prisma/client` / `bullmq` / `zod` / `ioredis` / `audit-log.service` / `truncateId` SSOT API のみ使用。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance preserved.
  - **Related contracts / 関連契約**: ADR-0011 Amendment 1 (PR-D-8) — per-type WorkerSupervisor scope expansion (§A.1 per-type Redis lock key + §A.2 GDPR Art.32(1)(b) processing-integrity expansion + §A.3 APPI 第23条 per-type expansion + §B.1 per-type BOOT_TOKEN secrets isolation + §B.2 legacy deprecation + §C staggered scheduling rationale + §D PR-D-9 class split footnote); INV-WORKER-LOCK-003 (expansion to embedding-backfill, +14 cases including v12 MF-V12-02 #13/#14); INV-SCHEMA-ENUM-004 (NEW WorkerType SSOT exhaustive switch); INV-DATA-DELETE-002 (audit_logs 4 new action cross-ref, GDPR Art.30 100%); CO-20 (PR-D-9 class split deadline 2026-06-30); CO-21 (legacy boot token `[DEPRECATION]` warn hard landing deadline 2026-05-31, superseded by per-type tokens). PR-D-9 (auto-spawn-on-enqueue) builds on this PR-D-8 foundation — see PR-D-9 entry below.

- **feat(worker+embedding): PR-D-9 EmbeddingBackfillWorker auto-spawn-on-enqueue + responsive backfill handler + parts.visual bbox resolution (v0.4.0 PR-D-9) [EU GDPR Art.5(1)(d)/17/30/32(1)(b)][JP APPI 第23条][US CCPA §1798.150][EU CRA 2024/2847]** / **feat(worker+embedding): PR-D-9 EmbeddingBackfillWorker auto-spawn-on-enqueue + responsive backfill handler + parts.visual bbox resolution (v0.4.0 PR-D-9)**:
  - **JP**: PR-D-8 verify セッションで stripe.com page.analyze (job `019dc8dd-8f84-768e-bf70-5cc56aad95bd`, 568 s, 2026-04-26) において silent stall を観測。`EmbeddingBackfillWorker` が `WorkerSupervisor` 配下で per-type Map に登録されていても、auto-spawn handshake が `analyze.tool.ts` / `batch-analyze.tool.ts` 呼び出し境界で wired されていなかったため、production backfill drain が手動介入依存となっていた。本 PR で 3 領域を統合 landing:
    1. **Auto-spawn handshake (Wave 1)**: `apps/mcp-server/src/tools/page/_shared/worker-bootstrap.ts` (NEW per Plan v1.1 §5.1.17) を `analyze.tool.ts:320` + `batch-analyze.tool.ts:256` から呼び出し、`getWorkerSupervisor().ensureAllWorkersRunningStaggered()` で primary `page` heartbeat 確立 → secondary `embedding-backfill` spawn を staggered 実行。`ENABLE_BACKFILL_AUTOSPAWN` env-var (strict `=== "true"` parse, default = enabled) で fail-safe rollback gate 提供。
    2. **Vision unload precondition (UNB-IMPL-1)**: `apps/mcp-server/src/services/vision/vision-unload-handshake.ts` (NEW, 229 LoC) が `OllamaReadinessProbeService.probe()` 経由で Ollama `/api/ps` を 3s timeout で照会し、Vision residual loaded を検出 → secondary backfill spawn を defer ("vision-loaded" reason + audit_logs emit)。32GB tier RSS budget 健全性 (~10.6GB Vision + ~1.9GB child + ~4GB peak page = ~16.5GB を回避) を担保。Probe failure 時は **fail-closed** (defer + `[SLO_MARKER] vision_probe_failed` warn) — GDPR Art.32(1)(b) "ongoing availability and resilience" 文脈で不確定状態下の進行は availability risk と判定。
    3. **Responsive backfill handler (Wave 3)**: `embedding-backfill.service.ts:1050-1075` (`getMissingResponsiveEmbeddings`) + `:1090-1104` (`countMissingResponsiveEmbeddings`) に `AND EXISTS (SELECT 1 FROM web_pages WHERE id = ?::uuid)` 防御節を追加し、GDPR Art.17 `data.delete` 同期削除中の TOCTOU resurrection invariant を保証 (PR2d LCC-M-2 contract 継承)。`ResponsiveProcessor.processInProcess` の `generatedCount:0` 不整合に対し diagnostic log emission + assertion を追加 (PII 配慮: `truncateId(webPageId, 8)` per ` CWE-532)。
    4. **parts.visual bbox resolution (Wave 4)**: `apps/mcp-server/src/services/part/part-bbox-playwright.service.ts` に failure classification (iframe / shadow_dom / dom_disposed / unknown) を追加。`embedding-backfill.service.ts` 配下の `classifyBboxFailure(reason): SkipReasonClassification` private helper (CC delta ≤ +2) で 4-way switch 化し、`bbox_unresolvable` skipReason emit と `audit_logs.embedding_part_visual_skipped` SSOT 経由 (`apps/mcp-server/src/audit/audit-actions.ts`) で routing。

    **Standing regression (UNB-IMPL-2)**:
    - INV-WORKER-LOCK-003 case #15-#19 を `inv-worker-lock-003-embedding-backfill-supervisor.test.ts:1928-2042` に統合 (auto-spawn idempotency + staggered ordering + Conflict-2 audit emit + Vision unload precondition)
    - INV-EMBEDDING-INTEGRITY-001 case #1-#6 を `tests/regression/standing/large-page/inv-embedding-integrity-001-bbox-and-responsive.test.ts` (NEW, 613 LoC, 10 tests) に landing (responsive missing rows + bbox classification + `bbox_unresolvable` audit emit + GDPR Art.17 TOCTOU mirror + reload budget enforcement)
    - INV-SCHEMA-ENUM-004 を `inv-schema-enum-004.test.ts` 内で 16-value `EmbeddingSkipReason` exhaustiveness に拡張 (TS-only `as const` SSOT per ADR-0018 §Decision 1 Supplement; Prisma surface 除外 = 3-surface cascade)
    - Integration drain test `tests/integration/page-analyze-backfill-drain.test.ts` (NEW, 293 LoC, 4 tests) を `process.env.CI` gate 経由で landing (fixture URL allowlist 127.0.0.1:38765 + 4 negative SSRF tests, CWE-918)

    **新規環境変数**: `ENABLE_BACKFILL_AUTOSPAWN` (default true, opt-out gate), `BBOX_RESOLVE_RELOAD_ENABLED` (default false, runtime safety), `BBOX_RESOLVE_RELOAD_ENABLED_MAX_RELOADS_PER_PAGE` (default 5), `BBOX_RESOLVE_RELOAD_TOTAL_TIMEOUT_MS` (default 60000)

    **WorkerSupervisor LoC**: 1499 → **1497** (-2 LoC delta, CO-PRDD9-01 contract STRENGTHENED from delta=0; max-lines:1500 ESLint guard cleared)

    **互換性破壊**: なし (Backward compatible API; `ENABLE_BACKFILL_AUTOSPAWN=false` で legacy `ensureWorkerRunning` 経路にロールバック可能)

  - **EN**: PR-D-8 verify session observed silent stall on stripe.com page.analyze (job `019dc8dd-8f84-768e-bf70-5cc56aad95bd`, 568 s, 2026-04-26). Even though `EmbeddingBackfillWorker` was registered per-type under `WorkerSupervisor`, the auto-spawn handshake at the `analyze.tool.ts` / `batch-analyze.tool.ts` invocation boundary was not wired, so production backfill drain depended on manual intervention. This PR consolidates 3 areas:
    1. **Auto-spawn handshake (Wave 1)**: `apps/mcp-server/src/tools/page/_shared/worker-bootstrap.ts` (NEW per Plan v1.1 §5.1.17) is invoked from `analyze.tool.ts:320` + `batch-analyze.tool.ts:256` and calls `getWorkerSupervisor().ensureAllWorkersRunningStaggered()`, which staggers primary `page` heartbeat establishment → secondary `embedding-backfill` spawn. `ENABLE_BACKFILL_AUTOSPAWN` env-var (strict `=== "true"` parse, default = enabled) provides a fail-safe rollback gate.
    2. **Vision unload precondition (UNB-IMPL-1)**: `apps/mcp-server/src/services/vision/vision-unload-handshake.ts` (NEW, 229 LoC) queries Ollama `/api/ps` via `OllamaReadinessProbeService.probe()` with 3s timeout, detects Vision residual loaded → defers secondary backfill spawn ("vision-loaded" reason + `audit_logs` emit). Preserves the 32GB tier RSS budget health (avoiding ~10.6GB Vision + ~1.9GB child + ~4GB peak page = ~16.5GB). On probe failure: **fail-closed** (defer + `[SLO_MARKER] vision_probe_failed` warn) — under GDPR Art.32(1)(b) "ongoing availability and resilience", proceeding under indeterminate state is itself an availability risk.
    3. **Responsive backfill handler (Wave 3)**: Adds the `AND EXISTS (SELECT 1 FROM web_pages WHERE id = ?::uuid)` defensive predicate to `embedding-backfill.service.ts:1050-1075` (`getMissingResponsiveEmbeddings`) + `:1090-1104` (`countMissingResponsiveEmbeddings`), guaranteeing the GDPR Art.17 `data.delete` synchronous-deletion TOCTOU resurrection invariant (inheriting the PR2d LCC-M-2 contract). Adds diagnostic log emission + assertion for `ResponsiveProcessor.processInProcess` `generatedCount:0` inconsistency (PII-aware: `truncateId(webPageId, 8)` per ` CWE-532).
    4. **parts.visual bbox resolution (Wave 4)**: Adds failure classification (iframe / shadow_dom / dom_disposed / unknown) to `apps/mcp-server/src/services/part/part-bbox-playwright.service.ts`. The 4-way switch is extracted into the `classifyBboxFailure(reason): SkipReasonClassification` private helper (CC delta ≤ +2) within `embedding-backfill.service.ts`, routing via `bbox_unresolvable` skipReason emit through the `audit_logs.embedding_part_visual_skipped` SSOT (`apps/mcp-server/src/audit/audit-actions.ts`).

    **Standing regression (UNB-IMPL-2)**:
    - INV-WORKER-LOCK-003 case #15-#19 consolidated into `inv-worker-lock-003-embedding-backfill-supervisor.test.ts:1928-2042` (auto-spawn idempotency + staggered ordering + Conflict-2 audit emit + Vision unload precondition)
    - INV-EMBEDDING-INTEGRITY-001 case #1-#6 landed in `tests/regression/standing/large-page/inv-embedding-integrity-001-bbox-and-responsive.test.ts` (NEW, 613 LoC, 10 tests; responsive missing rows + bbox classification + `bbox_unresolvable` audit emit + GDPR Art.17 TOCTOU mirror + reload budget enforcement)
    - INV-SCHEMA-ENUM-004 extended within `inv-schema-enum-004.test.ts` for 16-value `EmbeddingSkipReason` exhaustiveness (TS-only `as const` SSOT per ADR-0018 §Decision 1 Supplement; Prisma surface excluded = 3-surface cascade)
    - Integration drain test `tests/integration/page-analyze-backfill-drain.test.ts` (NEW, 293 LoC, 4 tests) landed via `process.env.CI` gate (fixture URL allowlist 127.0.0.1:38765 + 4 negative SSRF tests, CWE-918)

    **New environment variables**: `ENABLE_BACKFILL_AUTOSPAWN` (default true, opt-out gate), `BBOX_RESOLVE_RELOAD_ENABLED` (default false, runtime safety), `BBOX_RESOLVE_RELOAD_ENABLED_MAX_RELOADS_PER_PAGE` (default 5), `BBOX_RESOLVE_RELOAD_TOTAL_TIMEOUT_MS` (default 60000)

    **WorkerSupervisor LoC**: 1499 → **1497** (-2 LoC delta; CO-PRDD9-01 contract STRENGTHENED from delta=0; max-lines:1500 ESLint guard cleared)

    **Backward compatibility**: Preserved (Backward compatible API; rollback to legacy `ensureWorkerRunning` path via `ENABLE_BACKFILL_AUTOSPAWN=false`)

  - **Related contracts / 関連契約**: ADR-0011 Amendment 2 (Vision unload precondition handshake at `apps/mcp-server/src/services/vision/ollama-readiness-probe.ts` + 7-component RSS budget table at line 403-576); ADR-0018 §Decision 1 Supplement (EmbeddingSkipReason TS-only `as const` SSOT extension to 16 values + `audit_logs.action` SSOT constants per `apps/mcp-server/src/audit/audit-actions.ts` at line 187+); INV-WORKER-LOCK-003 (5 new cases #15-#19); INV-EMBEDDING-INTEGRITY-001 (6 new cases #1-#6); INV-SCHEMA-ENUM-004 (16-value EmbeddingSkipReason exhaustiveness); INV-DATA-DELETE-002 (cross-tag for case #5 GDPR Art.17 TOCTOU)

  - **IO anchors / IO anchor**: Plan Decision v1 CONDITIONAL → Plan Audit Re-Decision v2 CONDITIONAL → Plan v1.1 17 C-XX incorporated → SEC sign-off `019dc95d-c617-7cd1-a7c5-bb778f5b6739` + LCC sign-off `019dc95f-575c-7459-af8b-8da9562e9297` → Phase 2 Impl → IO Impl Decision §10 `019dc9a7-4a77-7a7f-812d-d09da79f7b25` → UNB-IMPL-1 (vision-unload-handshake.ts) + UNB-IMPL-2 (INV standing tests) + UNB-IMPL-3 (TPA targeted re-audit APPROVE 96/100 `019dc9ca-9346-7237-8b60-bbd635faf803`) + UNB-IMPL-4 (IO Impl Re-Decision §11 APPROVE) → **Phase 3 docs-sync entry GO**

  - **Compliance / 法務・コンプライアンス**:
    - **[EU] GDPR Art.5(1)(d) accuracy**: backfill drain reliability now supervised — eliminates persistent stall window
    - **[EU] GDPR Art.17(1)(a) Right to Erasure**: synchronous deletion contract preserved + defensive WHERE clause prevents post-deletion resurrection (TOCTOU invariant per INV-EMBEDDING-INTEGRITY-001 case #5)
    - **[EU] GDPR Art.30 records of processing activities**: 4 `audit_logs` actions wired through SSOT (`embedding_part_visual_skipped` + `embedding_backfill_autospawn_failed` + Vision unload deferral + autospawn audit emit); 365-day retention + `truncateId` PII minimisation preserved
    - **[EU] GDPR Art.32(1)(b) ongoing resilience**: Vision unload precondition + autospawn fail-closed (RSS budget integrity); state-of-the-art technical measure per EU CRA 2024/2847 evidence
    - **[JP] APPI 第23条 (安全管理措置) / Article 23 (Security control measures)**: technical safeguarding measures extended to backfill auto-spawn boundary + audit trail completeness
    - **[US] CCPA §1798.150 reasonable security**: proactive Worker-lifecycle audit trail + fail-closed defaults
    - **[EU] EU CRA 2024/2847 (2026-09-11 in force)**: CycloneDX 1.6 SBOM drift 0 (no new dependency added); Vision-unload handshake + defensive WHERE clause as state-of-the-art evidence

  - **CRA SBOM impact / CRA SBOM 影響**: **No dependency added, SBOM unchanged** — PR-D-9 は既存 `@prisma/client` / `bullmq` / `zod` / `ioredis` / `audit-log.service` / `truncateId` SSOT API のみ使用、外部パッケージ追加なし。CycloneDX 1.6 SBOM drift ゼロ。EU CRA 2026-09-11 compliance 保持。/ No new dependency; PR-D-9 uses only existing `@prisma/client` / `bullmq` / `zod` / `ioredis` / `audit-log.service` / `truncateId` SSOT APIs. CycloneDX 1.6 SBOM drift zero. EU CRA 2026-09-11 compliance preserved.
